# 🏁 小九 → 阿狸咪 紧急交接 2026-07-21 20:30

> 当前架构：**☁️云主力 + 💻本兜底**（2026-07-21 17:33 用户终决方案）
> silent_failover 每 30min 自动化巡检

---

## 🚨 一、当前状态

### 云端（GitHub Actions）
- hb_cloud.json last_time: **2026-07-21 12:16:47**（8h 前）
- 云端 workflow 文件：9 个（`cloud_intraday / cloud_post_close / cloud_data_fetch / cloud_scanner / cloud_backup / cloud_weekly / cloud_self_heal / safety-net / test_data_source`）
- 全部配置了 `schedule:` cron + `workflow_dispatch:` 手动触发
- trigger_cloud_dispatch.py（发令枪）存在 repo-temp/，GitHub PAT 在 .gh_pat 中
- **⚠️ 云端从本机看在跑但不够频繁** — 需要你来确认 GitHub Actions 页面状态

### 本机（小九·单位机）
- **独家 ACTIVE 任务**（云端无对应 cron，本机永 ACTIVE）：
  - 08:00 自动交接检查（读阿狸咪）
  - 08:45 盘前更新（依赖中国源本地访问）
  - 09:00 C盘空间预警
  - 09:25 打新开盘前刷新（轻量兜底）
  - 17:25 neodata token 刷新（需本地存 token）
  - 17:31 neodata 数据抓取（与云端并行作为冗余）
  - 19:30 收盘最终部署（阿狸咪失败兜底）
  - 全任务心跳守卫（每 30 分钟，audit 防御层核心）
  - 全时段云端健康监控（每 30 分钟 09:00-16:30）
- **兜底任务**：6 个（intraday 09:30/10:31/11:46/13:31/14:31 + post_close 16:31）
  - 默认 PAUSED（不跑）
  - silent_failover 检测云端沉默>30min → 自动 ACTIVE
  - 当前状态：**ACTIVE**（silent_failover 自动激活——云端 hb 19h 旧，触发了兜底）

### 用户拍板的架构
```
☁️ 云端 = 主力（Primary）— GitHub Actions 24/7
   ├ cloud_intraday: 09:30/10:31/11:46/13:31/14:31（5档盘中）
   ├ cloud_post_close: 15:30/16:15/16:30（3档收盘）
   ├ cloud_data_fetch: 17:31
   ├ cloud_scanner: 18:31
   └ safety-net: 每15分 09-19（兜底可用性监控）

💻 本机 = 兜底（Backup）
   ├ 独家 ACTIVE（云端无对应 cron 的 9 个任务）
   └ 兜底 PAUSED（云端在跑时 6 个盘中任务被 silent_failover 保持 PAUSED）

🔁 silent_failover（audit_automations 第 9 道关）
   └ 每 30min 读 hb_cloud.json last_time
   └ 沉默>30min → 自动 UPDATE 本机兜底任务 status='ACTIVE'
   └ 云端恢复（silent_min ≤ 30min） → 自动 PAUSED 回去
```

---

## 🗺️ 二、什么是 silent_failover（你需知道的）

1. **工作方式**：`audit_automations.py` 第 9 道关 `silent_failover()`，每 30 分钟经由 `check_all_deploys.py` 防御层调用
2. **检测机制**：读 `data/hb_cloud.json` 的 `last_time` 字段
   - 沉默 >30min → `UPDATE status='ACTIVE'` 激活本机兜底
   - WebBuddy 调度器会立即按 rrule 跑
   - 沉默 ≤30min → `UPDATE status='PAUSED'` 让位给云端
3. **状态文件**：`.model_state.json`（不出仓库，本地持久化）
4. **容错**：发令枪脚本（`trigger_cloud_dispatch.py`）可手动 dispatch 任何云端 workflow

---

## 🛠️ 三、你（阿狸咪）到家用机后需做的

### 第一步：确认云端 GitHub Actions 状态
1. 浏览器打开 https://github.com/ah-quant999/quant-scanner-v6/actions
2. 查最近 24h 内 workflow runs：
   - **有绿色 ✅ = 云端在跑** → silent_failover 会在下次巡检自动 PAUSED 回本机
   - **都是红色 ❌ = 云端有错** → 点击红色 run 看失败原因
   - **全空白 = cron 被禁用** → 去 GitHub Settings → Actions → General 检查"Allow GitHub Actions to create and approve pull requests" 是否开启

### 第二步：如果云端全空白/禁用
```
1. GitHub → Settings → Actions → General
2. Enable: "Allow GitHub Actions"
3. Enable: "Allow GitHub Actions to create and approve pull requests"
4. 回到 Actions tab, 手动 trigger 一个 workflow（cloud_intraday: 用 workflow_dispatch）
```

### 第三步：如果云端在跑但慢（如延迟 >30min）
- 不用做任何事
- silent_failover 会自动兜底，云端补上来后自动 PAUSED 回去

### 第四步：发令枪手动触发云端（备用方案）
```bash
cd /e/workspace/stock-scanner/repo-temp
C:/Users/Administrator/AppData/Local/Microsoft/WindowsApps/python.exe trigger_cloud_dispatch.py --dry-run
# 若 dry-run 确认正常，去掉 --dry-run:
C:/Users/Administrator/AppData/Local/Microsoft/WindowsApps/python.exe trigger_cloud_dispatch.py
```

---

## 📋 四、今天的关键改动清单（所有已推 main）

| 时间 | commit | 改动 | 谁改的 |
|------|--------|------|--------|
| 17:33 | `21aeca91` | 架构升级云主力+本兜底（silent_failover + 6 PAUSED + 文档架构图）| 小九 |
| 17:31 | `c98fd7b9` | 精选预判信号卡升级（52周新高主信号、换掉分析师转向/政策密度空数据）| 小九 |
| 17:14 | `401d4115` | 逻辑详解页加6节算法详解 + 修三方心跳陈旧 bug | 小九 |
| 17:05 | `50b74995` | quota_guard hy3额度耗尽自动切 deepseek-v4-flash | 小九 |
| 16:51 | `3eacf466` | neodata token 时效检查 + ops_status 一致性 | 小九 |
| 16:38 | `4f4c37cf` | audit_automations 永久审计脚本 + 防御层 | 小九 |
| 16:19 | `b4626953` | TASK_MATRIX 全量审计对齐22条任务 + 算法说明 | 小九 |

### 模型改名铁律
- 不要用 `ds-V4-FLASH`（API 不认识）
- 必须用 `deepseek-v4-flash`（全小写）
- quota_guard 会自动修复拼写错 + 自动切 hy3→deepseek-v4-flash

---

## 🔔 五、明早（07-22）首跑提醒

| 时间 | 事情 | 说明 |
|------|------|------|
| 08:00 | 交接检查 | 阿狸咪昨晚推送被小九读到 |
| 08:30 | **每日深度巡检首跑** | audit_automations 全 9 道关，silent_failover 检查云端沉默 >
30min→自动 PAUSED 回本机兜底 |
| 08:45 | 盘前更新独家 | 小九中国源本地访问 |
| 09:25 | 打新兜底 | 轻量独立跑 |
| 09:30 | cloud_intraday（云端主） | silent_failover 自动保持 PAUSED |
| **全天** | 3 核防御 | 云端 cron + silent_failover 每 30min + 审计 9 道关 |

---

## ⚡ 六、紧急联系方式

- 小九（单位机）：通过 README 末尾联系方式 / 交接文档
- 云端 workflow 异常 → 去 GitHub Actions 页面看红色失败 run
- silent_failover 异常 → 看 `data/.ops_status.json` 的 `audit` 段
- 最坏情况：手动改 `trigger_cloud_dispatch.py --dry-run` 检查 → 去 `--dry-run` 跑

---

*生成时间：2026-07-21 20:30 · 维护：小九（CAT）*
