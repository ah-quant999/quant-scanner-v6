# 🔄 小九 → 阿狸咪 交接（2026-07-13 晚）

> **来自**：小九（本机 / 白天主力）｜ **时间**：2026-07-13 16:06
> **通道**：origin/main（`git pull --rebase origin main` 即可读到）

---

## 一、⚠️ 紧急：repo-temp 迁出坚果云（你侧也得搞）

今日修复了 `E:\workspace` 是坚果云 Junction 导致的 `deploy_now.py` 偶发 `index.lock` 抢锁问题：

| 项目 | 本机(LEMONCAT) | 你(CAT) |
|------|---------------|---------|
| 根目录 `E:\workspace\stock-scanner` | 仍在坚果云 ✅（无 .git，无 index.lock 风险） | 同左 |
| **repo-temp**（含 .git） | 已迁出 → 物理在 **D:\stock-scanner-repo\repo-temp**，原地建 junction 兜回 ✅ | **你侧若也在坚果云内，同样会 index.lock 抢锁** |
| 执行 | `robocopy` + 删旧 + 建 junction | **建议你同样把 `repo-temp` 迁出坚果云同步目录** |

**对你的影响**：你的 `repo-temp/.git` 若也在坚果云内，`deploy_now` 释放锁时大概率遇到 `index.lock already exists`。建议你侧也做 robocopy 迁移 + junction。

---

## 二、今日代码变更（请 `git pull --rebase origin main`）

### 2.1 优先级高 — 影响运行

| 改了什么 | 影响 |
|---------|------|
| **`scanner.py`** fix: 港股 code zfill(5) + BaoStock 每30只恢复 | 修复 `scanner.py watch` 仅扫 92/157 只静默丢弃 bug |
| **`fetch_sector_fund_flow.py`** THRESHOLD 5.0亿→1.0亿 | 修复主力板块空（今日最大净流入4.6亿，原阈值过高） |
| **`fetch_sector_fund_flow.py`** history 防清空保护 | 修复 `load_history` 异常→`save_history({})`→永久卡1天 |
| **`deploy_now.py`** tmpdir 改用 `os.environ['TEMP']`+正斜杠 | 修复 Git Bash 子进程静默 push 失败（反斜杠路径） |
| **`deploy_now.py`** 加 `ls-remote` 校验 | 部署后验证远程 SHA，不再误报 SUCCESS |
| **`run_intraday_scan.py`** 板块资金语义重试 | 行业数据缺失→60秒后自动重试 |
| **`index_master.html`** 板块资金 render 降级 | sects_in 为空时从 top_list 兜底 |
| **`index_master.html`** F窗口+缠论/羊群效应 display 修复 | 健康看板两项显示 ❌ 已修 |
| **`index_master.html`** 中期趋势 20d/60d 传参修复 | 传的是 t5d→改回 t20d/t60d |

### 2.2 新功能

| 什么功能 | 备注 |
|---------|------|
| **CRDS 逆势龙头评分系统**（`calc_crds.py` + `index_master.html#crdsCard`） | 大跌日涨停选股：精选级2只(先导基电CRDS51/黄河旋风CRDS37)，进阶级3只 |
| **standalone/crisis_radar.html** 独立页 | 19项危机指标，综合指数40.4"关注" |

### 2.3 数据/配置变更

| 事项 | 说明 |
|-----|------|
| `guanlan_extractor.py` 恢复真实抓取 | 原来被误改 stub，已恢复 API 优先+名字反查版 |
| 09:20/09:45 自动化补跑脚本 | 漏跑的 `fetch_sh_index_fib`/`fetch_main_stock`/`fetch_macro_data` 等8个已加进 prompt |
| `.deploy_lock` 冲突处置 | 抢锁时先 `git pull --rebase origin main` 再 `--force` |

---

## 三、你本机待办

- [ ] **repo-temp 迁出坚果云**（robocopy 到 D:/stock-scanner-repo + 删旧 + 建 junction）
- [ ] `git pull --rebase origin main` 拉取今日所有修复
- [ ] **重启坚果云客户端**（让 `customExtRules.conf` 目录规则生效）

---

## 四、开放回路（等你回执）

1. **你本机 repo-temp 迁出坚果云** 搞定了吗？
2. `HANDOVER_小九_2026-07-13.md（早）` 里留的 39 个装饰改动按 (A) 丢弃处理了，没问题吧？
3. `.gitignore` 白名单已补 `generate_recommend.py`/`update_multi_resonance_daily.py`/`push_notify.py` 三行了吗？没有的话建议补上。

---

小九 签发 🔧 — 2026-07-13 16:06
