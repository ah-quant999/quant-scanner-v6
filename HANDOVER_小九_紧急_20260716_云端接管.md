# 🚨 紧急交接 — 云端今日首次自动接管部署

**签发**：小九（CAT） 2026-07-16 08:52
**阅者**：阿狸咪（每 2 小时上线，请优先处理 P0）

## 一句话
今天 **09:20** 是云端 workflow 文件入库后**第一次真正排程自动运行**。目标：云端自动更新+部署，双机只监控。需要你暂停本机部署兜底，并在 09:25 后确认云端是否接管成功。

## 背景（为什么之前都是本地部署）
云端 4 个 workflow（盘前盘中 / 收盘分段 / 17:31 抓取 / 18:31 扫描）是 **7/15 深夜才首次 push 到 main** 的。7/15 白天排程时它们还不存在 → 云端从未自动跑过 → 当天 21 笔部署全是双机本地兜底。**不是云端坏了，是云端还没机会跑。**

## 小九本机现状（已符合目标 ✅）
本机所有部署/扫描/盘中更新自动化**已全部 PAUSED**，仅保留：
- 全时段云端健康监控（>3h 无云端部署会告警）
- 心跳监控-云端部署是否存活
- 17:31 neodata 三表抓取（推 main，给云端供数据，保留）
- 08:00 交接检查 / C盘预警 / 网络健康检查

## 阿狸咪待办（按优先级）

### P0 — 暂停你本机的「部署兜底」类自动化
本机 WorkBuddy 把以下**设为 PAUSED**（只保留监控/监听/备份/交接类 ACTIVE）：
- `19:31 部署兜底`（产生 `[lock] deploy by CAT` 那个）
- `17:45 收盘一段兜底`、`19:05 收盘二段兜底`（含扫描+部署）
- 任何「auto source sync / 推 gh-pages」类本地部署任务

**保留**：URGENT监听、19:30 交接检查、21:10 备份兜底、健康监控。

目的：别和云端抢 gh-pages，否则云端挂了你也不知道、还以为云端在跑。

### P0 — 09:25 起验证云端是否接管
在 repo-temp 目录执行：
```bash
git ls-remote origin gh-pages
```
或打开 GitHub 仓库 **Actions** 页 / gh-pages 分支，看最新部署提交作者：
- ✅ 出现 **`cloud-bot`** 或 **`github-actions[bot]`** → 云端已成功接管，目标达成。
- ⚠️ 仍只有 **`ah-quant999`**（本地）提交 → 云端部署没成功，立即走 P1。

### P1 — 若云端没接管（09:20 后无云端部署提交）
1. 打开 GitHub **Actions** 页，看 `☁️ 盘前+盘中任务` 是否 run、哪步标红。
2. 重点看 `🚀 部署到 GitHub Pages` 步骤（peaceiris）：若标红，截图写入下次交接。
3. 临时把你本机部署兜底 **恢复 ACTIVE** 兜底（别让站点断更）。
4. 交接文档反馈，小九下次上线修。

### P2 — 确认监控在跑
小九本机「心跳监控-云端部署是否存活」「全时段云端健康监控」ACTIVE，云端 >3h 无新部署会告警。你看到告警 = 云端挂了，按 P1 处理。

## 回滚原则
- 云端连续 **2 个交易日** 部署失败 → 恢复双机部署兜底（回旧模式）。
- 云端稳定 3 天 → 双机部署类永久 PAUSED，仅监控。

## 铁律提醒（避免误操作）
- 不要手改 `index_master.html` / `update_data_v2.py` / `deploy_now.py` 然后本地部署——会被云端 checkout 还原，且和云端冲突。
- 调 UI/算法请等云端稳定后统一改、push main，让云端构建部署。
- `data/*.json` `repo-temp/*.py` 勿误删。

---
**小九批注**：今日已加固云端部署——加 `pages: write` 权限、部署前诊断 dist、部署后 `ls-remote` 验证、提交作者标记 `cloud-bot` 便于区分。09:20 首跑后看结果。
