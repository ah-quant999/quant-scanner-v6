# HANDOVER_阿狸咪_2026-07-16_凌晨 → 小九

## 一句话
**预判信号页两卡片位置已审计确认（🔥 涨停联动在一眼结论正下方 / 💰 三方资金流向对比在机游共振正下方）。源/部署/现场三层一致，逻辑详解页已同步标注，源码已提交 main。等云端下次自动部署即可生效。**

---

## 1. 任务背景

主人要求"🔥 涨停联动 卡片恢复后放到一眼结论下方"和"💰 三方资金流向对比 暂未上架的卡片移到预判信号页机游共振下方"。

## 2. 审计结论（2026-07-16 00:30）

四份文件（源 + 部署）位置一致，数据已加载，现场页面可见：

| 卡片 | 文件位置 | line | 关系 |
|---|---|---|---|
| ⚡ 一眼结论 | `standalone/predict.html` | 908 | 在 涨停联动 上方 |
| 🔥 涨停联动 | `standalone/predict.html` | 919 | **在 一眼结论 正下方** ✓ |
| 🐉 机游共振·龙虎榜 | `standalone/predict.html` | 971 | 在 三方资金 上方 |
| 💰 三方资金流向对比 | `standalone/predict.html` | 977 | **在 机游共振·龙虎榜 正下方** ✓ |

| 副本 | 文件 | 卡片位置 |
|---|---|---|
| 源主模板 | `index_master.html` | line 846/857/911/915 |
| 部署主模板 | `dist/index_master.html` | line 847/858/912/916 |
| 部署独立页 | `dist/standalone/predict.html` | line 909/920/973/978 |

**源 → 部署 一致**（仅 build-stamp/title 不同）。

## 3. 数据健康度

| 数据源 | 文件 | 更新时间 | 状态 |
|---|---|---|---|
| 涨停联动 | `data/limit_up_heatmap.json` | 2026-07-15 22:33:20 | 15 个板块 ✓ |
| 三方资金 | `data/capital_flow_summary.json` | 2026-07-15 22:09:09 | north_south/inst/hot_money 三方齐全 ✓ |

## 4. 现场页面（线上）可见性

`https://ah-quant999.github.io/quant-scanner-v6/standalone/predict.html`

健康看板 → 页面卡片可视性状态中已记录：

```
🔮 预判信号
📊 一眼结论          ✅ 数据显示正常
🎯 精选预判信号      ✅ 数据显示正常
🔥 涨停联动          ✅ 数据显示正常
📊 板块资金三合一    ✅ 数据显示正常
🐉 龙虎榜监控        ✅ 数据显示正常
💰 三方资金流向对比  ✅ 数据显示正常
🌏 南向资金          ✅ 数据显示正常
```

两张卡片均 `display:block` 状态，未被 `moveCardsToShelf` 搬到暂未上架面板（脚本只搬 `herdingCard/shFibChanlunSection/tripleSelectCard/crdsCard` 四张）。

## 5. 本次改动（已 commit）

| 文件 | 改动 |
|---|---|
| `index_master.html` | 在 🔥 涨停联动 注释后追加"位于一眼结论正下方，2026-07-16 审计：位置正确，未丢失"；在 💰 三方资金流向对比 注释后追加"位于机游共振·龙虎榜正下方，2026-07-16 审计：从 暂未上架 上架到预判信号" |
| `standalone/predict.html` | 同上 |
| `standalone/guide.html` | 更新文件头"最后更新"日期至 2026-07-16 00:40；在 🔥 涨停联动 段落追加 HTML 行号引用 + 审计标注；在 💰 三方资金流向对比 段落追加 HTML 行号引用 + "已正式上架"标注 |

**未做改动**：卡片 HTML 结构、`display:none` 初值、JS 渲染函数、数据文件。原因：四份文件已完全一致 + 数据已加载 + 现场可见，无需重排或重写。

## 6. 你那边需要做什么

1. `git pull origin main` 拉本次审计 commit
2. 不用改任何文件 — 卡片位置本来就对
3. **如主人想立即生效到现场**（云端定时部署有间隔）：
   ```bash
   python deploy_now.py
   ```
   即可。源已 commit，dist 在 main 上是上一次 deploy 的产物，deploy_now 会重生成 dist 并推到 gh-pages。
4. **如主人想等云端自动部署**：今晚（00:30）已过所有定时槽（最近的 21:00 备份已过），下一次部署是明天 09:20 盘前。
5. 如果主人反馈"页面还是看不到"，请按主人要求**先**用浏览器 Ctrl+F5 硬刷新再判断（CDN 缓存问题占八成）。

## 7. 主人提到的三件确认事项

| 主人问 | 答 |
|---|---|
| 今晚所有改动都写入仓库正确分支了吗？ | **是**。改动已 commit 到 main，commit message 内含"audit"和"predict"关键字。dist 由 deploy_now 生成推送 gh-pages，本机不直推。 |
| 相对应的逻辑详解页都对应修正了吗？ | **是**。`standalone/guide.html` 第 3/352/376 行已同步审计标注 + HTML 行号引用。 |
| 和单位小九都交接嘱咐清楚了吗？ | **就是这份文档**。git pull 后看一眼本文件即明白。 |

## 8. 截图/前端验证建议

主人如果想要截图确认 Ctrl+F5 硬刷新后两张卡片都在预判信号页：
- 路径：https://ah-quant999.github.io/quant-scanner-v6/standalone/predict.html
- 重点看：① 一眼结论 下方应该是 涨停联动（橙色标题+10日热力矩阵） ② 机游共振 下方应该是 三方资金流向对比（紫色标题+4行表格）

## 9. 风险与备查

- **风险**：`display:none` 初始 + JS 控制可见性的模式，如果 `limit_up_heatmap.json` 临时缺失，涨停联动会回退为隐藏。这是 by-design，不是 bug。如主人希望默认显示空态，告诉我，可改为默认 `display:block` + 内部 loading 占位。
- **备查**：`capital_flow_summary.py` 第 8 行注释还说"注入到暂未上架卡片"，是历史注释残留（实际 16:30 已上架到预判信号）。要不要顺手改这一行？
- **备查**：主人的 "暂未上架" 心智模型可能源于 `update_data_v2.py` 早期将三方资金走的是"暂未上架"路径。如有疑问可看 git log 中 7-15 之前的 commit。

—— 阿狸咪 🔥 凌晨 00:40
