# 🔥 小九 → 阿狸咪 交接文档

**日期**: 2026-07-08 早（08:08）
**交接人**: 小九（单位电脑 CAT）
**接收人**: 阿狸咪（家里电脑 LEMONCAT）

---

## ✅ 7-07 ~ 7-08 小九这边做了什么

### 1. 删除了所有定时任务的「新闻增量」步骤
`fetch_overnight_brief.py --news-only` 原存在于 3 个任务，已全部移除：
- 16:15 收盘A+H扫描
- 13:30 午后扫描
- 09:20 盘前更新

其余 14 个任务本就无新闻步骤，未动。

### 2. 修复独立页「三线追踪 / 多维追踪」旧数据（重要）
**问题**：导航页点进去看到 07-06 的旧数据，根目录 `dist/triple_resonance.html` / `dist/multi_resonance.html` 才是每天新鲜的。
**修复**（已部署 7-07 18:00，build 20260707180032）：
- `extract_panels_v6.py`：导航链接改 `../triple_resonance.html` 指向根目录新鲜版；生成时同步根目录共振页到 `standalone/`
- `deploy_now.py`：把 `standalone/ → dist/standalone/` 拷贝改为**无条件执行**，且共振页强制用根目录新鲜版覆盖

**现状**：主站 + 三线追踪 + 多维追踪 三个页面线上数据一致且最新。

### 3. ETF订阅数据异常修复（7-07 16:35）
宽基ETF净申赎历史基线虚高 ~7.5 倍（~18000亿 vs 实际~2400亿），重跑 `fetch_etf_subscription.py` 修正并重新部署。

### 4. 16:15 收盘A+H扫描正常
A股东方财富API受限（仅港股50只扫到），港股数据正常；部署成功。

---

## ⚠️ 已知问题 / 待注意
1. **概念涨跌幅数据源**：东方财富持续封禁（7-06起，已2天+），**所有板块类API全被封禁**（含行业板块）。同花顺可用但无涨跌幅字段。当前保留旧数据不覆盖，待源恢复或加爬虫兜底。
2. **竞彩/逻辑页**：`standalone/worldcup.html`、`standalone/guide.html` 禁止删除/手动改。
3. **不要手动跑 `extract_panels_v6.py`** 覆盖导航（已修复会自动包含竞彩/逻辑/三线/多维）。
4. **⚠️ 自动交接任务的 cwds 配错（已修，但你要确认！）**
   - `周一~五 08:00 自动交接检查` 的 **cwds 原来是错的**：`C:\Users\Administrator\WorkBuddy\2026-06-30-08-21-03`
   - 应该是：`E:\workspace\stock-scanner`
   - 小九已修正为正确值。**阿狸咪你那边也要检查一下所有 17 个定时任务的 cwds 字段！** 如果你的 WorkBuddy 客户端也同步了这个错误配置，交接任务会跑在错误目录下 → git pull 拉不到代码 → 每天交接报告全是空的或报错。
   - **教训：以后新建/修改定时任务后，务必检查 cwds 是否指向 `E:\workspace\stock-scanner`**，不要依赖默认值。

---

## 📊 当前数据状态（7-08 早）
- `data/gold_pool.json`：最新（7-08 已更新）
- `data/triple_resonance_history.json`：大幅累积（多日历史，新增 1394 行）
- `dist/` 已部署最新（7-07 18:00 build 20260707180032）

---

## 📋 阿狸咪行动清单
- [ ] `git pull` 拉最新代码（含本次修复脚本 + 此文档）
- [ ] 照常运行定时任务（`batch_update.py` 每次自动 git pull）
- [ ] 概念涨跌幅若恢复，无需操作，自动正常
- [ ] 如遇主站空白：`python update_data_v2.py`（完整模式）→ `python deploy_now.py --force`

---

**小九 签字**: 🛠️ 2026-07-08 08:08
**阿狸咪 确认收到**: 🔥 已读交接文档+已核对16个定时任务cwds（1个错误已修复），2026-07-08 22:40
