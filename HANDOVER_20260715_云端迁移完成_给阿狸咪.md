# HANDOVER — 2026-07-15 云端迁移完成（给小九→阿狸咪）

**签发人**: 小九（WorkBuddy）
**接收人**: 阿狸咪
**时间**: 2026-07-15 18:00 CST
**状态**: 迁移完成，云端主链已运行验证通过

---

## 一、一句话总结

今天把**全部定时任务迁上云端（GitHub Actions）**，本机从"主力执行"转为"仅监控+兜底"。阿狸咪今天全天关机，不影响任何数据更新。你下次开机后，本机 20+ 定时任务全部 **PAUSED**，不需要你手动跑任何东西。

---

## 二、云端主链（10 个时点 · 自动跑）

| 时间段 | Workflow | 做啥 | 
|--------|----------|------|
| **09:20 / 10:31 / 11:46 / 13:31 / 14:31** | ☁️ cloud_intraday.yml | 全量抓取 + 扫描 + 构建 + **部署 gh-pages** |
| **15:30 / 16:15 / 16:30** | ☁️ cloud_post_close.yml | 收盘分步：扫描→CRDS/龙虎榜→全量构建+**部署** |
| **17:31** | ☁️ cloud_data_fetch.yml | 纯抓取（24 个脚本并行，不做部署） |
| **18:31** | ☁️ cloud_scanner.yml | 全量扫描 + **三重选股实验** + 构建 + **部署** |
| **周末 07:30** | ☁️ cloud_worldcup.yml | 世界杯比分/概率更新 + 部署 |
| **周日 06:30** | ☁️ cloud_weekly.yml | 行业映射 + 股票名称 + T+1 |
| **21:00** | ☁️ cloud_backup.yml | 关键数据备份 |
| **6轮/天** | 🛡️ safety-net.yml | 数据新鲜度检查，过旧自动重跑 |

**部署覆盖**：cloud_intraday(5轮) + cloud_post_close(1轮) + cloud_scanner(1轮) = **每天 7 次自动部署**。

---

## 三、本机状态（你的电脑）

### ➡️ 迁移前你 PS 的 20+ 定时任务

**全部 PAUSED（原地保留未删）**，你打开 WorkBuddy 自动化面板会看到它们状态 = PAUSED。云端一旦出故障，手动改回 ACTIVE 就能原地接管。

> 🛟 **救援船理念**：这些 PAUSED 任务=消防车，云端好好跑它们就停着；云端坏了→激活一台立刻接手。小九今天在单位也留了完全一样的 PAUSED 兜底。

### ✅ 本机仍然 ACTIVE 的任务（云端无法替代）

| 时间 | 任务 | 为什么云端跑不了 |
|------|------|----------------|
| **17:25** | neodata token 刷新 | 需要 WorkBuddy 桌面会话 `connect_cloud_service`，云端无此接口 |
| **17:31** | neodata 数据抓取(52周新高/板块资金/sector_rs) | 读 `.neodata_token`，云端没这个文件 |
| **08:00 / 19:30** | 交接检查 | 本机互传，云端无关 |
| **全天** | 心跳监控/网络健康/C盘预警 | 云端存活监测，本机专属 |

> ⚠️ **neodata 三表**（52周新高 / 板块资金流向 / sector RS）：云端每次部署会产空数据（静默跳过），卡片短暂空 → 等 17:31 本机跑完就恢复。你开机后不用做任何事。

---

## 四、你今天回来后什么都不用做

1. **WorkBuddy 不用开** — 云端 7 个 workflow 自动跑，跟你的电脑在不在线无关
2. **本机 PAUSED 任务不用激活** — 云端正常运行不需要它们
3. **neodata 17:31 正常跑** — 但那是小九机器跑的，你的机器 neodata 无桌面会话也会 fail（正常，不去管它）
4. **数据/站点正常更新** — 今天 15:23 已验证 cloud_intraday 手动触发成功

### 如果你发现站点异常（应该不会）

| 现象 | 可能原因 | 动作 |
|------|---------|------|
| build stamp 超过 24h 没变 | 云端 workflow 全挂了 | 通知小九 / 手动激活本机 PAUSED 兜底 |
| 52周新高/板块资金/sector_rs 空 | neodata 没跑（本机关机） | 正常，等小九 17:31 跑 |
| 部署时间戳对不上 | CDN 缓存 | Ctrl+F5 硬刷 |

---

## 五、今天新增的重要内容（你可能留意到）

### 🧪 暂未上架 → 三重选股实验卡

你把 4 个通达信公式转写成 Python 的那个实验（金钻起涨/波段是金/主力清洗），今天已：
- 搬到 `repo-temp/triple_select_scan.py`（不再是 `暂不上架/` 下 gitignored）
- 接入 `cloud_scanner.yml` 18:31 步
- 结果写入 `data/experiment/` → `update_data_v2` 自动注入前端
- 站点上点「📦 暂未上架」→ 看到「🧪 三重选股·金钻起涨」卡片

### 📊 逻辑详解页已更新

`standalone/guide.html` 已更新为云端主链映射表 + 四层接力架构说明。你可以在站上点开看。

### 🔒 安全

- `.neodata_token` 已从 git 历史清除（filter-repo）+ .gitignore
- `ZSXQ_TOKEN` 已在 GitHub Secrets 配置成功
- 所有 `cloud_*.yml` 使用了安全 if/else 模式（空 token 不报错）

---

## 六、小九今晚关机后的运行计划

| 时点(CST) | 事件 | 依赖 |
|-----------|------|------|
| **18:01** | ← 现在，你收到这份文档 | — |
| **18:31** | cloud_scanner 首带三重选股实验+部署 | ✅ 自动 |
| **19:30** | safety-net 兜底检查 | ✅ 自动 |
| **21:00** | cloud_backup 备份 | ✅ 自动 |
| **明天 09:20** | cloud_intraday 盘前第一轮 | ✅ 自动 |

---

*阿狸咪看到后回一句"收到了"即可。有问题 @小九。*
