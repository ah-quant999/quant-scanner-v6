# 17:30 后收盘阶段主备分离逻辑

> 适用对象：阿狸咪（家用机）+ 小九（单位机）
> 目的：避免 17:30 之后双机同时跑全量收盘任务，造成冗余、数据冲突和 TOKEN/资源浪费。

---

## 1. 核心原则

- **17:30 是切换点**：白天由小九主导，17:30 起阿狸咪接管收盘主流程，小九只作为兜底/备份。
- **主备只能有一个在跑全量**：阿狸咪主流程成功完成，则小九的兜底步骤原则上应跳过；只有检测到阿狸咪失败或数据未更新，小九才顶上。
- **心跳/数据新鲜度是判断依据**：不猜、不默认，只看客观信号（文件时间戳、HANDOVER_LOG、gh-pages 部署时间）。

---

## 2. 角色分工

| 时间 | 阿狸咪（主） | 小九（备） | 谁主导 |
|---|---|---|---|
| 17:30 | 收盘数据抓取（全量） | 等待/自检 | 阿狸咪 |
| 17:35 | 若自身 17:30 成功，不额外动作 | 收盘一段补跑（仅在阿狸咪 17:30 失败时） | 阿狸咪主，小九兜底 |
| 18:30 | 收盘扫描生成 | 收盘扫描生成（仅在阿狸咪 18:30 仍无产出时） | 阿狸咪主，小九兜底 |
| 18:35 | 收盘二段补跑部署 | 收盘二段补跑（仅在阿狸咪 18:35 仍失败时） | 阿狸咪主，小九兜底 |
| 19:30 | 收盘最终部署 + 日历跟踪 | 自动交接检查（读阿狸咪） | 阿狸咪主，小九只读 |
| 19:35 | 收盘最终兜底检查 | 不跑 | 阿狸咪 |

> 小九的 17:35/18:30/18:35 任务不是默认必须跑，而是**条件触发**。

---

## 3. 阿狸咪主流程成功的判定标准

以下 4 项全部满足，视为阿狸咪主流程成功，小九无需兜底：

1. **gold_pool.json** 今日 19:30 之后有更新（mtime > 今日 19:30）。
2. **scan_result.json** 今日 19:30 之后有更新。
3. **HANDOVER_LOG.jsonl** 中存在 `mode=close_deploy` 且 `success=true` 的今日记录（19:30 前后）。
4. **gh-pages 分支** 最新提交时间在今日 19:30 之后（表示 deploy 真的上去了）。

任一条件不满足，触发小九兜底。

---

## 4. 小九兜底触发条件

在每个备份节点之前，小九机器先运行 `evening_failover_check.py`：

| 检查点 | 检查内容 | 兜底动作 |
|---|---|---|
| 17:35 | 阿狸咪 17:30 收盘数据抓取是否成功？ | 若失败，运行 `batch_update.py close_p1` 补跑 |
| 18:30 | 阿狸咪 18:30 收盘扫描是否生成？ | 若 gold_pool/scan 仍旧，运行扫描生成 |
| 18:35 | 阿狸咪 18:35 二段补跑是否成功？ | 若失败，运行 `batch_update.py close_p2` 补跑 |

兜底脚本只运行缺失的环节，不重复跑已成功的部分。

---

## 5. 当前已存在的冗余问题

根据 2026-07-10 小九机器上的时间表，以下任务仍按"默认全量跑"设计：

- 17:35 收盘一段补跑
- 18:30 收盘扫描生成
- 18:35 收盘二段补跑
- 19:30 自动交接检查（读阿狸咪）

这些任务需要改造为**先检查阿狸咪状态，再决定是否执行**。改造方式：在各自自动化 prompt 前加一步 `evening_failover_check.py` 判断。

---

## 6. 建议的自动化 prompt 模板

小九侧兜底任务 prompt 应改为类似：

```text
执行判断：
cd E:/workspace/stock-scanner && python evening_failover_check.py

如果输出显示"阿狸咪主流程成功，小九无需兜底"，直接回复"✅ 阿狸咪已接管，小九兜底跳过"。
如果输出显示需要兜底，执行：
cd E:/workspace/stock-scanner && python batch_update.py <对应模式>
```

---

## 7. 长期目标

- 取消小九机器上 17:35/18:30/18:35 的默认全量调度，改为**条件触发**。
- 保留 19:30 只读检查（不跑数据，只读阿狸咪交接）。
- 小九 08:00 心跳监控-阿狸咪是否存活 与 阿狸咪 18:25 心跳监控-小九是否存活 形成闭环。

---

## 8. 冲突处理

若双机同时进入主流程（例如坚果云同步延迟导致小九没检测到阿狸咪成功）：

- 以**先完成 deploy** 的一方为准。
- 后完成的一方在 deploy 前必须检查 gh-pages 最新提交时间，若已更新则直接退出。
- `deploy_now.py` 应自带"先拉后推、冲突即退出"的锁机制，避免覆盖。
