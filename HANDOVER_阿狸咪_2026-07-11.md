# 交接单 · 阿狸咪 → 小九（2026-07-11 周六）

## 一、重要：修复了线上停更（7/3 → 7/10）
- **现象**：今天发现线上 gh-pages 停在 7/3，本地 dist 已是 7/10 最新版，整整一周没更新。
- **根因（推断）**：双机部署锁去重机制下，某侧（小九或阿狸咪）的 push 未真正成功，而 `deploy_now.py` 被锁占用时直接 `return 0`，调度层误判"部署成功"，另一侧被静默跳过 → 线上停更无人察觉。
  - 佐证：7/10 阿狸咪 19:31 close_deploy 记 `success=false`（push_notify.py 失败）；7/9、7/10 阿狸咪部署可能被小九锁跳过。
- **已修复并上线**：重跑 `deploy_now.py --force`，推送 `ec54844`（2026-07-11 08:16），build-stamp `20260711081644`，线上已恢复 7/10 最新数据。

## 二、根治脚本（已 commit + push main，请 git pull 同步）
- commit `fe535c7`：
  1. 被锁跳过改为显式 ⚠️ 告警 + `return 2`（0=成功 / 1=真错误 / 2=被跳过未部署），不再静默成功。
  2. 新增 `_ghpages_stale_seconds()` 线上陈旧度检测：被锁跳过时若发现 gh-pages 已 >1h 未更新，升级为"疑似停更"严重告警。
- **小九你下次部署前务必 `git pull`**，否则旧脚本仍可能静默跳过。

## 三、确认小九侧也要查的事
- 请回看小九机器 7/9、7/10 的部署日志（HANDOVER_LOG.jsonl），确认那两天 close_deploy 是否也 push 失败但未察觉。新版脚本会显式报警。
- 当前 `.deploy_lock` 本机无遗留，锁状态正常。

## 四、小九 7/8 交接待办闭环确认
- [x] cwds 配错（自动交接检查）：阿狸咪 7/8 已核对修复，16 个任务 cwds 全指向 `E:\workspace\stock-scanner`。
- [ ] 概念涨跌幅东财封禁：仍未恢复（本机 IP 限流），保留旧数据不覆盖；待小九单位机侧自动恢复，无需操作。
- [x] `standalone/worldcup.html`、`standalone/guide.html` 禁删：知悉，未动。
- [x] 勿手动跑 `extract_panels_v6.py` 覆盖导航：知悉。

## 五、当前状态
- main 已同步最新（含 fe535c7 修复），小九 fetch 即一致。
- 线上 = 7/10 最新数据（周六无行情，无需重抓）。
- 本机角色 ALIMI，部署锁无遗留。

---
## 六、重要：自动化轮询空转浪费 token（阿狸咪侧已修，请小九同步改）

之前那张截图（post_close 15:30 运行记录里大量 "Still running. Continuing to wait."）根因：自动化 prompt 只写"执行命令等它跑完"，没规定等待方式 → AI 把命令丢后台后每 ~30 秒 `TaskOutput` 轮询一次，长任务(10min)空转 ~20 轮，每轮白烧 token。

**阿狸咪侧已修**：给 21 个跑长 python 命令的自动化 prompt 都追加了【等待方式铁律】。

**请小九同步**（复制到你自己每个跑长 python 命令的 WorkBuddy 自动化 prompt 末尾）：

> 【等待方式铁律】运行长命令（batch_update / 其他 python 脚本）时：用 `run_in_background=true` 启动后，只调用【一次】`TaskOutput(block=true, timeout=600000)` 阻塞等待直到结束；严禁反复轮询 / sleep / 多次 TaskOutput / 主动查进度。空转每轮都烧 token。

适用对象：你机器上任何 WorkBuddy 长任务自动化（batch_update 全家、兜底、run_intraday_scan、fetch_ipo、收盘部署、行业映射周一、股票名称周五、世界杯、备份等）。
⚠️ 注意：你的 **Windows 任务计划程序**（`setup_xiaojiu_tasks.ps1`）是前台阻塞运行，不轮询、不烧 token，**无需改**；铁律只针对 WorkBuddy 自动化里 AI 跑命令的场景。若你单位机也用 WorkBuddy 自动化跑这些任务，务必加上。

## 七、周末 / 假期别空跑（已落地，小九 git pull 即生效）

**问题**：你的 Windows 每日任务（`-Daily -At`）周六日 / 法定假日也会启动 `batch_update.py`，休市日抓行情全失败 / 空转，浪费 token 还把陈旧数据部署上去。

**已修（commit `f1f711c`，已入 main）**：在 `batch_update.py` 的 `main()` 加了「非交易日跳过」守卫，调用新增的 `is_trading_day.py`（A股交易日判断）。日历数据来源：上交所 2026 年休市安排公告 + 国务院办公厅 2026 年节假日安排通知，**均已核实、非编造**。

**小九你只需**：`git pull`，你的每日任务自动生效，**零改动调度器**。

休市日各模式行为：

| 模式 | 休市日行为 |
|---|---|
| pre_market / morning_scan / morning_plus / morning_report / afternoon(×3) | **跳过**（不抓行情、不部署） |
| close | 周日 / 法定假日**跳过**；**周六仍跑**（T+1 全量刷新） |
| pre_brief（世界杯） | **常驻运行**（体育数据周末照更） |
| backup（21:00 备份） | **常驻运行** |
| weekend_light（若有） | **常驻运行** |

**调休补班已处理**：2026 年六个周末上班日（1/4、2/14、2/28、5/9、9/20、10/10）正确放行为交易日，不会误跳过真交易日导致丢数据。

⚠️ **年度维护义务**：`is_trading_day.py` 里的 `HOLIDAY_RANGES` / `MAKEUP_TRADING_DAYS` 是 2026 硬编码。每年 12 月交易所 / 国务院发布新一年安排后，**必须同步更新**，否则次年会误判交易日。文件顶部已写清来源与更新说明。

**阿狸咪侧同步受益**：我之前的盘中任务只加了 `BYDAY=MO-FR`（只管周末），工作日法定假日仍会跑；现在守卫一并堵上工作日假期的空跑，双机都干净了。

---

*写于 2026-07-11 08:25，阿狸咪（家里机，常在线主机）*
*补充于 2026-07-11 16:40，阿狸咪 — 第六/七节：token 铁律 + 非交易日守卫（f1f711c）*
