# 📋 修改交接文档（2026-07-08 晚间 → 小九接手）

> **阿狸咪** 23:38 修完部署
> 所有修改均已部署到 https://ah-quant999.github.io/quant-scanner-v6/
> **源码已推送 main 分支**，小九明天直接 `git pull` 即可

---

## 一、🧱 部署阻断：两个文件有 git stash 冲突标记（已修复）🔴

### 现象
`deploy_now.py`（无 --force）在 09:15 pre_market、19:30 close_deploy 被审计拦截失败，只有靠 --force 才能部署。

### 根因
`git stash` 操作后未正确处理冲突，两个数据文件混入了冲突标记：

| 文件 | 冲突内容 | 修复方式 |
|------|---------|---------|
| `data/gold_pool.json` | upstream 19:34 vs stashed 19:31 | 保留较新版本（19:34） |
| `data/triple_resonance_history.json` | _stock_price_history 段冲突 | 删 6 行标记，保留 stashed 侧实盘数据 |

### ⚠️ 小九必看
这两个文件 **不在 git 仓库里**（`data/*` 被 .gitignore）。明天上班先确认坚果云同步到了修复版：
- 打开 `data/gold_pool.json` 首行搜 `<<<<<` → 有就手动删
- 打开 `data/triple_resonance_history.json` 首行搜 `<<<<<` → 有就手动删
- **如果在你的电脑上跑 `deploy_now.py` 报错，先检查这两个文件有没有残留冲突标记**

---

## 二、🎯 股池构成卡片全 0（已修复）🔴

### 现象
金股观测页「股池构成」5 个数字全是 0，但候选池有 367 只数据。

### 根因
`index_master.html` 的 `window.CANDIDATE_POOL` 占位符放在文件末尾（451472 行），但渲染代码在 440370 行就读它——读到空对象。`update_data_v2.py` 正确注入了数据也没用，因为注入晚了。

### 修复
占位符从 `</script>` 前移到数据区开头（1235 行，SCAN_DATA 之前）。

**现在顺序正确：**
- 1236 行：`window.CANDIDATE_POOL = {...}`（定义）
- 渲染函数：`const cpObj = window.CANDIDATE_POOL || {}`（使用）

### ⚠️ 铁律（必须记住）
`window.CANDIDATE_POOL = {};` **必须在渲染函数之前定义。** 如果你在 index_master.html 里调整数据区结构，千万不要把它移到后面。
未来新增任何数据占位符同理：放在数据区开头（1235 行附近），不要在 `</script>` 前。

---

## 三、🎫 观测候选股池移到健康看板（已修复）

### 改了什么
- 金股观测页下方的「📡 观测候选股池」卡**已移除**
- 移到了健康看板 → 「暂未上架」面板（仅管理员可见）
- 股池构成的数字摘要仍在金股观测页的「股池构成」卡片展示

### 小九验证
明天打开首页 → 健康看板 → 暂未上架，应该看到「观测候选股池」卡片

---

## 四、🏆 世界杯 07:30 改为抓完即发（已改）

### 改了什么
原：07:30 只抓数据，等 09:15 才上线 → 延迟 ~1.5 小时
改后：07:30 抓完 → `update_data_v2.py` 注入 → `deploy_now.py --force` 三连跑，直接上线

### 影响
世界杯赛事页明天 07:30 左右就更新，不用等到 09:15。

---

## 五、⚙️ deploy_audit.py 去掉了 signal_backtest 僵尸检查（已修）

### 改了什么
`deploy_audit.py` 原来检查 `signal_backtest.json` 是否存在——但**没有任何脚本生成这个文件**，它一直不存在。审计因此报 ERROR → 09:15 和 19:30 的普通部署被阻断。
已降级为 INFO 不阻塞。

### 影响
明天 pre_market（09:15）和 close_deploy（19:30）可以跑普通 `deploy_now.py` 通过审计了，不再需要 `--force`。

---

## 六、🛡️ 安全：这些改动不会丢

| 改动项 | 存放位置 | 覆盖风险 |
|--------|---------|---------|
| `index_master.html`（CANDIDATE_POOL 位置修复 + 候选池移动） | 已 push main | ❌ 无风险 |
| `deploy_audit.py`（去僵尸检查） | 已 push main | ❌ 无风险 |
| 世界杯定时任务改三步连跑 | WorkBuddy 数据库 | ❌ 无风险（不在 git） |
| `data/gold_pool.json` 冲突修复 | 本地（坚果云同步） | ⚠️ 确认坚果云同步 |
| `data/triple_resonance_history.json` 冲突修复 | 本地（坚果云同步） | ⚠️ 确认坚果云同步 |

### 明天早上小九的操作

```bash
# 1. 拉最新源码（保险）
cd E:/workspace/stock-scanner
git pull

# 2. 检查数据文件有无残留冲突标记
grep -n "<<<<<<< " data/gold_pool.json data/triple_resonance_history.json
# 如果有输出 → 手动删掉那几行冲突标记

# 3. 放心跑，不会因为 signal_backtest 被卡了
python deploy_now.py
```

---

## 七、🤖 新增三个 watchdog 兜底定时任务（23:41 创建）

主人要求：数据缺失、部署延迟不应再靠人工发现，必须自动化兜底。已新增：

| 时间 | 任务名 | 作用 |
|------|--------|------|
| 每天 17:35 | 收盘一段检查补跑部署 | 检查 herding_data / sh_index_fib / sector_fund_flow / etf_subscription / macro_data，缺失/非今天则补跑 close_p1 → 注入 → 部署 |
| 每天 18:35 | 收盘二段检查补跑部署 | 检查 gold_pool / candidate_pool / scan_result / lhb_result，缺失/非今天则补跑 close_p2 → 注入 → 部署 |
| 每天 19:35 | 收盘最终兜底检查部署 | 检查所有收盘关键文件，缺失则补跑，最后统一注入 + 部署 |

这三个任务会自动判断交易日，周末和假期会跳过。

### 小九需要知道
- 以后即使 17:30 / 18:30 / 19:30 某个任务失败，5 分钟后 watchdog 会再检查一次并自动补跑+部署
- 不需要你再手动盯着网页数据是不是昨日的
- 但 **代码级 bug（比如 git stash 冲突标记、占位符位置错误）仍可能让部署失败，无法自动修复** → 这种时候才会需要人工介入

---

**阿狸咪确认签字 ✅** — 2026-07-08 23:38
