# 选股三件套改造 — 暂不上架方案

## 背景
2026-07-14 回测基线测量结果：
- 四腿纯技术（缠论/金钻/四量图/上涨趋势）信号整体准确率：1日35.7% / 3日32.1% / 5日10.7%
- 后反推指标四量图_机构变红（实为 C>WMA20(中点线)）占信号事件68%，准确率最差
- 四条腿在样本期内全部负 alpha，超过信号误触发的正常范畴

## 改动内容（三件套）

### 1. `_market_regime()` 函数（新增）
- 位置：`scanner.py`，约 line 849
- 功能：从已抓数据读取涨跌家数 breadth、北向资金净流向、板块资金流入广度 → 输出 regime=0/1/2
- 返回值：0=下跌/回避, 1=震荡/正常, 2=上涨/积极
- 依赖：`data/sh_sz_history.json`、`data/north_fund.json`、`data/sector_fund_flow.json`

### 2. 收紧 `calc_siliang_signal`（四量图_机构变红）
- 原：`机构变红 = C > 牛线1`（几乎永远为真）
- 改：`机构变红 = C>牛线1 AND C>MA10 AND V>=MAV10*0.8`
- 加趋势确认 + 均量门禁

### 3. `update_gold_pool` 双门禁
- **Regime 门禁**：下跌市(regime=0) → signal≥3 入池；正常(1) → ≥2；上涨(2) → ≥1
- **质量门禁**：ST/*ST/退市 → 直接跳过（信号来源 + 研报来源均过滤）

## 文件位置
修改后的文件保存在 `repo-temp/暂不上架/` 下：
- `scanner.py`（含全部三处改动）
- `backtest_signals_v2.py`（价源 mootdx + 每腿胜率）

## 激活方式
```bash
cd repo-temp
cp 暂不上架/scanner.py scanner.py
```
下次扫描（18:30 自动化）自动生效。或主动触发：
```bash
cd repo-temp && C:/Users/Administrator/AppData/Local/Microsoft/WindowsApps/python.exe scanner.py
```

## 观察要点
1. 金股池数量变化（上涨市放宽 → 略多；下跌市收紧 → 明显减少）
2. 研报来源金股不受 regime 门禁影响（始终进入）
3. ST/*ST 自动被挡在池外
4. 如需回滚，`git checkout scanner.py` 恢复原版

## 2026-07-14 快照
- 修改时市场状态：regime=2（上涨/积极），涨跌家数4210:1247，北向流入110亿，板块40/40净流入
- 7/14 原版金股池 size 见 `data/gold_pool.json`
