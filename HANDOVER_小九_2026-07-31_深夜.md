# HANDOVER_小九_2026-07-31_深夜

> 阿狸咪 → 小九。主人已睡，所有事按这个执行。

---

## ⚠️ 最关键一件事（先看）

**v6 仓的 `v8/` 子目录已被删除**（commit `7362b2f6` 已推 origin）。

- ✅ 主人决定：v6 是 v6，v8 是 v8，**两个仓彻底分离**
- ✅ 你的 v8 工作区在 `E:\workspace\quant-scanner-v8\`（独立仓）
- ❌ **不要再用 v6 仓的 v8/ 了**（已不存在）—— 你今天 00:30 的 `6c9d341` deploy 就是从 quant-scanner-v8 仓推的，是对的
- ❌ **不要再跑 `v6/v8/deploy_v8.py`**（已随子目录删除）

---

## v8 独立仓当前状态

| 项 | 状态 | 备注 |
|---|---|---|
| HEAD | `6c9d341` (deploy 2026-07-31_00:30) | 你刚推的 |
| A 阶段 AI 速览 | ✅ **已由你 6c9d341 实现**（更详细版：8 指数+融资+地量+中信期货） | 我也独立做了，但完全重复，**已 abort 不推** |
| 北向大卡 | ✅ 已删（grid g-4 → g-3）| 你做的 |
| 数据新鲜度表北向行 | ✅ 已删 | 你做的 |
| 异动监控表北向行 | ✅ 已删 | 你做的 |
| B 阶段（接 JSON）| ❌ 待做 | 见下 |

---

## 主人拍板的 7-31 排程架构（已确定，必做）

**主人定调：v6 不要再动，保持现在这样，后期 v8 跑稳定了 v6 直接删除。**

### 排程表（云端优先 + 省 token）

| 时段 (CST) | 任务 | 执行方 | 备注 |
|---|---|---|---|
| 08:30 | 今日事件 + 北向EOD + 宏观 → 轻量部署 | ☁️ **云端** | ❌ 缺 workflow，需新建 `cloud_premarket.yml` |
| 09:05 | 中国数据（gold_pool/scan/sector 早盘）| 🖥️ **小九** | ❌ 缺小九 cron。中国源美区 runner 必超时 |
| 09:30-15:00 | 盘中每 30 分钟轻量刷新（指数+板块+ETF+异动+期货）| ☁️ 云端 | ⚠️ **当前 cloud_intraday.yml 漏 4 个整点**（10:00/11:00/13:00/14:00 缺）|
| 15:05 | 收盘超轻量（指数+板块）| ☁️ 云端 | ❌ 缺 workflow |
| 15:30 | 收盘全量（scan/lhb/gold/sector）| ☁️ 云端 | ✅ 已有 |
| 16:15/16:30 | 龙虎榜后共振 + 全模块刷新+部署 | ☁️ 云端 | ✅ 已有 |
| **17:00** | **算法 + 全量部署** | ☁️ **云端**（已确认无 LLM 调用，**0 token**）| ❌ 缺 workflow |
| 17:31 | 收盘数据抓取 | ☁️ 云端 | ✅ 已有 |
| 18:31 | 收盘扫描+部署 | ☁️ 云端 | ✅ 已有 |
| 21:00 | 备份 | ☁️ 云端 | ✅ 已有 |
| 周日 23:00 | 删除 >1 周无用数据（过白名单）| ☁️ 云端 | ❌ 缺 workflow |

**真正耗 AI token 的只有 09:05（小九跑非 AI 决策）+ 17:00（已确认 0 token，可上云）**—— 12 个时段里 **9 个已可云端跑**。

---

## B / B+ 阶段分工（主人拍板"并行"）

### B 阶段（阿狸咪，下次会话动）

v8 模板 JS 改造：把 v8 已有/新增卡片接 `data/intraday/*.json`：
- AI 速览 5 行（情绪/健康/指数/异动/期货）→ 读 crisis/market_health/indices/alerts/cffex_holdings
- 主力板块/概念涨跌 → 读 sector_money_flow/concept_rank
- ETF 资金热度 → 读 etf_heat
- 4 张指数卡 → 读 intraday/indices

### B+ 阶段（小九，1-2 天，**4 个事**）

1. **新建 `cloud_premarket.yml`**（08:30 今日事件+北向EOD+宏观）→ 轻量部署
2. **新建 `cloud_close_ultralight.yml`**（15:05 收盘超轻量）→ 推 indices+sector
3. **新建 `cloud_weekly_cleanup.yml`**（周日 23:00 清理）→ 走白名单：
   - ✅ 可删：`data/_cache/*.json`、临时 `*.log`、`__pycache__/`
   - ❌ 永删：`data/fundamental_quality.json`、`data/experiment/*.json`（累积资产）、`data/.ops_status.json`、`HANDOVER_*.md`、`repo-temp/*.py`、`.workbuddy/memory/`
4. **改 `cloud_intraday.yml`** 补 4 个整点 cron（10:00/11:00/13:00/14:00 CST = 02:00/03:00/05:00/06:00 UTC）

⚠️ 所有新增/改 workflow 配 cron 时区是 **UTC**（GitHub Actions 默认），CST = UTC+8。

### 17:00 算法上云端（**0 token 确认**）

`generate_recommend.py` 经 grep 验证**不含 LLM 调用**（无 openai/dashscope/qwen/llm/gpt/...）—— 是普通计算 + 重建 + 部署。**完全能上云端，0 AI token 消耗**。新建 `cloud_1700_algo.yml` 即可。

---

## v6 仓状态

- ✅ HEAD `7362b2f6` 已上 origin
- ✅ v6 仓**不再含 v8/**，干净
- ✅ v6 模板（`index_master.html`）保持周一版式（commit `806b8ff6`），按主人定调**不再改动**
- ✅ v6 北向大卡 + 运维北向行**保留不动**（保持现状）
- 🎯 主人原话："v6 不要再动了，就保持现在这样，不要再有任何改动，后期v8运行稳定了，v6就直接删除了"

---

## 阿狸咪接下来的事（下次会话）

1. 改 v8 独立仓 `index.html` JS：AI 速览 5 行 + 4 张指数卡 + 已有板块/概念卡 → 接 JSON
2. 不部署（按双机铁律，由小九跑 `deploy_v8.py`）
3. 等你的 B+ 4 个 workflow 上线

---

## 数据白名单（21:00 备份 + 周日 23:00 清理共用）

**永删清单**（任何删除脚本必须过这个清单）：
- `data/fundamental_quality.json`（基本面缓存，重建耗 token）
- `data/experiment/*.json`（历史回测样本，累积资产）
- `data/.ops_status.json`（看门狗状态）
- `HANDOVER_*.md`（交接单）
- `repo-temp/*.py`（生产脚本）
- `.workbuddy/memory/`
- `data/crisis.json` / `data/gold_pool.json`（核心数据，重建成本高）

**可定期清理**（>1 周可删）：
- `data/_cache/*.json`
- `data/intraday/` 旧日期
- 临时 `*.log`（非重要）
- `__pycache__/`

---

**阿狸咪 00:39 收工，主人已睡。明天小九上线第一时间做 B+ 的 4 个 workflow。**

**双机铁律重申**：v8 由小九（单位机）唯一部署者，阿狸咪只改 v8/index.html 源并 commit+push，**绝不跑 deploy_v8.py / update_v8.py**。
