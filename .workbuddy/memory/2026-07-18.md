## 2026-07-18 (周六) 21:10 — 备份兜底自动化

- **21:10 备份兜底**: 云端 21:00 备份未找到记录，阿狸咪补跑
  - `enhanced_backup.py` ✓ 25.7s，1/1 全部通过
  - 交接日志已写: ALIMI backup ✓
  - 耗时 37s（包含 6.8s 代码同步）

## 2026-07-18 (周六) 21:15 — 部署 lock 机制吞 commit 根因确认

- **用户质疑"为什么一直在修复/没写对分支/没验证程序"**，逐一实证：
  - 内容确实在 `origin/main`：`origin/main:deploy_now.py` 392 行有冲突扫描修复、`origin/main:data/gold_pool.json` 0 冲突标记、无旧 4096 扫描代码。`main...origin/main=0 0`。✅
  - 真跑了 `verify_data_vs_website.py --fast --gate`：freshness=PASS / build=PASS / data=WARN（非阻断），GATE 通过 exit 0。✅（之前只 grep 线上 HTML，未跑程序）
  - "一直在修"根因 = **部署 lock 自动同步把干净修复 commit 冲成游离**：`ed302660`/`3ac607ea` 仍是合法 commit 对象，但 `merge-base --is-ancestor` 返回不可达 HEAD，`git log` 只显示 3 个部署自动 commit。这正是 MEMORY 里记的「close_deploy_guarded.py lock 机制会覆盖同时期内其他提交」。内容因当时在工作树里被 autosync commit 带进 HEAD 得以保留，但 commit 身份丢失 → 观感上"修好又像没修"。
- **尝试 cherry-pick --allow-empty 复活游离 commit 失败**：因修复 commit 的父是部署 autosync commit（含整个 dist/ 树），3-way merge 把整棵 dist/ 卷进冲突。放弃此路——内容已在 origin/main，游离 commit 仅为历史整洁问题，不影响功能。
- **真问题待根治**：修改 `close_deploy_guarded.py` 使其不再覆盖同期其他 commit（改用 `git pull --rebase`/merge 而非 `git reset --hard`）。这是"一直在修"观感的根因修复，但改动部署安全机制风险高，需与用户确认后再动。
- 验证程序新揪出 3 个真 WARN（非阻断）：`main_stock.json 结构校验未通过`、`金股池 47/108 只不满足 signal≥2 或研报覆盖`、`mahoro_signals.json 被报缺失`（实际本地有，仅被 .gitignore 排除，验证程序在错误路径查找）。

## 2026-07-18 (周六) 21:30 — 根因修复已落地并推送 (commit 7c74da82)

- **根因确认并修复**：「lock 机制覆盖同期其他提交」= `_fix_unmerged_files()`（deploy_now.py:405）和 `sync_check.py:git_sync()` 在检测到任意 `.json` 含冲突标记时一刀切 `git reset --hard origin/main`，丢弃所有未推送的本地修复 commit。
- **修复方案**（最小改动、保留部署安全网）：
  - 改为精准还原「origin/main 中存在的已跟踪冲突文件」(`git checkout origin/main -- <file>`)，保留其他本地提交；
  - 仅**未跟踪文件含冲突 / 真·未合并状态**才回退整体 `reset --hard`（最后手段）。
  - deploy_now.py 与 sync_check.py 两处同步修改。
- **已编译校验** (py_compile 通过) + **已推送 origin/main** (`0db3b49a..7c74da82`, push exit 0)，`7c74da82` 为干净可达 commit（不再是游离 commit）。
- **关键闭环**：修复已在 origin/main，故即使未来部署走到兜底 `reset --hard`，也是重置到含本修复的 origin/main → 吞 commit 环路彻底切断。
- **环路/逻辑审计**：_fix_unmerged_files 单遍执行、os.walk 已剪枝无递归死循环、无新增 while；sync_check.git_sync 单遍未合并处理+pull；_acquire_deploy_lock 仅 1 次重试。均无新增无限循环风险。
- **全仓剩余 reset --hard**：仅 backup_* 归档副本与本次新增的「最后手段」兜底分支（正确保留）。
