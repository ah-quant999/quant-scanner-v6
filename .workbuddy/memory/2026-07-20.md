# 2026-07-20 工作日志

## 盘前自动化调整
- 08:43 用户说「08:50自动化已停、本机9:20开始跑」→ 已 PAUSED `小九-盘前全量抓取(推main)@08:50`（id: automation-1784254984669）
- 08:48 用户同意停云端对应重叠任务 → 已删除 cloud_intraday.yml 的 09:00 cron 行+相关步骤+deploy if 条件。提交 86809d4 已推 main。
- 云端保留：10:31/11:46/13:31/14:31 四个盘中 slot（含完整数据抓取+扫描+构建+部署）
- 本机盘前主力保留：09:14 全盘扫描 + 09:20 独家部署（均为 ACTIVE）

## 09:20 盘前部署（小九独家部署）

- `batch_update.py pre_market` 执行约22min45s
- git pull → 10步流程 → 重试失败步骤 → 交接日志
- build_candidate_pool.py 两次超时（家用机网络），scanner.py 重试成功
- **deploy_now.py --force 部署成功 ✅**（72.0s）
- 网站验证通过：标题"九宝量化 v6.0 (20260720081542)"正常加载
- 双机同步检查记录：07/20 09:38 UTC

## 09:39 用户确认保留「小九-本机中国源数据定时抓取(推main)」automation（id: 1784174169718）
- 用户问是否可删（以为双机已覆盖）。查证结论：**不可删**。
- 证据：cloud_intraday.yml L91-92 注释明确「中国数据源美区必60s超时，已全部移出云端，改由本机双机(小九/阿狸咪 :40 自动化)在中国网络抓取并push main」。本机 :40 正是云端依赖的中国源数据来源。
- 双机其它任务（09:20盘前/19:30收盘 via batch_update）只覆盖部分脚本或仅盘前/收盘；盘中(09:40-15:40)这15个中国源文件只有 :40 每小时刷新。删→盘中冻结在早盘值。
- 用户决策：**保持现状，不删**。后台 :40 任务继续运行推送盘中数据。

## 10:31 审计 cloud_intraday.yml 09:30 改动
- 审计：13个引用脚本全存在；run_intraday_scan --skip-standalone / report_heartbeat --role cloud --mode cloud_intraday / ensure_standalone_sync --inject-buildstamp 参数均正确；YAML 有效
- 发现 BUG：删 09:00 cron 时部署闸门 if:schedule!='0 1 * * *' 被一并删，09:30 无脑部署 → 云端09:30首次pull只拿到小九09:38推main前的旧中国源，部署时覆盖小九今日数据(即"旧数据"根因)
- 修复：构建前端前加 `git pull --rebase origin main` 再同步一次(commit e4007e9 已推 main)
- 结论：09:30 盘中扫描脚本链完整可用，覆盖冲突已堵住

## 11:27 前端去重修复
- 「绿灯·可操作」卡片内，lightText（L451112）已含 危机雷达/北向净流入/市场温度 三个数据点；但 L451113-45117 又以详情行重复了一遍同样的三个值。
- **已删除 L451113-45117 重复详情行**，保留 lightText 一行即可。index_master.html 直接改。

## 14:06 候选池看门狗（automation-1784524253654）执行 + 覆盖陷阱修复
- 检测候选池陈旧（update_time 11:29:08，~157min>60min）→ 触发重建。
- **踩中覆盖陷阱**：首轮 build 成功(14:11:08) 但 `deploy_now.py` 的 `reset --hard origin/main` 把新鲜候选池回退成远端陈旧版(11:29:08)，首次部署推了陈旧数据。
- **手动补铁律**：重建新鲜(14:23:39) → 先 `commit+push data/candidate_pool.json` 到 main → 再 `deploy_now --force`。线上核验通过：gh-pages candidate_pool.json update_time=14:23:39，353只。
- **根因修复（脚本补丁）**：`watch_candidate_pool.py` 原流程 build→update_data_v2→deploy 违反"先 push main 再部署"铁律。已在 `_rebuild_and_deploy()` 重建后插入 `_sync_candidate_pool_to_main()`（推候选池到 main，non-fast-forward 时 pull --rebase 重试，失败则跳过部署），脚本已提交推送 main。下次看门狗自动生效。
- 清理：删除本次 2 个 git stash（1临时+1 deploy 空 autostash）；未删任何 data/*.json/backup。工作树 65 个 M 文件为并发定时任务 live 改动，未触碰。

## 16:31 盘后刷新部署自动化（automation-1784516583790，首次运行）
- 执行：git pull --rebase → 第一组9个fetch（全OK，最慢 fetch_lhb ~2.5min）→ 第二组7个更新/生成（6 OK；generate_one_look.py 失败 rc=2=本仓库不存在，跳过；update_multi_resonance_daily 无≥70分跳过）→ update_data_v2.py RC=0 重建前端 → deploy_now.py --force RC=0。
- 部署遇 main non-fast-forward（云端已推），脚本自动 reset --hard origin/main 清冲突→重拉模板→`update_data_v2.py --fast` 重建→推 gh-pages（Build stamp 20260720164512，92文件）。锁 LEMONCAT，心跳上报成功。
- **线上验证通过**：标题"九宝量化 v6.0 (20260720164512)" = 2026-07-20 16:45:12，与今日一致。
- 整轮约25分钟（东财/同花顺家用机限流）。页面健康看板 crds_result.total_scanned=0 等为既有 WARN，非本次故障。

## 17:11 候选池看门狗（automation-1784524243949）执行 + _run 三参 bug 修复
- 当前 17:11 已过交易时段(>15:00)，看门狗按设计跳过重建，exit 0（安全）。
- **发现并修复回归 bug**：上轮16:13记录称已给 `_run` 增加可选 `label` 参数，但文件实际未落地——`def _run(cmd_list, timeout=300)` 仍两参，而 154/165/173 行以三参(`label`字符串)调用。盘中真正触发重建时会 `TypeError` 崩溃（在 `_rebuild_and_deploy` 调用点抛出，未被 `_run` 内部 try 捕获），使看门狗整体失效。已真正修复：`_run` 改为 `def _run(cmd_list, timeout=300, label="")` 并在有 label 时打印执行日志。
- py_compile 通过；3-arg 调用实测能正常 spawn 子进程（已手动终止避免非交易时段误跑重构建）。
- 纯修复，未删除任何数据源/备份文件。

## 17:40 收盘一段兜底补跑（automation-1783525364826，阿狸咪本地）
- 周一交易日，执行三步全通过。部署 URL: https://ah-quant999.github.io/quant-scanner-v6/（Build stamp 20260720174914，head=6e7773ea8803）
- **踩坑：git stash 冲突残留** — 当天工作树留有一批合并冲突标记：`data/gold_pool.json`(line 11921) + 7个 `standalone/*.html`(gold/query/predict/overview/shmonitor/health/index)。
  - batch_update close_p1 里 fetch_maharo_signals.py 读 gold_pool.json 报 `JSONDecodeError: Expecting property name...line 11921`（`<<<<<<< Updated upstream`/`=======`/`>>>>>>> Stashed changes`）。手动保留新 update_time(17:30:59)删标记 → JSON校验通过(376候选) → 重跑 maharo 成功。close_p1 最终 26/28（唯一失败即此项，已修复）。
  - deploy_now --force 自愈了 standalone/*.html 的7个冲突（精准还原+reset origin/main），无需手动。
- update_data_v2.py：两轮宏观(17:46/17:47)+全模块回写成功，冒烟测试2个非致命括号警告。
- **通用教训**：遇 JSONDecodeError line xxxx 先 `grep -nE '^(<<<<<<<|=======|>>>>>>>)' 文件` 查冲突标记；gold_pool 冲突保留新时间戳版。
