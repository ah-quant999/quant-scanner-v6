# 2026-07-27 安全修复（凭据泄露闭环 + 密码暴露消除）

## 已完成
1. **main 历史擦除泄露文件**：`git filter-branch --index-filter 'git rm --cached --ignore-unmatch dist/data/.wc_jwt_cache.json dist/data/maharo_signals.json' -- main` 重写历史，随后 `git push origin main --force`。`origin/main` HEAD = `965991f3`（forced update from bb575445）。`git log main --` 已无泄露文件。
2. **密码明文暴露消除**：
   - `index.html` / `index_master.html` 明文 `cat999`/`hjd666` → 占位符 `__PWD__`/`__GUEST_PWD__`。
   - `deploy_now.py` L968 注入段改为从 env `QB_PWD`/`QB_GUEST_PWD` 或本地 gitignored `.site_pw.json` 读取，**fail-closed**（无则不注入明文默认）。
   - `update_data_v2.py` 删除根模板密码注入（只生成占位符模板），真实注入统一由 `deploy_now.py` 在 dist 副本完成。
   - 生成 `.site_pw.json`（mode 600, gitignored, 未提交）含新强密码（admin/guest 各 16 位字母数字）。
3. **safe_pull 改 reset --hard**：`git_safe_sync.py::safe_pull` 从 `git pull --rebase` 改为 `git fetch` + `git reset --hard origin/main`，以支持 main 历史被 force-push 重写后双机干净对齐，避免旧历史 rebase 回灌。

## 关键坑
- 本轮 Edit 对 `deploy_now.py`/`update_data_v2.py`/`index_master.html` 的改动一度未落盘（疑似上轮 `git reset --hard` 后工作树状态问题），后用 python 精确重写 + 同脚本内验证才生效；`index.html` 改动此前 Edit 已生效。教训：改大文件后用独立 grep 验证工作树内容，勿轻信 Edit 的 "Successfully edited"。
- `git filter-branch` 要求干净工作树，先 `git stash push -u` 暂存 memory 改动，重写后 `git stash drop`（memory 稍后重写，不污染历史）。
- 校验"历史无泄露文件"须用 `git log main --`（分支），勿用 `git log --all`（会含 `refs/original/` 备份误报）。

## 待小九侧执行
- 小九机器一次性 `git fetch origin main && git reset --hard origin/main`（拿新 safe_pull + H'）。
- 小九仓库根放 `.site_pw.json`（含新密码），否则部署 fail-closed。
- 详见 `HANDOVER_阿狸咪_2026-07-27_晨.md`（本地未提交）。

## 新密码存储
- 本机 `.site_pw.json`（gitignored）。值勿写入公开仓库/记忆。


## 三重共识顶栏对齐（07-27，金股观测 ↔ 历史追踪）
- 问题：金股观测页 renderTop10Daily 比历史追踪页 triple_consensus.html 多显示「观察(70≤评分<75)」分组，两页口径不一致（主人截图指出对应不上）。
- 修复：index_master.html renderTop10Daily 删除 observation 分组（定义行、空状态判断、renderGroup 调用三处），只保留 重点关注(3/3 严格)+优先观察(主站≥75)，与历史追踪页对齐。
- 提交：源码 90370ac5；dist 同步 bb2320af / c09e9456（origin/main + gh-pages 全层 `观察（2/3 候补 · 评分 ≥70）` 计数=0，已验证）。
- 踩坑：auto-sync 会在部署后把本地 dist/ 回退成 origin/main 旧版（count=1）；用"重建+校验+commit+push"单进程原子脚本(_push_dist2.py)才把 origin/main dist 刷成最新。勿手写 git checkout 覆盖 dist。
- 独立页 triple_consensus.html 此前已隐藏观察组，本次未动。
- 已写 HANDOVER_阿狸咪_2026-07-27_周末汇总.md 汇总周末改动发给小九（不含明文密码，引用 .site_pw.json）。
