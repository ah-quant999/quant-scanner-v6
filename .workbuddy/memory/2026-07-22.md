# 2026-07-22 工作日志

## 23:15 ③+⑤合并 + ⑧回测修复 + 收盘后close_summary
- ③和⑤合并为1张：共振候选股列表下面直接显示止盈止损表（删掉⑤独立卡）
- ⑧回测卡修复：去掉 early return 守卫 + 加 8s 超时安全网（卡住后显示重试链接）+ data-fetched 标记
- close_summary: 新增收盘后汇总快照 + AI速览/ETF卡片 15:00后显示"📌 收盘后汇总"标签
- commit 128b93d7 push main + deploy gh-pages（进行中）

## 22:48 卡片位置调整（用户授权）
- 4张图要求：①成交金额→数据监控·涨跌家数前面 ②两融+宽基ETF→数据监控最下面（并排同宽）③中信期货→总览与ETF/板块并排3列
- 已下架对应4个原位改为占位注释
- commit c3312017 push main + deploy gh-pages（进行中）

## 22:40 上交所大盘资金流时间轴（用户授权·暂未上架）
- 用户看到图片后要求同款图表（上交所每日主力净流入+累计折线）
- 数据源：东财 push2his fflow 接口（secid=1.000001 上证指数） + push2 兜底今日
- fetch_market_fund_flow.py：新脚本，双源降级（push2his→push2→保留旧数据），增量合并
- 前端：ECharts 5.4.3 双Y轴 → 柱(单日净流入,红正绿负) + 折线(累计,粉色). 60日/120日/1年/全部 tab + dataZoom slider
- 集成：batch_update.py pre_market 并行组、update_data_v2.py marker 注入
- 当前种子数据：1天（2026-07-22 真实 -29.59亿），网络恢复后自动累积历史
- 已 push main (0b0dcc6a) + deploy gh-pages（进行中）

## 22:35 基本面质量分A级评分优化
- 用户反馈：7月21日接入基本面A=40后，前9只A级全部冲上80+(最高98)，但其中8只在下跌（20日跌幅-14%~-47%）
- 回测：7月21日接入基本面前TOP10最高分仅53，加A=40后跳到98，权重过高
- 修改：A级从固定40分改为基准15分+roe/营收浮动(15~25分)
  - roe≥20%+2、≥30%+5；营收≥100%+3、≥200%+5
  - B/C/D维持原值(5/0/-10)不变
- 结果：最高分78，无80+假高分，A级内部自然分化（香农芯创营收377%得20、佰维roe21%得17、普通A级15）
- commit 49e6349 push main

## 22:00 模型配置规则调整
- 按用户要求：9-11点任务用 `hy3`，其余全部 `deepseek-v4-flash`
- 变更6个ACTIVE自动化：模型审计(09/19)→hy3、紧急指令监听→hy3、周六T+1→hy3；盘中补跑12:31/14:16/15:16→deepseek-v4-flash
- 另改2个14:30/18:30交接→deepseek-v4-flash
- 累计18个ACTIVE自动化全部校验通过 ✅

## 21:10 本地备份兜底（自动化 backup）
- 直接执行不检查云端
- 双机代码同步 ✓（19.3s）+ enhanced_backup.py ✓（92.2s）
- 1/1 全部通过，交接日志 `ALIMI backup ✓` 已写入
- git pull 干净无更新。全组16个脚本依次执行：
  - **fetch 组9个全OK**：lhb(95只龙虎榜)、52w_high(183只)、sh_index_fib/sz_index_fib(双指数斐波那契)、suspension_alert(7停牌+47涨停)、fomc(利率4.5%,下次7/29)、margin(60天)、sh_sz_history(60天)、concept_ranking(374概念,40涨跌排名)、sector_fund_flow(东财不可用→P0本地累加107个板块真实5日)
  - **更新组**：triple_resonance(scan_data缺失,历史304只)、multi_resonance(跳过,无≥70)、calc_crds(165只→143有效)、generate_recommend(废弃跳过)、generate_top10(20只,TOP1-2各98分)、generate_one_look(不存在,RC=2跳过)、cockpit_backtest(11信号,胜率50%)
- update_data_v2: 全量重建 build-stamp 20260722163911 → 部署再建 20260722164039
- deploy_now.py --force: ✅ 成功→gh-pages(97文件), hb_xiaojiu 16:41:09, 心跳已写
- WebFetch 验证：标题「九宝量化 v6.0 (20260722164039)」=今日 ✓

## 14:31 盘中刷新部署（intraday_14_31 自动化）
- git pull 干净无更新。run_intraday_scan.py --skip-standalone: 9步全✅（14:26→14:39，约12.5分钟），无失败。
- update_data_v2.py: 前端重建 build-stamp 20260722144208，index 2,851,418 字符；冒烟 4 处历史括号告警但继续。
- deploy_now.py --force: ✅ 成功，gh-pages build stamp 20260722144402，head df773c0f9516，独立页同步校验通过，hb_xiaojiu 上报 14:44:31。
- 心跳：`2026-07-22 14:45:13 | xiaojiu | intraday_14_31 | DONE`。
- WebFetch 验证：标题「九宝量化 v6.0 (20260722144402)」=今日；盘中卡片均 07/22（概念14:37/板块14:37/ETF14:37/成交14:38/两融14:39/监控14:34）。健康看板部署 07-22 14:44 ✓。

## 13:31 盘中刷新部署（intraday_13_31 自动化）
- run_intraday_scan.py --skip-standalone: 9步全✅（13:31→13:45，exit 0），无失败；盘中数据源刷新至 13:3x（概念/板块/ETF/市场快报/成交历史/两融）。
- update_data_v2.py: 前端重建（CLOUD_UPDATE_TIME=2026-07-22 13:48，build-stamp 20260722134837）。冒烟报 4 处括号不匹配告警但继续部署。
- 防 dist 回退: 先 commit+push 新鲜 dist/ + 3个data文件 到 main（e696fd67..c4a464c7）再 deploy_now.py --force 成功（gh-pages build stamp 20260722135048，推 97 文件，hb_xiaojiu.json 上报 13:51:22）。
- 心跳：`2026-07-22 13:51:58 | xiaojiu | intraday_13_31 | DONE`。
- WebFetch 验证: 标题「九宝量化 v6.0 (20260722135048)」=今日；各盘中源时间戳 07/22 13:3x，健康看板部署 07-22 13:50 ✓。

## 13:49 candidate_pool 看门狗（半点自动化）
- 检测候选池 update_time=12:40:18（63分钟>60阈值）→ 陈旧，自动重建+部署成功（366只，13:44:47）。
- 线上核验：fetch gh-pages 后确认线上=13:44:47(366)，与本地一致（首次读旧版是 local remote-tracking ref 未刷新误判，非 dist-reset 陷阱）。
- 心跳已写 _heartbeat.log。未删除任何数据源/备份（纯修复型）。

## 09:40 本机中国源数据定时抓取 (自动化)
- 15个脚本：OK=11, FAIL=4（保留旧数据，属正常）
- fetch_market_alerts.py akshare 板块数据卡住，180s 超时 kill
- git push main 成功，9 files changed
- _heartbeat.log 已追加

## 09:45 模型回切 bug 修复（用户授权）
- 现象：.model_state.json 陈旧——hy3_available=false、hy3_429_count=9（7/21旧429），实际 hy3 当前可用（用户确认对话即用 hy3 无报错）
- 隐患：状态文件缺 switched_tasks 字段，quota_guard() 即使 429 清零也回切失败（restore_count=0，任务永久卡 deepseek-v4-flash）
- 修复：① 重写 .model_state.json（hy3_available=true、429_count=0、success_count=3、补 switched_tasks=24个任务ID）② automation_update 把全部 24 个任务 model_id 改回 hy3
- 验证：DB 模型分布已全为 hy3（24/24）
- 闭环：下次 09:00/19:00 审计若再连续2次429→切 deepseek-v4-flash（记录 switched_tasks），连续3次无429→切回 hy3

## 10:01 审计任务 prompt 注释更新
- 任务 1784623753813 原 prompt 末句"model_id 必须是 deepseek-v4-flash"与现状(hy3 优先)冲突，已改为"hy3(优先)或 deepseek-v4-flash(兜底)均可；禁用 ds-V4-FLASH 等拼错变体"

## 10:05 驾驶舱⑧号卡修复（用户授权）
- 现象：截图显示 ⑧ 号卡 stuck 在"加载回测数据..."，无数据；用户要求颜色改为浅紫丁香色。
- 根因：`renderBacktestTdx()` 中低样本警告 `html += ...` 写在 `var html = ...` 声明之前，触发 `ReferenceError: html is not defined`，被 `.catch(function(){})` 静默吞掉，DOM 未更新。
- 修复：① 将 `var html = '<div>...'` 提前到低样本警告之前；② ⑧ 号卡标题/左边框颜色从 `#c2185b`（与③号卡同色）改为 `#ba68c8`（浅紫丁香色）。
- 范围：index_master.html + dist/index.html + dist/index_master.html + standalone/*.html + dist/standalone/*.html 共 15 个文件同步修正。
- 验证：`data/backtest_tdx.json` 存在（211KB），修复后数据可正常渲染；grep 确认无残留旧色/旧顺序。

## 10:30 预判信号页(predict.html)卡片重排
- 用户诉求：盘后卡片沉底、盘中置顶；一眼结论"不需要就删、否则置顶"→ 用户选「保留·置顶」。
- 最终顺序：一眼结论(顶) → 板块资金三合一(盘中) → 涨停联动(盘后) → 机游共振(T+1)。
- 踩坑：第一版 reorder 脚本用 `text[k:k+5]=='</div'` 配平 div，丢失闭合 `>`（截成 `</div` + 孤立 `>`），文件损坏。修复：配平用 `text[k:k+6]=='</div>'` 含 `>`。
- 关键发现：git HEAD 的 `dist/standalone/predict.html` 仅 2.36MB（旧快照），工作副本 3.55MB（含近期卡片）。**勿从 git 恢复 dist**（会丢近期卡片）；用 `/tmp` 备份（本次重排前 cp 的 3.55MB 干净副本）恢复。
- `.gitignore` 忽略 `standalone/` 与 `dist/standalone/`，`git add` 须 `-f` 强制加（与脚本白名单同理）。
- 提交推送：`4b54084d`，2 files changed（dist 因 HEAD 陈旧差异大，顺带把 dist 拉到最新，修正了滞后）。
- 验证：panel-predict 四卡片 div 全配平、注释四行一一对应、dist 与 standalone 顺序一致。用户需 Ctrl+F5 硬刷。

## 22:35 基本面质量分A级评分优化
- 用户反馈：7月21日接入基本面A=40后，前9只A级全部冲上80+(最高98)，但其中8只在下跌
- 回测：7月21日接入基本面前TOP10最高分53，A=40权重过高
- 修改：A级从固定40分改为基准15分+roe/营收浮动(15~25分)
  - roe≥20%+2、≥30%+5；营收≥100%+3、≥200%+5
  - B/C/D维持原值(5/0/-10)不变
- 结果：最高分78，无80+假高分，A级分化（营收377%→20、roe21%→17、普通A→15）
- commit 49e6349 → cherry-pick到c3312017 → push main → deploy ✅ 线上验证通过
- 逻辑详解页更新(commit ace59a0d)：
  - standalone/guide.html + dist/standalone/guide.html 同步加入"基本面质量分"表格行
  - 总分公式：基础~100 + 增强±13 + 资金~15 + 板块~5 + 机构~10 + 基本面(-10~+25) = 上限~158
  - A/B/C/D各档细则、roe/营收调整规则完整列出
  - 主站(逻辑详解tab通过iframe加载)与手机独立页(直接访问)同步部署，gh-pages已生效

## 12:39 candidate_pool 看门狗 — 重建并部署成功（根治 dist-reset 陷阱已生效）
- 触发：半点自动化调度（本机小九）。候选池 update_time=2026-07-22 11:27:21，年龄 72 分钟 > 60 → 陈旧，启动重建。
- 流程：12:40:18 ✓ 重建(367只) → 12:40:24 ✓ 推 main(data/candidate_pool.json, 1文件) → 12:42:16 ✓ 前端构建 → 12:42:23 ✓ 推 main(dist/data/candidate_pool.json + dist/index.html + dist/index_master.html, 3文件) → 12:44:44 ✓ 强制部署 → ✅ 成功（总耗时 5m38s）。
- **关键：origin/main 的 watch_candidate_pool.py 已含 根治修复（`_sync_to_main` 在前端构建后把新鲜 dist 候选池+index 一并推 main），本次 `✓ 候选池已推送到 main (3 个文件)` 即该修复生效。deploy 的 reset --hard 回退到的 origin/main dist 已是新鲜版，未再踩 07-22 11:24 的 dist-reset 陷阱。**
- **线上核验通过**：`git fetch origin gh-pages` 后 `git show origin/gh-pages:data/candidate_pool.json` → update_time=2026-07-22 12:40:18（367只），与本地一致（首查显示 11:27:21 系本地缓存 ref 未 fetch，非真陈旧）。
- 心跳：`2026-07-22 12:46:12 | xiaojiu | candidate_pool_watchdog | DONE` 已写入 `_heartbeat.log`。
- 未删除任何 data/*.json 数据源或 backup_* 备份（纯修复型，无删除）。

## 17:00 候选池看门狗（整点自动化）
- 运行 `watch_candidate_pool.py`。当前 17:00 已过交易时段(>15:00)，看门狗按设计跳过重建，exit 0（安全）。
- 候选池 update_time=2026-07-22 14:47:16（收盘最终态），未触发重建/部署。
- 心跳 `17:00:45 | xiaojiu | candidate_pool_watchdog | DONE` 已写入 _heartbeat.log。
- 未删除任何数据源/备份文件（纯修复）。

## 17:57 候选池看门狗（整点自动化）
- 运行 `watch_candidate_pool.py`。当前 17:57 已过交易时段(>15:00)，看门狗按设计跳过重建，exit 0（安全）。
- 候选池 `update_time=2026-07-22 14:47:16`（收盘最终态，~3h 前），未触发重建/部署。
- 心跳 `17:57:04 | xiaojiu | candidate_pool_watchdog | DONE` 已写入 _heartbeat.log。
- 未删除任何数据源/备份文件（纯修复）。

## 23:32 国家队ETF/AI速览卡片时间戳缺失修复
- 用户反馈：国家队ETF卡片显示"更新于 今日 (盘后更新)"，没有时分（15:30）
- 根因：CLOSE_SUMMARY.update_time 是 ISO 格式 `2026-07-22T15:30:00`（含 T），
  但 fmtDataTime 按空格分隔解析，导致 timePart 为空 → "今日 "（无时分）
- 修复：调用 setPostMarketTime 前先 .replace('T', ' ') 标准化
  - 涉及 2 处卡片：国家队ETF (ntEtfFlowTime) + AI速览 (aiSummaryTime)
  - index_master.html + dist/index.html + dist/index_master.html 同步
- commit eb0abdcc push main → deploy ✅
- build-stamp 20260722233632 gh-pages 已生效

## 23:39 驾驶舱3处问题修复
- 用户4个问题：
  1. "三维共振和三线的历史追踪打不开了"（image 1 显示涨停联动卡内容空，未深查）
  2. "驾驶舱这个叫合并？都是香农芯创，写在一行不行"（紫卡交易计划重复显示）
  3. "序号5没了就把6改5，后面的也顺序改"（驾驶舱卡片编号⑤空缺）
  4. "依旧空着！一天白等了"（⑧ K线信号60日回测卡 stuck "加载回测数据"）
- 修复：
  - 序号重排：⑥→⑤、⑦→⑥、⑧→⑦（⑤空缺跳过）
  - 合并紫卡交易计划到③列表（一行展示 名称/分数/止盈/止损/胜率/信号）
  - K线回测卡：fetch 加 AbortController 20s超时 + 安全网延长到15s
- commit ce9f9c72 push main → deploy ✅
- build-stamp 20260722234706 gh-pages 已生效
