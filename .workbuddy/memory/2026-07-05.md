# 2026-07-05 周日

- 15:25 盘中自动化 post_close：batch_update.py post_close 执行完成，10/10 全部成功，3.7 分钟。
- 16:12 修复 worldcup 数据 + 独立页：data/worldcup.json 的 groups 字段从 true 恢复为数组；extract_panels_v6.py 扩展 PANELS 列表支持 worldcup 和 guide；重新生成全部独立页并部署。
- 19:57 根因定位：deploy_now.py 的 _regen_standalone_if_needed() 只运行 extract_panels_v6.py 但不复制 standalone/ -> dist/standalone/，导致 gh-pages 上 standalone/ 是旧版本。已修复 deploy_now.py 添加自动同步步骤。
