# 2026-07-07 工作日志

## 21:00 定时备份任务
- 执行 `python batch_update.py backup`，成功（19.4s）
- 备份目录 `backup_20260707/data/`，50+文件已生成

## 22:00 更新逻辑详解页定时任务映射表
- **文件**: `index_master.html` (renderUpdateSchedule JS函数) + `standalone/guide.html` + `dist/standalone/guide.html`
- **变更**: 定时任务从 11个 → **15个**
  - 原19:30大任务拆为三步：17:30收盘数据抓取 → 18:30收盘扫描生成 → 19:30最终部署
  - 21:00独立为纯备份任务
  - 新增 周五19:30 每周股票名称更新
  - 新增 周二07:30 T+1数据刷新
- 铁律调度表同步更新
- 已提交推送 main (b09074ad)
- 注意：**不得暴露源码脚本名、数据源名称**（铁律第二条）

## 22:38 前端覆盖完整性核查（用户追问"还有什么前端没定时任务/部署跟踪"）
- **方法**：用 `data/` 目录47个数据文件作"任务产出物指纹"，对比前端区块与更新时间标记统计
- **结论1（定时任务更新）**：基本全覆盖。47个数据文件对应所有主站模块。**之前写入guide.html的15任务映射表有描述遗漏**（非系统真缺口）：危机监测、斐波那契窗口、中信期货、精选预判信号、重要日历、健康看板、AI市场速览等模块底层都有数据文件在更新，只是映射表归类时未显式列出 → 该映射表需补全以免误导
- **结论2（部署跟踪=更新时间标记）真缺口**：
  - `multi_resonance.html`（多维共振独立页）：**0个"更新于"时间戳标记**（数据有更新，但页面无新鲜度显示）← 最该修
  - `worldcup.html`：仅页脚1个"最后更新"
  - `live_page.html` / `prototype_limit_up_heatmap.html` / `zsxq_debug.html`：0标记（辅助/调试/原型页，非用户主用，可忽略）
- **结论3（规划中）**：全球危机监测独立页面——目前主站区块复用 `macro_data.json` 的 global_macro 子结构（有任务更新），但无独立任务+独立页面（记忆提及用户计划新增）
- 待办：①补全guide.html映射表遗漏模块 ②给multi_resonance.html加"更新于"时间戳（需用户授权）

## 23:1x 部署修复 + 三页对齐 + 小九交接（用户睡前最终交付）
- **三页定时任务映射表对齐（业务语言，不暴露脚本/数据源）**：
  - `index_master.html` renderUpdateSchedule（15卡片，含危机监测/斐波那契/中信期货/预判信号/全球危机/重要日历/AI速览）
  - `standalone/guide.html`（逻辑详解页）静态15卡片
  - `standalone/health.html`（健康看板）15卡片
  - 根目录 `multi_resonance.html` 模板 + standalone 副本 均加 `dataTimeLabel`（"📅 数据更新于"时间戳）
- **部署失败根因与修复**：
  - 现象：gh-pages 推送失败 `ssh: connect to host ssh.github.com port 443: Connection timed out`（443隧道偶发超时）
  - 修复：本机 `~/.ssh/config` 由 `github.com→ssh.github.com:443` 改为直连 `github.com:22`（家里网络允许22端口，单位小九仍用443不受影响）
  - 校验：`origin/main` 与 `origin/gh-pages` 经 git 直查均确认 15卡片内容到位（raw CDN 抓取有误判，以 git 直查为准）
- **最终交付**：`python deploy_now.py --force` 成功（commit 689aa1a5, deploy 23:14），三页+时间戳全部上线
- **小九交接**：已写 `交接文档_20260707_给小九.md`（变更清单+部署状态+SSH改动范围+白天注意事项）

## 关键教训（沉淀）
- guide.html 非 extract_panels 生成（不在 PANELS 列表），是仓库提交的静态文件；部署 `_ensure_dist_fresh` 从 origin/main 拉取，故 guide.html 改动必须 commit+push 到 main 才能存活，直接改 standalone 副本会被拉取覆盖。
- 验证线上部署应以 `git show origin/gh-pages:路径` 直查，raw.githubusercontent / Pages CDN 有缓存误判。
