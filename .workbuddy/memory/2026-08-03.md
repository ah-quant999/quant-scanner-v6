# 2026-08-03 工作日志

## 07:01-07:10 兜底体系审计 + 长期架构决策
- **审计结论**：阿狸咪本机 23 个 automation 中 22 个已 PAUSED，仅剩 3 个 ACTIVE；全部 9:00-19:00 工作日的兜底已由 GitHub Actions 云端 `v8_safety_net.yml`（每 30 分钟查 raw_data > 2h stale → dispatch v8_cn_fetch 全量补跑）承担。夜间 19:00-次日 08:25 有 9 小时空白、周末仅周日 09:00 一次全量兜底。
- **每次更新耗时实测**：v8_cn_fetch 盘中 3-8 分钟（timeout 20）、盘后全量 5-15 分钟；v8_algo_run 盘后算法链 15-30 分钟（timeout 120）；v8_build_deploy 构建+推 Pages 5-10 分钟（timeout 15）；盘后端到端 18:30 触发 → 用户看到新版约 20-40 分钟。
- **架构决策（主人拍板）**：彻底改为「小九单机(单位机) + 云端」长期架构，**阿狸咪即使恢复联网也绝不接手任何生产任务**，避免双机重复麻烦。
- **执行动作**：
  1. PAUSED `automation-1785510427927`（v8 周末云端监督与兜底部署，阿狸咪本机生产任务）。
  2. 更新 MEMORY.md 排班铁律 → 锁定"阿狸咪不接手"长期架构。
  3. 更新 HANDOVER_小九_2026-08-03.md 风险段 → 小九无需担心双机冲突；若阿狸咪误上线立即 PAUSE 她本机 automation。
  4. commit `d8ea7e0` push v8/main，本地与 origin 同步。
- **保留**：`automation-1783696499491`（阿狸咪紧急指令监听）作为主人兜底呼叫通道，监听≠接手生产，不影响小九。
- 待主人决定是否走"方案 B"（加夜间 4 次 safety_net + 周六 21:00 盘后兜底 + 阈值 2h→1h），本次仅商讨未实施。

## 后续 v6 全退役（主人指令：v6 全部停运，定时任务也删除，只留 v8）
- **本机 automation**：soft-delete 19 个 v6 生产定时任务（盘中补跑/收盘部署/备份兜底/看门狗/IPO/周度元数据/心跳监控等）。剩余 ACTIVE 仅 2 个纯 v8：1783696499491(紧急指令监听) + 1783523003845(自动交接检查19:30)；1785510427927(v8周末监督) 仍 PAUSED。
- **v6 仓 GitHub Actions**：删 9 个 production workflow（cloud_backup/cloud_data_fetch/cloud_intraday/cloud_post_close/cloud_scanner/cloud_self_heal/cloud_weekly/safety-net/test_data_source），仅留 pages.yml.disabled。**pre-commit 钩子按 DO_NOT_DELETE.md 拦截删除 → 先改清单移除 `.github/workflows/*.yml` 保护行再提交**（铁律）。
- **commit `6a475241` push v6/main** → v6 全部云端定时任务下线。
- 残留：`hb: cloud cloud_weekly` 是删文件前已在途的最后一趟残跑（不中断在途 job）；heartbeat-bot 仍可能推 `data/hb_cloud.json`（纯存活 ping，非数据生产，无害）。
- 结论：v6 数据生产 + 定时任务全停；系统完全依赖 v8 仓（中国节点 + 云端 Actions）；小九单机顶双机长期架构锁定。

## 20:08 本地预览部署（主人固定地址）
- **需求**：主站最新、本地 v8 旧（停在 d8ea7e0/08-02），主人要用固定 `file:///E:/workspace/quant-scanner-v8/index.html` 本地预览。
- **操作**：本地 v8 仓库 `git fetch` + `git merge --ff-only origin/main` → 快进到 `7743d1f`（08-03 18:38，主站真相源）。无受控文件改动（仅 81 个未跟踪临时脚本未动），ff-merge 不丢任何东西。清理临时 worktree `quant-scanner-v8-live`。
- **结果**：`index.html` 573KB、`data/` 51 个 `.js`、RISK_GAUGE/AI_MARKET_BRIEF/RUNNER_STATUS 三大新功能数据齐全。固定路径已是主站最新 → 主人可直接浏览器打开 `file:///E:/workspace/quant-scanner-v8/index.html`。
- **注意**：此举只动本地受控树（快进），未 push（origin/main 已由 CI 推送，无需再推）、未改任何代码、未触发部署脚本。

## 20:08 附：08-03 v8 新版审核结论（待小九修复）
- ✅ 代码质量全过：内联 JS(21块/396KB) node --check OK；新增 Python(fetch_risk_gauge/v8_cloud_watchdog/update_v8) py_compile OK；新增数据 JS 语法 OK；前端无真断链（crds_result/ipo_score 是正则误匹配 .json 前缀）。
- 🔴 **真问题**：v8 盘后算法类数据 08-03 周一未刷新，全停 7-31~8-02：gold_pool/triple_consensus→08-02 17:54、lhb_data→08-01 07:15、cockpit_advice/maharo→08-01 15:36、inst_trade→07-31 17:45、sector_rs→07-31 17:28、candidate→07-31 18:30。盘前/盘中类(ETF/INDEX/板块资金)正常到 08-03 13:30。**判定**：v8 盘后算法链(close_p2 / v8_algo_run / step_v8_self_sufficiency)今日未成功执行，待小九查 18:30 那轮是否触发或算法脚本报错。

## 20:13 全面定时任务审计（按主人时间预期逐前端核对）
- **方法**：抽 12 个 workflow 真实 cron + index.html 自带排程表(doc 3543-3601) + 各 data/raw_data update_time 交叉比对。
- **结论① 配置层：✅ 全部对齐，无遗漏、无孤儿前端。**
  - 今日数据页 → `v8_cn_fetch` 08:25(UTC00:25 盘前，<9点 ✓)
  - 实时数据页 → `v8_cn_fetch` 09:40/10:00/10:30/11:00/11:30/13:00/13:30/14:00/14:30/15:10(UTC 01:40~07:10) + 15:30(收盘 ETF_DAILY_MONITOR/EXPERIMENT)；`v8_risk_gauge` 每30分(北京08:00-16:30 & 19:00-22:00)；`AI_MARKET_BRIEF` 由 gen_market_brief.py 随 cn_fetch 生成
  - 盘后/选股/共振 → `v8_algo_run` 18:30(UTC10:30 算法链) + `v8_algo` 17:00(UTC09:00 体检)
  - 部署 → `v8_build_deploy` 由 push(raw_data/**,index.html) + workflow_run(cn_fetch,risk_gauge) 自动触发，每个前端都覆盖
  - 孤儿核查：index.html 引用 50 模块全有生产者；crds_result.js/ipo_score.js 是正则误匹配(.json 兜底路径)，主源 CRDS_CARD_DATA.js/IPO_DATA.js 均存在
- **结论② 执行层（今日 08-03，截至 20:13）：🔴 盘后/选股/共振页全停滞 raw_data 08-01~08-02。** gold_pool/triple_consensus→08-02 17:54、lhb_history→08-02 19:05、lhb_data/sz_fib/sh_fib→08-01 07:12、triple_track/top10_daily/crds→08-01、triple_history/mahoro/cockpit_*→08-01 15:36、candidate→07-31 18:30、sector_rs→07-31、inst_trade→07-31 17:45、sector_fund_flow_trend→07-31 23:02。18:30 `v8_algo_run` 今次未产出新数据（或仍在跑，timeout120 最晚 20:30 出结果）。
  - ⚠️ `VOLATILITY`(今日数据页模块) raw_data 08-01 17:45 也 stale；`INDEX_QUOTES` raw_data 08-01 12:57（wrapper 10:11）。其余今日/实时页正常(08-03 10:11~13:30)。
- **关键提示**：`data/*.js` 的 update_time 是"打包部署时间"非"数据生产时间"——今日 10:38 那次 build_deploy 把全部 stale raw_data 重新包装，导致盘后页显示"更新于 10:38"却是 08-01/08-02 旧数据（新鲜度标签有误导）。
- 待小九：查 Actions 中 `v8_algo_run`@18:30 运行状态；20:30 后复看 raw_data 时间确认是否只是延迟。

## 20:14 UI 调整：极端风险联动策略卡归入「实时数据」
- **主人指令**：截图中的「极端风险联动策略」也是实时数据，移到「实时风险温度计」卡下方。
- **执行**：`E:\workspace\quant-scanner-v8\index.html` 中把 `riskLinkageCard` 从「盘后数据」区（原 line 1931）切到「实时数据」区 `riskGaugeCard` 正下方；标题由「盘后决策框架」改为「实时决策框架」，注释同步更新。
- **状态**：仅本地修改，未 commit/push（按 UI 改动先本地预览、等主人说「上线」再部署的规矩）。
- **旁线**：northbound-calendar 北向 workflow（run 30811623319）仍在 `in_progress`（已跑约 24 分钟，卡在 step6「Run daily update and push」），继续监视。

## 20:22 northbound-calendar 根因定位 + 修复（主人指令：算好部署后查因→修复→汇报）
- **主人提问**：前面手动触发是为什么？算好部署后查因修复汇报。
- **澄清**：手动触发的是 `northbound-calendar` 仓独立 workflow（生成 daily-insight.html 那个独立站），**不是 v8 盘后数据**。v8 盘后数据 08-03 停滞是另一回事（v8_algo_run 未跑，待小九查）。
- **为什么手动触发**：4 个自动更新 workflow 在 2026-07-30 09:15 被 `disabled_manually`，页面从 07-29 起停滞；手动触发是为了立刻刷到最新交易日，之后自动更新接上。
- **根因①（07-29 失败→07-30 被禁用→页面停滞）**：**并发 push 冲突**。北向/机游/`important_monthly`/`t1_morning_fallback` 共 4 个 workflow 都往 `calendar-pages` 同一分支推；07-29T10:42 北向 dispatch 与机游定时 run 并发，互相 `! [rejected] ... fetch first`，连撞 `MAX_RETRIES=3` 后 exit 1。log 实证：13:16/14:52/16:32 三次「提交成功→推送被拒」。
- **根因②（08-03 我手动 dispatch 时卡死）**：① 我 11:58 同时 dispatch 北向+机游造成并发冲突；② 更关键：workflow **无 `timeout-minutes`**，东财接口偶发卡顿时 run 无限挂起——正常 8 分钟（07-29 11:50→11:58）跑完，08-03 那趟 30+ 分钟 `updated_at` 纹丝不动，确认 hang。已取消该 run（结论 cancelled）。
- **修复（已 commit+push calendar-pages，cb21409）**：4 个 workflow 全部加 `concurrency.group: nb-calendar-update`（串行化，后者排队，消除并发 push 冲突）+ `timeout-minutes: 30`（防无限挂起，失败后快速暴露）。
- **注意**：此仓 git push 走 HTTPS + Git Credential Manager，后台非交互 push 会卡在凭据提示；改用 `x-access-token:<PAT>@github.com` 直推成功。
- **重跑**：修复后单独 dispatch 北向（workflow 313634762, lookback=2），后台轮询 L9JzaX 监视完成+部署。
- **待北向部署后**：WebFetch 校验 daily-insight.html 数据日期已更新到 08-03。

## 21:00 选股策略页审计（铁证·API 实测）

**结论：选股策略页 / 盘后数据页 / 共振日历页 全部不是今晚算的——且 v8 盘后算法 CI 自动化自创建以来从未运行过。**

- **GitHub API 实测（api.github.com 通，沙箱可查）**：
  - `v8_algo_run.yml`（ID 324833339，盘后算法链唯一权威生产者，cron `30 10 * * 1-5`=18:30 北京，runs-on `[self-hosted, cn]`）→ **total runs = 0**（07-31 创建以来一次没跑）。
  - `v8_sync_v6_data.yml`（324813640，应急同步·已停用定时）→ 同样 **total runs = 0**。
  - 对照：`v8_cn_fetch`（同为 `self-hosted, cn`）= 30 runs（今日正常）、`v8_build_deploy`=103 runs（今日正常）。
  - `v8_algo_run` state = **active**（未被禁用），且当前 repo 无 queued 任务。
- **根因判定**：同一 `cn` 自托管 runner 能正常跑 cn_fetch，证明 runner 健康、在线；但 algo_run 的 **schedule 事件从未被 GitHub 调度器派发**（连 queued 都没创建）。即 GitHub 侧对该 workflow 的 cron 调度停摆——典型 GitHub 已知怪象：schedule 偶发失效，需 re-touch（再 push 一次 workflow 文件）或 `workflow_dispatch` 手动触发一次来重新注册。
- **为什么看起来"部分有 08-01/08-02 数据"**：那些（gold_pool 08-02 17:54 / top10·crds 08-01 15:29 / mahoro·cockpit 08-01 15:36 等）是 小九本地**手动**跑算法脚本+推送的残留，不是 CI 功劳。自 08-03 起连手动都没做 → 选股策略页最新数据停在 07-31（candidate）、07-31（sector_rs）、08-02（triple/gold_pool）。
- **修复建议（待小九/主人执行，本沙箱只读不写）**：
  1. **立刻补今晚数据**：在 GitHub UI/API 对 `v8_algo_run` 点 `workflow_dispatch` 手动触发一次（cn runner 在线，能立刻算完 push）。
  2. **根治**：编辑并 push 一次 `.github/workflows/v8_algo_run.yml`（哪怕只改注释），强制 GitHub 重新注册 schedule；或加一条冗余 cron。
  3. 长期建议：把盘后算法链从"纯 schedule"改为"`workflow_run`(依赖 v8_cn_fetch 15:30) 或 push(raw_data) 触发"，避免依赖单一易失效的 schedule。
- **影响范围**：仅 选股策略页 / 盘后数据页 / 共振日历页 三类（占今天审计的 3 个 page）；今日数据页、实时数据页（由 cn_fetch 产出）正常，与用户预期时间对齐无误。

## 21:30-21:55 v8 完全脱离 v6（跨仓依赖审计+切断）
- **审计铁证**：v8 算法链**本就自包含**，正常运营已无 v6 运行时依赖。v8 的算法脚本是带原生钩子的**增强版**（比 v6 新，含多处修复），`migrate_v6_algos.py` 明确警告"切勿重跑覆盖"。
- **真正缺口（仅 2 个，已复制进 v8）**：
  1. `algorithms/fetch_sector_fund_flow_westock.py` — westock 第三源拉取器，孤儿脚本 `fetch_orphan_sector_fund_flow.py` 引用但原缺失（之前静默跳过）。
  2. `raw_data/sector_fund_flow_history.json` — 板块资金流趋势历史累加器（144KB），原只存 v6，v8 冷启动会从 v6 复制；现内置 v8。
- **切断的 v6 路径字面量（改默认值指向 v8 仓内）**：`v8_cloud_watchdog.py`(token 候选由 v6 .workbuddy → v8 自身 .workbuddy)、`fetch_orphan_sector_fund_flow.py`/`fetch_orphan_suspension.py`/`run_algorithms.py`(V6_DATA_DIR 默认 → v8 路径)。
- **禁用仅有的主动拉 v6 工作流**（`if:false`）：`v8_sync_v6_data.yml`、`v8_sync_legacy.yml`（原定时已停用、仅留手动 dispatch；现手动也失效）。
- **交付**：v8 分支 `feat/v8-detach-v6`(commit bb17bf7) 已 push origin；紧急交接 `URGENT_阿狸咪_2026-08-03_v8脱离v6.md` 已 push v6 main(5486c212)。小九验证全链自包含后 merge 到 main，即可退役 v6 仓。
- **余留 stock-scanner 字面量（均非运行时）**：孤儿脚本文档注释 / `migrate_v6_algos.py`(迁移工具) / `sync_v6_to_v8.py`(V6_TO_V8 映射辅助，仅 stage_to_raw 导入、不读 v6) / `_patch_add_commodity_from_v6.py`(临时脚本)。
- **铁律（写入长期记忆）**：v8 算法任何改动只在 `quant-scanner-v8` 仓做；**严禁把 v6 算法代码覆盖回 v8**（会回退掉 v8 修复增强）。
- **DO_NOT_DELETE.md 加固**：v8 仓 `DO_NOT_DELETE.md` 新增「🔴 v8 算法铁律（严禁从 v6 回退覆盖）」章节（commit 5eca4b1，分支 feat/v8-detach-v6），并修正 sync_v6_to_v8.py / v8_algo_run / v8_sync_v6_data 三处的脱离 v6 后过时描述。pre-commit hook 已含此清单，删除清单内文件会被拦截。

## 22:00+ northbound-calendar 回测卡死·代码级根因与修复（续 20:22）
- **为何此前 30min timeout 仍卡满 30min**：cb21409 的 `timeout-minutes:30` 只防"无限挂起"，但单日仍卡满——**真正挂死点是 DNS/getaddrinfo 不在 `requests` 的 `timeout` 覆盖内**。CI runner 中途 DNS 不可达时 `getaddrinfo` 无限等，整条 139 交易日顺序循环被一个坏交易日堵死，直到 job 30min 超时被杀（实测卡 2026-04-10）。日志"缺 📡"是 `python3` 块缓冲 stdout 的假象，非真实卡点。
- **修复（4 文件，经 GitHub Contents API 写 calendar-pages；本机 github.com:443 不通、仅 api.github.com 通）**：
  1. `backtest_northbound_industry.py`：① `socket.setdefaulttimeout(20)` 给所有 socket（含 DNS）设 20s 硬上限；② `fetch_kline_tencent` 加 (5,10) 拆分超时 + 3 次重试，失败快速返回 []；③ 抽 `process_day()` + `_run_day_with_timeout()`（ThreadPoolExecutor 单日 150s 墙钟护栏），坏交易日只跳过不拖垮整轮；④ `main()` 起 `sys.stdout.reconfigure(line_buffering=True)`。
  2. `backtest_resonance.py` / `backtest_bull_hunter.py`：同样加 `socket.setdefaulttimeout(20)`（同款无护栏漏洞）。
  3. `northbound_daily_update.yml`：`python3` → `python3 -u`（回测 3 步全改，实时日志）。
- **部署验证**：dispatch `northbound_daily_update`（run 30818072661，13:27Z）用修复后脚本运行；Pages 源 = `calendar-pages`/root，commit 即自动发布。轮询待 completion 后查 `data/northbound_industry_backtest.json` 的 `update_time` 是否 2026-08-03 确认不再 hang。
- **本机网络铁律**：阿狸咪机 `github.com:443` 不通但 `api.github.com` 通 → 一律走 REST API（Contents PUT 写文件 / actions dispatches 触发 / actions runs 查状态），git push 不可用。PAT = `E:\workspace\stock-scanner\.gh_pat`。

## 21:5x v8 盘后算法链 checkout 失败审计 + 修复（用户追数据）
- **真根因（推翻"只修定时"）**：定时修复后 `v8_algo_run` 今晚 21:23 真的被调度触发了（`run#30817739173`），但**首步 `actions/checkout` 在自托管 cn runner（Windows 持久化工作目录）上 `failure`**——算法一行没跑。21:34 的 `v8_build_deploy (#30818542717)` 只把陈旧 raw_data（gold_pool/triple→08-02、lhb→08-01、candidate→07-31）重新打包部署 → 主站盘后/选股/共振页全是旧数据。根因=自托管 runner 脏树/dubious ownership 致 checkout abort（非算法 bug）。
- **修复（push v8 main @ 2384b5d）**：checkout 前加清理前置步（`git config safe.directory '*'` + `git reset --hard` + `git clean -fdx -e out -e algorithms/out -e algorithms/data`）+ checkout `clean:true`。YAML 已 pyyaml 校验。分支 `feat/v8-detach-v6` rebase 到含此修复的 main（force-push 9a899c8）。
- **沙箱无法自产数据**：托管 venv 有 akshare 1.18.64，但东财 `push2.eastmoney.com` 不可达（HTTP:000）、无 mootdx → 行情源被墙。唯一能产数据的是 cn runner。
- **用 PAT 触发跑数（关键动作）**：读 `.gh_pat` 经 GitHub API `POST .../workflows/324833339/dispatches`（`{"ref":"main"}`）→ **HTTP:204 成功**。`run#30820713215` (22:01 北京时间) 已 `in_progress`，checkout 通过，算法正在跑。ETA 15–30min → api_push 推 raw_data → build_deploy 自动部署。
- **交接**：`URGENT_阿狸咪_2026-08-03_算法链checkout修复.md` 已 push v6 main（24272229），含 cn 机器本地重跑命令（兜底）。
- **用户认知修正**：之前说"v8 算法更强"没错（算法脚本是增强版）；数据陈旧不是算法弱，是 **checkout 步在自托管 runner 失败**，现已加固+触发。
