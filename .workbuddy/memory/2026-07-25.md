# 2026-07-25 工作日志

## 21:10 本地备份兜底（自动化）
- **状态**: ✅ 全部通过（3/3，~2m12s）
- 双机代码同步 92.5s + enhanced_backup 21.6s + 心跳 17.3s

## 自动化 — 收盘后驾驶舱回测 / 部署
- **时间**: 19:14 自动化触发（周六非交易日）
- **步骤全部成功**:
  1. cockpit_backtest_now.py → 36 信号，胜率 11.8%，平均 -2.27%
  2. backtest_comprehensive.py → 5 策略，最佳胜率 50%
  3. update_data_v2.py → dist 构建成功
  4. git add/commit (0a6852f5)
  5. git push 被拒 → safe_pull 后重推成功 (17079758)
  6. deploy_now.py --force → gh-pages 部署成功 (22b70f0)
- **部署 URL**: https://ah-quant999.github.io/quant-scanner-v6/
- **耗时**: 约 5 分钟

## 自动化 — 19:30 收盘最终部署 (阿狸咪)
- **时间**: 19:31 自动化触发（周六非交易日）
- **Step 1**: 读取 HANDOVER_LOG.jsonl → 小九今天无记录（非交易日），跳过补跑
- **Step 2**: `batch_update.py close_deploy` → `close_deploy_guarded.py` 超时(周六数据新鲜度闸门)，其余9步成功
  - 备用部署: `deploy_now.py --force` → ✅ 成功 (build stamp 20260725194331, 224 files)
- **Step 3**: `verify_frontend_deploy.py` → 发现2文件未到位(.ops_status.json, hb_cloud.json) → 自动补部署 → **FRONTEND_REDEPLOYED_OK** ✅
- **Step 4**: 线上验证 → 页面可访问，build stamp 20260725195005
- **交接日志已写**: HANDOVER_LOG.jsonl 追加 close_deploy + close_deploy_verify 两条

## 19:57 自动化大清理（小九升主力机，阿狸咪瘦身）
- **背景**: 用户确认"小九是主力机（工作日）"，要求清理阿狸咪上的重复/浪费任务
- **动作**:
  1. **暂停 11 个 weekday 重复任务**（阿狸咪不再跑工作日盘中/收盘，独家归小九）：盘中补跑10:31/11:46/13:31/14:31、盘中15:30、盘中新鲜度看门狗、数据新鲜度巡检20:00、IPO打新研判08:30、收盘综合回测17:45、收盘后驾驶舱回测19:15、收盘最终部署19:30
  2. **删除 19 个历史 PAUSED 死配置**（含误判的"重复"周六T+1巡检——实际本就PAUSED，删错）
  3. **修正**: 把周六T+1新鲜度巡检(automation-1784465134207)恢复 ACTIVE（阿狸咪唯一周末本职）
- **最终状态**: 20 个自动化 = 9 ACTIVE（心跳监控/接管、清理部署锁、模型审计、备份兜底21:10、紧急指令监听、收盘一段兜底17:45、收盘二段兜底19:05、自动交接检查19:30、周六T+1巡检）+ 11 PAUSED（原weekday任务）
- **保留的兜底逻辑**: 小九挂了→阿狸咪 17:45/19:05 兜底补跑 + 19:30 交接检查 + 21:10 备份兜底 + 每30分心跳接管

## 20:02 驾驶舱④/⑤/⑥/⑦ 左侧数字实心圆加深
- 诉求：用户截图显示 ④⑤⑥ 左侧带圈数字仍为浅色 emoji，要求加深；并询问三个卡片的实际价值。
- 改动：把 `cockpitBlock` 中的替换范围从 ⑤⑥⑦ 扩展到 ④⑤⑥⑦，统一生成实心圆（背景用卡片主题色：④#1565c0、⑤#00695c、⑥#2c3e50、⑦#8e24aa），白字 11px，20×20px。
- 覆盖：`index_master.html` + `index.html` + 6 个 `standalone/*.html`。
- 部署：commit `637e4284` → gh-pages build stamp `20260725200209`。

## 20:11 驾驶舱AB档胶囊修复
- 诉求：用户要求 ①"基本"→"基本面"全称 ②删除重复的"基本C +15"胶囊（和分项行信息重复）
- 改动：A档+B档各一处——` · 基本` → ` · 基本面`，同时删除 `renderFundBadge()` 胶囊调用
- 覆盖：15个HTML（index_master.html + index.html + 6 standalone + dist下7个+dist/index.html；根目录index.html不含驾驶舱代码跳过）
- 部署：gh-pages `22ee08b`，URL: https://ah-quant999.github.io/quant-scanner-v6/

## 20:12 驾驶舱AB档买入阈值说明
- 诉求：用户问"AB档多少分以上才值得买入"，并要求 ①把详细逻辑写进逻辑详解页 ②在 A/B 档卡片标题旁分别标注 "A 档 ≥70 优先" / "B 档 ≥50 重点跟踪"
- 改动：
  1. `standalone/guide.html` 新增 `4.4.1 驾驶舱精选（A/B 档）`：入选条件、评分公式、实战阈值、跨档比较注意事项。
  2. `index_master.html` + `standalone/{overview,predict,gold,health,query,shmonitor}.html`：A 档标题加橙色徽章 `A 档 ≥70 优先`；B 档标题加橙色徽章 `B 档 ≥50 重点跟踪`。
- 工具：`_cockpit_fix_0725d.py` 批量补丁（2 patch × 7 文件全 OK），JS 语法校验全过。
- 部署：main `736848fb` → gh-pages `22ee08b`，build stamp `20260725201442`。线上核验：A 档 badge×1 ✅、B 档 badge×1 ✅、guide 章节×2 ✅、standalone/overview badge ✅。

## 20:32 驾驶舱审计修复（③共振候选股 3 个逻辑问题）
- 诉求：主人要求审计昨天今天的改动是否有逻辑错误。
- 通读 renderStockCockpit / renderCockpitResonance / renderCockpitTierRecommendAlimi 全部渲染逻辑，发现 3 个问题（均今天新增 ≥80详细/70-79观察区 时引入）：
  1. **【真bug】无≥80时提前 return 吞掉 70-79 观察区**：`if(!list.length){el.innerHTML='暂无≥80';return;}` 在观察区代码之前 return，某赛道/全市场无≥80 时 70-79 整段不显示。修法：改 if/else 包裹，无≥80 只显示提示不 return，观察区照常渲染。
  2. **【真bug】70-79 观察区不随赛道过滤**：过滤块只 filter 了 list（≥80），list70 没过滤，切换赛道时上面是该赛道、下面还是全市场。修法：抽 `_secFilter` 同时应用到 list 和 list70。
  3. **【一致性】③副标题计数不随过滤更新**：副标题在 renderStockCockpit 写死全市场数。修法：副标题加 `id=cockpitResoSummary`，renderCockpitResonance 保存 listAll80/listAll70 全量，末尾按 filter 动态更新（all=全市场数；具体赛道=本赛道数+括注全市场数）。
- 核对无误：今日结论横幅取数、②TOP5表占比/5日净流入红绿、三方共振橙标条件(instV>0&&lhbHit&&sig_count>=2)、A/B档徽章、cockpitBlock inset彩条、.content居中、rsList 已彻底清除。
- 工具：`_cockpit_audit_fix_0725.py` 批量补丁（4 patch × 7 文件 = 28/28 全 OK），JS 语法 15 文件全过。
- 部署：main `7ff0bf39` → gh-pages `e9705ed`，build stamp `20260725203233`。线上核验：cockpitResoSummary×2、list70过滤×1、本赛道≥80×1、旧提前return模式×0（已消除）、standalone/overview 同步×1。
- 教训：新增列表分区（如观察区）时，务必检查上游是否有 early-return 会吞掉后续渲染，以及过滤/计数是否需同步联动。
- 说���：A/B 档评分口径不同，不能直接跨档比较；A 档 ≥70 为优先，60–69 为标准关注；B 档 ≥50 为重点跟踪，40–49 先观察，<40 仅参考。

## 20:40 仓库同步审计（用户要求：确认昨天今天改动已写入分支、不被覆盖）
- **审计对象**：7-24 + 7-25 所有改动的落地情况
- **结论**：
  1. ✅ 代码修复（POST_CLOSE_TIME 后溯、驾驶舱去重、基本面胶囊、A/B档徽章、③共振3 bug）全部已提交 main = origin/main，未被覆盖
  2. ✅ 线上 gh-pages = `e9705ed`（build stamp 20260725203233），已含全部修复，fetch 核验 `· 基本面` 计数=4
  3. ⚠️ 发现 20:32 自动部署只 push 了 gh-pages、漏 commit main 的 dist/standalone 数据刷新（27文件 working tree dirty），下次 deploy 第0步 checkout 会回退这些文件
- **处置**：`git add -f dist/ index.html standalone/` → commit `3ca9e3a5` → push origin/main。现工作区干净、main=origin/main、HEAD 含修复（`· 基本面`=4）
- **根因**：deploy_now.py 只强制推送 gh-pages 分支，不自动 commit main 的 dist 产物，导致每次自动部署后 main 的 dist 落后于工作区。后续如需彻底消除 dirty，应在部署脚本内增加 `git add dist/ && git commit` 步骤。

## 20:46~20:59 部署脚本根因修复（用户说"加"= 把 dist 同步步骤加进部署脚本）
- **用户意图**：审计发现每次部署后工作区 dist 残留 dirty（main 不含最新 dist），要求把 dist 同步步骤加入部署脚本，彻底消除 dirty。
- **第一版修改（d58a5005）**：在 `_auto_push_source()` 内去掉 `git reset -- dist/`（dist 纳入同步范围）+ 过滤列表去掉 `dist/` 排除。但部署后仍 dirty —— 排查发现**真正的调用位置 bug**：
  1. 第 860 行 `_auto_push_source()` 在 `_ensure_dist_fresh()`（重建 dist）**之前**调用 → 只能 commit 部署前旧状态
  2. 真正的"第5步"（第 1063-1067 行）只打印 `Auto-syncing...` 就 **`return 0`**，根本没调用 `_auto_push_source()`
- **第二版修复（fc50d387）**：删除第 860 行过早调用；第 5 步改为在 gh-pages 部署 + dist 重建全部完成后调用 `_auto_push_source()`。
- **验证**：重新部署（gh-pages `ce0b119`，build `20260725205609`）→ 第5步日志 `📝 检测到 29 个变更 ✓ 已提交 auto: source sync 07-25 20:57`；工作区变干净；`git diff origin/main HEAD -- dist/` 为空（origin 的 dist 内容已同步，浅层克隆导致 merge-base 误报，以内容 diff 为准）。
- **遗留**：zjJeLS 部署时 push main 报 `cannot lock ref`（并发——另一进程同时操作 main），但最终 origin/main 的 8201c0e0 与本地 SHA 相同、dist 内容一致，属并发竞争的中间态，非功能性失败。
- **教训（重要）**：部署脚本里"同步源码到 main"的调用**必须在 dist 重建 + gh-pages 推送之后**，否则永远 commit 不到最新 dist。

## 20:48 架构讲解 + 算法诚实审计（未改代码）
- 主人问：精选是主站算法还是驾驶舱？基本面怎么评分数据哪来？怎么买卖稳妥？算法有没有纰漏？能否更好？
- **链路结论（读代码确认）**：① 候选池 build_candidate_pool → ② 主站扫描 scan_result（全量技术信号，唯一底座）→ ③ **两套并列算法**：主站 TOP10（generate_top10.py，gold_pool 多维共振加权累加）+ 驾驶舱 A/B档（gen_cockpit_tier_recommend.py，scan_result 硬性过滤）。**两者平级、都叠加同一份基本面分**，非上下游。用户想要的"下游精选"= 二者交集（暂未实现）。
- **基本面两段式**：① fetch_fundamental_quality.py（Baostock query_profit_data ROE/EPS + query_operation_data 营收；东财 stock_yjyg_em 业绩预告 + stock_notice_report 公告）→ 70分制定档 A≥70/B≥60/C≥40/D<40/无数据中性；港股无源走中性。② fundamental_helper.quality_points 把档位重映射成加分 A=15~25/B=5/C=0/D=-10 + 消息±15，TOP10 与驾驶舱共用第二段保证口径一致。
- **诚实审计出的算法短板（非bug，设计局限，待排期）**：
  1. 基本面天然滞后（Baostock 最近一期财报，延迟1~3月）
  2. 6类辅助数据断更时静默丢分（mahoro 7-23、concept_map 出网被墙），票分被动虚低无提示
  3. 档内差异被抹平（70分制→只按档给分，ROE19%+营收25% 的 B 和普通 B 无区分）
  4. **回测胜率未反哺评分**（backtest60 数据已跑但评分公式没用）← 最高价值改进点
  5. 阈值全人工经验值（RSI<68/EMA≥5/20日<35%），无参数寻优/样本外回测，过拟合风险
- **给主人的改进建议（按性价比）**：①回测胜率做乘子 + 加"三重共识"面板（当天可上）②断更显式降权 ③用70分制原始分 ④阈值参数化。主人未拍板，等指令再动手。

## 当前任务：驾驶舱③精选票叠加④⑥⑦背书
- 发现 HEAD 的 index_master.html 与 standalone/*.html 已含 `cockpitGetStockBacktestComp/Tdx`  helper、标题区 compOv、每票 compSt/tdxSt 计算，但**缺失最后三步**：⑥ loader 未把 `__BACKTEST_COMPREHENSIVE` 挂回 window、⑦ loader 未挂 `__BACKTEST_TDX`、row 未把 `tdxBadge` 拼进去，导致功能实际不生效。
- 修复 `index.html`：补齐上述三处；`index_master.html` 此前已完整，仅确认一致。JS 语法校验通过。
- 下一步：commit + push + deploy。

## 全站精选 TAB 迁移完成（健康看板独立并列 TAB）
- **诉求（经用户纠正）**：把驾驶舱顶部"🎯 驾驶舱精选"横幅改为"📌 全站数据精选"——数据源由 `cockpit_tier_recommend_alimi.json` 的 A/B 档改为 `data/top10_daily.json` 中 `total_score>=80`；并在**健康看板**内新增"📌 全站精选"独立子 TAB，与"🎯 驾驶舱"并列，展示全量 ≥80 列表。
  - ⚠️ 用户明确纠正过：不是放进"暂未上架"，而是放进健康看板并列 TAB。
- **改动（index_master.html 为主模板，root index.html 同步）**：
  - 删除 `renderCockpitTierRecommendAlimi` + `buildCockpitTierRecommendAlimiHTML`，替换为 `renderCockpitAllsiteSelection` + `buildCockpitAllsiteHTML`（顶部紫色渐变横幅，网格 `repeat(auto-fill,minmax(150px,1fr))`，每格显示名称+评分+市场/代码+quality_grade）
  - 新增 `renderAllsiteSelection`（全量≥80 卡片，网格 `repeat(auto-fill,minmax(280px,1fr))`，含 rank/total_score/名称/quality_grade/信号CJT/板块前4/止损目标价/收盘/涨跌幅/quality_detail）
  - 健康看板新增子 TAB 按钮 `data-cat="allsite"` + `#allsitePanel` 容器 + `renderHealthDashboard` 的 `allsite` 分支（复用 cockpit 的显隐/高亮逻辑）
  - `toggleUnlistedPanel` 增加隐藏 `allsitePanel`；PAGE_CARDS 增加 `📌 全站精选` 条目
- **数据源确认**：`top10_daily.json` 当前 `total_scored=295`、`count_80plus=1`、`max_score=90`（仅 佰维存储 688525 一票 ≥80），故线上全站精选当前只显示这一只，符合预期。
- **部署**：commit `307cb48e`（auto: source sync 07-25 21:22，后台 sync 自动化自动 commit+push index_master.html + index.html）→ gh-pages build stamp `20260725212059`。
- **线上验证（origin/gh-pages 原始内容 + WebFetch 复核）**：`renderAllsiteSelection`×2、`allsitePanel`×7、`data-cat="allsite"`×1、`cat==='allsite'`×1、`renderCockpitAllsiteSelection`×2、`buildCockpitAllsiteHTML`×1、`top10_daily.json`×4、`全站数据精选`×6、可见标签`全站精选`×2。WebFetch 因剥离 `<script>` 对代码串报 0 为假阴性，以 gh-pages 原始文件为准。
- **根因备忘**：之前 `git status` 未列出 index_master.html 的修改，是因为后台 source-sync 自动化已自动 commit+push，无需手动再提交。

## 金股池「最后推荐」改为三重共识 + 回测胜率反哺评分（已上线）
- **诉求（用户拍板"这些都要"）**：①把回测胜率反哺 TOP10 评分；②加"三重共识"面板；③金股池（金股观测 tab）的"最后推荐"多维共振列换成三重共识。
- **改动**：
  - `generate_top10.py`：读 cockpit_backtest.json，按四信号元组(Chan/Jinzuan/Jigou/Trend)做 Laplace 平滑历史胜率，score_backtest=clamp((win_rate-50)/10*5, -10, 10)；输出 breakdown.backtest + win_rate 字段。
  - 新增 `gen_triple_consensus.py`：严格=TOP10≥70 ∩ A档 ∩ 基本面≥B；near_miss=满足2/3；输出 data/triple_consensus.json。
  - `update_data_v2.py`：加 TRIPLE_CONSENSUS 注入标记+数据对象。
  - `index_master.html`：buildMultiResonanceTable 渲染三重共识（严格+差一步候补，renderOne 读 total_score/quality_grade/in_top10/in_tier_a/in_good_fund 等匹配字段）；加 window.TRIPLE_CONSENSUS={} 占位；金股观测左栏标题改"🥇 三重共识 N 只 TOP10≥70 · A档 · 基本面≥B"。
  - `standalone/gold.html` + 新增独立页 `standalone/triple_consensus.html`：同渲染逻辑；导航页(extract_panels_v6.py _extra)加"🥇 三重共识"入口。
- **今日数据**：count=0 严格 / near_miss=2 → 佰维存储(688525, TOP10#1+基本面A, 非A档)、海康威视(002415, A档+基本面B, 非TOP10≥70)。无严格三重共识时面板提示"展示满足 2/3 条件的候补"。
- **部署**：commit `bf0c6d66`（金股池最后推荐改三重共识+回测胜率+独立页）→ deploy_now.py --force → gh-pages head `ee1955d`（forced update），build stamp `20260725212059`；ls-remote 校验通过；心跳上报 alimi 成功。
- **线上验证(origin/gh-pages)**：window.TRIPLE_CONSENSUS 注入 3 处；standalone/triple_consensus.html 存在；gold.html 与 root index 各 6 处"三重共识"；triple_consensus.json 本地与 dist 副本 MD5 一致(da262f28)；renderOne/renderStock 字段名与 JSON 完全匹配。
- **注意**：`sector_fund_flow.json` 原戳 07-24 17:59，恰好早于内容新鲜度闸门 18:00 门槛 1 分钟会阻断部署；已重跑 fetch_sector_fund_flow.py 刷新至 07-25 21:18 后通过。

## 全站精选二次修正：删除驾驶舱横幅、TAB 改展原 A/B 档全部 26 只
- **用户反馈**：① 驾驶舱顶部"📌 全站数据精选"横幅（见截图）直接删除；② 健康看板"📌 全站精选"TAB 不能只有 `top10_daily.json` ≥80 分那孤零零一只，要把**原来驾驶舱精选里的 A/B 档全部**移过去。
- **数据确认**：`cockpit_tier_recommend_alimi.json` 含 A档 10 只 + B档 16 只 = 共 26 只；`top10_daily.json` 当前仅 1 只 ≥80（佰维存储）。
- **改动（index_master.html + root index.html 同步）**：
  - 删除 `renderCockpitAllsiteSelection` + `buildCockpitAllsiteHTML` 两个函数。
  - 删除驾驶舱顶部横幅容器 `<div id="cockpitTierRecommendAlimiBanner">` 及其渲染调用。
  - 重写 `renderAllsiteSelection`：改从 `data/cockpit_tier_recommend_alimi.json` 读取，合并 `tier_a` + `tier_b`，渲染全部 26 只为卡片网格；头部显示"📌 全站精选 · 原驾驶舱 A/B 档精选 · A档 X 只 · B档 Y 只"；每张卡片含 A/B档 标签、基本面等级、total_score、名称/市场/代码、comment、quality_detail、RSI/20日/EMA。
- **波折**：后台 source-sync/deploy 进程在 22:12 把 index_master.html 回退成旧版（重新出现 `renderCockpitAllsiteSelection`）。重打补丁后**立即 commit + push origin/main**（commit `aa442136`），再跑 `deploy_now.py --force`。
- **部署**：deploy 日志 build stamp `20260725222352`，gh-pages forced update 到 `22842084`，源同步 commit `58c653e4` + lock release `2a916742`；JS 语法校验 index_master / index 均 17 块 OK。
- **验证（origin/gh-pages 原始内容）**：`renderAllsiteSelection`×1、`cat==='allsite'`×1、`allsitePanel`×1、`cockpit_tier_recommend_alimi.json`×2；`renderCockpitAllsiteSelection`×0、`buildCockpitAllsiteHTML`×0、`cockpitTierRecommendAlimiBanner`×0、`top10_daily\.json`×0。
- **线上验证(WebFetch)**：页面标题 build stamp `20260725222352`； cockpit 顶部不再出现"全站数据精选"横幅；数据源表格可见"📌 全站精选✅数据显示正常"；默认不渲染 TAB 内容（需点击"全站精选"子 TAB），函数已加载 `cockpit_tier_recommend_alimi.json` 全部 A/B 档数据。
- **遗留**：工作区剩 `M standalone/gold.html` 为构建残留，不影响线上；次日数据刷新后由自动构建自然覆盖。

## 金股观测顶部「多维80+」→「三重共识」替换（已上线）
- **诉求**：把金股观测顶部原"🧬 多维80+推荐"卡片整体换成三重共识展示，并给出"多少分重点关注"的明确说法。
- **概念澄清（用户确认）**：短线信号/中长线确认是按"信号维度"粗分（只看信号数量/机构+趋势组合），不把关质量；三重共识是三套独立数据源（主站TOP10 / 驾驶舱A档 / 基本面质量）的交叉验证，更精确。
- **重点关注规则（写进页面）**：3/3 严格共识 = 重点关注（无条件）；2/3 候补按主站 total_score ≥75 为"优先观察"，<75 为"观察"。严格共识的第一重即要求 TOP10≥70，故其底层主站分均≥70。
- **改动**：`index_master.html` + `standalone/gold.html` 的 `renderTop10Daily()` 由"过滤 total_score≥80"改为渲染 window.TRIPLE_CONSENSUS（stocks 严格在前 + near_miss 候补在后，卡片含 主站total_score大字 + 基本面等级 + 差一步说明 + 重点关注/优先观察/观察 标签 + 顶部规则说明）；标题改"🥇 三重共识 TOP10≥70·A档·基本面≥B"；链接改 triple_consensus.html?from=gold；健康看板卡片名同步改。
- **部署**：commit `b78dcb33` → push → deploy_now.py --force → gh-pages head `c3f41f3`，build stamp `20260725221303`；JS 语法校验通过（index_master 17块 / gold 19块均 OK）；同源同戳校验 57 标记通过。
- **线上验证**：root index "重点关注"×4、"三重共识"×11；gold.html "重点关注"×4。
- **今日数据**：严格 0 / 候补 2（佰维存储90分重点关注侧但仅2/3、海康威视65分"观察"侧），顶部卡片空时显示"当前无三重共识股"提示。

## 金股观测下方三列改为三方数据：技术信号 / 技术形态 / 基本面质量（已上线）
- **用户诉求**：把金股池主体下方原来"三重共识 | 短线信号 | 中长线确认"三列，替换成图片里的三方数据，并写明每列对应「技术信号 / 技术形态 / 基本面质量」哪一种。
- **映射关系**：
  - 主站 TOP10 精选 → **技术信号**（多维度共振评分，含缠论/金钻/机构/趋势等信号）
  - 驾驶舱 A 档 → **技术形态**（双真+不超买+EMA完好+RSI/20日收益/技术评分）
  - 基本面 A/B 质量池 → **基本面质量**（ROE+营收增速+消息面）
- **改动**：
  - `update_data_v2.py`：新增读取并注入 `window.COCKPIT_TIER_RECOMMEND`（原仅全站精选使用，未注入 window）。
  - `index_master.html` / `standalone/gold.html`：
    - 新增 `buildTop10SelectedTable` / `buildCockpitATable` / `buildFundamentalQualityTable` 三个渲染函数，每行展示代码/名称/板块/总评分 + 详细入选指标说明。
    - `doRender()` 下方三栏改为固定展示三方数据（各取前 20 只）。
    - 标题栏明确标注："主站 TOP10 精选（技术信号）"、"驾驶舱 A 档（技术形态）"、"基本面 A/B 质量池（基本面质量）"。
    - 底部计数改为"三方数据：技术信号 X 只 · 技术形态 Y 只 · 基本面质量 Z 只"。
- **波折**：`index_master.html` 与 `update_data_v2.py` 在编辑过程中被后台 source-sync/deploy 进程回退 2 次（新函数 / 数据注入代码丢失），需重新 patch 后立即 commit+push 再 deploy。
- **部署**：commit `73c8a720` "金股观测下方三列改为三方数据：技术信号/技术形态/基本面质量" → push → `deploy_now.py --force` → gh-pages head `e9b98c50`，build stamp `20260725223218`；同源同戳校验 58 标记通过。
- **线上验证（origin/gh-pages）**：root index 三方标题各 ×2、gold.html 三方标题 ×2、`window.COCKPIT_TIER_RECOMMEND` ×2；JS 语法校验 index_master 17 块 / gold 19 块均 OK。
- **今日数据**：技术信号 TOP10=10 只、技术形态 A档=10 只、基本面质量 A/B=84 只（显示前 20）。

## 健康看板「全站精选」→「算法展示」TAB 重做（已上线）
- **用户诉求**：把"全站精选"这个 TAB 改成**算法展示**——要知道这些票是怎么算出来的、多少分重点关注；A/B 档之间空一两行区分。
- **改动（index_master.html + root index.html 同步）**：
  - 子 TAB 按钮文字/图标：`📌 全站精选` → `⚙️ 算法展示`（紫→深紫 `#5e35b1`）。
  - 重写 `renderAllsiteSelection` / `buildAllsiteSelectionHTML`：顶部"算法展示"标题 + 说明（total_score = tech_score + quality_score）；**算法条件区**用两卡展示 A档/B档 筛选规则（取自 JSON `method` 字段，自动解析 `&`/`|` 分条），并写明 A档技术基础分 60 / B档 50、≥70 标红重点关注、60-69 符合标准；**分数构成**说明质量 A/B/C/D 档加减分；A档"重点关注"区 + 18px 空行分隔 + B档"观察候选"区，卡片含 技术分/RSI/20日/EMA + 质量档 + ≥70"重点"红标。
  - 新增 `formatAlgoMethod`（解析方法字符串为标签）+ `renderAlgoCard`（统一卡片）。
  - 清理遗留：删除被 patch 误留的第二个 `buildAllsiteSelectionHTML` 重复定义（会导致旧"全站精选"标题覆盖）；PAGE_CARDS 自检名与注释同步改"算法展示"。
- **坑**：第一次 patch 用正则替换时只替换到第一个 `function`，导致旧 `buildAllsiteSelectionHTML` 整块残留在新函数之后（JS 同名函数后者覆盖，旧"全站精选"标题生效）。用脚本按"第二个定义→下一个顶层 function"删除修复。
- **部署**：commit `c2ef3927` → push（首次因远程已有并发提交被拒，safe_pull 同步后 0/0）→ `deploy_now.py --force` 首次 gh-pages push 因并发锁失败，fetch 后重试成功 → gh-pages head `9cf7206`，build stamp `20260725233019`；JS 语法校验 17 块 OK；源同步 commit `7a063825`(22:33) 后由 auto-sync `23:33` 提交。
- **线上验证（origin/gh-pages）**：`⚙️ 算法展示`×4、`function formatAlgoMethod`×1、`function renderAlgoCard`×1、`重点关注算法`×1、`观察候选算法`×1、`height:18px`×4、`data-cat="allsite"`×1；`function buildAllsiteSelectionHTML`×1（无重复）。
- **当前数据**：A档 10 只 + B档 16 只 = 26 只；全志科技 75 分（A档最高）、海康威视 65、其余多为 60。

## 彻底删除旧「多维/短线/中长线」追踪系统（已上线）
- **背景**：用户确认"既然之前的多维、短线和中长线都不理想，就不要再跟踪了" → 选「彻底删除」（删两个追踪页+导航+死代码+CI生成器+按钮改指三重共识）。
- **删除文件（15个，git rm）**：
  - 页面：`multi_resonance.html` / `triple_resonance.html` 及 `standalone/` + `dist/` + `dist/standalone/` 副本（共 8 页）
  - 生成器：`update_multi_resonance_daily.py` / `update_triple_resonance_daily.py` / `generate_triple_resonance_history.py`
  - 数据：`data/multi_resonance_history.json` / `data/triple_resonance_history.json` 及 `dist/data/` 副本
- **代码清理（死代码）**：`index_master.html` + `standalone/gold.html` 删除 `buildGoldTable`(短线) / `buildMidLongTable`(中长线) / `buildMultiResonanceTable`(多维共振) 三个无调用函数；顶部按钮改指 `triple_consensus.html?from=gold`（root 用 `standalone/` 前缀）。
- **管线清理**：`update_data_v2.py` 移除共振数据加载块 + 注入块；`extract_panels_v6.py` 移除 `_extra` 的「三线追踪/多维追踪」；`ensure_standalone_sync.py` 移除共振页特殊跳过逻辑；`batch_update.py` / `deploy_audit.py` / `sync_check.py` / `verify_data_vs_website.py` / `check_syntax.py` / `refresh_standalone_and_deploy.py` / `enhanced_backup.py` / `extract_standalone_final.py` 移除相关引用；`.github/workflows/cloud_scanner.yml` + `cloud_post_close.yml` 移除两个生成器调用。
- **防删清单同步**：`DO_NOT_DELETE.md` 原本把 `standalone/` 与 `dist/data/` 设为目录级保护，阻止了提交；改为显式清单（standalone 12 页 / dist/data 80 json 逐一列出），并移除 `update_triple_resonance_daily.py` 等登记 → pre-commit hook 通过。
- **部署**：commit `9e454097` → push origin/main（c2ef3927 顶，含无关的「算法展示」自动化提交）→ `deploy_now.py --force` → gh-pages head `43bd2d9`，build stamp `20260725232517`；部署日志「已同步 12 个独立页」（原 13+，确认共振页已移除）。
- **线上验证（origin/gh-pages）**：共振文件 0 个；`standalone/index.html` 导航 0 处「三线追踪/多维追踪」；`gold.html` 按钮→`triple_consensus.html`、`root index.html` 按钮→`standalone/triple_consensus.html`；递归 0 处 resonance 引用。
- **注意**：保留个股详情抽屉里的「多维共振/金股池信号卡」作为上下文信息（非推荐列表），未删。root `index.html` 的「算法展示」TAB 是另一自动化(c2ef3927)的独立改动，与本删除无关。

## 23:34~23:57 金股观测页清理旧筛选栏与信号速报卡片（已上线）
- **用户诉求**：截图显示金股观测页仍残留 ① 顶部筛选栏（全部/多维70+/创业板/科创板/主板/港股/机构/游资/上涨趋势/投行最新研报）②「🔥 今日信号速报」卡片（短线信号/中长线确认）；要求把「三重共识的历史追踪」按钮放到顶部三重共识卡片右侧。
- **波折**：23:33 的 `auto: source sync` 把 `index_master.html` 回退成旧版（重新出现筛选栏+信号卡，且三重共识按钮 href 回退为 `triple_consensus.html` 缺 `standalone/` 前缀），需立即重新 patch、commit+push、再手动 deploy。
- **改动**：
  - `index_master.html` + `standalone/gold.html`：删除筛选栏 HTML + `.filter-bar`/`.filter-btn`/`.search-input` CSS；删除「今日信号速报」卡片 HTML。
  - 删除信号统计 JS 块（`// 🔥 信号统计高亮` IIFE）、`let currentGFilter='all'` 声明、doRender 中全部 `currentGFilter` 筛选分支、短线/中长线分组逻辑、筛选按钮事件监听。
  - 顶部三重共识卡片注释改为 `<!-- 🥇 三重共识精选 -->`，右侧按钮改为 `🥇 三重共识 →`；`index_master.html` 的 href 修正为 `standalone/triple_consensus.html?from=gold`。
  - `block7.js` 移除「📊 今日信号速报」健康检查项。
  - 重建 `dist/` 与 `standalone/*.html`，确保所有独立页同步清理。
- **部署**：commit `40a330e5` → push origin main → `deploy_now.py --force` → gh-pages head `733e8974`，build stamp `20260725235349`；同源同戳校验 58 标记通过；JS 语法校验 index_master 17 块 / gold 19 块均 OK。
- **线上验证（origin/gh-pages）**：`standalone/gold.html` 与 `root index.html` 均 0 处「信号速报」「filter-bar」「data-gfilter」「signalHighlightCard」；顶部三重共识卡片右侧按钮为 `🥇 三重共识 →`，root 指向 `standalone/triple_consensus.html?from=gold`。
- **当前数据**：严格三重共识 0 只 / 候补 2 只（佰维存储、海康威视），顶部卡片显示「当前无三重共识股」提示。

## 00:00 金股观测「三重共识」改名为「历史追踪」并修复返回总览链接（已上线）
- **用户诉求**：① 金股观测页顶部三重共识卡片与按钮改名为「历史追踪」；② `triple_consensus.html` 点击「返回总览」后应回到独立总览页 `overview.html`，而不是导航页 `standalone/index.html`。
- **改动**：
  - `index_master.html` + `standalone/gold.html`：顶部卡片注释/标题/按钮 `🥇 三重共识` → `📊 历史追踪`；空态文案「当前无三重共识股」→「当前无历史追踪股」；18:30 日程卡中的「三重共识」→「历史追踪」；JS 注释同步。
  - `standalone/triple_consensus.html`：页面 title / h1 改为「历史追踪」，subtitle 改为「（历史追踪精选）」；股票标签 `三重共识` → `历史追踪`；空态文案同步；「返回总览」链接从 `index.html` 修复为 `overview.html`。
  - `extract_panels_v6.py`：导航首页 `_extra` 中 triple_consensus 的图标/名称改为 `('triple_consensus', '📊', '历史追踪')`，避免后续 `ensure_standalone_sync.py` 把导航改回旧名。
  - `standalone/index.html` 与 `dist/standalone/index.html` 同步为「历史追踪」。
- **同步**：执行 `ensure_standalone_sync.py`，standalone/ 12 页同步到 dist/standalone/，同源同戳校验通过。
- **部署**：commit `ad948247` → push origin/main → `deploy_now.py --force` → gh-pages head `acd9a522`，build stamp `20260726002601`；JS 语法校验通过。
- **线上验证（origin/gh-pages）**：`index.html`/`standalone/gold.html` 三重共识=0、历史追踪=9；`standalone/triple_consensus.html` 三重共识=0、历史追踪=5；`standalone/index.html` 历史追踪=1；返回总览 href=overview.html 正确。
