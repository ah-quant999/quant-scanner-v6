# 2026-07-12 工作日志

## 17:04 短线/中线市场温度计4改（用户截图驱动）
1. **标题改名**：「📊 三维综合评分 短线/中线参考」→「🌡️ 短线/中线市场温度计」
2. **三栏等分**：`scoreDim()` 外层 div 加 `flex:1;min-width:0;` 实现等宽
3. **时间内嵌+按钮恢复**：底部「数据更新：」删，改为标题行右侧「更新于 xxx」+「查看完整信号 →」
4. **独立页修正**：market_signal.html 删 hero-title 条、品牌名同步改、返回从 `../index.html`(跳主站) 改 `history.back()`
5. **生成脚本同步**：build_market_signal.py 4处改动防下次覆盖（title/brand/back link/hero-title/time format）
- 已部署 gh-pages + git push main（commit 931a496）

## 17:12 温度计4项微调（用户截图驱动）
1. **标题加粗**：`font-weight:700` 包裹标题文字
2. **时间紧跟卡片名**：去掉 `margin-left:auto`，时间紧跟名称后方，按钮仍靠右
3. **独立页删重复横幅**：hero-verdict（综合研判横幅）与入口卡完全重复，整块删除
4. **返回锚定数据监控Tab**：返回链接从 `history.back()` 改 `../index.html#shmonitor`；主站init加 hash 读取+ `switchTabByName()`，确保返回后直接落在数据监控页而非总览
5. build_market_signal.py 同步3处改动防覆盖

## 09:07 盘前全量自动化（automation-1780724098470）
- `batch_update.py pre_market` → 周日非交易日自动跳过，Exit Code 0，无报错。

## 16:07 修复两张入口卡片返回导航
- 修复 standalone/crisis_radar.html 与 standalone/market_signal.html 的「返回导航」链接：原 `href="index.html"` 在 GitHub Pages 项目站点路径下会指向 `standalone/index.html`（不存在），改为 `href="../index.html"` 正确回到主站。
- 已同步修改构建脚本 `build_crisis_radar.py`、`build_market_signal.py`，并通过 `refresh_standalone_and_deploy.py --skip-data` 重新生成页面并部署到 gh-pages。

## 10:53 危机雷达方法论段增加各维度数据来源明细
- 修改 `build_crisis_radar.py`：在「📐 方法论 & 数据说明」段新增 HTML 表格，按六维（流动性3项/利率3项/经济4项/房地产1项/全球7项/汇率1项）逐指标列出数据来源接口名。
- 原来的「数据源」汇总行保留并更新为更精确的描述。
- DXY 和 USDCNH 特别标注了「本地累加缓存+阈值兜底」（对应 MEMORY 中已知的伪分位问题）。
- 已重新构建 + deploy --force 部署成功 + git push main。

## 11:01 危机雷达六维指标卡片全面缩小
- `build_crisis_radar.py` CSS 全面收紧：数值字号 23→18px、卡片内边距 11→7px、section padding 16→10px、gap 10→7px 等。版面更紧凑。

## 11:09 方法论内容整合到逻辑详解页(guide.html)数据监控部分
- **guide.html** 数据监控区新增两大块（暗色主题，紧跟危机监测·趋势对比之后）：
  1. **🛰️ 六维预警雷达 — 方法论 & 数据说明**：完整危险分定义+维度分/综合指数公式+状态分档+**19项指标来源明细表**（六维逐指标列出akshare/东财/新浪接口名）
  2. **🧠 市场面信号 — 各指标数据来源与计算逻辑**：三维度评分模型(1-10分)+加减分条件表格+数据来源字段映射(SH_FIB/MARGIN_DATA等)
- market_signal.html 的 method-link 链接更新为 `guide.html#数据监控` 锚点。
- crisis_radar.html 自身保留完整方法论不变（独立页需自包含）。
- **index_master.html 卡片位置确认**：六维预警雷达(line 705) → 市场面信号(line 724) → 宏观观测(line 743)，顺序正确无需移动。
- `index_master.html` 市场面信号卡从展开式（结论条+三列评分）改为入口式摘要卡：
  - 标题栏：verdict 徽章 + 时间 + "查看完整信号 →"
  - 摘要区：左侧大号综合分(0-143) + 右侧三维度进度条（技术/资金/机构）
  - 点击跳转 standalone/market_signal.html 看完整细节
- 同步改写 JS `renderMacroOverview()` 渲染函数，适配新 DOM 元素（移除旧 scoreTech/scoreFund/scoreInst/macroVerdictBar，新增 macroVerdictBadge/macroEntryIndex/macroEntryDims）。
- 已 rebuild + deploy --force 成功。

## 11:48 修正 MEMORY.md 三处失实 deploy 记载（应主家要求）
- 经 `git log -S` 全量核查 + 全仓库 grep，确认以下 MEMORY 记载与代码现实不符，已更正：
  1. **临时目录泄漏修复(6a330c9a)**：该 commit 在仓库历史中不存在，deploy_now.py 任何版本都无 finally 清理逻辑 → 实为**从未修复**的隐患。
  2. **A+B 增量部署(KEEP_WHITELIST/.deploy_manifest/--dry-run)**：全仓库无任何实现 → 实为「全清式覆盖」部署，concept_history.json 等历史文件每次被清空。
  3. **dist/data/ 不进 Git**：实为早期被 `git add -f` 强制跟踪；2026-07-12 修复中 `_auto_push_source` 已加 `git reset -q -- dist/`(deploy_now.py:711)，此后源码同步不再提交 dist/ 改动。
- 根因：以上失实记载均「超前于实际代码」——MEMORY 记录了计划/设想但从未真正落地 main。

## 11:51 真正修复 deploy_now.py tmpdir 泄漏（应主家「修」指令）
- `deploy_now.py`：`main()` 的 `try` 前声明 `tmpdir=None`；`finally` 块在 `_release_deploy_lock()` 之前加 `shutil.rmtree(tmpdir, ignore_errors=True)` 兜底，覆盖 CDN bust/密码注入/IO 等异常路径的目录泄漏。原有 640/646/649 显式 rmtree 保留作已知失败路径即时清理。
- 语法校验通过；`git add deploy_now.py` 单文件提交 + `git -c http.version=HTTP/1.1 push origin main` → commit `26bb012`，已推送到 main。
- 同步更新 MEMORY.md 第11行：该隐患由「至今未修」改为「已于 2026-07-12 11:51 真正修复(commit 26bb012)」。

## 11:55 修复独立页部署缺失（market_signal.html 漏上 gh-pages）
- 用户反馈：六维预警雷达和市场面信号两张入口卡点击后页面打不开（ERR_CONNECTION_RESET）。
- 排查：`git ls-tree origin/gh-pages` 确认 `standalone/market_signal.html` **不在 gh-pages 分支**（crisis_radar.html 和 guide.html 在）。本地 dist/standalone/ 和 source standalone/ 均有该文件，但此前某次部署未将其推上 gh-pages。
- 执行 `deploy_now.py --force` 全量重部署，重新 fetch 验证三个独立页均在 gh-pages 上。问题修复。

## 11:56 审计闭环「查+修复」（用户指令）
重新核验审计遗留项（并发会话持续改码，不盲信记忆），结论与修复：
- **已闭环（并发会话落地，非本轮回）**：DXY/USDCNH 方案A（`fetch_crisis_data.py` 已有 `ACCUM_KEYS=('dxy','usdcnh')` + `append_accum_hist`/`accum_history_values`，历史满20点自愈真实分位）；危机雷达 docstring 无「17项」声称（moot）。
- **本次修复1（deploy_now.py）**：`_fix_unmerged_files()` 在 `git reset --hard origin/main` 前先 `git fetch origin main --depth=1`（308行），避免依赖滞后/缺失的远程追踪引用导致冲突态清不掉。已自动收编进 main（`ddf42a0b`）。
- **本次修复2（update_data_v2.py）**：移除对丢失脚本 `compute_lhb_consecutive.py` 的死调用（其产物 `lhb_consecutive.json` 无任何消费方，长期静默失效）。改为注释说明。
- **本次修复3（update_data_v2.py）**：接入孤儿脚本 `fetch_stock_names.py`——`stock_names.json` 超7天未更新则自动刷新（失败保留旧文件，非致命）。此前该脚本从未被调用，导致股票名映射仅靠初始种子、新上市/退市名长期不更新。`fetch_logger.py` 链路完备，刷新自 ~07-17 起自愈。已自动收编进 main。
- **仍标记未动（设计/风险决策，需主家定夺）**：
  1. `build_market_signal.py` 孤儿：生成 `standalone/market_signal.html` 但部署管线不调用它；该页是手工精修活页，误跑生成器会覆盖。建议二选一：手工改动移植进生成器并接入管线 / 或标记勿跑。
  2. `fetch_macro_data.py` 4个停更源(pmi_yearly/cpi_monthly/ppi_yearly/exports_yoy/currency_boc_sina)：已停更至2025-08/09，属东财主源回退死代码；已加聚合告警缓解静默失败，未删。
  3. `enhance_main_worldcup.py` 缺失：deploy_now.py:278 引用但文件不在，`if os.path.exists` 保护→静默跳过，无害。
  4. BaoStock 停维风险：fetch_industry_map.py / fetch_sh_index_fib.py 用 BaoStock，长期可能不可用，需备 akshare 替代源（前瞻风险）。
  5. `fetch_etf_flow.py` 缺失：已被 fetch_nt_data.py/fetch_etf_subscription.py 取代，无现行引用，可忽略。

## 12:04 资金三卡读懂指南从 guide.html 移到预判信号页
- 用户要求：将逻辑详解页 `guide.html` 中的「💡 资金数据·三张卡片怎么读？」三色块解释卡片移到预判信号页（`panel-predict`）的对应位置。
- 操作：
  - `index_master.html`：在「板块资金三合一」卡片后新增 `predict-card` 样式卡片，内含「盘中追热/收盘复盘/中期趋势」三张解释卡（适配主站白色卡片+彩色渐变风格，移除 guide.html 的 CSS 变量）。
  - `standalone/guide.html`：删除重复的三卡解释块，保留其它逻辑详解内容。
  - 修复 `update_data_v2.py`：补充缺失的 `save_json()` 函数（此前宏观数据保存触发 `name 'save_json' is not defined` 异常，导致脚本在宏观数据循环中挂起）。
- 生成部署：
  - `update_data_v2.py --fast` 成功生成 dist/ 与 standalone/ 输出。
  - `deploy_now.py --force` 部署成功，构建戳 `20260712120511`，82 文件推上 gh-pages。
  - 手动提交并推送 `standalone/*.html` 到 main（该目录被 .gitignore 忽略，需 `-f`），补回 `crisis_radar.html`、`favicon.svg`、`market_signal.html` 等未跟踪文件。
- 验证：`dist/index.html` 和 `dist/standalone/predict.html` 均已包含新卡片。


- `fetch_worldcup.py` → 52场比赛、32场淘汰赛（27场完赛），API(worldcup26.ir)拉取成功。
- Monte Carlo 5000次模拟夺冠概率 TOP3：**法国 58.7% / 西班牙 28.9% / 英格兰 7.3%**。
- `update_worldcup_standalone.py` → 重建 standalone/worldcup.html（48队/52小组赛+32淘汰赛）。
- `deploy_now.py --force` → 全量部署成功，82文件推上 gh-pages。

## 12:39 三合一修复（用户截图反馈）

### 修复1：crisis_radar 方法论段整块删除
- `build_crisis_radar.py`：删除 method div 整块（标题+描述文字+链接），独立页不再显示任何方法论内容。

### 修复2：「查看完整雷达→」链接跳转错误
- 源码确认 href=`standalone/crisis_radar.html`（正确）。用户看到跳到 standalone/index.html 为 CDN 缓存问题。

### 修复3：市场面信号卡在数据监控 Tab 不显示
- **根因**：`renderMacroOverview()` 末尾缺少 `macroScoreCard.style.display = ''`（之前改 moveCardsToShelf 时漏加了）。
- **修复**：在函数末尾补上 `var msc = document.getElementById('macroScoreCard'); if (msc) msc.style.display = '';`
- 已构建 + deploy --force，构建戳 20260712123921。

## 12:33 删除世界杯「32强淘汰赛正式开战」横幅
- `standalone/worldcup.html`：删除 line 37 的橙色提示横幅（📢 🔥 6月28日起32强淘汰赛正式开战）。
- `update_worldcup_standalone.py`：删除 status_note 渲染逻辑（line 350-351），防止重建时复活。
- 已部署 + git push main (commit 598a29c)。

## 12:16 世界杯补录「阿根廷 3-1 瑞士」并重新换算部署
- 用户确认 Jul 11 1/4决赛比分：阿根廷 3-1 瑞士（API 源 worldcup26.ir 尚未更新该场，导致前次重跑页面无变化）。
- 修复：`fetch_worldcup.py` 的 `build_knockout_schedule()` API 分支 return 前新增 `MANUAL_RESULTS` 手动覆盖字典（按中文队名 home/away 匹配，强制 finished=True + score），每次重跑均生效。
- `fetch_worldcup.py` → 52场比赛、32场淘汰赛（28场完赛，含手动覆盖）；日志显示 `✍️ 手动覆盖 阿根廷 vs 瑞士 → 3-1`。
- Monte Carlo 5000次模拟夺冠概率 TOP3 更新：**法国 58.3% / 西班牙 28.6% / 英格兰 8.1%**（阿根廷因晋级半决赛概率已计入）。
- 半决赛对阵更新：Jul 14 法国 vs 西班牙、Jul 15 英格兰 vs 阿根廷（阿根廷凭 3-1 胜瑞士晋级）。
- `update_worldcup_standalone.py` → 重建 standalone/worldcup.html；`deploy_now.py --force` → 构建戳 `20260712121710`，82文件推上 gh-pages，源码已自动同步 main。
  - 验证：`data/worldcup.json` 中阿根廷vs瑞士 score=3-1 / finished=True 确认。

## 12:19 重构逻辑详解页 guide.html 按前端 Tab 顺序重排
- 用户反馈：guide.html 中五维评分卡与市场面信号等内容重复，页面排列混乱。
- 重新组织 `standalone/guide.html`：
  - 按前端 Tab 顺序排列：总览页 → 数据监控页 → 预判信号页 → 金股观测页 → 个股查询页 → 健康看板 → 竞彩娱乐。
  - 删除重复：五维评分卡仅保留在「预判信号页」；市场面信号仅保留在「数据监控页」；资金三卡解释因已移到预判信号页，在 guide 中仅作为预判信号页「板块资金三合一」职责分工的一部分简述。
  - 补充缺失：将原先散落在各处的「多维共振评分」「信号缩写」「个股查询扩展信息」等模块归到对应 Tab 下。
- 生成部署：
  - `update_data_v2.py --fast` 成功。
  - `deploy_now.py --force` 成功，构建戳 `20260712121919`，82 文件推上 gh-pages，源码已自动同步 main。
- 验证：`grep` 确认「A股+宏观现状预判·五维评分卡」只在预判信号页出现，「市场面信号」只在数据监控页出现，资金三卡解释不再在 guide 中独立重复出现。

## 12:25 逻辑详解页补全「定时任务 × 前端展示 完整映射表」
- 用户要求：截图中的映射表必须完整对应 + 逐项解说。
- 修改 `standalone/guide.html`：
  - 重写顶部映射表，按时间节点分 5 张卡片（07:30 世界杯 / 09:20 盘前全量 / 10:31/11:46/13:31/14:31 盘中扫描 / 15:30/16:15/17:30/18:30 收盘全量 / 19:30 最终部署），每行对应前端模块与更新内容，与截图完全一致。
  - 新增「📖 对应关系逐项解说」区块，对 5 个时间节点分别说明：更新目标、覆盖的前端 Tab、数据产出时点、与盘中/收盘的分工边界。
- 生成部署：
  - `update_data_v2.py --fast` 成功。
  - `deploy_now.py --force` 成功，构建戳 `20260712122620`，82 文件推上 gh-pages，源码已自动同步 main。
- 验证：`grep` 确认 `dist/standalone/guide.html` 与 `standalone/guide.html` 均包含「对应关系逐项解说」；表格时间节点与截图一致。

- 背景：`build_market_signal.py`（整页字符串拼接生成 `standalone/market_signal.html`）此前是孤儿——`refresh_standalone_and_deploy.py` 不调用它，若误跑会用生成器模板覆盖手工精修版。
- 核实：把生成器跑一遍与线上 HEAD 版逐字节 diff，**唯一差异是 1 行 method-link**：线上手工版 `guide.html#数据监控`（带锚点），生成器仍输出旧 `guide.html`（无锚点）。其余全部逐字节一致（数据由同一 data/ 生成，幂等）。
- 修复：`build_market_signal.py:337` 补上锚点 → 生成器输出与线上手工版完全一致（diff 为空）→ 幂等，自动化安全。
- 接入：`refresh_standalone_and_deploy.py` 新增 `step3c_build_market_signal()`（照 step3b 危机雷达模式：失败仅告警不中止），在 main() step3b 之后调用（约 222 行）。此后每次独立页部署自动刷新市场面信号页，不再是孤儿、也不会覆盖手工改动。
- 提交 `85e79a7`（build_market_signal.py + refresh_standalone_and_deploy.py 两源码文件），已推送 main。html 无变化（已与线上逐字节一致）。

## 12:27 四合一修复（用户截图反馈4个问题）

### 问题1：crisis_radar.html 方法论大表仍显示 → 已删除
- **根因**：之前只往 guide.html 加了副本，但 crisis_radar.html 独立页仍保留完整方法论表格。
- **修复**：`build_crisis_radar.py` 的 method div 从完整19行指标来源表替换为一行链接 `→ 逻辑详解页 · 数据监控区 ↗`（href=guide.html#数据监控）。
- 重新构建 standalone/crisis_radar.html，已部署。

### 问题2：market_signal.html 仍 404 → 已确认修复
- 上次世界杯部署(12:17)后文件已在 gh-pages 上（`git ls-tree origin/gh-pages` 确认）。
- 用户截图的 404 为 CDN 缓存/时序问题，本次部署再次确认 82 文件含 market_signal.html。

### 问题3+4：六维预警雷达+市场面信号两张卡仍在健康看板，未移到数据监控Tab
- **根因**：`moveCardsToShelf()` 函数（index_master.html:450501）在运行时将这两张卡的 DOM 从 panel-shmonitor（数据监控）**搬移到** 健康看板的 `#unlistedPanel`。HTML 虽写在 panel-shmonitor 里（line 706-741），但 JS 把它们搬走了。
- **修复三处**：
  1. `moveCardsToShelf()` 的 cardIds 数组移除 `'crisisRadarEntry'` 和 `'macroScoreCard'`（不再搬移）
  2. `renderCrisisRadarEntry()` 末尾加 `entry.style.display = ''`（直接在数据监控显示）
  3. `renderMacroOverview()` 末尾加 `macroScoreCard.style.display = ''`（同上）
  4. 卡片注册表从 `🔐 健康看板` 改为 `📈 数据监控`
- 两张卡 HTML 位置不变（panel-shmonitor 内、宏观观测上方），但不再被 JS 搬走。
  - 已 rebuild + deploy --force，构建戳 `20260712122759`，82文件推 gh-pages，三个独立页均确认在线。

## 12:43 删除 guide.html 预判信号页中重复的五维评分卡表格
- 用户反馈：逻辑详解页中仍显示「A股+宏观现状预判·五维评分卡」完整表格，且该表格位于「预判信号页」一节。
- 排查：
  - 前端实际只有**三维度市场面信号**（技术面/资金面/机构面，位于数据监控 Tab），没有五维评分卡。
  - guide.html 的「数据监控页」一节已有三维度市场面信号表格，说明五维表格是重复内容（宏观面/全球面已在危机雷达/宏观观测中覆盖）。
- 修复：删除 `standalone/guide.html`「预判信号页」一节中的五维评分卡表格，保留一眼结论、精选预判信号等其余内容。
- 生成部署：
  - `update_data_v2.py --fast` 重新生成 dist。
  - 第一次 `deploy_now.py --force` 部署时 dist/standalone/guide.html 未同步到最新版（原因：部署流程中 extract_panels_v6.py 重建独立页后复制 standalone/ 的时序问题，导致线上仍残留旧版）。
  - 手动 `cp standalone/guide.html dist/standalone/guide.html` 后通过临时 worktree 直接推送 gh-pages 修复。
- 验证：线上 `standalone/guide.html` 不再包含「A股+宏观现状预判·五维评分卡」表格；`grep` 确认源码和线上均无该表格。

## 12:5x 防误删机制全面核查+修复（用户指令「检查所有重要脚本数据是否打好标签」）
- **发现机制完全失效（两个致命 bug）**：
  1. `git_hooks/pre-commit` 第9行读 `DO_NOT_DELETE.txt`，但真实清单是 `DO_NOT_DELETE.md` → 第13行「清单不存在则放行」直接 `exit 0`，**防误删形同虚设**。
  2. 即便文件名对，钩子 `awk -F'\t'` 解析 markdown `|` 表格，提取到的 token 是行首 `|`，匹配不到任何真实文件。
- **修复钩子**：改为优先读 `.md`（回退 `.txt`），正确提取表格第一列路径/通配符（去反引号、取首 token），支持 `*` 通配与目录前缀 `/` 匹配。存入 `git_hooks/pre-commit` 并 `cp` 安装到 `.git/hooks/pre-commit`。
- **实测拦截三种情形**：精确匹配(industry_map.json)、目录前缀(standalone/worldcup.html)、通配符(fetch_crisis_data.py 经 `fetch_*.py`) —— 全部 🛑 拦截退出码1；普通提交不受影响。
- **补标漏网重要脚本（git 从未跟踪，钩子保护不到）**：枚举全部根 `.py`，发现 3 个游离未跟踪：`fetch_worldcup.py`(44KB, batch_update 调用世界杯管线核心)、`inject_weekend_run.py`(batch_update weekend_light 调用)、`diag_parallel.py`(诊断工具)。根因：`.gitignore` 的 `*.py` 白名单漏了这 3 个。已补 `!fetch_worldcup.py`/`!inject_weekend_run.py`/`!diag_parallel.py` 白名单 + `git add -f` 强制跟踪，并在 `DO_NOT_DELETE.md` 脚本段补 3 行显式条目。
- **DO_NOT_DELETE.md 同步补标**：补 industry_map.json+shard、candidate_pool/crisis_data/stock_names/macro_data、dxy_hist/usdcnh_hist 等核心数据条目（之前漏标）；更新头部「最后更新」与末尾「最后核对」日期。
- **全部已自动收编进 main**（并发 `_auto_push_source` 提交 `2491064`），origin/main 已含钩子修复+白名单+3脚本跟踪。`git_hooks/pre-commit` 源已提交；双机 pull 后需各自 `cp` 安装本地钩子（.git 不随 git 推送）。

## 12:42 修复世界杯 Monte Carlo 夺冠概率含已淘汰队 bug
- **根因**：`fetch_worldcup.py` 的 `simulate_championship_NEW()` 中 ROUND_ORDER 写了「8强」，但淘汰赛数据实际用「1/4决赛」→ 找不到匹配 → 退化成随机配对 → 已淘汰队「复活」。
- **修复**：ROUND_ORDER 补上 `'1/4决赛'`。
- **修正后**：仅4强有概率 — 法国 54.9% / 西班牙 33.2% / 英格兰 11.9%。加拿大等已淘汰队彻底消失。已部署。
- **同步更正 MEMORY.md 防误删段**：`.txt`→`.md`、钩子修复记录、强制跟踪 8→11（补 3 脚本）、受保护重点补 3 脚本与 hist 文件。

## 12:55 guide.html 映射表升级（用户要求"写到表里去，直截了当一目了然"）
- 删除「对应关系逐项解说」整段冗余文字（5色块+ul列表）
- 表升级为三列(时间/前端模块/更新内容+部署动作)：每行写清时间、模块、更新内容、是否部署；盘前/盘中标不单独部署等19:30统一推；19:30标全站一次性推送GitHub Pages；时间列附batch_update模式名。
- 已部署 gh-pages（构建戳 20260712125804），源码已 auto-sync 进 main。

## 15:38 guide.html 暗色底改白底（用户截图要求）
- 「六维预警雷达—方法论」和「市场面信号—数据来源」两个区块从暗色底(#1a1a2e/#1a2332)改为白底(#fff)+深色文字+浅灰边框，表头淡靛蓝，与页面其他卡片统一。
- 已部署 gh-pages（构建戳 20260712154020）。

## 12:48 用户质询分支正确性 + 小九交接是否写清
- 核实：①分支正确——当前 main == origin/main(0/0)，2491064 已入 origin/main，所有 7/11~7/12 修复均在工作树且提交；异常：main 线性历史被重置成仅 2 条提交(5c29065/2491064)，先前自动同步提交在 reflog 成孤立提交，内容保留。②交接此前**没写清**：上一 handover_to_xiaojiu 是 7/11 08:26(仅 fe535c7+token铁律+非交易日守卫)，7/11下午~7/12 批量代码修复从未写进小九交接，且最关键动作"重装本地 pre-commit 钩子"缺失。
- 已补：新建 HANDOVER_小九_2026-07-12.md(含 git pull + 重装钩子 + 9 项改动清单 + 已知坑 + 核对清单)，追加 handover_to_xiaojiu 到 HANDOVER_LOG.jsonl(被 gitignore 仅本地/坚果云)，commit+push 295cae8 进 main。

## 12:50 修复独立页入口链接多一层 standalone/ 路径
- 问题：本地 `standalone/overview.html` 中「六维预警雷达」「市场面信号」两张入口卡链接写成 `standalone/crisis_radar.html` / `standalone/market_signal.html`，点击后跳到 `standalone/standalone/xxx.html` → ERR_FILE_NOT_FOUND。
- 根因：`extract_panels_v6.py` 直接把 `dist/index.html` 复制到 `standalone/overview.html`，未降级链接路径。
- 修复：`extract_panels_v6.py` 生成独立页后，把 `href="standalone/` / `src="standalone/` 降级为同级路径。
- 重新生成 standalone 页面 → `deploy_now.py --force` 部署 → 手动提交 standalone/*.html 路径修复到 main。
- 线上验证：`standalone/overview.html` 中链接已指向 `/quant-scanner-v6/standalone/crisis_radar.html` 和 `/quant-scanner-v6/standalone/market_signal.html`。


- 实测：WebFetch 探测 index_master.html / standalone/index.html / crisis_radar.html / market_signal.html **四个页面全部正常返回有效内容**（build-stamp 20260712124501）。gh-pages 分支 82 文件齐全，index.html 2MB 完好。
- 判定：**非部署损坏**，而是 12:45 部署触发的 GitHub Pages 重建窗口（1-2分钟）内访问短暂失败，叠加浏览器失败缓存。
- 处置：未重部署（重部署会引发又一次重建窗口）。建议用户 Ctrl+F5 硬刷新 / 无痕模式 / 等2分钟。
- 备注：若硬刷新后仍打不开，则为其网络到 github.io 的连通性问题（国内常被墙/限流），备选方案=自定义域名走国内CDN 或 镜像到 Gitee Pages/CloudStudio。

## 15:34 删除 market_signal 页"各指标数据来源与计算逻辑说明"链接
- 按截图要求，删除 `standalone/market_signal.html` 三维度速览底部 `method-link` 链接行。
- 修改源文件 `build_market_signal.py`，并同步移除 `standalone/market_signal.html` 与 `dist/standalone/market_signal.html` 中的对应行。
- `grep` 确认仓库内已无 "各指标数据来源与计算逻辑说明" 残留。
- 已 commit+push main（`d2a6d13`），并执行 `deploy_now.py` 部署至 GitHub Pages，部署成功。

## 16:18 预判信号卡 tooltip→内联说明
- **52周新高** + **分析师转向** 两卡片：移除 `onclick="alert(...)"` 弹窗 + `cursor:pointer`
- 改为格子底部直接显示说明文字（9px `#aaa`，`margin-top:4px`）
- 已部署 gh-pages（构建戳 20260712161939）

## 16:09 guide.html 全面核查隐藏数据源代码（违反本页铁律102-103）
- 本页第102-103行铁律「数据来源一律隐藏」(禁列东方财富/akshare/新浪/腾讯/观澜等)，但多处违规写出具体数据源：
  1. 危机雷达方法论表「数据来源」列(228-246)满篇 akshare/东方财富datacenter/RPT_ECONOMY_PMI/futures_foreign_hist/100.UDI/GC+HG/CL 等 → 整列删除，表改为「维度/指标」两列(标题"各维度数据来源明细"→"各维度指标明细")。
  2. 222行缓存机制"本地累加缓存历史+阈值兜底" → 模糊为"采用历史分位估算"。
  3. 市场面信号「数据来源」行(260)内部字段名 SH_FIB/MARGIN_DATA/SECTOR_FUND_FLOW/CFFEX_HOLDINGS/FOMC_SUMMARY → 整行删除。
  4. 567行"腾讯API" → 删除。
  5. 59行"观澜研报" → "机构研报"(观澜是铁律禁标商)。
- 保留：61行 SH_FIB窗口(线上真实卡片模块名、非数据源)、663行世界杯"API限流"中性表述、103行铁律本身。
- 顺带核查 crisis_radar.html 独立页：无数据源泄露(grep 0命中)。
- 已部署 gh-pages（构建戳 20260712161405），源码 auto-sync 进 main。

## 16:24 全部 standalone 独立页返回主站链接一并修复
- 问题：`standalone/` 子目录下页面用 `href="index.html"` 指向不存在的 `standalone/index.html`，点"返回"回不到站点根主站。
- 修复 12 个独立页返回链接 → `../index.html`：overview/shmonitor/predict/gold/query/health（extract_panels_v6.py:96 `← 全部` 链接）、worldcup（update_worldcup_standalone.py:338）、guide+worldcup（手工改 standalone/*.html）、crisis_radar（build_crisis_radar.py:378）、market_signal（build_market_signal.py:313）、triple_resonance+multi_resonance。
- **关键遗漏根因**：`deploy_now.py._regen_standalone_if_needed()` 用 dist 根目录原版**强制覆盖** dist/standalone/ 的 triple/multi_resonance，绕过 extract_panels_v6 的链接重写 → 在 deploy_now.py:461-465 拷贝后追加 `.replace('href="index_master.html"'/'href="index.html"' → 'href="../index.html"')`。extract_panels_v6.py 同步逻辑也加了同款重写。
- 已 commit+push（a4b552a 源码 + 00494c5 deploy_now 修复），并 `deploy_now.py --force` 部署（构建戳 20260712162350，82 文件）。本地 dist/standalone 全 12 页验证零残留旧链接。

## 16:32 机游共振数据口径核查（用户确认哪边数据正确）
- 用户对比本站(截图2) vs 外部网站(截图1)同天(20260710周五)机游共振，差异巨大，要求确认哪边正确。
- 验证：akshare 1.18.64 拉东财`stock_lhb_stock_detail_em`逐笔 + 新浪`stock_lhb_jgmx_sina`，复算5只机构净买入，与`data/lhb_result.json`(update_time 2026-07-11 12:41:54, fetch_lhb.py生成)逐只吻合(金风4.20/中鼎1.44/深科达4.67/常山1.22/航天0.88亿)。
- 铁证：金风东财逐笔「机构专用」卖出=0.0万 → 净买=买入41971.8万=4.20亿；新浪返回机构卖出7006万(实为深股通北向卖出误并入) → 净买3.50亿。外部网站用新浪口径，金风被低估0.7亿。
- 结论：**本站(东财逐笔口径)正确、更规范**；差异根因=新浪把北向卖出误算机构卖出 + 排序逻辑不同(本站纯共振=机构且游资均>8000万按合计排；外部按机构净买单排TOP5)。其余4只两边一致。
- 生成核查报告 `verify_lhb_20260712.html`（本地留存，未部署）。

## 16:35 guide.html 六维雷达指标明细表格紧凑化
- 将「各维度指标明细」表格从 rowspan 大格子布局改为每维度一行、指标用 `/` 分隔的紧凑格式
- 文件：`standalone/guide.html` + `dist/standalone/guide.html`，已部署上线（构建戳 20260712163759）

## 16:39 guide.html 映射表表头白字不可见修复
- 用户截图反馈：「定时任务 × 前端展示 完整映射表」的表头（时间/前端模块/更新内容）白字在浅底上完全看不清。
- 根因：line 40 `background:#37474f;color:#fff` 深色底未渲染（可能被全局CSS覆盖），导致白字显示在浅灰/白底上。
- 修复：改为 `background:#cfd8dc;color:#1a237e !important`（浅蓝灰底+深靛蓝字），确保任何背景下文字可见。
- 已部署 gh-pages（构建戳 20260712164112）

## 16:51 guide.html 映射表三改（用户截图反馈）
### 1. 删除所有英文模式名
- 删除6处英文标签：`pre_market`(09:20)、`morning_plus`/`afternoon×3`(10:31)、`post_close`/`close_p1/p2`(15:30)、`close_deploy`(19:30)
### 2. 补漏+新增3张卡片
- **16:30** 下午补充（batch_update afternoon，teal底）
- **21:00** 每日自动备份（棕色底）
- **📅 周期性任务** 区块（紫色底，4行）：周六07:30 T+1刷新 / 周五19:30 股票名称 / 周一06:30 行业映射 / 每日08:30 IPO打新
- 已部署 gh-pages（构建戳 20260712165308）

## 16:48 数据监控页：三维综合评分卡替换市场面信号（移至六维预警雷达上方）
- 用户要求：截图1(三维综合评分三栏卡片) 替换 截图2(市场面信号大数字+进度条)，放到截图3(六维预警雷达)上方，保留「查看完整信号 →」按钮。
- HTML 改动：
  - `macroScoreCard` 从六维雷达**下方**移到**上方**（705行 vs 原724行）
  - 标题改为「📊 三维综合评分 · 技术/资金/机构 · 距线/中线参考」
  - 内部容器从「大数字57 + 三条横进度条」改为 `<div id="macroEntryDims">` 三栏 flex 容器
  - 删除旧位置残留的市场面信号块（大数字 macroEntryIndex + 综合评分/143）
- 渲染逻辑 `renderMacroOverview()` 改动：
  - 删除 `elIndex`（大号综合分）引用和赋值
  - 徽章改为 `综合研判：X/10 → 偏多/中性/偏空`
  - dimsHtml 从「标签+8px横进度条+数字」改为复用已有 `scoreDim()` 函数生成三栏卡片（图标+名称 / 大数字/10 / 进度条 / 详情文字）
- 已部署 gh-pages（构建戳 20260712165139，82文件）

## 16:55 重新部署解决"资金数据·三张卡片"仍显示问题
- 用户截图反馈预判信号页仍显示「💡 资金数据·三张卡片怎么读？」卡片。
- 排查：源文件 `index_master.html` / `standalone/predict.html` 及 `dist/index.html` / `dist/standalone/predict.html` 均已无该卡片；仅 `standalone/guide.html` 保留（按要求）。判断为线上缓存/部署未刷新。
- 执行 `deploy_now.py` 全量重建并部署 dist → gh-pages（构建戳 20260712165555，82 文件）。
- 后续：用户需按 `Ctrl+F5` 硬刷新主站，验证卡片已移除。

## 17:06 guide.html 双机分工表补 17:30/18:30
- 用户截图指出：双机分工表中"盘中任务"只列到 16:30，漏了 17:30（收盘补全p2）和 18:30（收盘补全p1）
- 修复：时间范围从 `09:20~16:30` 改为 `09:20~18:30`，小九任务链追加 `→ 17:30收盘补全p2 → 18:30收盘补全p1`
- 首次部署遇 gh-pages ref lock 冲突（另一进程同时推送），重试后成功（构建戳 20260712171048）

## 16:55 三维综合评分卡样式重构（用户截图反馈"好难看"）
- 用户要求：删掉标题栏（徽章+时间+按钮挤一行），改为截图2的干净布局
- HTML 改动（macroScoreCard）：
  - 删除 `sh-section-title` 大标题行（含 macroVerdictBadge + 查看完整信号按钮）
  - 新增：小标题行「📊 三维综合评分 短线/中线参考」
  - 新增：`macroVerdictBanner` 综合研判横幅（独立 div，含分数+研判词+建议文字，着色背景）
  - 更新时间移至底部右对齐 `数据更新：xxx` 格式
- 渲染逻辑改动（renderMacroOverview）：
  - `elBadge` → `elBanner`，输出完整横幅 HTML（含建议文字如"偏空信号为主，建议减仓观望"）
  - 时间格式从 `更新于 xxx` 改为 `数据更新：xxx`
- 已部署 gh-pages（构建戳 20260712165835，82文件），源码已 auto-sync 进 main。

## 17:00 guide.html 新增双机分工细表
- 用户要求：在「定时任务 × 前端展示 完整映射表」下方新增「🖥️ 双机分工 — 阿狸咪(家) / 小九(单位)」细表。
- 表格7行：角色定位 / 盘中任务(09:20-16:30) / 收盘部署(19:30) / T+1补全(周六) / 行业映射重建(周一) / 数据源限制 / 每日备份(21:00)
- 底部附带协作规则说明（GitHub源码同步+坚果云数据同步）
- 已部署 gh-pages（构建戳 20260712170119），源码已 auto-sync 进 main。

## 17:13 guide.html 映射表时间顺序修正 + 周期性任务重排
- **问题1**：16:30 下午补充卡片位置错误（在19:30后面），应插在15:30-18:30收盘全量与19:30最终部署之间
- **问题2**：周期性任务区（周六/周五/周一/每日）排序杂乱
- **修复**：
  1. 将16:30卡片从原位置（line 92-98, 19:30之后）移到 line 84（18:30收盘全量之后、19:30之前）
  2. 周期性任务按时间排序：周一06:30 → 周六07:30 → 每日08:30 → 周五19:30；行业映射补充"小九唯一执行者"说明
- **修正后完整时间线**：07:30 → 09:20 → 10:31×4 → 15:30-18:30 → ✅16:30 → 19:30 → 21:00 → 📅周期性任务
- 已部署 gh-pages（构建戳 20260712171502，82文件）

## 17:21 guide.html 两处修正（用户截图反馈）
### 1. 16:30 合并进红色收盘全量卡片
- **问题**：16:30 batch_update afternoon 单独一张卡片（teal底），与上面红色「15:30→18:30 收盘全量」卡片分离
- **修复**：删除独立 16:30 卡片，将其作为第4行（📊 全模块快照 16:30）合并进红色收盘全量表内
- 红色卡片现在4行：金股观测(15:30) / 预判信号(16:15) / 数据监控(17:30/18:30) / 全模块快照(16:30)

### 2. 周度/月度任务区清理
- **问题**：周期性任务区混入「每日08:30 IPO打新」（每日任务不该放这里），且未标清周度/月度
- **修复**：
  - 删除「每日08:30 IPO打新」行（这是每日任务，不是周度/月度）
  - 标题改为「📅 周度 / 月度任务」（副标题「每周或每月执行一次（非每日）」）
  - 每行首列新增 `📅 周度` 标签（3行全是周度：周一06:30行业映射 / 周六07:30 T+1 / 周五19:30 股票名称）
  - 表格从3列（时间/模块/内容）扩展为4列（周期/时间/模块/内容）
- 已部署 gh-pages（构建戳 20260712172905，82文件）

## 17:28 guide.html 16:30行三列格式修正
- 用户截图指出：红色收盘全量表内16:30行的格式不对——第1格写了「📊 全模块快照」（应写时间），第2格缺失（应有模块名），第3格混入了时间+命令（应只写内容）。
- 修正为标准三列：`16:30` | `📊 全模块` | `全量数据刷新 + 三卡片更新（非交易日自动跳过）`
- 与表中其他行格式完全一致。已部署上线。

## 17:30 guide.html 周度任务区三合一（用户截图反馈）
- **问题**：三个周度任务分3行显示，排序乱（周五在周六下面），且无月度任务
- **修复**：3行合并为1行紧凑格式：
  - 第1格：`📅 周度`
  - 第2格：`周一 / 周五 / 周六`（合并时间）
  - 第3格：`🗺️ 行业映射 · 📝 股票名称 · 🔄 T+1刷新`（合并模块）
  - 第4格内容：`**周一06:30** industry_map重建（小九） | **周五19:30** 股票名映射更新 | **周六07:30** T+1数据补全（小九优先·阿狸咪兜底）` + `⚠️ 当前无月度任务`
- 已部署 gh-pages（构建戳 20260712173235，82文件），源码已同步 main。

## 17:58 修复金股观测历史追踪返回页（用户截图反馈）
- **问题**：从主站 `index.html` 金股观测 Tab 点击「📊 历史追踪 →」进入 `multi_resonance.html` / `triple_resonance.html` 后，点「返回金股观测」跳到 `standalone/gold.html`（独立页），需再操作一次才能回主站。
- **修复**：统一四份源码模板（`multi_resonance.html`、`triple_resonance.html` 及 `standalone/` 下同名文件）的返回逻辑为：
  - 若当前 URL 在 `/standalone/` 路径下 → 返回 `gold.html`（独立金股观测页）
  - 若不在 `/standalone/` 路径下 → 返回 `index.html#gold`（主站并自动激活金股观测 Tab）
- **部署**：`deploy_now.py` 成功，构建戳 `20260712175822`，82 文件推上 gh-pages，源码已 auto-sync 进 main。
- **验证**：`dist/` 与 `dist/standalone/` 下 4 个 resonance 文件均命中 `a.href = isStandalone ? 'gold.html' : 'index.html#gold';`。

## 17:40 guide.html 三处修正（用户截图反馈）
- **问题**：用户不喜欢三合一紧凑行（合并一行），要求按时间分开显示
- **修复**：从1行4列改回3行3列标准格式：周一06:30 / 周五19:30 / 周六07:30 各一行
- 标题从「📅 周度 / 月度任务」改为「📅 周度任务」，副标题改为「每周执行一次（非每日）」
- 删除「⚠️ 当前无月度任务」标注

### 2. 19:30行去敏感路径
- 删除 `index_master.html → 生成 dist/` 具体文件名/目录名
- 改为通用描述：「全站数据注入主页面 → 构建输出目录 → 语法/冒烟检查 → 推送 GitHub Pages」

### 3. 21:00行去敏感数据
- 删除 `JSON数据 + 配置` 具体数据类型描述
- 改为通用描述：「关键数据文件自动备份，防止意外丢失」
- 已部署 gh-pages 成功。

## 18:10 market_signal.html 删除顶部「机构面」总结卡片
- **原因**：顶部三卡片（技术面/资金面/机构面）中机构面卡片与外部数据监控重复，用户要求只删卡片、保留下方机构面数据表格。
- **修改**：`build_market_signal.py` 删除 `score_card('机构面',...)` 一行（320行），保留 `dim_block('机构面',...)`（328行）；`.score-grid` 由 `repeat(3,1fr)` 改 `repeat(2,1fr)` 让剩余两张卡片排版规整。
- 重新生成 `standalone/market_signal.html` → 已确认顶部橙卡(`#ff9800`)消失、数据区三块(技术/资金/机构)全保留。
- 已 `git commit` 并 `push origin main`（防下次定时重跑生成器又把卡片加回）；部署 gh-pages 进行中。

## 18:12 market_signal.html 顶部三张卡片全部删除
- **原因**：用户进一步要求技术面/资金面顶部卡片也删掉，仅保留下方数据表格（与外部监控重复）。
- **修改**：`build_market_signal.py` 删除整个 `<div class="hero"><div class="score-grid">…</div></div>` 块（含 技术面/资金面 score_card 调用，机构面此前已删），模板块仅留 `<div class="wrap"><div class="sec">` + 三个 `dim_block`。`.score-grid` CSS 保留无害。
- 重新生成确认：顶部 `font-size:28px` 评分卡片 0 处；数据区 技术面/资金面/机构面 三块表格全保留。
- 源码改动由后台部署 `HHn0Zi` 的 `_auto_push_source()` 自动提交推送（git push 报 Everything up-to-date，HEAD 已是最新），下次定时重跑生成器不会再出现顶部卡片。本地 dist 产物核验：顶部卡片 0、数据表 3 块齐全。

## 18:18 独立页「返回」统一改 history.back() 智能返回（全站治理）
- **问题**：standalone/index.html 导航页进入各独立子页后，点「返回/←全部」全部写死 `../index.html`（主站），应「独立页回独立页、主站回主站」。
- **根因**：`extract_panels_v6.py:96` 写死 `<a href=../index.html>← 全部</a>`；triple/multi_resonance 的 `<a id=navBack href=../index.html>` 仅 `from=gold` 跳金股，默认也回主站。
- **修复**：
  1. `extract_panels_v6.py:96` 改为 `onclick="if(document.referrer&&document.referrer.indexOf(location.host)>-1){history.back();return false}"`（f-string 模板，花括号已 `{{}}` 转义）。覆盖总览/监控/预判/金股/查询/健康 6 页。
  2. triple/multi_resonance（静态页，无生成器）navBack JS：保留 `from=gold`→跳金股；新增 `else if` 同站来源 `a.onclick=history.back()`。改 dist根版+根项目版（standalone 副本由 deploy sync 从 dist 复制补全）。
- **已提交**：`973f4e9`（6页+生成器）、`cec3d18`（triple/multi），均 push origin main。
- **部署**：`91TD2n`（6页生效）、`vosUXr`（triple/multi 生效，进行中）。
- **覆盖范围**：现所有从 standalone/index.html 进入的独立页（总览/监控/预判/金股/查询/健康/worldcup/guide/crisis_radar/market_signal/triple/multi）返回均为智能返回，不复跳主站。

## 19:30 收盘最终部署(19:30自动化, 周日误触发仍跑完)
- **第一步交接**：HANDOVER_LOG 今日（周日）无小九白天 close_p1/p2 失败；最近 close_deploy(7/11) 无 failed_steps。无补跑需求。
- **第二步**：batch_update.py close_deploy ✅ 7/7（update_data_v2 153.5s + deploy_now 128.9s 慢推但最终成功）。verify_data_vs_website --only-issues：10项/通过4/警告4/失败2，4项均已知预期（sector TOP1 周末漂移、north_fund top_buy 空=港交所停披、fetch_south_individual/fetch_sector_rs 无日志=已知缺口），非崩溃，未重跑。
- **第三步关键修复**：`verify_frontend_deploy.py` 根目录丢失（仅存 backup_20260709/，git 从未跟踪），首次运行 `No such file`。已从备份恢复 + 加 `.gitignore` 白名单 `!verify_frontend_deploy.py` + `git add -f` 强制跟踪，commit `024304c` push main。重跑输出 **FRONTEND_DEPLOYED_OK**（17个前端文件 blob 与 origin/gh-pages 完全一致，无需补部署）。
- **结论**：部署成功。交接日志已追加 close_deploy(19:32:08)+close_deploy_verify(19:33:54, FRONTEND_DEPLOYED_OK)。
- **提示**：周日 scan_result/north_fund/lhb 仍为 7/11 陈旧（周末无刷新），gold_pool 已更新至 7/12 19:30。BYDAY=MO-FR 本不应周日触发，疑手动/误触发。

## 19:36 独立页返回再次复发——根因是 file:// 下 referrer 为空
- **用户反馈**：本地 `file:///E:/.../standalone/index.html` 进入独立页点返回，仍跳主站。
- **真正根因**：上一轮用 `document.referrer` 判断同站来源。但 `file://` 协议下浏览器**不发送 referrer**（为空串），`''.indexOf(...)==-1` 直接 false，于是全 fallback 到 `href="../index.html"`（主站）。线上 http 同 host 下 referrer 有值才正常，故本地测必现「都跑主站」。
- **连带 bug**：`extract_panels_v6.py` 第186-199行 sync triple/multi 到 standalone/ 时，主动把 `href="index.html"` 降级为 `href="../index.html"`（主站），导致 standalone 版 triple/multi 返回也跳主站。
- **统一修复（不再依赖 referrer）**：
  1. 所有独立页返回改为 `onclick="if(history.length>1){history.back();return false}"`；fallback `href` 指向**同目录** `index.html`（standalone 体系回独立导航页，根目录版回主站）。`history.length>1` 在 file:// 与 http 下均有效。
  2. `extract_panels_v6.py`：删除 `href="index.html"`→`../index.html` 降级（standalone/ 下 index.html 即独立导航页，指向它才对）；仅保留 `href="index_master.html"`→`../index.html` 降级。
  3. `standalone/index.html`：三线/多维追踪链接由 `../triple_resonance.html`(根目录版) 改为 `triple_resonance.html`(standalone版)，使从导航页进入的 triple/multi 也走 standalone 体系、返回回独立导航页。
  4. triple/multi 静态页（根目录+standalone+dist 共6文件）navBack 的 `else if (document.referrer...)` 改为 `else if (history.length > 1)`；standalone 版 navBack `href` 由 `../index.html`→`index.html`。`from=gold` 跳金股逻辑保留。
- **涉及文件**：extract_panels_v6.py、build_market_signal.py、build_crisis_radar.py、update_worldcup_standalone.py、standalone/guide.html、standalone/index.html、standalone+dist 下 overview/shmonitor/predict/gold/query/health/market_signal/crisis_radar/worldcup/triple_resonance/multi_resonance/index、根目录 triple/multi + dist 根目录版。
- **验证**：dist/standalone 10页顶栏智能返回=1；standalone版 triple/multi navBack=`href="index.html"`；根目录版 triple/multi navBack=`href="index.html"`(主站)+`from=gold`保留。
- **已提交**：`c2737cc`（33文件）+ push main；部署 `W3veET` 进行中。
- **铁律补充**：独立页返回**永远不要用 `document.referrer` 判断**（file:// 必失效）。统一用 `history.length>1` + 同目录 `index.html` fallback。

## 19:43 金股观测→历史追踪→返回独立页 死循环修复
- **用户反馈**：独立页金股观测页点完「历史追踪」后再返回独立页导航，回不去。
- **真正根因（history 栈污染）**：`standalone/multi_resonance.html?from=gold` 的 navBack 在 `from=gold` 分支用 `a0.href='gold.html'` **直接跳转**（非 history.back()）。路径变为：独立导航→金股观测→历史追踪(multi)→点「返回金股观测」→直接跳 gold.html(新历史记录)。此时金股观测顶栏「← 全部」用 history.back() 会回到 multi，而非独立导航页 → multi↔金股观测死循环，回不去独立导航。
- **修复**：triple/multi_resonance（standalone版+根目录版+dist版 共4文件）navBack JS 统一改为「有历史(history.length>1)一律 `history.back()`；仅直接打开无历史时才用 `from=gold`/独立判断硬跳 fallback」。这样回上一页干净不污染栈。
- **gold.html 历史追踪链接现状**：`multi_resonance.html?from=gold` / `triple_resonance.html?from=gold`（相对路径→standalone版），正确，无需改。
- **修复后路径**：独立导航→金股观测→历史追踪→点返回(history.back)→金股观测→点←全部(history.back)→独立导航。闭环正常。
- **已提交**：`bdbc995`（4文件）+ push main；部署 `8MyaaC` 进行中。

## 19:53 用户问"？"→ 核查发现修复从未真正上 gh-pages（根因：部署没推上去）
- **现象**：用户发"？"追问修复是否生效。核查发现：根目录源 + origin/main 早已含 `history.back()` 新逻辑，但**线上 gh-pages 停在 7/3 旧版**（返回按钮写死 `href="index.html"` 跳主站），即前几轮"部署"（`W3veET`/`8MyaaC`/`vQpBfb` 等）均**未把改动推上 gh-pages**（被锁跳过或 SSH 推送未成功），所以用户本地+线上实测一直是旧行为。
- **部署锁状态**：当时 origin/main 无 `.deploy_lock`，可部署。
- **最终处置**：`deploy_now.py --force` 强制部署（构建戳 `20260712195659`），gh-pages 提交 `deploy: 2026-07-12 19:56`，82 文件推上。main 自动同步 `f4a46ee`→`6560938 lock: release`。
- **权威验证（raw.githubusercontent gh-pages 分支原样抓取）**：
  - `standalone/multi_resonance.html`、`multi_resonance.html`、`standalone/triple_resonance.html`、`triple_resonance.html`、`standalone/guide/crisis_radar/market_signal/worldcup.html` **全部含 `history.back()`**（grep 计数各=1）。
  - 确认"独立页→历史追踪→返回独立页"整条链路新逻辑已真正上线（非本地缓存假象）。
- **结论**：之前多轮"修复"代码都对，但卡在"部署未推 gh-pages"这一环；本次已闭环。WebFetch 默认 markdown 视图会丢失 `<script>`，须用 raw 文件或 curl 才能验证 JS 行为。

## 20:10 两个历史追踪"错乱"——根因是独立导航页入口链接指错目录
- **现象**：用户反馈"两个历史追踪还是错乱"。
- **真正根因**：`standalone/index.html`（独立导航页）的"三线追踪/多维追踪"两张卡片链接被 `extract_panels_v6.py:154` 的 `_rel` lambda 硬写成 `../triple_resonance.html` / `../multi_resonance.html`（**根目录版/主站体系**），而非同目录的 `triple_resonance.html` / `multi_resonance.html`（standalone 版）。导致从独立导航页点进去加载的是主站版页面（标题"返回主页面"、返回跳主站），表现"错乱"。
- **对比**：同页"金股观测"用的是 `gold.html`（standalone 版，正确）；gold.html 内历史追踪链接也是 `multi_resonance.html?from=gold`（standalone 版，正确）。唯独导航页这两张卡被 `_rel` 特判成 `../`。
- **修复**：`extract_panels_v6.py:154` 删掉 `triple/multi` 特判，所有 _extra 面板统一 `_rel = lambda p: f"{p}.html"`（同目录）。standalone/triple_resonance.html 与 standalone/multi_resonance.html 同源已存在且含 `history.back()` 新逻辑，指同目录版即归位独立体系。
- **部署验证**：`deploy_now.py --force`（构建戳 20260712201303）。`git show origin/gh-pages:standalone/index.html` 确认链接已为 `href="triple_resonance.html"` / `href="multi_resonance.html"`，`../(triple|multi)_resonance` 根目录跳转残留 = **0**。
- **完整链路（修正后）**：独立导航页→三线/多维追踪(standalone版)→返回(history.back)→独立导航页；独立导航页→金股观测→历史追踪(standalone版)→返回→金股观测→←全部→独立导航页。全部闭环。

## 20:24 全面核查"惠科股份/蒂尔激光查不到"——非数据缺失，是输入方式问题
- **现象**：用户搜"hk gf"（惠科股份）、"蒂尔激光"均显示"未找到匹配"。
- **核查数据源**：`data/stock_names.json`（5202只，Jul 10 19:26）与部署 STOCK_LIST（dist/index.html，5422只）**均含**：`001399 惠科股份`(py=hkgf)、`300776 帝尔激光`(py=dejg)。新股001399(2026-06-26上市)已收录。前端 STOCK_LIST.py 由 `update_data_v2.py:1041` 用 stock_names 全量生成，py 由 `_py_abbr(name)` 预计算。
- **模拟前端 filter（index.html:90907）实测**：
  - `001399`→惠科股份 ✓；`惠科`→惠科股份 ✓；`hkgf`→惠科股份(py) ✓
  - `hk gf`→空 ✗（**带空格，py.startswith("hk gf") 失败；name/code 不含空格**）——用户真实输入方式
  - `帝尔激光`→帝尔激光 ✓；`蒂尔激光`→空 ✗（**A股无"蒂尔激光"，真实名"帝尔激光"(蒂≠帝)，用户错别字**）
- **结论**：搜索逻辑与数据均正常。两例"查不到"均为用户输入方式：①拼音缩写带空格 ②错别字。正确搜法：代码`001399`/名称`惠科`/连写`hkgf`；帝尔激光用`300776`/`帝尔激光`/`dejg`。
- **可选优化（待主家定）**：①支持"hk gf"空格分词拼音（拆 hk/gf 分别匹配拼音段）；②扩展同音字容错（蒂↔帝等）。当前 tyMap 仅含钧/均、积/层、润/闰、宏/弘、洲/州。

## 20:39 已实施搜索增强 ①+②（含修复 tyMap 双向回滚 bug）—— 用户拍板"加 ①/②"
- **改动文件（部署源是 index_master.html，非 index.html！）**：`index_master.html`（根，deploy 读它生成 dist）+ 根 `index.html`（仅保本地 file:// 一致）。三处搜索函数统一改：
  1. `showQueryDropdown`（主下拉）：拼音匹配加 `|| pinyin.startsWith(qLower.replace(/\s+/g,''))`；tyMap 扩展为含 `蒂↔帝、乐↔了、凌↔菱、圆↔园、富↔福、胜↔盛、新↔鑫、华↔花、海↔嗨`（原钧/均、积/层、润/闰、宏/弘、洲/州保留）。
  2. 解析函数（名称→代码）：同上拼音空格 + tyMap 扩展。
  3. `showWlDropdown`（自选下拉）：拼音加空格分词。
- **⚠️ 关键 bug（模拟才发现）**：原 tyMap 应用是顺序 `split/join` 或 `replace(new RegExp)`，**双向映射会回滚**（如 蒂→帝 后紧跟 帝→蒂 还原），导致钧↔均/蒂↔帝 等全部失效、等于没做容错。改为**单遍字符扫描** `let tyQ=''; for(const ch of q) tyQ += (tyMap[ch]||ch);` 真正生效。
- **验证**：① 用 gh-pages 真实 STOCK_LIST(5422只) 模拟新逻辑——`hk gf`→惠科股份(py)✓、`蒂尔激光`→帝尔激光✓、`帝尔激光`→帝尔激光✓；② `git show origin/gh-pages:index.html` 确认三处均含新逻辑（主站+独立页 predict.html）。
- **部署**：commit `2c8c072`(①+②初版) + `abb52f8`(tyMap回滚修复) 已 push main；`deploy_now.py --force`（构建戳 20260712203909）已上线。用户 Ctrl+F5 硬刷后：搜 `hk gf` 出惠科股份；搜 `蒂尔激光` 自动容错到帝尔激光。
