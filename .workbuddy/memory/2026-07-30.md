# 2026-07-30 工作日志

## v8 修复（已推送 main 0e7f572，GitHub Pages 从 main 直接服务=已上线）
- 修复盘后 ETF 三卡"空卡"根因：`renderEtfHeat/renderEtfPulse/renderEtfDaily` 原误置于运维子页 IIFE，盘后脚本调用时报 `is not defined`。已重建到盘后脚本作用域内，并加 etf-heat/etf-pulse CSS。
- ETF 成交额热度榜：按宽基/行业/主题/跨境分类各 TOP4，显示涨/跌家数+合计成交额（数据源 ETF_INTRADAY_HEAT.js，盘中实时）。
- ETF 5分钟量能脉冲：近4个5min切片小柱+买卖力判定（ETF_PULSE.js）。
- ETF 日监控卡：数据源未接入时优雅显示"待接入"（ETF_DAILY_MONITOR.js 目前 no_data，需小九接 westock data_fund_flow / data_etf）。
- 新增 v6备忘录 tab：v6_memo.html（iframe 加载 v6 逻辑详解页）已上线。
- 清理孤儿卡片报错：renderThermo/renderCrisis 写到的 phThermoBody/phCrisisBody 在 v8 布局中已无对应卡片，加空卡片守卫消除控制台报错。
- 浏览器实测：pmLastErr=(none)，ETF/Sector/Macro/Fib/Mff 卡均渲染，v6备忘录 iframe 加载 101KB 逻辑详解。

## v6 回滚状态（关键）
- 本地 v6 已 `git reset --hard 4fa4c42`（2026-07-29 07:45 的"好版本"），本地 `dist/index.html` 即 8am 可看副本，可直接打开浏览。
- **但 origin/main 现在领先本地 234 个提交**（小九/云端 cloud_scanner、heartbeat 等自 8am 起的 87 文件/~900k 行工作），**未推送回滚**。
- v6 部署铁律：deploy_now.py 第0步强制 `git checkout origin/main -- index_master.html update_data_v2.py`，即线上站永远用 origin/main 模板重建。故仅本地回滚**不会改变线上站**，线上仍显示较新版本。
- 要把线上 v6 真正回退到 8am，必须 `git push --force origin 4fa4c42:main`，会**销毁小九/云端 234 个提交（含 v8/ 子树）**——破坏性大，已按兵不动，等主人定夺。
- 结论建议：v6 逻辑已迁入 v8 的"v6备忘录"tab，可看性已由 v8 满足；不建议强推回滚销毁小九工作。若主人坚持，再执行强推+deploy_now.py --force。

## 待小九补（数据端）
- ETF_DAILY_MONITOR：接 westock data_fund_flow / data_etf，填充股票型ETF资金流TOP10，方可做"国家队进出"盘后长线跟踪。
- 真·国家队净申赎盘中不可得（无公开盘中净申赎数据），盘中只能以宽基ETF量能脉冲近似“动静与力度”。

## 波动率“暂无数据”修复（23:25-23:51）
- 根因：`calc_volatility_watch.py` 只写 `data/experiment/volatility_watch_YYYYMMDD.json`，未同步 `data/volatility_watch.json`；且近期收盘链未生成新日期文件，导致 v8/驾驶舱“短期波动结构”卡显示暂无数据。
- 修复：在 `calc_volatility_watch.py` 中追加同步写出 `data/volatility_watch.json`（experiment 日期版仍保留用于回溯）。
- 已执行：calc_volatility_watch.py → update_data_v2.py --fast → commit+push main → 补跑被新鲜度闸门拦截的 fetch_limit_up_heatmap.py / fetch_sector_fund_flow.py / fetch_crisis_data.py → deploy_now.py --force。
- 部署结果：gh-pages 已更新（a28f293），线上 https://ah-quant999.github.io/quant-scanner-v6/ 正在构建；本地 `dist/index_master.html` 中 VOLATILITY_WATCH_DATA 已更新至 2026-07-30，available=true，复合信号“恐慌下行”。
- 插曲：push 阶段遇到本地未完成的 interactive rebase 冲突（f8e2d4c9 / 8972ab89），已 `git rebase --abort` 后按规范重新 add/commit/push。

## CRDS 逆势龙头默认展开（23:55-00:00）
- 主人要求：CRDS·逆势龙头 5 个折叠面板（三项全满足 / 两项满足 / 10日≥2板 / 大跌日逆势板 / 量能确认）全部默认展开显示，不再折叠。
- 修改：`standalone/shmonitor.html` 与 `dist/standalone/shmonitor.html` 中 5 个 body 的 `display:none` → `display:block`，箭头 `▾` → `▴`；保留点击折叠能力。
- 已提交 main（6aa24b52）并部署（gh-pages 34b95db），线上地址：https://ah-quant999.github.io/quant-scanner-v6/

## v8 国家队ETF资金流向接入 + 暂未上架回测三卡（00:02-00:15）
- 主人反馈：v8 运维面板「暂未上架」仍为空白；「已下架」里的「国家队ETF资金流向」显示数据暂不可用。
- 根因 1：`fetch_etf_intraday_heat.py` 输出 JSON 只有 top_active/top_inflow，v8 `renderDelisted` 读取 `ETF_INTRADAY_HEAT.etfs`，字段缺失导致提示无数据。
- 修复 1：修改 `fetch_etf_intraday_heat.py` 在 result 中增加 `etfs` 字段（与 top_active 同构，amount 以亿为单位）；并同步补写当前 `data/etf_intraday_heat.json`。
- 根因 2：v8 `renderUnlisted` 末尾没有回测公式三卡，导致用户预期中的「后台说明卡」缺失。
- 修复 2：在 `v8/index.html` 的 `renderUnlisted` 末尾新增「回测 N 日胜率 / 严格 vs 宽松 / 阈值敏感性」三张后台说明卡。
- 已执行：`v8/update_v8.py` 重新构建 → `v8/deploy_v8.py` 部署到 quant-scanner-v8 main → 提交并推送 v6 main（726020ea）。
- 线上地址：https://ah-quant999.github.io/quant-scanner-v8/
