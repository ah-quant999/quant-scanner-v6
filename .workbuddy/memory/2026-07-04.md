## 2026-07-04

### 盘前更新 (09:20 自动化触发)
- 执行 `python batch_update.py pre_market`
- 发现3个缺失文件导致失败：`guanlan_extractor.py`、`generate_recommend.py`、`extract_standalone_final.py`
- 创建 stub 文件（标注已废弃/占位），重跑后 9/9 全部通过
- 部署成功，dist commit: `1234ceb1`

### 修复详情
- `guanlan_extractor.py` → 功能已迁移至 westock-data，stub 直接 exit(0)
- `generate_recommend.py` → 功能已迁移至 scanner.py，stub 直接 exit(0)
- `extract_standalone_final.py` → 独立页已由 extract_panels_v6.py 生成，stub 仅检查占位文件

### 收盘后更新 (17:25 自动化触发)
- 执行 `python batch_update.py post_close`，10/10 全部通过
- sync_check.py 首次 git 报错（exit=1），自动重试后成功
- sync_check.py:235 SyntaxWarning：无效转义序列 `\i`，不影响运行但可顺手修
- 部署完成：deploy_now.py --force 成功

### 盘中14:30更新 (17:32 自动化触发)
- 执行 `python batch_update.py afternoon`，10/10 全部通过，0失败
- 步骤包括：双机同步、scanner、概念排行、市场预警、板块资金流、update_data_v2、enhance_dist、refresh_standalone、sync_check、deploy_now
- 部署成功，交接日志已写入 CAT afternoon
