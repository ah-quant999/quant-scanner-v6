# 2026-08-01 工作日志

## 06:21 澄清 v8 双分支/双仓矛盾（回答主人截图提问）
- 查 `quant-scanner-v8` 分支：
  - `main` = 当前部署源（HEAD `63a7da9`，含宏观卡片增强 `c649576` + 分时段改造 `63a7da9`）。
  - `v8` = 锁死的快照分支（HEAD `a40c2c6`，与 `main` 的最近公共祖先就是 `a40c2c6`），即「origin/v8 分支已锁死防丢」的那个备份，**不是并行开发分支**。
- 查 `stock-scanner` 分支：
  - `main` = v6 主开发线；`gh-pages`/`gh-pages-temp` = v6 站点部署线。
  - `origin/v8-temp` = 小九在 v6 仓 `v8/` 子目录删除后（7-31 00:33 commit `7362b2f6`）另起的远程分支，只含 `v8/index.html`，HEAD `d812eed8`（涨停热力矩阵找回）。
- 结论：小九的 `v8-temp` 是 v6 仓里的「事后补丁分支」，与项目决策「v8 只在 `quant-scanner-v8` 独立仓开发」相冲突。`quant-scanner-v8` 里的 `v8` 分支只是旧快照，不影响 `main` 部署。
- 建议：把 `stock-scanner/v8-temp` 的改动（`d812eed8`）迁到 `quant-scanner-v8/main`，然后删除/归档 `stock-scanner/v8-temp`，避免双真相源。

## 06:28 执行「1迁 2补 3删除，只留唯一分支」
- **1 迁**：将 `stock-scanner/v8-temp` 中涨停热力矩阵卡片的正确位置（`sec-rt` 末尾、盘后数据之前）迁移到 `quant-scanner-v8/main`。`main` 已含完整卡片 CSS/JS 与 `window.LIMIT_UP_HEATMAP` 数据接入，仅需调整 DOM 位置。
- **2 补**：数据层已预先在 `main` 接好，本次验证通过：
  - `update_v8.py` 映射 `limit_up_heatmap.json → LIMIT_UP_HEATMAP.js`（category=intraday）。
  - `cloud_fetch_v8.py` 含 `f_limit_up_heatmap()` 并在 `main(intraday)` 中调度。
  - `fetch_limit_up_heatmap_v8.py` 存在，`raw_data/limit_up_heatmap.json` 与 `data/LIMIT_UP_HEATMAP.js` 已生成且字段正确。
  - 运行 `update_v8.py --category intraday` 成功，无 diff，数据新鲜。
- **3 删除**：
  - `git push origin --delete v8-temp`（`stock-scanner` 远程分支）。
  - `git push origin --delete v8`（`quant-scanner-v8` 远程分支）。
  - `git branch -D v8`（`quant-scanner-v8` 本地快照分支）。
- **提交与部署**：`quant-scanner-v8` 提交 `b300320` "fix(v8): 涨停热力矩阵卡片位置对齐 v8-temp，置于 sec-rt 末尾/盘后数据之前"，已 push `main → origin/main`。GitHub Pages 从 main 出，push 即触发部署。
- **当前分支状态**：
  - `quant-scanner-v8`：仅 `main` + `origin/main`。
  - `stock-scanner`：已无 `v8`/`v8-temp` 类 refs。
- **记忆更新**：`MEMORY.md` 已同步为「v8 唯一开发+部署源 = `quant-scanner-v8/main`，两仓 v8 分支已删」。

## 06:50 定位日历(V8_CAL)更新链路（回答"8/1 日历何时更新"）
- 现状：`data/V8_CAL.js` 仍为 **7月**（`month=2026年7月`, `update_time=2026-07-31 22:23:19`），8月日历尚未生成。
- 生成机制：`calendar_seed.json` 明确「程序每月自动渲染**当前月**」；财报事件内嵌在 `cloud_fetch_v8.py` 的 `f_v8_cal()`（从 v6 移植），`calendar_seed.json` 仅补偶发事件。
- 抓取调度 `v8_cn_fetch.yml`：V8_CAL 属 **premarket 盘前类**，cron = 工作日 08:25 CST + **周六 09:00 CST**(`0 1 * * 6`)。由自托管 cn runner 跑 `cloud_fetch_v8.py --category premarket` → 推 raw_data。
- 构建部署 `v8_build_deploy.yml`：`push raw_data/**` → `update_v8.py --detect-changes` selective build → 部署 main（Pages 源）。
- 结论：**今天(周六8/1) 09:00 盘前抓取**会按当前月渲染→切到8月并自动部署；此后工作日每天 08:25 刷新。前提：**自托管 cn runner 在线**（周末机器若离线则该次不执行）。云端调度与本地 git/网络无关。

## 07:00 继续周末改造：v6→v8 同步桥 + 时间/归属修正（按主人最新指示）
- 主人指示：不等周一，周末完成 21 个 post_close 冻结模块；盘后改 15:00 收盘数据 / 17:00 算法换算部署；ETF_DAILY_MONITOR 因 ETF 三连板归 intraday，板块资金三连板盘中追热保持 intraday。
- 已执行：
  - 修正 `v8_cn_fetch.yml`：15:30 收盘数据（仅 EXPERIMENT），盘中时段含 ETF_DAILY_MONITOR。
  - 修正 `v8_build_deploy.yml`：盘后兜底从 16:40 改为 17:00。
  - 修正 `update_v8.py` + `cloud_fetch_v8.py` `CATEGORY_MAP`：ETF_DAILY_MONITOR → intraday。
  - 新增 `sync_v6_to_v8.py`：读取 `stock-scanner/data/` 按类别同步到 v8 `raw_data/`。
  - 新增 `.github/workflows/v8_sync_v6_data.yml`：cn runner 08:40 盘前 / 16:45 盘后自动同步。
  - 本周末一次性同步 22 个原冻结模块 raw_data（post_close 19 + premarket 3），本地 `update_v8.py` 构建成功。
  - `guard_v8_freshness.py`：原冻结模块全部移入 WARN（48h），FROZEN 清空。
  - 提交 push `145c726` + `342b456`；GitHub Actions `v8_build_deploy.yml` #19 成功（12s），线上站点可访问。
- 当前体检：0 冻结 / 9 个 WARN 陈旧（v6 源文件本身 7-27~7-28，待 v6 下次跑批后自动恢复）/ 1 个无时间戳（STOCK_LIST）。
- 周一需小九确认：`v8_sync_v6_data.yml` 在 cn runner 能读到 `E:\workspace\stock-scanner\data`；确认 v6 跑批时间早于 16:45；处理 SH_SZ_HISTORY 硬编码内联；给 5 个模块补时间戳。

## 06:51 修复实时数据区统一时间硬编码问题（回答主人截图提问）
- 问题：`sec-rt` 顶部统一时间 `<span class="fresh">更新于 今日 15:20</span>` 是**硬编码**，不随实际盘中数据刷新而变化。
- 修复：给该 span 加 `id="rtUpdateTime"`，在 `initPostMarket()` 中动态取所有 intraday 数据（ETF_PULSE / ETF_INTRADAY_HEAT / ETF_SUBSCRIPTION / ETF_DAILY_MONITOR / SECTOR_FUND_FLOW / INDEX_QUOTES / CONCEPT_RANKING / LIMIT_UP_HEATMAP / CAPITAL_FLOW_DATA / MARKET_FUND_FLOW_DATA）的 `update_time` 最晚值，通过 `setTime()` 渲染相对时间。
- 已做：
  - `index.html` 改动通过 node 内联脚本语法检查（10/10 OK）。
  - 提交 `c7e5cd9` "fix(v8): 实时数据区统一时间改为动态取盘中数据最晚 update_time，移除硬编码 15:20"，已 push `main → origin/main`。
- 现状核对：当前本地 intraday 数据最晚为 `SECTOR_FUND_FLOW.js` 的 `2026-07-31 23:02`（交易日周五），因此实时数据区现在会显示**"昨日 23:02"**；今天（8/1 周六非交易日）盘中第一次抓取在工作日调度里不会触发，下周一 10:30 起才会刷新。
- 调度确认：盘中数据统一走 `v8_cn_fetch.yml` 的 intraday cron（10:30/11:30/13:05/14:00/15:05）+ 15:30 post_close（仅 EXPERIMENT）；构建走 `v8_build_deploy.yml` 的 push 触发 `--detect-changes` selective build + 17:00 盘后兜底。selective 机制保证盘中任务不会覆盖盘前/盘后数据，避免 v6 式全量覆盖 bug。

## 07:02 修复盘后卡片硬编码 + 资金流时间轴归属 + 板块资金趋势图（回答主人截图提问）
- 问题：
  1. 市场温度计 / 六维共振雷达 仍显示硬编码「更新于 今日 15:20」且数值不动（温度计 68°、雷达 [72,65,80,55,68,60] 均为静态）。
  2. 资金流时间轴（`MARKET_FUND_FLOW_DATA`）被归在 intraday，每次盘中跑都被单日覆盖，导致历史序列始终只有 1 天。
  3. 板块资金三合一「趋势」区只有 5/20/60 日 TOP5 列表，缺可视化趋势图。
- 修复：
  - `index.html`：市场温度计卡片加 `id="phThermoBody"` / `id="phThermoTime"`；重写 `renderThermo()` 把综合评分 0-10 映射为 0°-100° 动态 gauge，红/黄/绿随分数变化，并显示技术/资金/机构三维度分解。
  - `index.html`：六维雷达卡片加 `id="phRadarTime"`；移除硬编码 SVG 自执行函数；新增 `renderSixDim()` 从 `SH_FIB / MARGIN_DATA / CFFEX_HOLDINGS / CRISIS_DATA / SECTOR_FUND_FLOW / MARKET_FUND_FLOW_DATA` 动态计算量能/情绪/资金/估值/技术/外围六维并重绘雷达图。
  - `cloud_fetch_v8.py`：`f_market_fund_flow_data()` 改为读取已有 `raw_data/market_fund_flow_data.json` 追加新一天，保留最近 252 个交易日；同步更新 `CATEGORY_MAP` 把 `MARKET_FUND_FLOW_DATA` 从 intraday 移到 post_close。
  - `update_v8.py`：同步 `CATEGORY_MAP` 把 `MARKET_FUND_FLOW_DATA` 改为 post_close。
  - `.github/workflows/v8_cn_fetch.yml`：把 15:30 cron 从 intraday 改为 post_close，并更新顶部注释。
  - `index.html`：板块资金三合一 Block 3 顶部增加「市场资金流向趋势」折线+柱状图（读取 `window.MARKET_FUND_FLOW_DATA.daily`，显示近 30 日，数据≥2 天时才渲染）。
- 验证：node 内联脚本语法检查 10/10 OK；`update_v8.py --category post_close` 构建成功；`py_compile` 通过。
- 提交：
  - `f65cf81` "fix(v8): 盘后市场温度计/六维雷达动态化；资金流时间轴改为post_close累积历史；避免盘中覆盖"
  - `81fdf1e` "feat(v8): 板块资金三合一趋势区增加市场资金流向折线图"
- 盘后卡片调度梳理（sec-ph）：见回复表格。核心结论：**盘后区卡片均已被调度覆盖**，但数据源本身有盘中/盘前/盘后之分，无法全部统一到盘后；selective build 保证不跨类别覆盖。

## 07:22 四算法卡片（三重共识/驾驶舱/全站精选/逆势龙头）调度审计与最新化
- 现状审计：
  - 算法代码全部在 v6：`gen_triple_consensus.py`、`gen_triple_track.py`、`gen_cockpit_tier_recommend.py`、`gen_cockpit_advice.py`、`calc_crds.py`。
  - v6 跑批：`batch_update.py close_p2` 18:30 生成 triple/cockpit/backtest 等算法产出；`close`（周六 07:30 T+1 全量）会重算 CRDS/top10 等。
  - v8 同步：`.github/workflows/v8_sync_v6_data.yml` 原 16:45 盘后同步，**早于 v6 close_p2 的 18:30**，导致 v8 算法卡每天拿到的是前一日产出。
  - 前端展示：三重共识已显示 `update_time`；全站精选显示 `gen_time`；驾驶舱显示组件最晚时间；逆势龙头原时间为“刚刚/今日N时前”非统一格式。
- 修复：
  - `v8_sync_v6_data.yml`：盘后同步从 `16:45` 推迟到 `19:00`（cron `0 11 * * 1-5`），注释说明等待 v6 close_p2 18:30 产出。
  - `index.html`：三重共识增加 `(数据时间 XXXX)`；逆势龙头时间统一为 `今日/昨日/N天前 HH:MM`。
- 手动补跑最新结果（因 v6 close_p2 昨晚未完整产出，候选池仍 7-31 14:10）：
  - `gen_triple_consensus.py` → 0 只严格 / 5 只差一步（update_time 2026-08-01 07:28:26）。
  - `gen_triple_track.py` → 跟踪池 8 只（严格 0 / 差一步 8）。
  - `gen_cockpit_tier_recommend.py` → A档 4 只 / B档 15 只（scan_result 仍为 7-30）。
  - `gen_cockpit_advice.py` → 关注池 8 只 / 回避 3 只。
  - `sync_v6_to_v8.py --category post_close --push` → 同步 19 个 post_close 模块；因 remote 领先，本地 rebase 后重建 `data/*.js` 并推送。
- 提交：
  - `quant-scanner-v8`：`016cffd` "v8 build: post_close ..."（含 raw_data 同步、index.html/workflow 修复、data/*.js 构建），已 push origin/main。
  - `stock-scanner`：`59624b8e` "data(v6): 8/1 早间数据刷新 + 四算法重跑..."；`41f01bb0` "chore: 移除误加入索引的 _tdx_cache/ 等被忽略文件"。
- 注意：v6 候选池 `candidate_pool.json` 仍为 2026-07-31 14:10，因今早 `close` 模式 `build_candidate_pool.py` 失败/超时但流程继续（设计为“非关键”）。待周一正常交易日 close_p2 跑完后，四算法会自动用当日最新候选池刷新。
