# 2026-07-23 工作日志

## 16:31 盘后刷新部署
- **流程**: git pull --rebase → 11 个 fetch 脚本 → 6 个更新/生成脚本 → update_data_v2.py → deploy_now.py --force → 心跳 → 验证
- **结果**: 全部成功，build stamp = 20260723164053

## 17:59 ��机中国源数据定时刷新 (自动化)
- **15个脚本**: 13✅ / 1❌(fetch_up_down_stats) / 1⏳(fetch_sector_fund_flow 后台挂起)
- **推送**: 65a3a5af (23 files, 998 insertions)
- **部署**: ✅ build stamp = 20260723181824

### 关键数据
- **fetch_lhb**: 75 只龙虎榜，机游共振 2 只（托伦斯/红板科技）
- **fetch_52w_high**: 269 只创52周新高
- **fetch_concept_ranking**: 374 概念板块，兵装重组 +6.39% 领涨
- **fetch_sector_fund_flow**: 40 个板块大幅流入，锂电池 +106亿(连7天)居首；东财 akshare 历史接口服务器不可用
- **fetch_suspension_alert**: 7 只停牌，116 只涨停近触发
- **update_triple_resonance_daily**: scan_data.json 缺失，历史追踪 327 只
- **update_multi_resonance_daily**: 覆盖更新，历史 11 天
- **calc_crds**: 230 只→209 有效，精选级 0，进阶级 5
- **generate_recommend**: 已废弃跳过
- **generate_top10**: 20 只，TOP1 香农芯创 75 分
- **generate_one_look**: 文件不存在(RC=2)，跳过
- **cockpit_backtest_now**: 17 信号，胜率 50%
- **update_data_v2**: 4,488,892 字符，冒烟 4 警告继续部署
- **deploy_now --force**: 自动提交源码→推 main→重建 dist→推 gh-pages（100 文件）

## 16:51 candidate_pool_watchdog 整点运行
- 运行 `watch_candidate_pool.py`。当前 16:51 已过交易时段(>15:00)，看门狗按设计跳过重建，exit 0（安全）。
- 写统一心跳到 _heartbeat.log。未删除任何数据源/备份文件。

## 17:55 全时段云端健康监控 (:30 档)
- 交易日判断: TRADE
- 云端 build stamp: 20260723174736（8 分钟前），距上次部署 7.8 min
- 结案: ✅ OK — 云端 75 分钟内正常部署，本机无需动作
- 心跳已写入 _heartbeat.log

## 18:00 全任务部署探测守卫 (自动化 :05 档)
- **check_all_deploys.py** 运行结果: ✅ 17 OK / ❌ **5 MISSING** / ⏳ 3 WAIT / ⏭️ 0 SKIP
- **MISSING清单**（按时间优先级）:
  1. 09:14 盘前全盘扫描
  2. 10:31 盘中刷新部署
  3. 14:31 盘中刷新部署
  4. 15:30/16:15/16:30 云端收盘分阶段
  5. 17:31 云端收盘数据抓取+部署
- 附加: audit 14 errors(0 fixed)，站点交叉验证超时
- 退出码1(MISSING导致)，符合设计预期

## 17:02 candidate_pool_watchdog 半点运行
- 运行 `watch_candidate_pool.py`。当前 17:02 已过交易时段边界(>15:00)，`_is_trading_hours()` 返回 False，看门狗按设计跳过重建。
- 心跳：`2026-07-23 17:02:11 | xiaojiu | candidate_pool_watchdog | DONE` 已写入 `_heartbeat.log`。
- 纯修复型运行，未触碰任何 data/*.json 数据源或 backup_* 备份。

## 18:00 candidate_pool_watchdog 半点运行（收盘后跳过）
- 运行 `watch_candidate_pool.py`：当前 18:00 非交易时段（>15:00），看门狗直接跳过（exit 0），未触发重建/部署。
- 心跳：`2026-07-23 18:00:53 | xiaojiu | candidate_pool_watchdog | DONE` 已写入 `_heartbeat.log`。
- 纯修复型运行，未触碰任何 data/*.json 或 backup_* 备份。

## 18:45 云端发令枪 :45 档执行
- 运行 `trigger_cloud_dispatch.py`：build-stamp 读取 SSL 超时(视为0)；cloud_intraday.yml 各档位均被近60min云端成功运行/今日已触发跳过；cloud_post_close slot=30 今日已触发(18:00)
- **触发**: `cloud_post_close.yml` slot='15 8 * * 1-5' → HTTP 204 ✅
- 因 MAX_DISPATCH_PER_RUN=1，cloud_data_fetch.yml/cloud_scanner.yml 留待下一周期判
- 心跳: `2026-07-23 18:54:38 | xiaojiu | cloud_dispatch_trigger | DONE` 已写入 `_heartbeat.log`
- 日志: 已追加 `.fetch_log/trigger_cloud_dispatch.log`

## 18:55 云端发令枪 :30 档执行
- 运行 `trigger_cloud_dispatch.py`：10 个档位判定，build-stamp 读取超时
- cloud_intraday.yml 各档均因云端近60min已成功运行/今日已触发跳过
- cloud_post_close 档位均已触发(18:00/18:54)
- **触发**: `cloud_data_fetch.yml` → HTTP:204 ✅
- 本轮 MAX_DISPATCH=1 已耗尽，剩余下一周期
- 心跳: `2026-07-23 18:55:01 | xiaojiu | cloud_dispatch_trigger | DONE` 已写入 `_heartbeat.log`
- 日志: 已追加 `.fetch_log/trigger_cloud_dispatch.log`

## 19:48 全时段云端健康监控 (:30 档)
- 交易日判断: TRADE（周四）
- 云端 build stamp: 20260723193958（8 分钟前），距上次部署 8.1 min
- 结案: ✅ OK — 云端 75 分钟内正常部署，本机无需动作
- 心跳已写入 _heartbeat.log

## 18:58 candidate_pool_watchdog 半点运行（收盘后跳过）
- 运行 `watch_candidate_pool.py`：当前 18:58 非交易时段（>15:00），`_is_trading_hours()` 返回 False，看门狗 exit 0 直接休眠，未触发重建/部署。
- 心跳：`2026-07-23 18:58:41 | xiaojiu | candidate_pool_watchdog | DONE` 已写入 `_heartbeat.log`。
- 纯修复型运行，未触碰任何 data/*.json 或 backup_* 备份（无删除）。

## 20:53 candidate_pool_watchdog 半点运行（收盘后跳过）
- 运行 `watch_candidate_pool.py`：当前 20:53 非交易时段（>15:00），`_is_trading_hours()` 返回 False，看门狗 exit 0 直接休眠。
- 心跳：`2026-07-23 20:53:37 | xiaojiu | candidate_pool_watchdog | DONE` 已写入 `_heartbeat.log`。
- 纯修复型运行，未触碰任何 data/*.json 或 backup_* 备份（无删除）。

## 19:56 candidate_pool_watchdog 半点运行（收盘后跳过）
- 运行 `watch_candidate_pool.py`：当前 19:56 非交易时段（>15:00），`_is_trading_hours()` 返回 False，看门狗 exit 0 直接休眠。
- 候选池现状：`update_time=2026-07-23 19:18:41`（约 38 分钟前，403 只），收盘后不主动抓行情。
- 心跳：`2026-07-23 19:56:42 | xiaojiu | candidate_pool_watchdog | DONE` 已写入 `_heartbeat.log`。
- 纯修复型运行，未触碰任何 data/*.json 或 backup_* 备份（无删除）。

## 20:53 candidate_pool_watchdog 半点运行（收盘后跳过）
- 运行 `watch_candidate_pool.py`：当前 20:53 非交易时段（>15:00），`_is_trading_hours()` 返回 False，看门狗 exit 0 直接休眠。
- 心跳：`2026-07-23 20:53:37 | xiaojiu | candidate_pool_watchdog | DONE` 已写入 `_heartbeat.log`。
- 纯修复型运行，未触碰任何 data/*.json 或 backup_* 备份（无删除）。

## 20:39 candidate_pool_watchdog 整点运行（收盘后跳过）
- 运行 `watch_candidate_pool.py`：当前 20:39 非交易时段（>15:00），`_is_trading_hours()` 返回 False，看门狗 exit 0 直接休眠。
- 心跳：`2026-07-23 20:39:34 | xiaojiu | candidate_pool_watchdog | DONE` 已写入 `_heartbeat.log`。
- 纯修复型运行，未触碰任何 data/*.json 或 backup_* 备份（无删除）。

## 20:53 candidate_pool_watchdog 半点运行（收盘后跳过）
- 运行 `watch_candidate_pool.py`：当前 20:53 非交易时段（>15:00），`_is_trading_hours()` 返回 False，看门狗 exit 0 直接休眠。
- 心跳：`2026-07-23 20:53:37 | xiaojiu | candidate_pool_watchdog | DONE` 已写入 `_heartbeat.log`。
- 纯修复型运行，未触碰任何 data/*.json 或 backup_* 备份（无删除）。
2026-07-23 21:09 | 备份兜底 ✅ | GIT_SSH_COMMAND 补 StrictHostKeyChecking=no 修复 SSH 挂死 | 双机同步 7.4s + backup 16.7s | HANDOVER_LOG: ALIMI backup ✓
## 21:12 资金流时间轴定时任务调整
- **原状态**: `fetch_market_fund_flow.py` 无单独定时任务，仅在 `batch_update.py pre_market` 08:45 盘前并行抓取一次（今日部署后显示更新于 09:57）。
- **调整**: 将 `fetch_market_fund_flow.py` 加入 `batch_update.py close_p1` 的 Group 2 全量数据抓取（17:30，超时 60s），实现盘前+盘后双刷新。
- **部署链路**: close_p1 17:30 抓取 → close_deploy 19:31 经 update_data_v2.py 注入 → deploy_now.py 部署。
- **提交**: 652635ba `schedule(fetch_market_fund_flow): 盘后 close_p1 17:30 刷新上交所资金流时间轴` 已推 origin/main。

## 22:31 candidate_pool_watchdog 整点运行（收盘后跳过）
- 运行 `watch_candidate_pool.py`：当前 22:31 非交易时段（>15:00），`_is_trading_hours()` 返回 False，看门狗 exit 0 直接休眠，未触发重建/部署。
- 心跳：`2026-07-23 22:31:14 | xiaojiu | candidate_pool_watchdog | DONE` 已写入 `_heartbeat.log`。
- 纯修复型运行，未触碰任何 data/*.json 或 backup_* 备份（无删除）。

## 22:45 candidate_pool_watchdog 半点运行（收盘后跳过）
- 运行 `watch_candidate_pool.py`：当前 22:45 非交易时段（>15:00），`_is_trading_hours()` 返回 False，看门狗 exit 0 直接休眠，未触发重建/部署。
- 心跳：`2026-07-23 22:45:XX | xiaojiu | candidate_pool_watchdog | DONE` 已写入 `_heartbeat.log`。
- 纯修复型运行，未触碰任何 data/*.json 或 backup_* 备份（无删除）。

## 23:27 candidate_pool_watchdog 整点运行（收盘后跳过）
- 运行 `watch_candidate_pool.py`：当前 23:27 非交易时段（>15:00），`_is_trading_hours()` 返回 False，看门狗 exit 0 直接休眠，未触发重建/部署。
- 心跳：`2026-07-23 23:27:19 | xiaojiu | candidate_pool_watchdog | DONE` 已写入 `_heartbeat.log`。
- 纯修复型运行，未触碰任何 data/*.json 或 backup_* 备份（无删除）。

## 23:49 全任务心跳守卫(:05档)
- check_all_deploys.py: 4 MISSING / 19 OK / 2 WAIT / 0 SKIP
- MISSING: 19:30收盘最终部署(P0·连续4+轮)、14:31盘中(P1)、10:31盘中(P1)、09:14盘前全盘扫描(P1)
- 与22:52轮持平无变化，19:30 P0持续遗漏需人工介入
- 站点交叉验证: ⚠️ git fetch 30s超时

## 23:44 本机中国源数据定时抓取(自动化第3轮)
- **15/15 脚本全部成功** ✅
- git pull 因工作区残留 dist/ standalone/ 构建产物首次失败，stash后 rebase 遇 data/.ops_status.json 冲突(取本地版)
- **Commit**: 9219b36b → **Push**: ✅ → **Deploy**: ✅ (gh-pages a2f7885, 101 files)
- 工作区已还原 dist/ 保持干净

## 23:41 candidate_pool_watchdog 半点运行（收盘后跳过）
- 运行 `watch_candidate_pool.py`：当前 23:41 非交易时段（>15:00），`_is_trading_hours()` 返回 False，看门狗 exit 0 直接休眠，未触发重建/部署。
- 心跳：`2026-07-23 23:41:16 | xiaojiu | candidate_pool_watchdog | DONE` 已写入 `_heartbeat.log`。
- 纯修复型运行，未触碰任何 data/*.json 或 backup_* 备份（无删除）。
