# 2026-08-04

## 09:00 v8 开盘前快速部署（automation-1785397486704）✅ 08:57 完成
- repo-temp 快进到 fc0ad60a（阿狸咪昨晚 URGENT 文档入库）
- v8 独立仓 rebase 到 08:45 阿狸咪修复（d33e0f1：实时数据区时间戳 + SH_SZ_HISTORY 改用 akshare 恢复盘中刷新）+ 云端 08:53-08:58 自动构建
- **清理换行符地雷**：v8 仓 algorithms/scanner.py + fetch_inst_trade.py 历史索引 blob 是 CRLF、`.gitattributes` 要 LF → 任何 rebase 都被假 M 挡（checkout/stash/autocrlf=false 全无效，连 stash 后 rebase 都报 unstaged）。用 `git add --renormalize` + commit（0ce309c，内容零变化 3843±）一次性治本
- update_v8.py 重建 51 个 data/*.js；deploy_v8.py 推线上；本地 data 差异已 commit+push，云端同内容 commit 被 rebase 跳过，最终本地=线上=ba2b07e 干净
- 线上验证：V8_CAL.js update_time 2026-08-04 08:53:01 ✅
- heartbeat 08:57:16 DONE

## 新增经验（写入长期记忆候选）
- **v8 deploy_v8.py 只推线上、不提交本地 data/** → 每次部署后本地 51 个 data/*.js 残留未提交，下次 rebase 必被挡。部署后应立即 `git add data/ && git commit && push`
- 云端 Actions 对 main 高频 push（cn_fetch/build/deploy 多个 workflow），本地 v8 仓操作前必先 fetch+rebase

## 15:30 v8 收盘数据刷新部署（automation-1785388522458，非选股）
- 数据抓取 7/7 成功（cffex/etf_subscription/volatility/52w_high/limit_up_heatmap/herding；ETF_DAILY_MONITOR 无本机脚本、由云端管线生产）
- v8 同步仅 3 个本机产出（etf_subscription/volatility/w52_high → v8 raw_data），其余由云端 raw_data 提供（15:45 云端已推收盘版）
- update_v8.py 全量 52 数据源注入成功；deploy_v8.py 报"无变化跳过"（云端 15:46 v8 build workflow 自动部署同内容）；线上 200 OK 数据最新
- 经验：本地 commit data js 后云端 v8 build 自动重建会冲突（40 个 UU），解法 rebase --abort + reset --hard origin/main 信任云端 build；**今后只 commit 一次 raw_data，不重复 commit data js**
