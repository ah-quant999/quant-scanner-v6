# 2026-07-14 工作日志

## 收盘 close_p1 (17:31 定时任务)
- **第1次（修复前）**：26成功/2失败。`fetch_mahoro_signals.py` JSONDecodeError 崩溃
- **修复**：http_get 加独立 JSONDecodeError 捕获 + fetch_signals 防 data=None + __main__ 用 sys.exit(1) 替代 bare raise
- **第2次（修复后）**：28成功/0失败 ✓，全部通过

## 收盘二段 18:35 close_p2 检查补跑
- 判定：周二交易日，4个数据文件均已更新到今天
- 跳过 batch_close_p2 / update_data_v2 → 仅执行 deploy_now.py --force
- ⚠️ 首次 PowerShell 找不到 git 失败 → 改用 Git Bash 重跑成功
- 部署 84 files, build stamp 20260714183242 ✅
- 源码自动推送 main（小九可拉）

## 收盘 close_p2 (18:31 定时任务) — 手动重跑
- **第1次**：fetch_analyst_ratings.py TIMEOUT（家用机东财慢，60s 超时不够）
- **修复**：batch_update.py 中 fetch_analyst_ratings.py 超时 60s → 180s（close 和 close_p2 两处同时改）
- **第2次（修复后）**：14/14 全部通过 ✓，总计耗时 ~4min

## 晚间排查+修复 (19:31~21:10)
- 19:31 close_deploy 自动化跑成功（HANDOVER_LOG 第80行）
- 主人反馈最近选股不准 → 排查数据链路
- 发现 scanner 全扫仅 51/387 只，fail=287，金股池 213 只评分全部=0
- 根因：家用机数据源全线崩溃：mootdx=0, baostock=1, eastmoney=0, qq=50, fail=287
- **修复 mootdx TrafficStatSocket泄漏**（commit `f1383ff`）：
  - `_tdx_reset()` 从仅 gc.collect() 改为实际调用 `client.close()` + 置 None 让下次重建
  - 新增 `_tdx_count_call()` 统一计数，每 50 次自动触发重置
  - `_fetch_board_top_mootdx()` 的每批 stocks()/quotes() 后计数（之前完全不计数）
- **创建云端方案**（commit `7329e61`）：
  - 新建 `requirements.txt`（依赖清单）
  - 新建 `.github/workflows/cloud_scanner.yml`（18:31 CST 自动触发）
  - 全流程：数据抓取→扫描→生成→构建前端→部署到 gh-pages
  - 需要 GitHub Secret: ZSXQ_TOKEN
- **扩展全天上云**（commit `69c12fa`）：
  - 新增 `cloud_intraday.yml` — 覆盖09:15/10:30/11:45/13:30/14:30/15:35六个盘中时间点
  - 新增 `cloud_data_fetch.yml` — 17:31全量数据抓取(替换close_p1)
  - 调整 `cloud_scanner.yml` — 18:31扫描+部署(数据抓取段做兜底)
- **补全所有定时任务上云**（commit `0ed8f63`）:
  - 7个workflows完整覆盖定时任务完整映射表
  - 详见 MEMORY.md 云端方案段落
  - 双机不再需要每天开机盯盘做主力任务
- **云端实验全记录**（commit `aaef43a` ~ `659819d`）:
  - run 16 成功：mootdx(通达信TCP)直连298/350只A股，仅2只fail，5分钟扫完
  - 核心改动：scanner.py CLOUD_RUNNER模式 + py_mini_racer必装 + 扫描/数据抓取并行
  - 结论：云端可行（公开仓库免费不限额度），双机降级为兜底角色
  - cloud_scanner.yml push触发器已移除，恢复18:31 CST定时调度

## 22:14 顶栏角标 UI 修复
- 主人截图：紫色"多维80+ 0只" + 青色"三线 1"，要求"三线"也加"只"单位
- 改 `writeAllCounts()` 440832：`'三线 ' + sanxianCount` → `'三线 ' + sanxianCount + '只'`
- 跑 `update_data_v2.py --fast` → dist 94811 行同步
- 跑 `deploy_now.py`：重建dist+部署gh-pages 5aee726b38f2，main推送完成
- build stamp: 20260714221449
- 待主人 Ctrl+F5 硬刷验证
