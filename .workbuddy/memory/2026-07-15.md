# 2026-07-15 工作日志

## 22:46 通达信公式从暂未上架移到逻辑详解页
## 23:31 三卡修复 + 新卡资金流向全部落地

### 🛠️ 修复项
1. **12:30 盘中部署失败** — cloud_intraday.yml 注入凭据步骤缺容错（NEODATA_TOKEN secret不可用时全步失败），加 continue-on-error: true
2. **金钻起涨路径 bug** — triple_select_scan.py:276 写到 ../../data/experiment/（云端路径错），改为 data/experiment/
3. **CRDS 自动化缺口** — batch_update.py 没 calc_crds.py，CRDS 自07-13未刷新；cloud_scanner.yml 加 calc_crds.py

### 🆕 新卡：三方资金流向对比
- capital_flow_summary.py — 读取 north_fund + LHB + inst_trade + sector_fund_flow
- update_data_v2.py 注入 + cloud_scanner.yml 云端自动化
- index_master.html 暂未上架新卡 + renderCapitalFlowCard() 函数
- .gitignore 白名单加 capital_flow_summary.* + data/experiment/*.json
- 部署 hash: 54a5c9b (main), gh-pages 已上线
- 操作：CRDS 逆势龙头通达信公式（合并版）从 index_master.html + 6 个 standalone 暂未上架面板，移到 standalone/guide.html（逻辑详解）第八节对应位置
- 原占位符 "见 逆势龙头_通达信公式.tdx" 替换为完整公式代码块
- 部署：deploy_now.py 自动提交并部署到 gh-pages，提交哈希 2e1f591
- 验证：grep 全文仅 dist/standalone/guide.html + standalone/guide.html 两处保留公式

## 21:54 close_p2 自动触发 + 修复重跑
- 问题：build_candidate_pool.py 在家用机无限挂起，两次超时
- 修复1：batch_update.py 三处 build_candidate_pool 超时 120s→300s
- 修复2：build_candidate_pool.py 数据源优先级反转（akshare优先→mootdx兜底）
- 修复3：build_candidate_pool.py 恢复 mootdx 原始简洁版（已改为兜底不会触发）
- 最终：14步中13步 ✅（候选池未重建用旧数据），仅 build_candidate_pool TIMEOUT
- 根因：home 机东财 API 慢到 5min 无法返回，HTTP 请求缺连接超时
- 待修复：需给 akshare HTTP 调用加连接超时，或跳过A股排名直接读本地文件

## 收盘最终部署 (22:13-22:24)

### 第一步：读取小九交接
- 读取 HANDOVER_LOG.jsonl
- 今日 close_p1 最终在 22:11 成功（第3次重试成功）
- 失败步骤: fetch_concept_ranking.py (东财None崩溃), build_candidate_pool.py (2次失败), fetch_herding_data.py TIMEOUT (2次)
- 核心数据均已就绪，不需补跑

### 第二步：收盘最终部署
- `batch_update.py close_deploy` 步骤1-5 ✅
- `deploy_now.py` 第一次 TIMEOUT (180s)，重试也超时
- **解决**: 直接调用 `deploy_now.py --force` ✅ 成功（绕过 batch_update 子进程超时限制）
  - 85个文件推送至 gh-pages, build stamp: 20260715222142
  - 验证通过: LHB_DATA, HERRING_DATA, NORTH_FUND_DATA, MAIN_STOCK_DATA
- `verify_data_vs_website.py --only-issues` ✅ 5通过/3警告/2失败（北向个股缺失为已知问题）

### 第三步：前端部署安全网
- `verify_frontend_deploy.py` → **FRONTEND_DEPLOYED_OK** ✅（本地17个文件与gh-pages完全一致）

### 已知问题
- `build_candidate_pool.py` 今日2次失败（需小九排查，家用机数据源受限）
- `deploy_now.py` 在 batch_update 子进程内易超时（180s限制不足），直接调用 `--force` 可绕过

## 22:30-23:00 — 阿狸咪任务重构 + 交接文件夹定规
- 暂停16个：盘中(9:16/9:47/10:31/11:46/13:31/14:31/15:31/16:31) + 收盘主力(17:31/18:31) + 周期(行业映射/股票名/T+1/世界杯) + 21:00备份 + 18:25心跳
- 保留7个：IPO研判(08:30) + 一段兜底(17:45) + 二段兜底(19:05) + 交接检查(19:30) + 部署兜底(19:31) + URGENT监听每2h(加云端健康检查>3h) + 备份兜底(21:10)
- 新规：交接文件夹定为项目根目录，禁止 MSG_TO_ALIMI_* / repo-temp / 旧命名
- 已写 HANDOVER_阿狸咪_2026-07-15_晚.md 通知小九

## 23:00-23:48 — 前端修复（三方资金/json/IPO日子/板块空/健康看板三方监控）
- 修复 capital_flow_summary.py: inst.direction 不再 f-string list of dict（之前显示JSON），改为简洁文字
- 修复 guide.html: 删除"📝 数据脚本 & 部署"列所有脚本名泄露 + 本机专属/架构说明段重写 + CRDS 数据流无脚本名 + 保护性CSS防止白字
- 修复 index_master.html 三大问题：
  1. 板块资金空数据时显示5日趋势替代"暂无"
  2. IPO卡片加申购日/上市日显示
  3. 健康看板顶部加三方监控(云端/小九/阿狸咪)从HANDOVER_LOG提取
- update_data_v2.py 加三方日志提取注入OPS_STATUS

## 23:46-24:00 — 逻辑详解页 + IPO 卡片双修
- 逻辑详解页：删除"阿狸咪 08:00 旁听"行（早于云端首跑没意义），safety-net 阈值改"盘中 2h / 盘后 3h"，代码在 .github/workflows/safety-net.yml 加时间判断
- IPO 速览 C1 卡片：加"推荐/不推荐"徽章（用 s.recommend + s.tag_color/s.bg_color 着色）
- 可转债去重：副标题渲染时若 track==board（如都是"可转债"）则跳过
- 修改文件：index_master.html + dist/index.html + dist/index_master.html（同步3处）
- 部署：main 推送完成 + gh-pages 部署中（push 冲突过一次重试）
- 提交：30ebd86 → amend → 6530dd2 (3 files)
