# 2026-07-31 工作日志

## v8 盘后资金区改造：趋势图 → 短/中/长三卡片（00:25-00:50）
- 主人反馈：资金流时间轴 ECharts 图「最后一天要写 26 不要 2026」「没有更长数据」「隔得远、数字看错行」，要求改成参考图那样的并排卡片，并增加 T+60 长线窗口。
- 数据现状：
  - `market_fund_flow.json` 仅 2026-01-21 至今 124 天（东财 push2his 接口限制），无法提供 2024/2025 年长历史。
  - `sector_fund_flow_history.json` 当前只累积了约 10 天，故 `trend_60d` 为空；`trend_5d`/`trend_20d` 已有数据。
- 修改：
  - 删除 `v8/index.html` 盘后区「资金流时间轴·中长线」ECharts 卡片（含 renderMff/renderMffChart 函数、ph-mff-toolbar 样式）。
  - 在同一位置新增 `.grid.g-3` 三列等高铁卡片：短线 5日 / 中线 20日 / 长线 60日累计净流入 TOP。
  - `renderSector` 中删除「板块资金三合一」内部原有的 5日/20日 子块，避免重复。
  - 新增 `renderTrendCard()`：按金额降序，金额相同按连续流入天数降序；取 TOP8；长线 60日无数据时显示「暂未累积到数据区间」。
  - 增加 `.ph-trend-card/.ph-trend-body/.ph-trend-row` 等 CSS，确保三卡片同高、行不串行。
- 已执行：`v8/update_v8.py` 构建 → `v8/deploy_v8.py` 部署 → 准备提交 main。
- 线上地址：https://ah-quant999.github.io/quant-scanner-v8/

## 00:34 v6 仓 v8/ 子目录彻底删除（v6 与 v8 分离）
- 用户决定 v6 与 v8 彻底分离。删 v6/v8/ 子目录（10 文件 / 8183 行），commit 7362b2f6 → push 66d56e31..7362b2f6 成功。
- 删除前核查：v8 独立仓 deploy_v8.py 已自洽（推自己 main，不依赖 v6）；v6/v8/ 原是小九活跃开发工作区(00:31 还在改)且与独立仓重复；全仓无其他脚本依赖 v8/；DO_NOT_DELETE 无 v8 条目。
- 后续必办：通知小九——以后 v8 只在 quant-scanner-v8 独立仓开发，v6 仓不再含 v8/。git 历史保留可恢复。

## 19:30 自动交接检查（读小九交接）
- 小九 7-31 白天未写交接文档：无 HANDOVER_小九_2026-07-31*（两份 7-31 文档均为阿狸咪凌晨 00:39 / 早 07:10 写给她的）；HANDOVER_LOG.jsonl 自 7-29 11:12 停更（139 行）；7-31 v6 仓 git 全部 commit 均为 ah-quant999，小九零提交。
- 但线上 v8 已恢复（WebFetch 确认：日历 + 打新速览 + 暗色 AI 速览 + 概念热度 16:56 + 事件日历 17:01 均在线）→ 早 07:10 紧急交接的"回退修复"指令小九已执行完毕 ✓（部署在 quant-scanner-v8 独立仓，不在 v6 仓留痕）。
- 小九最近一份正式交接 = 7-30 18:22 傍晚单（v8 六项修复，origin/main=0782b3b2）。
- 遗留待确认：B+ 4 个 workflow（cloud_premarket/close_ultralight/weekly_cleanup/intraday 补整点）是否上线（云端、本仓不可见）；B 阶段（v8 JS 接 JSON）阿狸咪未动；本地 v6 仓 lhb_result.json 停在 7-28 20:55。
- 影响：我之前 commit 90f75656（A阶段 AI 速览）所在的 v6/v8/index.html 已随删除消失；A 阶段改动需改到 quant-scanner-v8 独立仓（未做，等用户确认）。

## 21:59 坚果云补查（主人质疑"没交接？肯定有"）
- 补查范围：E:\Nutstore\九宝量化备份、E:\我的坚果云\quant-data、E:\Nutstore\1（空壳）、E:\workspace\quant-scanner-v8 独立仓。
- 结论不变：小九 7-31 **确实无任何交接回执**。证据：
  - 坚果云今天(7-31)修改的 .md/.txt = 0 个；交接/小九/HANDOVER 文件名命中仅 6 月及 7-01 旧档。
  - quant-data 整体停更于 7-03（overnight_brief 仍 6-29），非今日汇报源。
  - v8 仓 7-31 提交全是 ah-quant999（阿狸咪），最后提交 07:29；无小九 commit、无小九视角 HANDOVER。
  - 九宝量化备份\handover_小九 最新仅 7-01。
- 第一版(19:30)只查了 v6 仓本地，漏查坚果云是疏漏；补查后确认小九今日既未写交接、也未同步坚果云、git 零提交。线上 v8 已恢复 ≠ 留了回执。
- 推断：小九坚果云同步可能未开，或今以"直接部署+口头"方式交接未落文档。建议主人回家直接问小九要今日交接单。

## 00:39 v8 独立仓 A 阶段确认（与小九 6c9d341 重复已 abort）
- 00:36 切到 v8 独立仓（E:\workspace\quant-scanner-v8）重做 A 阶段（之前误改 v6/v8/ 子目录），commit 0b13ce1。
- push 时 rebase 发现小九 6c9d341 00:30 已独立实现所有 A 阶段内容（AI 速览更详细版：8 指数+融资+地量+中信期货；删北向大卡 grid g-4→g-3；删运维+异动北向行）。
- 我的 0b13ce1 vs 小九 6c9d341 index.html diff 完全为空（我俩完全并行做了同一件事）—— git rebase --abort + git reset --hard origin/main 丢弃我的 0b13ce1，**不推噪音**。
- 不强推、不破小九工作（双机铁律守住）。
- 给小九写了 HANDOVER_小九_2026-07-31_深夜.md（v6/v8 分离 + A 阶段已由他完成 + B/B+ 阶段 4 个新 workflow 待做 + 数据白名单）。

## 主人拍板 7-31 排程架构（关键决定）
1. **v6 不要再动**（v6 是 v6，后期跑稳了 v6 直接删）
2. **08:30 / 15:05 / 17:00 / 周日23:00 4 个时段新建云端 workflow**（小九侧 B+ 阶段）
3. **09:30-15:00 盘中 4 个整点（10/11/13/14）补 cron**（小九侧）
4. **17:00 算法上云端已确认 0 token**（generate_recommend.py 无 LLM 调用）
5. **B 阶段（v8 接 JSON）+ B+ 阶段（云端 4 workflow）并行**
6. **数据白名单**（永删清单已写入 HANDOVER）
7. **不部署（双机铁律）**，小九跑 deploy_v8.py

## 06:50 v8 AI市场速览5行改暗色(主人06:50反馈"和风格不搭")
- 改前:5行用Material浅色调色板(白底卡+#f3e5f5/#e8f5e9/#e3f2fd/#fff3e0/#fce4ec行底),与下面3张深色指数卡不搭
- 改后:外层用v8暗色变量(var(--card)/var(--border)/var(--txt)/var(--sub)),5行底色改为对应色相暗色透明10%+pill 20%透明+亮色字
- 色相锚点保留(紫/绿/青/金/粉),警示红改var(--up)
- commit 723c123 → push 6c9d341..723c123 成功
- 不部署,等小九跑 deploy_v8.py

## 06:53 v8 个股查询升级为 v6 风格并移至选股策略最左侧
- 主人反馈：第一张图 v8 当前查询太简陋；第二张图 v6 查询页能查多维共振/金股池/龙虎榜/主力资金/近10日走势/EMA趋势/研报覆盖/投行覆盖/停牌状态，要求移到选股策略最左侧单独 tab。
- 修改：
  - 删除 `v8/index.html` 实时数据区「个股实时查询」卡片。
  - 在选股策略区 `st-tabs` 最左侧新增「🔍 个股查询」tab，默认激活。
  - 新增 `st-query` 面板：搜索框 + 下拉提示 + 查询格式/覆盖范围说明条 + 9 块扩展信息介绍。
  - 重写查询函数 `doV8Query`/`showV8QueryDropdown`/`selectV8QueryStock`，支持代码/名称/拼音首字母匹配。
  - 查询结果从 `window.TOP10_DAILY`、`window.GOLD_POOL`、`window.LHB_DATA`、`window.INST_TRADE`、`window.MAHORO`、`window.SUSPENSION_ALERT` 等现有 `window.X` 数据源读取，遵守 v8 铁律不 fetch。
  - 渲染各模块卡片：多维共振评分/信号、金股池四格条、龙虎榜分类与净买、主力资金、近10日走势折线、EMA趋势评分、研报覆盖、投行覆盖、停牌识别。
- 校验：div 912/912、section 6/6；无 `qInput/qResult/doQuery()` 残留。
- 已 commit 649bc24 → push 723c123..649bc24 成功。
- dist 未重建，等主人本地确认后再部署。

## 06:56 v8 ETF资金热度改4大类×TOP5流入+TOP5流出横向bar(主人反馈)
- 旧HTML:4大类(宽基/行业/主题/跨境)×4只写死+文字,无bar
- 新HTML:4大类容器(etfHeatByCat+etfHeatSummary+etfHeatTime),JS渲染
- 新JS函数renderEtfHeatByCategory(etfHeat):每类合并top_inflow5+top_outflow5=10只,按|main_net_inflow|映射bar宽度,4色锚点(青/紫/金/粉)
- 数据源:window.ETF_INTRADAY_HEAT.categories(已内联在HTML,部署前后可用)
- 整体max-height:380px+overflow-y:auto,grid g-2排2列2行
- commit 0409fdf, push 649bc24..0409fdf(小九06:58推649bc24"个股查询v6风格"无冲突)
- 不部署,等小九跑deploy_v8.py

## 17:26-17:38 自动化 neodata 每日抓取（automation-1784086687244）执行成功
- 三表更新：52w_high 17:26 / sector_fund_flow 17:28 / sector_rs 17:28（update_time）
- gh-pages 部署成功：commit 2668f848 @17:35:16，build-stamp 20260731173456 与本地 dist 一致
- source sync ea83a70c @17:36:30，main 本地=远端 0/0，工作区干净
- 心跳已写 _heartbeat.log
- **重要坑**：`fetch_neodata_daily.py:88` 给 deploy_now.py 的 subprocess timeout 只有 300s，
  而完整部署要约 5 分钟，收尾阶段被砍 → 打印 "TIMEOUT after 300s / WARN: deploy_now.py 返回 -1"。
  这是**误报**，实际部署已成功。判定成败必须实证：fetch gh-pages 比 commit 时间 + 比 build-stamp +
  `git rev-list --left-right --count HEAD...origin/main` 应为 0 0。
  风险：更慢的一次可能在 gh-pages push **中途**被砍。建议 timeout 300→600（改后需 git add -f+commit+push）。
- sector_rs 的 neodata 缺全周期字段属常态，自动降级同花顺 90 板块（约 3 分钟），非故障。

## 08:00 自动化监听续做：v8 工作树清理收尾 + 小九涨停热力矩阵交接
- 工作树「并发污染」核验：未提交改动实为 阿狸咪周末分时段 selective fetch/build 改造，已被并发进程 commit+push 为 `63a7da9`（main=origin/main=63a7da9），工作树已干净，**无丢失**。含 update_v8.py/cloud_fetch_v8.py 的 category 支持、删 RECOMMEND/SCAN_DATA 死数据、workflow 改造、新增 cloud_weekly_cleanup.yml。
- 宏观观测卡片增强（c649576，用户原始需求，父提交）经核验 index.html 内 ph-macro-table-v2/renderMacro/v2-arrow 共 22 处仍在，未被 63a7da9 冲掉，线上交付完好。
- 小九 18:10 交接（HANDOVER_小九_给阿狸咪_2026-07-31_涨停热力矩阵找回.md，📬普通）：涨停热力矩阵卡容器已在，但 `data/limit_up_heatmap.json` 缺失致「加载中」；小九已将卡片补到 stock-scanner 仓 `v8-temp`(d812eed8)。⚠️ 矛盾：交接称 v8 模板真相源=stock-scanner v8-temp，与「quant-scanner-v8 独立仓=唯一部署源」冲突——**需主人澄清后再动部署**。本地 quant-scanner-v8/index.html 已含 limitUpHeatCard 渲染(4处)，仅缺数据文件。
- 云端健康检查：curl GitHub Pages HTTP:000（沙箱出网受限），需人工浏览器核验。
