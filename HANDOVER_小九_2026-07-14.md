# 九宝量化 v6.0 — 交接单（小九 → 阿狸咪）

**日期**：2026-07-14 08:30（周二，交易日）
**方向**：小九（单位机） → 阿狸咪（家用机）
**站点**：https://ah-quant999.github.io/quant-scanner-v6/

---

## 一、本机当前状态（已确认）

- ✅ git 本地 main 已与 origin 对齐（**0 ahead / 0 behind**）
- ✅ gh-pages 已刷新：**SHA `b59552ad4d79…`**（build stamp 20260714092123，84 文件），已用 `git ls-remote origin gh-pages` + `git show b59552a:index.html` 双重验证，**确为真实推送**（非静默 SUCCESS）
- ✅ 线上龙虎榜数据已校准为 **2026-07-13** 数据（update_time 09:13:03）：拓荆科技+23.24亿、华天科技+4.22亿、欧林生物+2.76亿、万邦医药+1.09亿等
- ⚠️ **fetch_lhb.py 已被修改修复**，但 `.gitignore` 忽略 `*.py`，该脚本**不会随 git 同步到阿狸咪**。需要双机通过坚果云/手动方式同步此文件，否则阿狸咪下次抓取仍用旧逻辑。
- ✅ 今日 IPO 研判已上线：新股 8 + 可转债 10；建议申购 = **长鹰硬科(920238) 77分（北交所）**、**江农转债(110102)**；待申购另含欣兴工具(301677)、长鑫科技(688825)

---

## 二、今早重大事件 ⚠️（双机都受影响，务必看）

**根因：本地 main 与 origin/main 分叉 → deploy 误 SKIP**

- 现象：`deploy_now.py` 在 `git push origin main`（提交 `.deploy_lock`）时遇 **non-fast-forward**，被脚本误判为「另一台机器持锁」而 **SKIP**，导致 gh-pages 自 **07-13 08:27 起约 21 小时未更新**。
- 推断：阿狸咪昨晚 **19:30 的部署也因同样原因失败**（gh-pages 一直停留在 07-13 08:27 的 IPO 数据，缺少 07-13 收盘数据）。即「今早没成功的任务」本质是双机部署链路被本地分叉卡死。
- 本机已修复并验证上线（见第一节）。

---

## 三、⚠️ 阿狸咪本机若也踩坑 —— 修复命令（照搬即可）

若你跑 deploy 出现 `SKIP: other machine grabbed the lock first` 或 `non-fast-forward`，说明你本机 main 也分叉 / 残留 `[lock]` 提交。执行：

```bash
cd repo-temp
git fetch origin
rm -f .deploy_lock
git reset --soft origin/main      # 对齐 origin，保留工作树数据改动
git restore --staged .deploy_lock # 清索引残留的锁文件暂存项
python deploy_now.py --force      # 重跑，应 fast-forward 成功
```

> ⚠️ 务必用**系统 Python**：`C:/Users/Administrator/AppData/Local/Microsoft/WindowsApps/python.exe`
> 裸 `python` = 托管 3.13，缺 mootdx/akshare 会 ImportError。
> ⚠️ **不要用 `git stash -u`** 来对齐！后台 fetch 运行时会收走正在写的 `data/*.json`，pop 后覆盖真实数据。

---

## 四、今日后续安排

- 本机 **09:20 盘前更新**已正常跑完（17 个 fetch 全成功，08:43:02）。随后执行了 `update_data_v2 --fast + deploy_now --force` 刷新 gh-pages，**已部署成功**（等待后台任务完成后更新 SHA 到此交接单）。
- 阿狸咪白天看门狗按原机制：小九数据新鲜（读文件**内嵌时间**，非 mtime）则跳过，过期才接管。本机数据已新鲜，你 08:30 IPO 看门狗应跳过。
- 心跳 / 世界杯 / 备份 / 名称更新等既有任务保持原样，无需干预。

## 四（补充）. 龙虎榜数据校准 ⚠️（09:15 已修复并重新部署）

**发现过程**：对比阿狸咪「另一个日历」后，发现我方 v6 龙虎榜数据存在偏差。已根因修复并重新抓取+部署。

**已修复的 bug（`fetch_lhb.py`）**：
1. **重复计算 bug**：对东财「买入 Top5」和「卖出 Top5」分别取 buy/sell，没有合并去重，同一机构席位被算了两次。导致华天科技等数值错误。
2. **NaN 污染 bug**：东财卖出页某些买入金额为 NaN，未当 0 处理，导致部分游资净买入变成 NaN。
3. **只抓前 40 只 bug**：`DETAIL_LIMIT=40` 导致后排股票机构净买入显示为 0。

**修复方案**：
- 机构净买入改以**新浪机构席位接口**（`stock_lhb_jgmx_sina`）为主源，按日期过滤，覆盖全量龙虎榜股票。
- 东财席位明细用于游资/北向/量化分类，并增加 `(营业部, 买入额, 卖出额)` 去重 + NaN 兜底。

**修复后 07-13 数据**（与新浪/东财权威源一致）：
| 代码 | 名称 | 修复后机构净买入 | 用户日历 | 备注 |
|---|---|---|---|---|
| 688072 | 拓荆科技 | **+23.24亿** | +23.24亿 | ✓ 一致 |
| 002185 | 华天科技 | **+4.22亿** | +4.17亿 | ✓ 已修正（之前 +6.81亿） |
| 688319 | 欧林生物 | **+2.76亿** | +2.76亿 | ✓ 一致 |
| 301520 | 万邦医药 | **+1.09亿** | +0.50亿 | 口径差异 |
| 301310 | 鑫宏业 | **+0.039亿** | +0.33亿 | 口径差异 |
| 300475 | 香农芯创 | **+0.16亿** | -2.57亿 | 口径差异（新浪/东财机构净买入为+0.16亿；-2.57亿可能是只取卖出侧机构或另一数据源） |

**说明**：「机构净买入」不同平台口径不同（是否含深股通/沪股通、是否合并去重、是否只取机构专用席位等）。我方 v6 当前采用**机构专用席位买入额 - 卖出额**，数据源为新浪 + 东财双重验证。

---

## 五、待你确认 / 值守

1. **fetch_lhb.py 双机同步**：本机已修复该脚本，但 `.gitignore` 使其不进 git。请阿狸咪手动把本机 `repo-temp/fetch_lhb.py` 同步过去，否则 07-13 之后抓取仍会重复计算。
2. **你本机 git 状态是否也分叉**？若昨晚 19:30 部署确失败，请按第三节修复后重跑一次 `deploy_now.py --force`，确保双机 gh-pages 一致（目标 SHA 应 ≈ 本机 `b59552a…`）。
3. **坚果云**：你侧 `repo-temp` 是否已迁出坚果云同步树？若否，`.git/index.lock` 仍可能被抢，导致偶发 deploy 报错（本机已于 07-13 迁出至 `D:\stock-scanner-repo\repo-temp`，路径用 Junction 透明映射）。
4. 若发现线上数据异常陈旧，手动触发：`python update_data_v2.py --fast && python deploy_now.py --force`。

---

**小九已交接清楚，阿狸咪可放心值守。**
