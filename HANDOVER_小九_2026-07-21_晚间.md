# 交接_小九_2026-07-21 晚间22:55

## 主人今晚完成的改动（必须保住！）

### 已推 main 的 4 个 commit（按时间顺序）
1. `53c8aac` - feat: 基本面质量分接入TOP10评分(A+40/B+5/D-10/C=0)+输出quality_grade
2. `330e486` - feat: 多维80+推荐卡显示基本面质量等级标签(A绿/B浅绿/C灰/D红)
3. `c50646b` - feat: 驾驶舱候选股列表+查询卡显示基本面质量等级标签

### generate_top10.py 关键改动（必须重新评分才生效）
- 加载 `data/fundamental_quality.json` 的 stocks dict
- 按 `{market}_{code}` 查 grade，赋分 A=+40 / B=+5 / D=-10 / C=0
- 输出加 `quality_grade` + `quality_score` 字段

### index_master.html 关键改动
- `buildBlock` 函数（多维80+推荐卡）增加 "基本A/B/C/D" 标签
- 驾驶舱候选股列表增加 quality 标签
- 个股查询卡片第1行"多维共振"区域增加 quality 标签

## 小九明天上班第一件事

**1. 拉 main 分支前确认有这些 commit**（必须！）：
```bash
cd E:\workspace\stock-scanner
git log --oneline -10
# 应该有 53c8aac / 330e486 / c50646b 这几个
```

**2. 拉最新代码**：
```bash
git pull --rebase origin main
```

**3. 跑一次 generate_top10.py 让 quality_grade 进 top10_daily.json**：
```bash
"C:\Users\HH20210606\.workbuddy\binaries\python\versions\3.13.12\python.exe" generate_top10.py
```

**4. 跑 update_data_v2.py + deploy_now.py**（盘中已用 --fast 跑过，盘后走完整流程）

## 警报：覆盖陷阱！

⚠️ `deploy_now.py` 的 `_rebuild_dist()` 会执行：
```python
_git("checkout", "origin/main", "--", "index_master.html", "deploy_now.py")
```

**这会把未 push 的本地修改覆盖！** 铁律：
- 任何本地修改 → 先 commit → 先 push main → 再跑 deploy
- 不要在 origin/main 还没同步前跑 deploy

## 不要做的事
- ❌ 不要手动改 index_master.html 然后不 commit 直接跑 deploy
- ❌ 不要在 generate_top10.py 里加奇怪的 try/except 吞错
- ❌ 不要清空 fundamental_quality.json（它是 A/B/C/D 数据源）

## 验证
跑完后检查 `data/top10_daily.json` 里 `top10[0]` 必须有 `quality_grade` 字段：
```bash
python -c "import json; d=json.load(open('data/top10_daily.json')); print(d['top10'][0].get('quality_grade','MISSING'))"
```
应该输出 A/B/C/D，不是 MISSING。

## 紧急联系
如发现 quality_grade 全是空，先看 `data/fundamental_quality.json` 是否有数据：
```bash
python -c "import json; d=json.load(open('data/fundamental_quality.json')); print(f'total={d.get(\"total\")}, stocks={len(d.get(\"stocks\",{}))}')"
```
应该 total>0, stocks>0。如果为0说明 fetch_fundamental_quality.py 没跑。

## 23:48 新增：通达信60天全量回测（重要！）

### 主人在睡了之前要求：回测全选股方法

新文件 `backtest_tdx.py` — 用 baostock（不是通达信，云端也能跑）拉金池股 60 天日 K，
检测 7 种价格信号 + 2 种组合信号，算 T+3/T+5 胜率。

### 跑一次的命令
```bash
python backtest_tdx.py
```
输出：`data/backtest_tdx.json`

### 结果摘要（574条信号）
```
上涨趋势 209笔  38.8% T+3胜  -1.39%   ← 熊市全亏
5日突破  115笔  43.5% T+3胜  -1.48%   ← 最佳单个信号
双信号   284笔  40.1% T+3胜  -1.16%
三信号    72笔  29.2% T+3胜  -2.90%
```
结论：**熊市60天，全信号负收益。不买就是赢。**

### 每天自动跑
- 云端 18:31 cloud_scanner 跑数据
- 本地 19:15 自动化（automation-1784647701016）跑回测 + 构建 + 部署
- 结果在 📦暂未上架 → 📅T+1盘后 → 📊60日回测·全信号胜率 卡片

### 小九明天确认
```bash
ls -la data/backtest_tdx.json
# 应该有文件且 > 1KB
python -c "import json; d=json.load(open('data/backtest_tdx.json')); print(f'信号总数: {sum(s[\"total\"] for s in d[\"summary\"].values())}')"
```
应该输出 "信号总数: 574"

### 金池股回测数据说明
- A股（sz/sh/bj）：baostock 拉 60 天日 K ✅ 有数据
- 港股（hk）：baostock 不支持，不参与回测
- 每日自动追加，坚持 30 天样本会丰富很多
