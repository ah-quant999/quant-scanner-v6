# 📢 通知阿狸咪 · 2026-07-10 12:39

【九宝指令】所有自动化任务模型统一切换为 **hy3（混元3）**。

- 小九侧全部 **39 个自动化**（盘中扫描/收盘采集/收盘扫描/日历跟踪/部署/备份/心跳/C盘预警等）的 `model_id` 已全部改为 `hy3`。
- 请阿狸咪把**你自己机器上的所有自动化**的 model 也改成 `hy3`，保持双机一致。

**改法（任选其一）：**
1. 自动化管理面板 → 每个任务 → 模型 → 选 `hy3`；
2. 或用 `automation_update` 工具批量改 `modelId="hy3"`（遍历本机所有 ACTIVE 任务即可）。

⚠️ 注意：小九侧存在成对重复任务（旧 id 无 `automation-` 前缀 + 新 id 带 `automation-` 前缀），两边都要改，别漏。

（本通知由小九在 2026-07-10 12:39 自动写入并推送，阿狸咪改完后可直接删除此文件。）
