# 交接文档 · 阿狸咪 → 小九（2026-07-19 夜间 · 盘前任务恢复）

> ⚠️ **小九周一（07-20）开机第一件事：恢复你的 09:20 盘前更新任务**
> 存放：项目根目录（坚果云同步树内），已 commit 进 origin/main。
> 主站：https://ah-quant999.github.io/quant-scanner-v6/

---

## 一、紧急行动项（开机就做）

### 1. 恢复小九 09:20 盘前更新任务

你说本机任务都暂停了。盘前主力是你，必须恢复。

**任务信息**（TASK_MATRIX 权威记录）：
- 时间：**09:20**（铁律保留）
- 命令：`python batch_update.py pre_market`
- 内容：guanlan 观澜台 + maharo 研报 + build_candidate_pool 候选池 + scanner 全量扫描 + 构建 + **`deploy_now.py --force` 部署 gh-pages**
- task id（你本机数据库）：`1781512265175`

**恢复方式（你自己判断用哪种）**：
- 如果是 WorkBuddy 自动化被 PAUSE → 改回 `ACTIVE`（status 字段）
- 如果是 schtasks 被 DISABLE → `schtasks /Change /TN "九宝量化-盘前更新" /Enable`
- 如果任务被删了 → 重新创建（参考下方命令）

**重建 schtasks 命令**（如需）：
```cmd
schtasks /Create /TN "九宝量化-盘前更新0920" /TR "C:\Users\<小九用户名>\AppData\Local\Programs\Python\Python314\python.exe E:\workspace\stock-scanner\batch_update.py pre_market" /SC DAILY /ST 09:20 /F
```
（Python 路径换成你本机实际路径，cwds 指向 `E:\workspace\stock-scanner`）

### 2. 同时确认 08:49 盘前全盘扫描是否要恢复

- 时间：**08:49**
- 命令：`python scanner.py --full`（或 batch_update 里对应模式）
- task id：`1781746700995`
- 作用：08:49 跑完全盘扫描，喂给 09:20 的 pre_market 部署

如果 08:49 也暂停了，建议一起恢复——它是 09:20 的前置数据源。

---

## 二、云端已调整：09:30 → 09:00（阿狸咪已改）

我已把云端 `cloud_intraday.yml` 的盘前 cron 从 **09:30 提前到 09:00**。

**改了什么**：
- cron：`30 1 * * *`（09:30 CST）→ `0 1 * * *`（09:00 CST）
- 金股观测 / 总览页两个 step 的 if 条件同步更新
- step name 从 "09:30" 改为 "09:00"

**为什么改**：
1. GitHub Actions 的 cron 在工作日白天（CST 09-16 点）延迟严重，官方承认会延迟 5-30 分钟甚至更久。
2. 实测 cloud_intraday 全部 21 条运行记录，**09:xx-11:xx 时段零运行**——09:30 的盘前 cron 从未在开盘前成功跑过。每天第一次更新实际是 10:31 盘中 slot。
3. 提前到 09:00，即使 cron 延迟 15 分钟，09:25 开始也能在 09:30 开盘前完成部署（执行约 17-20 分钟）。

**云端 09:00 与小九 09:20 的关系（冲突已消除）**：

⚠️ **审计发现冲突**：云端 09:00 和小九 09:20 时间窗口重叠（云端 cron 延迟 0-30min，实际 09:00-09:30 才开始跑，执行约 17-20min；小九 09:20 开始跑，执行约 38min）。两者都会 `git push gh-pages` 和 `git push main` → **push 竞争**。最坏情况：云端部署晚于小九完成 → 覆盖小九的中国源数据 → 主站变成云端国际源。

**解决方案（已实施）**：云端 09:00 slot **跳过 gh-pages 部署**，只做数据抓取+构建+回写 main。部署步骤加了 `if: github.event.schedule != '0 1 * * *'`。这样：
- **小九 09:20 是唯一部署者**（中国源数据 + 部署 gh-pages）
- 云端 09:00 只抓国际源 + 构建 + 回写 main（小九 pull 即可拿到国际源补充）
- **10:31 slot 恢复部署**（小九窗口已过，兜底）
- 手动 workflow_dispatch 触发仍部署（不受影响）

| 场景 | 结果 |
|------|------|
| 小九 09:20 正常跑 | 小九部署 → 主站中国源 ✅ |
| 小九没跑成 | 09:00-10:31 主站停周五，10:31 云端兜底部署 ✅ |
| 两者都跑 | 无竞争，小九唯一部署 ✅ |

---

## 三、双机盘前分工确认（对齐 TASK_MATRIX.md）

| 角色 | 时间 | 跑什么 | 部署 | 定位 |
|------|------|--------|------|------|
| **小九（单位机·白天主导）** | 08:49 | scanner full 全量扫描 | — | 09:20 前置数据 |
| **小九（铁律保留）** | **09:20** | `batch_update.py pre_market`（中国源+扫描+构建） | **`deploy_now.py --force`** | **盘前唯一部署者** |
| 云端 09:00 cron | 09:00 | 全量抓取（国际源）+ 扫描 + 构建 + 回写 main | **❌ 不部署**（避免 push 竞争） | 数据兜底 |
| 云端 10:31 cron | 10:31 | 全量抓取 + 扫描 + 构建 + 部署 | 部署 gh-pages | 小九没跑成时兜底 |
| 阿狸咪 19:30 | 19:30 | 收盘全量 + 部署 | `close_deploy_guarded.py` | 夜间主力 |

**核心原则不变**：白天（至 16:29）小九主导，阿狸咪夜间主力。盘前数据更新走双机（小九），不经过云端 cron 延迟。

---

## 四、你周一需要确认的事

1. ✅ **恢复 09:20 pre_market 任务**（最紧急，开机第一件事）
2. ✅ 确认 08:49 scanner full 是否一起恢复（09:20 的前置）
3. ✅ `git pull origin main` 拉取云端 09:00 调整 + 冲突消除补丁（已 push）
4. ✅ 周一 09:20 跑完后，看主站构建戳是否更新到 07-20 09:xx（确认部署成功）
5. ⚠️ **如果 09:20 没跑成**：云端 09:00 不会部署（已改），主站会停在周五。需你手动 `python deploy_now.py --force` 部署，或等 10:31 云端兜底自动部署。

---

## 五、背景

用户（主人）问"9点30部署一般需要多久？9点24或者26部署够吗"。我实测 GitHub Actions 运行记录发现：**09:30 cron 从未在开盘前跑过**（GitHub cron 白天延迟严重），每天第一次更新实际是 10:31。用户确认"就是中国数据双机跑双机部署"，要求：① 恢复小九 09:20 ② 改云端 09:30 提前 ③ 写交接给小九明天开机改。

云端改动 commit 见 `git log`。本文件随 push 进 main，你 `git pull` 即可见。
