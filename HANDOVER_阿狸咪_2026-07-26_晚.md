# HANDOVER 小九 — 2026-07-26 晚

**发件**：阿狸咪（家中机）  
**收件**：小九（单位机）  
**主题**：新旧算法残留审计完成 + deploy 强制刷新补丁交接

---

## 一、本次做了什么

主人要求审计两件事：

1. 旧「多维三线 / 短线 / 中长线」算法是否还有残留。
2. 新「严格三重共识 / 驾驶舱 A 档」算法是否还有没跟上的步骤。
3. 云端双机都要审清楚，并和小九交接。

结论：旧算法无功能残留；新算法本机全链路接通；双机本机侧 21 条自动化干净。但需要你（小九）在单位机跑一遍复核。

---

## 二、关键改动（已推 origin/main）

| 提交 | 改动 | 原因 |
|---|---|---|
| `c88ac878` | 清理根 `index.html` 两条死链 + 修正 `.gitignore` 旧白名单 | 旧页面已删，死链无意义；白名单保留不存在文件名 |
| `e33edb7f` | `deploy_now.py` 强制刷新 `gen_triple_consensus.py` 并重生成 A 档数据 | 之前云端/本机若本地脚本陈旧，会生成旧的 `基本面≥B` 数据并污染线上 |

### 2.1 为什么 deploy 要强制刷新 gen 脚本？

`gen_triple_consensus.py` 被 `.gitignore` 忽略，`git checkout origin/main` 不会更新它。如果某台机器上这份脚本还是旧版（criteria = 基本面≥B），那么：

- 直接跑 `gen_triple_consensus.py` → 生成 B 档 `data/triple_consensus.json`
- `update_data_v2.py --fast` → 把 B 档复制到 `dist/data/`
- `deploy_now.py --force` → 把 B 档推上 gh-pages

补丁逻辑（已加在 `deploy_now.py` 重建步骤）：

1. `git show origin/main:gen_triple_consensus.py > 本地 gen_triple_consensus.py`
2. 运行该脚本，生成 A 档 `data/triple_consensus.json`
3. 继续 `update_data_v2.py --fast` 和 gh-pages 推送

这样无论本地脚本多旧，**线上永远按 origin/main 最新逻辑**。

---

## 三、小九需要执行的操作

请在小九机器上按顺序执行：

```bash
cd E:\workspace\stock-scanner

# 1. 先 safe_pull，拿到最新 main
python git_safe_sync.py

# 2. 确认本地 gen 脚本已更新为 A 档逻辑
sed -n '94p' gen_triple_consensus.py
# 期望输出：    if grade == "A":

# 3. 确认 deploy_now.py 已带强制刷新
python -c "print('强制刷新存在:', 'git show origin/main:gen_triple_consensus.py' in open('deploy_now.py',encoding='utf-8').read())"

# 4. 跑一键审计脚本
python audit_v6_deploy_chain.py
```

审计脚本会输出：
- 生成器是否用 `grade == "A"`
- `data/` 和 `dist/data/` 的 criteria 是否为 `基本面A档`
- 前端页面是否还有 `基本面≥B`
- 死链、死代码、自动化 DB 状态

请把运行结果截图或贴回给我/主人。

---

## 四、如果审计脚本报 ❌ 怎么办

### 场景 A：criteria 仍是 `基本面≥B`

```bash
# 强制用最新脚本重生数据
python gen_triple_consensus.py
python update_data_v2.py --fast
python extract_panels_v6.py
python ensure_standalone_sync.py

# 确认
grep -c "基本面≥B" standalone/*.html dist/standalone/*.html
python -c "import json; print(json.load(open('dist/data/triple_consensus.json'))['criteria'])"
```

### 场景 B：deploy 时提示 safe_pull 失败 / 非快进

按老规矩：

```bash
python git_safe_sync.py
git push origin main
python deploy_now.py --force
```

不要手写 `git pull --autostash`。

### 场景 C：本地 `gen_triple_consensus.py` 被覆盖回旧版

没关系，现在 deploy 会强制刷新它。但如果你**不部署只跑生成器**，记得先：

```bash
git show origin/main:gen_triple_consensus.py > gen_triple_consensus.py
```

---

## 五、特别提醒

1. **不要直接运行 `_patch_gold_triple.py` 或 `_patch_index_triple.py`**：它们是一次性迁移脚本，靠精确字符串匹配，现在再跑会匹配 0 处并报错。
2. **根 `index.html` 不是线上文件**：线上用的是 `dist/index.html`，所以改根 `index.html` 不需要部署。
3. **所有 .py 必须 `git add -f`**：因为 `.gitignore` 全局忽略 `*.py`，新增/修改脚本后必须 `-f`，否则 push 不上。
4. **工作区有约 41 个历史 stash**：是之前 safe_pull 失败遗留，无业务内容，可择机 `git stash clear`（先确认没有 `stash@{?}` 含未保存工作）。

---

## 六、待你反馈

跑完 `python audit_v6_deploy_chain.py` 后，请告诉我：

1. 是否有任何 ❌ 或 ⚠️ 项？
2. 如果有，按「四、如果报 ❌」处理后的结果如何？
3. 是否需要我再远程协助？

---

**附件**：
- `AUDIT_2026-07-26.md`：完整审计报告
- `audit_v6_deploy_chain.py`：一键审计脚本

---

交接人：阿狸咪  
交接时间：2026-07-26 18:56
