# 九宝量化 · 阿狸咪 → 小九 交接（夜 → 昼）· 2026-07-29 晨

> 发送人：阿狸咪（云端 / 夜间机）　接收人：小九（单位 / 白天机）　主人状态：白天在 小九 上继续
> 用法：小九拉取后直接读本文即可接手，无需回看对话。所有"已上线"结论均经文件/产物核对。

---

## 〇、总状态一句话

| 项 | 状态 | 位置 |
|---|---|---|
| v6 生产站（ah-quant999.github.io/quant-scanner-v6） | **已部署上线**，含时间戳修复 + ETF 分类细分 | 远端 gh-pages tip `c406f92`，build stamp `20260729073405`，POST_CLOSE_TIME `2026-07-28 15:30:00` |
| v8 改版（九宝量化 v8.0） | **原型阶段，未上线**，昨晚 23:50 大幅迭代并验收 | `v8-prototype/design_v3_unified.html`（199KB），22:12 有备份 `.bak.20260728_221217` |
| 双机 | 阿狸咪 已交班；小九 08:00 接管白天主机 | — |

> ⚠️ 本地 git 历史被 squash：main 远端/本地均为 `c86d8fa8`（仅 2 条提交），此前对话里的 `200f88d`/`45678c59` 等 commit 已并入树、不再作为独立节点可达。本文一律以 **build stamp / POST_CLOSE_TIME / 远端 gh-pages tip** 为凭证，不引失效哈希。

---

## 一、v6 修复（已上线，小九白天无需重做）

### 1.1 时间戳显示修复（盘后卡片"N天前"脏数据）
**现象**：多个面板显示 `4天前 15:30`、`NaN天前` 等异常。
**根因（三处）**：
1. `fmtDataTime()` 未处理 ISO 格式（`2026-07-28T17:57:47`）→ `new Date()` 解析失败 → `NaN`。
2. `setPostMarketTime()` 无条件用 `POST_CLOSE_TIME` 归一化；当母版里 `POST_CLOSE_TIME` 被写死成旧值（2026-07-24）且比 rawTime 更旧时，显示 `4天前 15:30`。
3. 母版 `index_master.html` 中 `CLOUD_TIME_PLACEHOLDER` / `POST_CLOSE_TIME_PLACEHOLDER` / `__BUILD_STAMP__` 占位符被写死，导致 `update_data_v2.py` 注入失效。

**修复点（均在 `index_master.html`）**：
- `fmtDataTime()`：新增 `.replace('T',' ')` 统一分隔符 + `isNaN(ts)` 防御分支（返回 `stale/critical` 标记，不渲染 NaN）。
- `setPostMarketTime()`：新增闸门——`POST_CLOSE_TIME` 必须**比 rawTime 新且不超过 1 个交易日**才用于归一化；真实过期（daysAgo≥2）则保留红标不掩盖。
- IPO 打分卡渲染：同步加 `POST_CLOSE_TIME` 陈旧防御。
- 恢复占位符：`window.CLOUD_UPDATE_TIME="CLOUD_TIME_PLACEHOLDER"`、`window.POST_CLOSE_TIME="POST_CLOSE_TIME_PLACEHOLDER"`、`<meta name="build-stamp" content="__BUILD_STAMP__">`、`<title>` 去写死时间戳。

**部署结果**：POST_CLOSE_TIME=`2026-07-28 15:30:00`，各面板数据时间戳均为 2026-07-28 当日（CANDIDATE_POOL 14:30、SECTOR_FUND_FLOW 15:17、LIMIT_UP_HEATMAP 15:21、LHB 20:55、CFFEX/INST_TRADE 20:55）。已 push 到 gh-pages（远端 tip `c406f92`）。

### 1.2 ETF 盘中热度分类细分（比参考图"宽基/行业/主题/跨境"四分类更细）
**改动文件**：
- `fetch_etf_intraday_heat.py`：新增 ETF 分级分类规则（一级 6 类 + 二级 30+ 子类），按分类聚合 `count/up/down/net_inflow/top_active/top_inflow/top_outflow`，写入 `data/etf_intraday_heat.json` 的 `categories` 字段。分类优先级：**商品 > 跨境 > 宽基 > 行业 > 策略 > 主题**。
- `index_master.html` 总览页 ETF 卡片：新增一级标签行（全部/宽基/行业/主题/跨境/商品/策略）+ 二级子类筛选芯片；每类独立显示统计条（N只·▲涨/▼跌·净流入）+ TOP 活跃榜。全部标签 TOP10，分类/子类标签 TOP5（防半宽卡片过载）。
- `data/etf_intraday_heat.json` 现含 6 个一级分类：`宽基/行业/主题/策略/跨境/商品`。

**部署结果**：已随 1.1 同次部署上线（build stamp `20260729073405`）。线上 ETF 卡片可见六分类 tab（JS 运行时渲染，静态 HTML 看不到 tab 文字，需用浏览器/WebFetch 校验）。

---

## 二、v8 改版（原型阶段，昨晚 23:50 迭代）

### 2.1 文件位置
- 设计稿（协作基线）：`v8-prototype/design_v3_unified.html`（199KB / 2698 行）
- 昨晚 22:12 备份：`v8-prototype/design_v3_unified.html.bak.20260728_221217`（962 行，diff 基线）
- 验收截图（15 张，`v8-prototype/_verify_*.png`，23:xx 生成）：见 2.3
- 设计方向基线（六板块/暗色/红涨绿跌/运维+逻辑详解加锁）：`HANDOVER_小九_给阿狸咪_2026-07-28_v8.md`

### 2.2 昨晚具体改动（对比 22:12 备份：+435 / -166 行，43 处变更区）
1. **打新速览 tab 化（今日事件板块）**：新增 `待申购 / 待定价 / 待上市` + `今日申购 / 上市日追踪5日` 五视图；新增动态加载 + 内嵌兜底逻辑（`function renderDirect` 约 1344 行；打新加载 JS 约 1222 行起，约 1280 行）。删旧 `renderTab`，改 `renderDirect`。
2. **ETF 资金热度新增四分类（实时数据板块）**：line 442–480，宽基/行业/主题/跨境 独立展示（与 v6 参考图一致，但为**原型模拟数据**，未接真实管道）。
3. **逻辑详解子标签化（管理员锁）**：新增 `lgSubtabs`（六维共振雷达 / 斐波窗口 / 等子页）；删除原 `opTab1/opTab2` 结构，改 `lg-subtabs` 平铺切换。
4. **运维板块重构**：删除旧 `opTab1/opTab2`，改为 `部署状态 / 双机状态 / 自动化任务(11) / 数据新鲜度` 四块平铺。
5. **总量**：新增约 1280 行渲染 JS（文件 962 → 2698 行）；各模块"更新于 今日 15:xx"时间戳归一。
6. **仍为模拟数据**：设计稿全部图表用纯 SVG/CSS 画、不依赖 CDN；未接任何真实数据管道（符合"先改完版再上线"铁律）。

### 2.3 验收截图索引（v8-prototype/_verify_*.png）
| 模块 | 文件 |
|---|---|
| ETF 模块 | `_verify_etf_dist.png` / `_verify_etf_header.png` / `_verify_etf_left.png` / `_verify_etf_scroll.png` / `_verify_etf_visible.png` / `_verify_etf_final.png` |
| 逻辑详解各子页 | `_verify_logic_op.png` / `_verify_logic_ph.png` / `_verify_logic_rt.png` / `_verify_logic_st.png` / `_verify_logic_td.png` |
| 运维页 | `_verify_ops_delisted.png` / `_verify_ops_unlisted.png` / `_verify_ops_v6.png` |
| 顶部 header | `_verify_headers_ph.png` / `_verify_headers_today.png` |

### 2.4 待小九/主人白天推进（主人已拍板"先改完版再上线"）
1. 继续按主人反馈迭代 `v8-prototype/` 设计稿（UI / 板块 / 交互）。
2. 主人确认定稿 → 建独立仓库 `quant-scanner-v8` + GitHub Pages。
3. 前端模块化重写（拆分组件，废除 `index_master.html` 巨石）。
4. 数据管道重构（`safe_pull` 不冲未跟踪改动、`.gitignore` 白名单审计、敏感数据走 Secrets / 私有后端）。
5. 上线（gh-pages），切换默认访问。

---

## 三、安全铁律（v8 上线前必读，v6 已验证）
GitHub Pages 免费仓库 = 公开可读，**绝不入库**：
1. **管理员明文密码** —— 前端只用 SHA-256 哈希校验；常量在 `v8-prototype/design_v3_unified.html` 的 `ADMIN_HASH`。改密码重新哈希，明文只由主人线下告知。
2. **数据源 token / API key**（neodata、东财 token、`.gh_pat`、`.neodata_token`、zsxq_token 等）—— 走 GitHub Secrets 或私有后端，前端只渲染结果。
3. **核心算法**（选股策略权重/公式/参数、打分模型）—— 放私有仓库/后端，公开前端只展示产出。
4. **改版期不部署**：仅可 `git add -f` 设计稿 + 必要文档后 commit + push `main`，**勿 `git add .`、勿碰 `data/` 与敏感文件、勿触发部署**。

---

## 四、双机交接 & 下一步
- 阿狸咪 夜间任务已全部完成（含 09:00 模型ID审计自动化下次触发）；`.deploy_lock` 已释放。
- 小九 白天 08:00 接管：负责中国源（概念/板块资金流/涨跌家数/预警/龙虎榜/港股通/盘中）+ v8 改版主力。
- 心跳/闸门：`_heartbeat.log` + `check_all_deploys.py`；阿狸咪 白天禁 deploy/batch_update（除非 小九 心跳失联 >60min failover）。
- 若 小九 需 阿狸咪 决策，写 `MSG_TO_阿狸咪_*.md` 推回仓库，阿狸咪 夜间拉取处理。

## 五、已知坑（给小九提个醒）
- **git 历史被 squash**：本地/远端 main 均 `c86d8fa8`，旧 commit 哈希失效；推代码走 `git add -f <文件>` → `git commit` → `git fetch origin` → `git rebase origin/main` → `git push origin main`（**禁用 `git pull --autostash` / `safe_pull`**，会冲掉未推送改动）。
- **本地 gh-pages ref 是 stale**（`a1331f4e` 07-26）；远端 gh-pages 才是真 `c406f92`。校验线上效果用浏览器/WebFetch，别信 `git log gh-pages` 本地值。
- **ETF tab 是 JS 渲染**：`curl` 拉线上页面看不到"宽基"等字，属正常，用 WebFetch 或浏览器确认。
