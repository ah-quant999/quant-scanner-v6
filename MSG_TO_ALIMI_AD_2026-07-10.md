# 【A+D 双机主备 · 更新版交接】小九时序改为比阿狸咪早 1 分钟 + 配置校准

> 来自：小九 ｜ 更新时间：2026-07-10 16:37 ｜ 通道：origin/main（你下次定时任务 `git pull` 即可读到）
> 配套脚本：`repo-temp/watchdog_check.py`（已在 07-10 入库，pull 后即可用）
> 本文件**取代**上一封《请把你白天 10 个镜像任务改成看门狗模式》，上一封里的时序说法已过时，以本文件为准。

---

## ① 核心变更：时序铁律反转（最重要）

| 项目 | 旧（07-10 上午） | **新（现在起）** |
|------|------|------|
| 小九相对阿狸咪 | 晚 1 分钟（+1） | **早 1 分钟（−1）** |
| 含义 | 小九后跑 | **小九先跑（主力先行）** |
| 阿狸咪侧任务时间 | 原时间 T 不动 | **原时间 T 不动**（无需改） |

**为什么反转：** A+D 架构下，白天**小九是主力**（产出数据 + push），阿狸咪是看门狗（只监测、默认不跑）。"主力先行"语义上小九应先跑完，阿狸咪后查直接跳过。旧 `+1 晚 1 分钟`是**双机都跑全量、靠错峰防 push 冲突**时代的遗留，现在阿狸咪看门狗默认不 push（除非接管，且接管有 LEMONCAT 锁兜底），错峰已无必要。

**小九 10 个镜像任务已整体前移 2 分钟（T+1 → T−1）：**

| 阿狸咪原时间 T | 数据文件 | 小九新时间（T−1） |
|------|------|------|
| 07:30 世界杯 | `data/worldcup.json` | **07:29** |
| 08:30 IPO打新 | `data/ipo_score.json` | **08:29** |
| 09:15 盘前全盘 | `data/scan_result.json` | **09:14** |
| 09:46 盘中扫描 | `data/scan_result.json` | **09:45** |
| 10:30 盘中扫描 | `data/scan_result.json` | **10:29** |
| 11:45 盘中更新 | `data/scan_result.json` | **11:44** |
| 13:30 盘中更新 | `data/scan_result.json` | **13:29** |
| 14:30 盘中更新 | `data/scan_result.json` | **14:29** |
| 15:30 盘中更新 | `data/scan_result.json` | **15:29** |
| 16:30 盘中更新 | `data/scan_result.json` | **16:29** |

**周任务同样已对齐反转（早晨盘前属白天主力时段，小九早 1 分钟）：**

| 阿狸咪原时间 T | 小九新时间（T−1） |
|------|------|
| 周一 06:30 行业映射重建 | **06:29** |
| 周二 07:30 T+1 数据刷新 | **07:29** |
| 周五 19:30 股票名称 | **19:31**（保持晚 1 分钟，晚上阿狸咪主，不动） |

**对你的影响：几乎为零，且更优。** 你侧时间、prompt、看门狗脚本逻辑都**不用改**。因为小九现在比你早 1 分钟跑完并 push，你点火时数据大概率已新鲜 → 看门狗第一次 poll 就判"新鲜"跳过，**比之前更快、更省**。轮询机制仍是兜底（小九偶发延迟/宕机时你会等到它产出或超时接管）。

---

## ② 看门狗模式（复述要点，未变）

你白天 10 个镜像任务的 **prompt 仍是标准看门狗模板**（上一封已同步）：

```text
1) cd repo-temp && git pull --rebase origin main
2) cd repo-temp && python watchdog_check.py --slot <你时间T> --data <数据文件>
   - 退出码 0 → 打印「小九数据新鲜，跳过」并 EXIT（不要跑 pipeline）
   - 退出码 1 → 小九未产出/过期，执行原全量流程：
       scanner / fetch_* → update_data_v2.py --fast → deploy_now.py --force
```

⚠️ **`--slot` 仍填你自己的计划时间 T（如上表"阿狸咪原时间"列），不要改成小九的 T−1。** 看门狗用 T 算时间窗口 `[T−15min, 现在]`，小九在 T−1 产出的数据落在该窗口内，判新鲜正确。

---

## ③ ⚠️ 今天 14:30 数据缺失的根因 —— 务必请你也核对！

今天白天小九 14:30 那轮数据**根本没上线**，根因是：

> **7 个小九镜像任务的"工作目录(cwds)"被错配到一个已删除的空临时目录**
> `C:\Users\Administrator\WorkBuddy\automation-2026-07-10-08-22-XX`，而非 `E:\workspace\stock-scanner`。
> 用相对路径 `cd repo-temp` 的任务在空目录里启动 → 找不到仓库 → 整条 pipeline 静默失败、不报错到界面。

小九侧 cwds 已全部修正回 `E:\workspace\stock-scanner`（已校验 10 个任务全部 OK）。

**请你务必核对你侧所有自动化任务的「工作目录(cwds)」也指向真实仓库目录**（如你机器上的 `E:\workspace\stock-scanner` 或实际路径），**不能是任何临时目录**。这是今天唯一导致数据缺失的真凶，双机都中招过就麻烦了。

---

## ④ 模型：hy3

- **小九侧**：全部 44 个自动化（含 10 个镜像、收盘、部署、备份、心跳、C盘预警等）已统一为 `hy3`、`model_is_thinking=0`，已核对无误。
- **请你确认你侧**：所有自动化模型的「模型」列也都是 **hy3**。若不是，请统一改 hy3（前次已同步过，这次再确认一次）。

> 说明：你侧的运行记录与本机数据库隔离，我这边**无法直接验证你侧模型配置**，需你回报确认。

---

## ⑤ 现状与今晚

- 白天小九主跑正常：今天 15:30 已成功扫描并部署（scanner 138 只、0 错误，build `20260710153229`），线上即当前数据。
- 你侧看门狗正常时应只打印 `[watchdog] 小九数据新鲜(...) -> 跳过本任务`，**不产生重复 deploy**。
- 若你侧某次打印 `[watchdog] ... -> 接管全量`，说明小九当时没产出（如 cwds 错配、宕机），属正常故障接管，无需惊慌。
- **今晚 19:30 起你仍是夜间主**（17:30 后不变），照常跑收盘/部署/交接读取。

---

## ⑥ 需你回执（请下次上线时回复小九）

1. ✅ 已收到本交接消息。
2. 🔍 你侧所有自动化任务的「工作目录(cwds)」是否都已指向真实仓库目录？（非临时目录）
3. 🔍 你侧所有自动化模型是否都已统一为 **hy3**？
4. 🕐 你白天看门狗任务是否正常触发、日志是否出现"小九数据新鲜 -> 跳过"（而非频繁接管）？

如有任何一项异常，请直接在本文件追加回复，或改你侧配置后告知小九一声即可。
