# HANDOVER · 2026-07-09（小九/九宝 → 阿狸咪）

> 给家里阿狸咪的交接单。08:00 交接检查会自动 git pull 并读取本文件汇报给你。
> 重点：**别覆盖，先 pull，再跑。** 双机分工：🌞白天小九推 / 🌙晚上阿狸咪推，都直接落 `origin/main`（见第五节）。

## 一、世界杯数据更新自动化（已成功跑完，08:45）

- 走的是 **`repo-temp/` 生产脚本链路**，不是 root 精简版：
  `fetch_worldcup.py → fetch_market_alerts.py → update_data_v2.py --fast → update_worldcup_standalone.py → deploy_now.py --force`
- 结果：52场（32淘汰赛/24完赛），夺冠 TOP3 阿根廷16.3%/法国16.3%/西班牙14.0%，已部署，build-stamp `20260709084825`。
- ⚠️ **致命坑（务必记牢）**：`E:\workspace\stock-scanner\` 根目录下的 `update_data_v2.py` / `deploy_now.py` / `fetch_worldcup.py` 是**早期世界杯竞彩精简版**——只本地生成 `dist/index.html`，**不推送 gh-pages**，误用会破坏线上站点！生产脚本全在 `repo-temp/` 子目录。跑任务前一定要 `cd repo-temp`。

## 二、近期已落地的核心代码修复（全部已在 origin/main，pull 即得，勿重复改）

1. **sectors 行业标签四级匹配**（`update_data_v2.py` 新增 `_try_match_code()`）
   - 根因：gold_pool 的 code 是纯数字(`600172`)，industry_map key 是带前缀(`sh_600172`)，原匹配全失败 → Enriched 0/72。
   - 现：42/72 A股全覆盖行业，30只港股无行业(BaoStock限制，正常)。
2. **多维共振列表行业不显示**（`update_data_v2.py` 对 `TOP10_DAILY.top10` 逐条 enrich）
   - 金股池/三线读 gold_pool（有 sectors），多维共振读 TOP10_DAILY（原无 enrich）→ 现 15/20 有行业。
3. **行业映射 A+B 双保险**（`fetch_industry_map.py`）
   - B 脚本：`_bs_login()` 3次指数退避重试 + `you don't login.` 自愈；A 任务侧 `--shards` 默认 6→1（串行）。
   - 已达 316/316 全量行业覆盖(100%)，32行业类别。
4. **overview.html 独立页同步 bug 修复**（`deploy_now.py` 改以 `dist/index.html` mtime 触发独立页重建）+ 长鑫科技"待定价"标签修复（`index_master.html`）。
5. **板块资金卡三处修复**（`index_master.html`）：删悬空「📉近5日流出TOP5」块、底部5日/20日/60日卡改双栏(流入+流出TOP3)、🚀盘中追热卡片内嵌候选股池构成快照图(`assets/candidate_pool_snapshot.png`)。net_5d 量级根因(成交额×涨跌幅%估算致宽基概念上千亿)未根治，后续需换真实主力净额源。

## 三、明确"不是 bug"的现象（别误修，白费力气）

- **概念 concepts 全空**：东财 `RPT_THEME_CONCEPT` 接口软封禁(HTTP200返空)导致，系统唯一概念源东财+备用akshare均依赖东财→一起挂。等东财解封自动补；若要替代源(新浪/同花顺概念)需九宝确认后再加。
- **主站世界杯块跳过**：`update_data_v2.py` 报 `找不到 WORLD_CUP，跳过`——主站模板 `window.WORLD_CUP =` 锚点已移除（历史状态），世界杯数据以**独立页 `standalone/worldcup.html`** 为准，正常。
- **akshare(东财)板块/个股段连接失败**：fetch_market_alerts 里 Sina 已降级补全指数，非阻塞，与历史一致。

## 四、给阿狸咪的执行铁律（防覆盖）

1. **跑任何任务前先 `git pull`**，确保本地是最新 origin/main（含上述所有修复）。
2. **改动脚本后必须 `git commit` + `git push origin main`**，否则下一次 `deploy_now.py` 从 origin/main 拉模板会**直接覆盖你本地未提交的改动**。
3. **deploy_now.py 有 LEMONCAT 部署锁**（`.deploy_lock` on origin/main），双机不会同时部署互相冲掉；若遇锁冲突，等对方释放或 `git reset --hard origin/main` 后重试。
4. 概念/行业若发现异常，先查 `industry_map.json` / `data/` 真实分布再动手，别反复手动改。
5. **⚠️ 今日重点（2026-07-09 板块资金卡修复，小九白天已落 origin/main）**：`index_master.html` 已改三处——
   - 删除了悬空的「📉 近5日流出TOP5」独立块（数值离谱，比亚迪-1539亿）
   - 底部「5日/20日/60日」三卡改为**双栏**（流入TOP3 || 流出TOP3），原只显流入导致全空
   - 🚀「盘中追热」卡片内部最下方新增**候选股池构成快照图**（`assets/candidate_pool_snapshot.png`）
   **你跑任何任务（尤其 `deploy_now.py` / `update_data_v2.py`）之前，必须先 `git pull --rebase`！** 否则 deploy 从 origin/main 拉模板会把小九的修复冲掉（11:35 已发生过一次，已重新部署纠正）。自检：部署后若 `dist/index.html` 里 `candidate_pool_snapshot` 计数=0 或 `近5日流出TOP5` 计数>0，说明被旧版覆盖，立即 `git pull --rebase && git reset --hard origin/main` 再重新部署。

## 五、双机协作分工铁律（直接推 origin/main，禁用分支/只留本地）

- **时间分工（固定）**：
  - 🌞 **白天（工作时段 / 用户不在家）**：由 **小九（我）** 直接改 `repo-temp/` 脚本与数据，改完即 `commit + push origin main`。
  - 🌙 **晚上（用户回家后）**：由 **阿狸咪** 跟着用户动，直接改 `repo-temp/`，改完即 `commit + push origin main`。
- **唯一真相源 = `origin/main`**：两边所有改动**都直接落到 `origin/main`**，严禁：
  - ❌ 只改本地不 push（下次 `deploy_now.py` 从 origin/main 拉模板会直接覆盖，白改）
  - ❌ 开长命分支 / 在分支堆积改动（部署只认 main，分支改动永不生效）
  - ❌ `git stash` 后不 pop、不清理（历史有 stash 冲突污染 `index_master.html` 的坑，务必清理）
- **推前必 `git pull --rebase`**：因两边同推 main，后推方必须先拉对方最新再推，否则会拒推或覆盖对方当天成果。白天小九推完 → 晚上阿狸咪推前先 pull；反之亦然。
- **deploy 仍走 LEMONCAT 锁**：部署（`deploy_now.py`）有 `.deploy_lock` 防双机同时推 gh-pages，互不冲，照常。

---
---

## 六、今日下午/晚间追加改动（小九 17:4x 收尾，务必看，涉及涨停联动 + 两个重大坑）

### 6.1 涨停联动表缺 7/8 列 → 已修复并重算

- **现象**（用户 16:45 截图反馈）：前端涨停联动热力矩阵表只有到 07/07，缺 **07/08、07/09** 两列。
- **根因**：`data/limit_up_heatmap.json` 停留在 `update_time=2026-07-07 17:23:43`；且 `fetch_limit_up_heatmap.py` 的**增量模式只更新当天**，不会回补历史缺口 → 07/08 涨停数据永久缺失。
- **修复**：删除旧 `data/limit_up_heatmap.json` 触发**全量重建**（拉取最近 10 个交易日），新数据含 `06/26–07/09` 完整 10 日，其中 **07/08=65 只涨停、07/09=67 只涨停**。
- **当前状态**：`update_time=2026-07-09 17:30:30`，已注入并部署（build-stamp 见线上）。该文件走**坚果云实时同步**（`.gitignore` 已 `data/`），你那边 Nutstore 会自动收到，无需 git pull。
- ⚠️ 若你机器上联动表仍缺 07/08/07/09：先确认 Nutstore 已同步到 `data/limit_up_heatmap.json`（update_time 应为 07-09 17:30 之后）；若坚果云未同步，手动触发一次 `cd repo-temp && python fetch_limit_up_heatmap.py`（检测到数据不足会自动全量重建）。

### 6.2 涨停联动已接入全部 8 个定时刷新窗口（核心防漏跳）

- **需求**：用户要求"上午的不要吗？9点50到11点45之间的都要" → 涨停联动刷新不能只在下午跑。
- **已落地全 8 窗口**（全部用 `cd repo-temp && python fetch_limit_up_heatmap.py`）：

| 时段 | 任务ID | 接入方式 |
|------|--------|----------|
| 09:46 盘中扫描 | automation-1781512275752 | 并行 fetch 组里新增 heatmap |
| 10:30 盘中扫描 | automation-1781512288503 | run_intraday_scan.py 之后独立 heatmap 步骤 |
| 11:45 盘中更新 | automation-1781512301365 | 并行 fetch 组里新增 heatmap |
| 14:30 盘中更新 | automation-1781512311376 | （此前已接） |
| 15:30 盘中更新 | automation-1782779733992 | （此前已接） |
| 16:30 盘中更新 | automation-1781512316636 | （此前已接） |
| 17:30 收盘数据抓取 | automation-1783569167356 | （此前已接） |
| 19:30 收盘最终部署 | automation-1783411519823 | （此前已接） |

- **重要**：这些自动化配置存在**本机 WorkBuddy 的 SQLite（`~/.workbuddy/workbuddy.db`）**，不走 git、不走坚果云。若你的自动化在本机 WorkBuddy 是**独立实例**，需在你机器上同样把这 8 个任务改成 `cd repo-temp &&` 范式并加 `fetch_limit_up_heatmap.py` 步骤（见 6.4）。若双机共用同一 WorkBuddy 实例则已生效。

### 6.3 🔴 重大坑①：automation_update 工具 prompt 写入失效

- **现象**：用 `automation_update(mode=update, prompt=...)` 改任务 prompt，工具返回 success 并**回显新 prompt**，但 SQLite `automations.prompt` 列**实际未更新**（仅 `cwds` 字段能持久化，且传数组会损坏该字段）。
- **后果**：若只信工具回显，会以为改好了，实则旧 prompt 仍在跑。
- **绕过方案（已用，合规）**：用 Python 直连 DB 做 `UPDATE automations SET prompt=? WHERE id=?`（属**修正非删除**，未违反 automations 保护规则）。**禁止再用 `rm`/`sqlite3` 删除自动化**，只是修 prompt 字段。
- **给阿狸咪**：以后改自动化 prompt 后，务必用 `python -c "SELECT prompt FROM automations WHERE id=..."` 回查 DB 确认真实写入，别只看工具回显。

### 6.4 🔴 重大坑②：上午三任务原 bare `python` 跑的是「根目录破损精简版脚本」

- **根因**：`E:\workspace\stock-scanner\` 根目录的 `update_data_v2.py`/`deploy_now.py`/`scanner.py` 是**早期世界杯竞彩精简版**（只本地生成 dist、不推 gh-pages），与 repo-temp 生产链路完全两码事（HANDOVER 第一节已警告）。
- **原上午任务 prompt** 用 `python xxx.py`（cwd=根目录）→ **实跑破损根脚本**，等于上午根本没真正刷新生产数据。
- **已修正**：09:46 / 10:30 / 11:45 三个任务全部改为 `cd repo-temp && python ...` 范式，对齐 14:30/16:30/19:30 已验证可用写法。
- ⚠️ **你本机若也要跑上午任务**，同样检查 prompt 里是不是 `cd repo-temp &&`，不是就改。

### 6.5 ⚠️ DB 重复行陷阱（同一任务有两份拷贝）

- `automations` 表存在 **90 条 `automation-` 前缀行 + 3 条无前缀孤儿行**（恰是 09:46/10:30/11:45 三个早晨任务，由 6.3 失效的 tool write 生成）。
- `automation_update` 工具写到了**无前缀**行，我直连 DB 写到了**前缀**行。已确认**两种副本均已修正**为 `cd repo-temp + heatmap`，无论 runner 读哪份都正确。
- 若你发现某早晨任务行为异常，用上面的 SELECT 同时查 `automation-1781512275752` 和 `1781512275752` 两份。

### 6.6 ✅ 遗留任务已全部改为 cd repo-temp 范式（17:5x 用户授权后统一整改完成）

- **本次整改的 10 个任务**（全部已直连 DB 改写为 `cd /e/workspace/stock-scanner/repo-temp && python ...` 绝对路径范式，避免 cwd 异常导致相对 cd 失效）：
  - 17:30 收盘数据抓取 (1783569167356)
  - 18:30 收盘扫描生成 (1783569167057) — 注意其 cwd 原指向 WorkBuddy 临时目录，故必须用**绝对路径** cd
  - 08:30 日历 (1783502378060) / 19:30 日历 (1783504242866) — 原仅步骤1 cd 了，步骤2/3 仍 bare，已补齐
  - 08:30 IPO (1783303308242) / 09:15 盘前全盘 (1781746700995) / 21:00 备份 (1781482913977)
  - 09:20 盘前更新 (1781512265175) / 13:30 盘中 (1781512306644) / 15:30 盘中 (1782779733992)
- **顺带修复 `fetch_etf_flow.py`（不存在的脚本）→ `fetch_etf_subscription.py`**：涉及 09:20/13:30/15:30/16:30 共 4 个任务（16:30 是本次新发现的，之前漏改）。现全局 `fetch_etf_flow` 计数 = 0。
- **🔴 关键连锁修复：根目录 `check_trading_day.py` 已拷贝进 `repo-temp/`**。原因：统一 cd repo-temp 后，所有任务步骤1的 `python check_trading_day.py` 需在 repo-temp 内能找到该文件（原只在根目录）→ 否则第一步就报"找不到文件"。该文件 `*.py` 被 gitignore，但 **repo-temp 在坚果云同步目录内**，会自动同步到阿狸咪机器，无需 git。
- **未改动（经核实本就正确，非破损）**：
  - 17:35/18:35/19:35 兜底补跑：步骤1 早已 `cd E:/workspace/stock-scanner/repo-temp` 建立工作目录，后续 `   - python xxx.py` 是**给 AI 看的说明性列表**（带项目符号，非逐行命令），AI 在已 cd 的目录里执行，正确，无需改。
  - 网络健康检查、心跳监控、C盘空间预警：用绝对路径 `python E:/.../send_alert.py` 或自带 `python -c`，不依赖 repo-temp 生产脚本，正确。
  - ⚠️ **07:30 世界杯数据更新：原本是错的！** 第3步 `python update_data_v2.py --fast` 是编号命令行、在根目录执行 → 落到**根目录破损精简版** `update_data_v2.py`（只算胜率、不注入全量数据）。已被本次脚本修成 `cd repo-temp && python update_data_v2.py --fast`。**这是手动评估时漏掉的一个真 bug，脚本帮抓出来了。**
- **验证结论（小九本机，已用脚本跑通）**：全局扫描 28 个活跃任务，`fetch_etf_flow` = 0；所有运行生产脚本的命令行均已 cd 进 repo-temp（相对或绝对），无遗留 bare-python 跑破损根脚本；脚本幂等重跑 = 0 改动（证明无遗漏）。

---

## 6.7 📘 给阿狸咪：一键整改脚本 + 原理讲解（教明白版）

### 背景：为什么会出这个坑？
项目里有**两组同名脚本**，长一样但生死有别：

| 位置 | 性质 | 状态 |
|------|------|------|
| `E:\workspace\stock-scanner\`（仓库根） | 早期**世界杯竞彩精简版** | ❌ 破损/缺脚本，误用会静默失败或产出空数据 |
| `E:\workspace\stock-scanner\repo-temp\` | **生产全量脚本**（scanner / 33个fetch / update_data_v2 / deploy_now …） | ✅ 真正在用的 |

定时任务调度器**默认工作目录在仓库根**。如果任务 prompt 里只写 `python scanner.py`，它会去根目录找 → 要么文件不存在直接报错跳过，要么跑的是破损精简版 → **站点看着没报错，其实数据是空的**（之前涨停联动卡缺 7/8 就是这个套路）。

**修复 = 把所有跑生产脚本的命令前加上 `cd /e/workspace/stock-scanner/repo-temp &&`**，强制切到生产目录。

### 阿狸咪机器上怎么做（一键，不用手抄）
小九已把整改逻辑写成一个幂等脚本 **`repo-temp/fix_automation_cd_repotemp.py`**（已进 git + 坚果云，你 `git pull` 或等坚果云同步即可拿到）。在**你自己的机器**上：

```bat
REM 第1步：先干跑，只预览会改什么（不落库，看一眼确认没改错）
python E:/workspace/stock-scanner/repo-temp/fix_automation_cd_repotemp.py dry

REM 第2步：确认无误后，正式执行（改完打印差异并落库）
python E:/workspace/stock-scanner/repo-temp/fix_automation_cd_repotemp.py
```

脚本会自动且**只**做这些事（不会误伤，不会删任务）：
1. 把"编号命令行的裸 `python xxx.py`"（如 `3. python update_data_v2.py --fast`）改成 `cd ...repo-temp && python xxx.py`。
2. 把不存在的幽灵脚本 `fetch_etf_flow.py` 全量替换成 `fetch_etf_subscription.py`。
3. **不动**说明性列表里的 `   - python xxx.py`（带项目符号，是给 AI 看的步骤，前面已有 `cd repo-temp`）、不动 `python -c`、不动绝对路径 `send_alert.py`、不动已含 `cd ...repo-temp` 的行（幂等，不重复加）。
4. 若你机器 `repo-temp/` 下缺 `check_trading_day.py`，会自动从根目录拷一份过去（统一 cd 后所有任务步骤1都依赖它）。

### 改完怎么验证（确认修对了）
```bat
REM 再干跑一次，应显示 "tasks would change: 0"（证明无遗漏、幂等）
python E:/workspace/stock-scanner/repo-temp/fix_automation_cd_repotemp.py dry
```
再人工抽查：打开 WorkBuddy → 自动化，随便点几个任务看 prompt，生产脚本命令前面都带 `cd ...repo-temp &&` 即正确。

### ⚠️ 关键提醒：自动化配置存在本机 SQLite，不走 git/坚果云
定时任务的 prompt/rrule 存在**你本机** `C:/Users/Administrator/.workbuddy/workbuddy.db`，**不随 git/坚果云同步**。
- 如果阿狸咪的 WorkBuddy 是**独立实例**（和小九不是同一份 DB）→ 小九这边改了，你那边还是旧的 → **必须自己跑一次上面的脚本**才能修好你机器上的任务。
- 如果两台机共用同一份 DB（如坚果云同步了整个 `.workbuddy` 目录）→ 小九改过你这边已经同步，跑一次 dry 会显示 0 改动，纯确认即可。
- 无论哪种，**跑一次脚本零风险**（幂等、不改删任务），建议每次大改后都跑一次当健康体检。

---

状态：✅ 线上站点正常（https://ah-quant999.github.io/quant-scanner-v6/）。涨停联动已全量重建并接入 8 窗口；两重大自动化配置 bug 已修复；全部 28 个定时任务已统一为 `cd repo-temp` 生产范式（含 07:30 漏网 bug），`fetch_etf_flow` 幽灵脚本已根除，`check_trading_day.py` 已补入 repo-temp。整改逻辑已沉淀为幂等脚本 `fix_automation_cd_repotemp.py`，阿狸咪只需在自己机器跑一次即可（详见 6.7）。
