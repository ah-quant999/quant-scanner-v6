# 交接文档 · 阿狸咪 → 小九（2026-07-19 晚间）

> 目的：让小九周一（07-20）上班一眼掌握周末（07-18~07-19，含 07-17 收尾）全部源码改动与系统状态。
> 存放：项目根目录（坚果云同步树内），已 commit 进 origin/main。
> 主站：https://ah-quant999.github.io/quant-scanner-v6/

---

## 一、本周痛点根因（一句话）

数据陈旧没人自动修 + 云端监控闸门「只拦不修」→ 周五检测到不新鲜就 `exit 1` 取消部署 → 主站数据冻结在周五盘中，且全靠用户截图才发现。

**这两点已在周末根除。**

---

## 二、周末两天改了什么（按文件，均已在 origin/main）

| 文件 / 对象 | 改动 | 作用 | 提交 |
|---|---|---|---|
| `fetch_main_stock.py` | akshare 替代缺失的 westock-data CLI | 根治 `main_stock` 长期空数组 | `eeaf02f1` 类 |
| `verify_data_vs_website.py` | 消除三类误报：北向停更 / herding 周末 / main_stock 合法空值 | 监控不再假阳性报警 | `519bbbf6` 类 |
| `data_freshness_watchdog.py` | **新建**看门狗 | 每 30 分钟值守，发现陈旧自动重跑+重建+部署+推 main | `4ba2099a`→`5f265e69`→`b0a5990b` |
| 自动化 `数据新鲜度自动值守` | **新建**（automation-1784448887937） | rrule `HOURLY;BYMINUTE=30`，含周末，model=hy3，ACTIVE | — |
| `.github/workflows/cloud_scanner.yml` | 监控闸门「只拦不修」→**告警不阻断** | 第 207-215、234-239 行改为 `::warning` 无 `exit 1` | `b0a5990b` |
| `.github/workflows/cloud_data_fetch.yml` | 同上 | 第 150-156、180-185 行改为告警不阻断 | `b0a5990b` |
| `index_master.html` | 健康看板新增「🎯 选股驾驶舱」聚合页 + 共振评分赛道过滤 | 聚合大盘可操作区/最强赛道TOP3/≥70·≥80共振候选/机构龙虎榜/紫卡止盈止损；共振评分按赛道分组筛选 | `6874ee8` |
| `index_master.html` | 驾驶舱收口：顶部新增「出手信号灯」风控总开关 + ⑥三重选股补充候选 | 危机雷达综合分+北向近5日趋势+市场温度→红/黄/绿三档（该不该出手）；金钻起涨/波段多头/主力进场三重补充 | `1cfc47ca` |
| `cockpit_backtest_now.py` + `index_master.html` | **驾驶舱信号真实回测上线** | baostock拉真实收盘价回测 signal_count>=2 信号股(07-06→07-17, 8交易日)；驾驶舱新增 ⑦ 历史回测胜率块；**真实结果：27只A股, 胜率11.1%, 平均-22.6%**（下跌市样本，印证出手信号灯必要性） | `e6d9031` |
| `cockpit_backtest_now.py` + `index_master.html` + `cloud_post_close.yml` | **驾驶舱回测三修复**: ① 候选票显示历史胜率/平均收益；② 接入 cloud_post_close 16:30 阶段每日滚动；③ 口径统一为 `total_score>=70/80`（与驾驶舱一致） | 历史快照从 `data/history/top10_daily_YYYYMMDD.json` 筛选 ≥70 分，baostock 收盘价回测。当前样本仅 2026-07-17 2只（0%收益，因最新数据也到07-17），周一盘后开始滚动扩大。 | `326a73b5` |
| `data_freshness_watchdog.py` | 监控列表补 `sh_index_fib.json`（同源产出 sz_index_fib.json） | 闭合「选股驾驶舱」大盘可操作区依赖源的自动兜底 | 本次 |
| `deploy_now.py` | 心跳子进程挂起修复 + 残留锁清理 | 部署收尾不再挂死、锁必释放 | 前期已修 |

**内容级核验结论**（不信文字）：`grep -c "监控失败" cloud_scanner.yml cloud_data_fetch.yml` 两文件均 **0 命中**；保留两道合理硬门（核心 China 文件缺失 / 构建失败），那才是真·无数据可部署。

---

## 三、明天（周一 07-20）小九须知

1. **云端 18:31 起用新逻辑**：监控命中只告警、不再冻结部署。周五「点一下就停」的病灶已消失。
2. **本地看门狗每 30 分值守**（含周末）：若发现 12 个关键文件陈旧，自动重跑+部署+推 main，无需人工。
3. **双机兜底 ACTIVE**：阿狸咪家机 + 小九单位机各自定时任务仍在。
4. **周一盘后链自动重刷**：`15:30 / 16:15 / 16:30 / 17:31 / 18:31` 云端会把「周五上午快照」覆盖成盘后值。
5. **无需手动补跑**。若发现主站数据陈旧 >1 小时且非周末，先看 watchdog 自动化是否 ACTIVE、本地 `git pull` 是否同步；仍异常再手动 `deploy_now.py --force`（前提：先确认 main 已 push，否则 `_rebuild_dist()` 的 `git checkout origin/main` 会覆盖未推送改动）。
6. **「🎯 选股驾驶舱」已上线**（健康看板顶部常驻 section，commit `6874ee8`）：它是**聚合展示层，无独立更新任务**——显示的数据来自 `SH_FIB/SZ_FIB`（大盘可操作区）、`sector_fund_flow`+`sector_rs`（最强赛道）、`top10_daily`（共振候选）、`maharo`+`lhb`（机构/龙虎榜）等已存在源，随现有盘后链（云端 15:30→18:31）+ 看门狗（每 30 分）+ 本地兜底持续刷新并重新部署。周一盘后自动显示最新共振与赛道分组，小九无需单独操作。

---

## 四、当前部署状态（实测）

- 主站 gh-pages 最后部署：**2026-07-19 21:40:28**（构建戳 `20260719214011`，含驾驶舱回测统一口径版：候选票显示历史胜率/平均收益 + ⑦历史回测胜率块）。
- 13 个看门狗监控文件（scan_result / north_fund / herding / crisis / sector_fund_flow / concept_ranking / market_alerts / main_stock / mahoro / gold_pool / etf_subscription / sector_rs / **sh_index_fib**）在主站均为**今日时间戳**（sh_index_fib 周日定格周五盘后属正常，周一盘后自动更新）。
- 其余约 30 个 JSON 停在周五（周末不刷新属正常），周一盘后自动重刷。

---

## 五、残余风险 / 待办（小九可顺手盯）

- ✅ `sector_rs.json` 已刷到 **07-19 16:04**（东财限流问题已修复），不再是 07-17；驾驶舱「板块 RS」显示今日数据。
- ✅ `sh_index_fib.json` / `sz_index_fib.json`（驾驶舱「大盘可操作区」依赖源）已补进看门狗监控（本次 commit），周一盘后自动重刷；当前周日定格 07-17 19:31（周五盘后）属正常。
- 小九 + 阿狸咪双故障属罕见情形，届时云端仍部署「最新可用」不冻结（已接受此妥协）。

---

## 六、操作速查（如需手动）

```bash
# 1. 看门狗手动跑一次（带 SSH 15s 超时护栏）
cd /e/workspace/stock-scanner
export GIT_SSH_COMMAND="ssh -o ConnectTimeout=15"
python data_freshness_watchdog.py

# 2. 强制部署当前构建（先确认 main 已 push）
python deploy_now.py --force

# 3. 查看门狗自动化是否 ACTIVE
#   → WorkBuddy 自动化面板：「数据新鲜度自动值守」
```

---

## 七、本次一并提交的运维日志

- `HANDOVER_LOG.jsonl`（根目录，自动化交接记录）
- `.workbuddy/memory/2026-07-19.md`（今日全量审计权威记录）
- `.workbuddy/automations/*/memory.md`（各自动化运行记忆）

> 以上均按用户要求归档进 main，确保小九 `git pull` 后能看到阿狸咪周末全部动作。

---

## 八、全局闭环审计（21:53，commit 8b216c4b）

用户要求"审计确认所有的前端数据更新部署都闭环了"。交叉比对三维度：①8个cloud_*.yml定时任务 ②前端依赖46个data/*.json ③看门狗CRITICAL监控列表。

**审计结论**：

| 环节 | 状态 | 说明 |
|------|------|------|
| ① 产出闭环 | ✅ | 前端依赖46个文件均有定时产出任务（cloud_data_fetch 17:31全量抓取18脚本 + cloud_post_close + cloud_scanner + cloud_weekly） |
| ② 部署闭环 | ✅ | cloud_data_fetch 17:31 + cloud_scanner 18:31 均有 update_data_v2注入 + ensure_standalone_sync + 部署gh-pages + verify_data_vs_website闸门 |
| ③ 监控闭环 | ⚠️→✅ | 看门狗原14个→补到27个，覆盖所有每日/每周刷新的核心展示数据 |

**本次补入看门狗的13个文件**：
- market类(7): top10_daily / nt_data / crds_result / capital_flow_summary / recommend / margin_data / sh_sz_history
- non_market类(6): macro_data / industry_map / stock_names / guanlan_reports / overnight_timeline / fomc_summary

**未补入监控的文件及原因**：
- `scanner.py`产出的核心文件(scan_result/watch_result/52w_high)：scanner.py太重(cloud给2400s，watchdog只300s)，由cloud每日两次+部署闸门兜底，不入watchdog自愈。
- 运维状态类(.ops_status/hb_*)：非展示数据。
- 实验数据(triple_select_*)：手动产出。
- 派生类(candidate_pool)：gold_pool已监控且同源脚本。

**双机依赖确认**：north_fund.json + sh_sz_history.json 云端不抓(美区无中国网络)，由双机负责。均已有看门狗兜底（north_fund原有，sh_sz_history本次补入）。

> 小九周一无需额外操作，看门狗自动化会自动巡检27个文件。若某文件陈旧，watchdog会自动重跑对应脚本修复。
