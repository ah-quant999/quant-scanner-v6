# 九宝量化 V8.0 — 傍晚交接单（小九 → 阿狸咪）

**日期**：2026-07-30 18:22
**方向**：小九（单位机） → 阿狸咪（家用机）
**交接人**：小九侧 AI　**接收**：阿狸咪（用户已回家，家用机接管代码编辑）

---

## 一、今日(07-30)已完成并 push 的修复（对应你上午 8 张截图需求，全部落地）

`origin/main` = **`0782b3b2`**（本地与其一致，无未提交 v8/data 改动）

1. **驾驶舱回测空白修复**：`renderCockpitAdvice` / `renderBacktestTdx` 原用 `J()` fetch 本地 json（必 404，因 v8 不发布 data/）→ 改为读注入变量 `window.COCKPIT_ADVICE` / `window.BACKTEST_TDX`，带数据缺失友好提示。
2. **ETF 两张卡空白修复**：`renderEtfPulse/Daily` 早于 `DATA.etf_*` 赋值 → 在 `initPostMarket()` 调用前先注入 `DATA.etf_pulse` / `DATA.etf_daily`；移除 `etfDailyCard` 硬编码「⚠️ 无数据」badge。
3. **CRDS 逆势龙头全面展开**：5 个子区块 `display:none`→`block`，箭头 `▾`→`▴`，max-height 放大。
4. **已下架面板全面接入数据**：`renderDelisted` 重写为 10 张真实数据卡（SH_FIB / 金股池停牌 / 波动率 / 精选预判 / 四路资金 / 大盘资金流 / 期指升贴水 / 两融 / ETF净申赎 / 国家队ETF），数据缺失显示友好提示。
5. **逻辑详解页全面复核（5 pane 全前端覆盖，零遗漏）**：
   - 选股策略 pane 新增「CRDS 逆势龙头 - 算法说明」卡（三大条件 / 打分公式 / 分层 / 大盘有效性 5 档 / 名字清洗 / 与涨停联动区别 / 输出链路）。
   - 实时数据 pane 新增 3 张 ETF 说明卡（资金热度 / 5分钟量能脉冲 / 华宝风格日监控），均含**数据源 + 与资金热度区别**。
   - 运维 pane「已下架」说明更新为「2026-07-30 起全面接入真实数据（10 张卡）」并列出 10 个真实源。
6. **ETF 日监控数据源更正**：`v8/fetch_etf_daily_monitor.py` 原调用失效的 `ak.fund_etf_fund_daily_rank` → 改 `ak.fund_etf_spot_em()`（取「主力净流入-净额」），重新生成 `data/etf_daily_monitor.json`（真实 ~1557 只 ETF，`no_data=False`）。

## 二、T+1 重复冲突疑问澄清

标 T+1 的是「📊 股票型ETF日监控」（盘后 T+1，全市场 1557 只 ETF 当日主力净流入排名）；另一张是「🔥 ETF资金热度」（盘中 T+0 净申赎份额趋势）。**维度不同、互补不重复**，逻辑详解页已写明区别，无需合并。

## 三、双机冲突已解决（致命铁律守住）

`cloud-bot` 的 `ef785d95` 曾把脚本**回退成失效 API**、数据回退 `no_data=True`。经 `git fetch` + `git rebase origin/main`（禁 `pull --rebase`）→ stash 无关自动化改动 → rebase 干净重放 → stash pop → push 成功。`0782b3b2` 是 `fund_etf_spot_em` 正确版。

## 四、线上部署状态与待办

- 部署由**小九定时任务**（`update_v8.py` + `deploy_v8.py`）负责，推到独立仓库 `quant-scanner-v8`（main）→ GitHub Pages。
- 最近一次小九定时 deploy（15:30 / 17:00）**早于本次 push（17:xx）**，故线上 v8 可能暂未含本次「逻辑详解 + ETF 修正」。
- 待小九下次定时 deploy（含周末 T+1 15:30）自动取新模板上线；或用户/小九手动触发。
- ⚠️ 若线上逻辑详解页仍空白/旧版，属正常未部署，**勿手动 deploy**。

## 五、双机铁律重申（务必遵守）

1. **模板同步**：`v8/index.html` 任一处改动必须 `git add` + `commit` + `push` 主仓库（stock-scanner），对方 `git pull` 同步；否则对方定时任务用本地旧模板跑 `update_v8`+`deploy` 会把线上覆盖回旧版。
2. **单部署者**：小九 = 唯一部署者（数据+构建+`deploy_v8`，含周末 T+1 15:30）；阿狸咪 = 纯代码编辑（18:00 后 + 周末），**只改不部署，绝不跑 `deploy_v8.py`**。
3. **git 规则**：本仓禁 `git pull --rebase`（会坏），用 `git fetch` + `git rebase origin/main`。
4. **安全**：公开仓库绝不存明文密码 / 数据源 token / 核心算法；敏感走 GitHub Secrets 或私有后端。

## 六、阿狸咪回家后可做 / 不可做

- ✅ 18:00 后 + 周末：可编辑 `v8/index.html`（前端逻辑 / 说明卡）、`v8-prototype/design_v3_unified.html`，改完必须 commit + push。
- ✅ 可继续完善逻辑详解页其余前端说明、新增卡片、调样式。
- ❌ 绝不跑 `deploy_v8.py` / `update_v8.py`（部署权仅小九）。
- ❌ 勿删除 / 折叠已展开功能；过期内容直接删，不隐藏。
- ❌ 勿动 `data/*.json`（核心源）、`repo-temp/*.py` 生产代码、`backup_*`，清理前先 `ls` 确认。

## 七、已知风险

- `cloud-bot` 用陈旧模板跑构建会把 ETF 修正回退 → 已确认 `0782b3b2` 是正确版，下次部署不会回退。
- 本机自动化错过槽位不补跑（机器睡眠/关机 = 整段静默丢失）；守卫会检测 + 补跑。
- 若本地 `v8/dist/` 为空，属构建中间态（update_v8 重建前会清空），下次 `update_v8.py` 会重建，不影响源码模板。

## 八、未尽 / 可继续项

- 逻辑详解页其余前端（盘后数据 / 今日事件）说明已齐，如需更细算法可继续补。
- ETF 资金热度卡（v6 暂缓，全力 v8）若在 v8 需补，可后续做。
- 全面核对线上部署后效果（Ctrl+F5 硬刷新验证逻辑详解页）。

---
总体 v8 协作基线详见：`HANDOVER_小九_给阿狸咪_2026-07-28_v8.md`
