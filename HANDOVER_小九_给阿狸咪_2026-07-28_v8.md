# 九宝量化 v8.0 改版计划交接（小九 → 阿狸咪）· 2026-07-28 17:40

> 交接人：小九（本机/白天主机）　接收人：阿狸咪（云端/夜间周末接管）
> 状态：**v8 已上线**（独立仓库 quant-scanner-v8，与 v6 并行运行）。主人指令"先改完版再统一上线"已于 07-30 完成：v8 已部署至独立 Pages 仓库。v6 站点的自动部署任务已于 07-30 清理停止，但其数据抓取管线保留供 v8 共用。

---

## 一、总体决策（主人拍板）
1. **重新开一个新站**，命名 **九宝量化 v8.0**，与现生产站 v6（ah-quant999.github.io/quant-scanner-v6）并行，互不干扰，定稿后再切换/上线。
2. **连数据管道一起重构**（不只是 UI 复刻）：前端模块化拆分（废除单文件 index_master.html 巨石）、抓取/扫描/部署脚本重写，根治 v6 的 `safe_pull` 冲掉改动、`.gitignore` 白名单遗漏等老毛病。
3. **先改完版再上线**：当前只在本地/仓库原型目录迭代，**不部署、不建 v8 正式仓库分支直到定稿**。
4. **管理员密码**：由主人直接告知阿狸咪（本仓库与任何文档均不留存明文密码）。前端以 **SHA-256 哈希**校验，哈希常量在 `v8-prototype/design_v3_unified.html` 的 `ADMIN_HASH`。部署铁律见第三节。

---

## 二、站点结构（已定稿方向，暗色 · 红涨绿跌）
顶部主导航 **6 个板块**：

| 顺序 | 板块 | 可见性 | 内容 |
|---|---|---|---|
| 1 | 📅 今日事件 | 公开 | 打新速览（新股/可转债/配债+申购评级）+ 今日关键事件速览 + 重要日历（北向日历暗色移植：点击当日放大 + 14 类图例） |
| 2 | 📡 实时数据 | 公开 | 大盘指数条、温度计、六维雷达(SVG)、斐波窗口、主力板块资金流、概念涨跌排名、上证+深证成交额、近一月涨跌家数、涨停联动、**ETF 资金热度(宽基/行业/主题/跨境四分类)**、个股实时查询 |
| 3 | 🌙 盘后数据 | 公开 | 金股池、龙虎榜、多维共振、涨停跌停、复盘摘要 |
| 4 | 🎯 选股策略 | 公开 | 逆势龙头/斐波回踩/量价共振/北向增持/高股息防御 五策略卡 + 今日信号 |
| 5 | 🔒 运维 | **仅管理员** | 部署状态、双机心跳、自动化任务(11)、数据新鲜度 |
| 6 | 📖 逻辑详解 | **仅管理员** | 温度/雷达/斐波/资金口径、策略数学、数据源频率 |

- 运维与逻辑详解默认 🔒 灰显，输入管理员口令后解锁（前端 SHA-256 校验 + localStorage 持久）。
- 设计稿为单文件可交互原型（模拟数据），所有图表用纯 SVG/CSS 画，不依赖任何 CDN，离线可开。

---

## 三、安全铁律（公开免费仓库必须遵守，v6 已验证）
**GitHub Pages 免费仓库 = 公开可读。绝不入库以下内容：**
1. **管理员明文密码** —— 前端只用 SHA-256 哈希校验；改密码时重新哈希，明文只由主人线下告知协作者。
2. **数据源 token / API key** —— 如 neodata、东方财富 token、`.gh_pat`、`.neodata_token`、zsxq_token 等。一律走 **GitHub Secrets（注入构建）** 或私有后端，前端只渲染结果。
3. **核心算法** —— 选股策略权重/公式/参数、打分模型细节。算法逻辑放私有仓库或后端，公开前端只展示产出。

v6 先例（必须遵守）：gh-pages 分支无任何 `maharo_signals` / `zsxq_token` / 敏感 token 文件。v8 延续此纪律。

---

## 四、当前文件位置（阿狸咪可拉取）
- **原型（已入库，供阿狸咪接手迭代）**：`v8-prototype/design_v3_unified.html`
- 小九本机最新版：`E:\workspace\stock-scanner\redesign-v7\design_v3_unified.html`（与仓库版同步，以仓库版为准协作）
- 本交接文档：`HANDOVER_小九_给阿狸咪_2026-07-28_v8.md`

> 注：仅上述 2 个文件本次入库（`v8-prototype/` 与设计稿、本 HANDOVER）。仓库内大量 `data/*.json` 改动与 `.gh_pat`/`.neodata_token` 等运行时/敏感文件**未触碰、未提交**。

---

## 五、双机分工（延续现有机制）
- **小九（本机）**：工作日 08:00~17:00 唯一主机，负责中国源（概念/板块资金流/涨跌家数/预警/龙虎榜/港股通/盘中）+ 本次 v8 改版主力。
- **阿狸咪（云端）**：夜间 + 周末自由接管；负责美区/国际源 + 扫描计算。改版期间阿狸咪可继续迭代 `v8-prototype/` 设计稿。
- 心跳/闸门：`_heartbeat.log` + `check_all_deploys.py`；白天阿狸咪禁 deploy/batch_update（除非小九心跳失联 >60min failover）。**改版期不部署，故 deploy 约束暂不适用，但 push 文档/原型不受限。**

---

## 六、下一步（待主人确认后推进）
1. 继续按主人反馈迭代 `v8-prototype/` 设计稿（UI/板块/交互）。
2. 主人确认定稿 → 建独立仓库 `quant-scanner-v8` + GitHub Pages。
3. 前端模块化重写（拆分组件，废除 index_master.html 巨石）。
4. 数据管道重构（scanner/fetch/update/deploy）：`safe_pull` 不冲未跟踪改动、`.gitignore` 白名单自动审计、敏感数据走 Secrets/私有后端。
5. 上线（gh-pages），切换默认访问。

---

## 七、阿狸咪接手提示
- 改版以 `v8-prototype/design_v3_unified.html` 为协作基线，改完 commit + push 到 `main`（**仅此文件 + 必要文档，勿 `git add .`**，勿碰 data/ 与敏感文件，勿触发部署）。
- 管理员口令见主人线下告知；代码中只有 SHA-256 哈希，无法直接反推。
- 若有疑问或需小九决策，写在 `MSG_TO_小九_*.md` 推回仓库，小九白天拉取处理。

---

# 2026-07-30 操作交接更新（小九 → 阿狸咪）

> 本節推翻 07-28 文档中的"设计稿/未上线"状态，记录 v8 实际上线后的真实运行与排程安排。阿狸咪按此即可独立值守。

## 1. v8 现状（已上线）
- **独立仓库**：`git@github.com:ah-quant999/quant-scanner-v8.git` 的 `main` 分支 = GitHub Pages 源。
- **线上地址**：https://ah-quant999.github.io/quant-scanner-v8/
- **构建源铁律**：`repo-temp/v8/index.html` 是模板；`v8/update_v8.py` 从 `data/*.json` 注入为 `window.X` 重建 `v8/dist/index.html`；`v8/deploy_v8.py` 推到 quant-scanner-v8 仓库 main。**改 UI 只在 `v8/index.html`**，改完必须跑 `update_v8.py` 全量重建再 `deploy_v8.py`。
- **数据读取铁律**：v8 不发布 `data/` 目录，前端只读注入的 `window.X`，禁止 `fetch('../data/...')`。

## 2. 今日（07-30）已落地改动
1. **选股策略四模块移植并部署**：三重共识 / 驾驶舱 / 全站精选 / 逆势龙头 全部从 `window.X` 注入读数据，已真实更新数据并上线（实测四子页均正常渲染）。
2. **打新标签颜色按语义统一**：建议申购=红(#ef5350) / 谨慎观望=橙(#ffb74d) / 不建议=绿(#26a69a)。
3. **小九 15:30 选股四模块盘后更新部署(v8)** 任务已建并 ACTIVE（`automation-1785388522458`）。
4. **新建守卫脚本** `v8/alimi_backup_deploy.py`（已强制 `git add -f` 入 main，阿狸咪 `git pull` 即得）。逻辑：检测 quant-scanner-v8 今日是否已部署 → 是则直接跳过（绝不双机争抢）；否则尽力重跑生成器链（`calc_crds` 联网失败保留旧值 / 各生成器失败保留现有 json 不中断）+ `update_v8` + `deploy_v8` + 写 `_heartbeat.log`。
5. **修复** `deploy_v8.py` commit message 时间戳（原 shell `$(date ...)` 转义失败写字面量 → 改用 `datetime` 真实时间戳）。
6. **清理小九本机 7 个 v6 站点部署任务**（见第 3 节）。

## 3. v6 定时任务清理（用户指令：全部删除，含云端+双机）
- **小九本机已删 7 个 v6 站点部署任务**（均 PAUSED，推 v6 gh-pages）：盘前 08:45 / 盘中 09:30·10:31·11:46·13:31 / 盘后 16:31 / 收盘 19:30(代阿狸咪)。
- ⚠️ **本机是小九，只能删小九的**。云端和阿狸咪本机的 v6 站点部署任务需你在各自机器上删除：在自动化列表里找名称含"部署/刷新部署"且指向 v6（调用 `update_data_v2.py` / `deploy_now.py` / 推 `stock-scanner` 的 gh-pages）的任务，软删除即可。
- **保留项（未删，v8 依赖其产出的 `data/*.json`）**：neodata 抓取、china_source、候选池看门狗、危机雷达、打新开盘前刷新、个股元数据维护、neodata token 刷新、双机值守、08:00 交接检查。

## 4. 阿狸咪需创建的两个兜底自动化（18:30 / 20:00）
> 本机是小九无法跨机建阿狸咪自动化，请在**阿狸咪机器**的 WorkBuddy 中创建（创建前先 `git pull` 拉到 v8 脚手架）。

| 项 | 自动化① | 自动化② |
|---|---|---|
| name | 阿狸咪-18:30 v8选股四模块兜底部署 | 阿狸咪-20:00 v8选股四模块兜底部署(二次) |
| rrule | `FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=18;BYMINUTE=30` | `FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=20;BYMINUTE=0` |
| cwds | 阿狸咪本机 `E:/workspace/stock-scanner/repo-temp`（按实际路径改） | 同左 |
| modelId | `deepseek-v4-flash` | `deepseek-v4-flash` |
| status | ACTIVE | ACTIVE |
| prompt | `运行 python v8/alimi_backup_deploy.py 完成 v8 选股四模块兜底部署。脚本会自动检查 quant-scanner-v8 今日是否已由小九15:30部署：已部署则跳过；未部署则重跑生成器链(calc_crds联网失败保留旧值)+update_v8+deploy_v8。运行后确认 _heartbeat.log 有新行。若本机 python 缺 pandas/mootdx，请用本机正确的 python 全路径替换命令中的 python。` | 同左（二次兜底，正常日会Detect到今日已部署而跳过） |

**双机防重复机制**：两台机器都查同一个 quant-scanner-v8 远程日期 → 小九 15:30 成功则阿狸咪两个任务都判"今日已部署"直接跳过；仅当小九 15:30 整日失败时，阿狸咪才用本机现有 `data/` 兜底补部署（符合"保留上次真实数据"约定）。

## 5. 阿狸咪值守要点
- 首次：`git pull` 拉到 v8 脚手架（`alimi_backup_deploy.py` / `deploy_v8.py` / `update_v8.py` 已强制入 main）。
- 需本机 `python` 装有 pandas / mootdx（裸 `python` 不行就用全路径）。
- 需对 `quant-scanner-v8` 有 push 权限（同 GitHub SSH key）。
- 日常：小九 15:30 正常时阿狸咪自动跳过；仅小九整日失败才兜底（可能用陈旧 data）。
- 心跳：兜底运行写 `_heartbeat.log`。

## 6. 当前小九自动化清单（07-30 删后，共 11 条）
- ✅ `小九-15:30 选股四模块盘后更新部署(v8)` ACTIVE — **v8 主责**
- ✅ `周五晚 个股元数据维护` ACTIVE — 数据
- ⏸ `小九-17:35 危机雷达数据兜底` PAUSED — 数据
- ✅ `小九-candidate_pool看门狗-半点` ACTIVE — 数据
- ✅ `小九-candidate_pool看门狗-整点` ACTIVE — 数据
- ⏸ `小九-每日17:31 neodata数据抓取` PAUSED — 数据
- ✅ `小九-每日刷新neodata token` ACTIVE — 基础设施
- ✅ `小九-夜间周末值守(每6h检测阿狸咪)` ACTIVE — 双机协调
- ⏸ `小九-09:25 打新开盘前刷新` PAUSED — 数据
- ✅ `周一~五 08:00 自动交接检查` ACTIVE — 运维
- ⏸ `C盘空间预警` PAUSED — 工具

## 7. 已知 / 待办
- 驾驶舱内"回测"子区块依赖 fetch 文件，v8 架构下不显示（已 catch，不影响主内容）。
- 候选池看门狗 / neodata 抓取等仍 PAUSED/ACTIVE 混合，属 v6 数据管线、v8 共用，保留。
- 若将来 v8 要独立数据管线，再单独建，不依赖 v6 任务。
- **待阿狸咪侧**：创建第 4 节两个兜底自动化；删除云端/阿狸咪本机的 v6 站点部署任务。
