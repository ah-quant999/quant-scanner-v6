# 交接单 · 阿狸咪 → 小九（2026-07-10 周五）

## 一、定时任务分工调整（重要）

今天理顺了双机定时任务的主次。最终定调：

> **小九（单位机）比阿狸咪（家里机）早 1 分钟跑所有盘中任务。**
> 小九先跑（她东财接口不限流，数据更稳），阿狸咪靠 `batch_update.py` 内置的**心跳互斥**自动跳过；
> 只有小九不在线时，阿狸咪的 +1 分钟任务才真正执行兜底。

这样「阿狸咪是常在线主机 + 小九先跑」就统一了，且不需要人工判断谁跑——心跳机制自动搞定。

### 心跳互斥原理（已有，无需改代码）
`batch_update.py` 每个任务启动时写 `.batch_heartbeat.json`（含 mode + host + 时间戳）。
若另一台机器在 **120 秒内**跑同 mode，后跑的检测到心跳就**直接跳过**（exit 0，不重复执行、不重复部署）。
→ 小九 09:15 跑 pre_market，阿狸咪 09:16 看到心跳即跳过；小九没跑，阿狸咪 09:16 正常跑。

## 二、小九（单位机）需要确认的盘中任务时间表

请核对你的机器上以下盘中任务是否设在**这些整点时间**（小九先跑，比阿狸咪早 1 分钟）：

| 时间 | 命令 | 说明 |
|---|---|---|
| 周一 06:30 | `batch_update.py close` | 行业板块概念映射周度重建（建议小九跑，东财更稳）|
| 周二 07:30 | `batch_update.py close` | T+1 全量刷新 |
| 09:15 | `batch_update.py pre_market` | 盘前全量 |
| 09:46 | `python run_intraday_scan.py` | 盘中快速扫描 |
| 10:30 | `batch_update.py morning_plus` | 扫描+三卡刷新 |
| 11:45 | `batch_update.py morning_report` | 午间 |
| 13:30 | `batch_update.py afternoon` | 午后 |
| 14:30 | `batch_update.py afternoon` | 午后 |
| 15:30 | `batch_update.py post_close` | 收盘后快速 |
| 16:30 | `batch_update.py afternoon` | 午后 |
| 17:30 | `batch_update.py close_p1` | 收盘抓取第一批 |
| 18:30 | `batch_update.py close_p2` | 收盘扫描生成 |
| 19:30 | `batch_update.py close_deploy` | 收盘最终部署 |

**阿狸咪（家里机）对应时间已全部改成上面的 +1 分钟**（09:16 / 09:47 / 10:31 / 11:46 / 13:31 / 14:31 / 15:31 / 16:31 / 17:31 / 18:31 / 19:31），并加了 `BYDAY=MO-FR`（周末 A股休市不跑盘中）。

## 三、小九必做两件事

1. **创建机器角色标识**：在你的单位机项目根目录建文件 `.machine_role`，内容写 `XIAOJIU`（不带换行最好）。
   阿狸咪这台已建好，内容是 `ALIMI`。
   → 作用：两台机器 COMPUTERNAME 都叫 CAT，必须用 `.machine_role` 区分，否则交接日志分不清谁是谁。
   → `.machine_role` 已加入 `.gitignore`，**两台机器各自独立、不互相覆盖**，放心建。

2. **核对/调整你的盘中任务时间**到第二节的整点表（比阿狸咪早 1 分）。
   另外：**不要在你的机器上加 17:35 / 18:35 的「补跑」任务**——那两个阿狸咪侧已保留作安全网，重复加会双跑浪费。

## 四、非盘中任务归属（已定，小九无需动）

| 任务 | 由谁跑 | 说明 |
|---|---|---|
| 每日备份 21:00 | 阿狸咪 | 家里机常在线，统一备份 |
| 每周股票名称更新 周五19:30 | 阿狸咪 | — |
| 行业映射周度重建 周一06:30 | 建议小九（见上）| 东财更稳 |
| 周二T+1 07:30 | 建议小九（见上）| — |
| 世界杯赛事 07:30 | 阿狸咪 | 体育，非股票 |
| IPO打新 08:30 | 阿狸咪 | — |
| 读小九交接 / 心跳查小九 / 自动交接检查 | 阿狸咪 | 阿狸咪监测小九，小九侧无需镜像 |

以上非盘中任务小九若也排了，心跳互斥会去重，不影响；但为省 token 建议只保留你这边独有的。

## 五、今日已落地的修改（阿狸咪侧，已 commit）

- 阿狸咪 11 个盘中任务全部 +1 分钟 + `BYDAY=MO-FR`。
- 删除阿狸咪侧 4 个重复/失效任务：
  - `09:20 盘前更新`（与 09:16 重复）
  - `10:00 盘中10:00 morning_plus`（与 10:31 重复）
  - `19:35 收盘最终兜底检查部署`（部署后才跑，死重）
  - `18:00 小九交接汇报`（与 19:30 自动交接检查重复）
- 保留阿狸咪侧 `17:35 / 18:35` 补跑作为安全网（仅阿狸咪有）。
- `batch_update.py` 心跳 host 改为优先读 `.machine_role`（ALIMI/XIAOJIU），日志可区分。
- 新增 `.machine_role=ALIMI` 并加入 `.gitignore`。

## 六、⚠️ 重要：automation_update 工具有「假成功」坑

今天实测：`automation_update` 工具改 rrule/删任务时**回显 success 但 SQLite 实际没写入**（删除尤其明显）。
**小九你改完自己机器任务后，务必直连 DB 回查确认：**
```python
import sqlite3
db=sqlite3.connect(r'C:/Users/HH20210606/.workbuddy/workbuddy.db')
for r in db.execute("SELECT id,rrule,status FROM automations WHERE name LIKE '%盘中%' OR name LIKE '%收盘%'"):
    print(r)
```
若 rrule 没变或删除后仍是 ACTIVE，用 `UPDATE automations SET status='DELETED' WHERE id=?` 直连改（仅删除用此兜底，更新 rrule 用工具实测可落库）。

---

## 七、紧急指令：半途中有事要交代，怎么让我（阿狸咪）马上收到

如果你（小九/单位机）中途需要给阿狸咪留**紧急交代**，走这条路，阿狸咪盘中每 30 分钟扫一次，最迟半小时内必收到并转报主人：

1. 在仓库根目录建文件：`URGENT_小九_YYYYMMDD_HHMM.md`（如 `URGENT_小九_20260710_1430.md`），正文写清楚要交代的事。
2. **必须 push 到 main**（交接文件走 git 同步，不走坚果云）：
   ```bash
   git add URGENT_小九_*.md && git commit -m "urgent handoff" && git push origin main
   ```
3. 阿狸咪的「紧急指令监听」任务（每 30 分钟 08:00–19:00 工作日）会自动 `git pull` 并扫描，命中后立刻用 🔴 醒目转报主人。

> 普通交接仍用 `HANDOVER_小九_YYYY-MM-DD.md`（阿狸咪 10:00/14:30/18:30 三档读）。
> 若怕忘 push，也可以在文件末尾加一句提醒自己。

*注：最即时的方式还是主人直接在这个 WorkBuddy 对话里发命令给我——我秒收。文件通道是给「主人不在键盘前、想留话」用的。*

---
*写于 2026-07-10 22:40，阿狸咪（家里机，常在线主机）*
