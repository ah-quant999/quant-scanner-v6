# 九宝量化 v6 — 数据可靠性审计报告
**审计时间**：2026-07-19（周日）｜**审计人**：阿狸咪
**方法**：基于实际代码/batch_update.py/HANDOVER_LOG.jsonl/数据文件时间戳交叉验证，不凭记忆下结论。

---

## 一、已修复（不再是问题，已逐项验证）

| # | 旧问题 | 验证结论 |
|---|--------|----------|
| 1 | `fetch_sector_rs`/`enhance_dist`/`fetch_concept_ranking` 从未被调用 | ✅ `batch_update.py` 中已多处调用（line 90/116/137/154/169/196/199/241/268/271 等） |
| 2 | `fetch_etf_flow.py` 缺失致 ETF 卡片不更新 | ✅ 2026-07-09 已根除"幽灵脚本"，全量替换为 `fetch_etf_subscription.py`（全局计数=0）。前端 ETF 资金卡片实际用 `window.NT_DATA`，由 `fetch_nt_data.py` 提供 |
| 3 | `update_data_v2.py` 的 `find_block_end` 覆盖停牌/警示 | ✅ 已修复：line 1653-1660，SUSPENSION_ALERT 数据空时跳过替换、保留旧 8停牌+8警示 |
| 4 | `deploy_now.py` 的 `git stash pop` 冲突只警告不清理 | ✅ 已修复：2026-07-18 根因修复，`_fix_unmerged_files()` 精准还原已跟踪冲突文件，不再一刀切 `reset --hard` 冲掉合法提交 |

---

## 二、仍不靠谱的环节（按风险等级）

### 🔴 高 · 自动化 git 同步 SSH 裸奔（时效硬伤）
- **证据**：`batch_update.py:442` `git pull --autostash` 与 `:467` `git stash pop` **均未设** `GIT_SSH_COMMAND`/`ConnectTimeout`/`http.version`；而 `deploy_now.py` 的 git 命令都加了 `-c http.version=HTTP/1.1`。周六 HANDOVER_LOG 实测：`"close_deploy SSH挂死→手动rebuild+deploy"`。
- **后果**：SSH 异常时 `subprocess` 靠 timeout(120/30s) 杀进程 → pull 失败 → 日志提示"可能使用旧版代码"继续跑。小九/云端的代码更新可能拉不到。
- **修复**：在 `batch_update.py` 的 git 调用前注入 `GIT_SSH_COMMAND="ssh -o ConnectTimeout=15"`，并统一加 `-c http.version=HTTP/1.1`（与 deploy_now.py 一致）。

### 🔴 高 · 盘中数据无本地兜底（及时性空档）
- **证据**：本地盘中自动化 `10:30/11:45/13:30/14:30/15:30/16:30` 全部 **PAUSED**；仅云端 `cloud_intraday.yml` 四轮 + `09:46 补充`(PAUSED)。
- **后果**：若云端盘中失败，10:30–16:30 盘中数据不刷新，直到 17:35/18:35 收盘兜底。盘中"最新"无法保证。
- **建议**：将 `09:46 补充`改为常驻；或新增"云端盘中失败→阿狸咪补跑"告警链路。

### 🟡 中 · 数据新鲜度/健康监控脚本未接入
- **证据**：`check_data_freshness.py` / `watchdog_check.py` / `verify_data_sources.py` 三个脚本存在，但 `batch_update.py` 与 `.github/workflows/*.yml` 均**未调用**（grep 零匹配）。
- **后果**：数据若 stale / 数据源失败，系统不会主动告警，依赖人工发现或 `verify_frontend_deploy` 的部署差异网。
- **建议**：在 `close_deploy` 后接入 `check_data_freshness.py`，形成"数据是否新鲜"的闭环校验。

### 🟡 中 · 家用机跑全量易失败，失败不一定可见
- **证据**：周六 T+1（close 模式）后 `sector_rs.json`/`herding_data.json`/`industry_map.json` 仍停**周五**（07-17）；而 `scan_result`/`gold_pool`/`lhb` 等已刷到周六（07-18）。推断 `fetch_sector_rs`/`fetch_herding_data`/`fetch_industry_map` 在家用机因网络（东财限流/BaoStock）失败未写盘。
- **后果**：日级/周级数据（板块相对强度、羊群、行业映射）可能落后。今天是周日非交易日，当前值=周五收盘尚可接受；但工作日若小九/云端也失败则 stale。
- **建议**：这些脚本加失败重试 + 显式写入 HANDOVER_LOG 的 failed_steps，确保可见。

### 🟢 低（正确标注，非 bug）· 北向资金长期为空
- 港交所 2024-05 停披露，`north_fund` 为空，系统已标注"停止"。依赖此数据的分析/卡片部分会缺——属真实世界的源缺失，不是系统故障。

### ❓ 待确认 · 数据一致性校验覆盖范围
- 收盘 `close_deploy` 自动化 prompt 含 `verify_data_vs_website.py --only-issues`，但 `batch_update.py` 的 `close_deploy` 模式步骤列表里没有它（由自动化命令行单独调用，确认在跑）。
- 盘中 `--fast` 验证（记忆称 7 次/日）需确认是否仍在盘中扫描自动化里触发——当前盘中扫描自动化 prompt 未见此调用。

---

## 三、立即修复优先级建议

| 优先级 | 项 | 工作量 | 价值 |
|--------|----|--------|------|
| P0 | git SSH 超时注入（batch_update.py） | ~10 分钟 | 消除自动化卡死/用旧代码，直接保时效 |
| P1 | 盘中本地兜底/云端失败告警 | 中 | 保盘中数据及时性 |
| P1 | 接入 `check_data_freshness.py` 监控 | 小 | 数据 stale 主动发现 |
| P2 | `fetch_sector_rs`/`herding`/`industry_map` 失败重试+写 HANDOVER 失败步 | 中 | 日/周级数据可靠性 |

---

## 四、结论
昨天的大规模修复已闭合四大类：**脚本调用缺口、ETF 幽灵脚本、停牌警示覆盖、stash 冲突污染**——均已验证修复。

**当前最大真实风险**：
1. 自动化 git 同步 SSH 无超时（已实测周六挂死，靠 subprocess 兜底但会用旧代码）；
2. 盘中数据无本地兜底（独依赖云端，云端盘中失败则盘中不刷新）。

其余为监控可见性与家用机弱网的边缘风险。**系统目前"最真实"可保证（周末视角）：核心扫描/资金流/龙虎榜/候选池已刷到周六；板块强度/羊群/行业映射停在周五收盘值，周末属正常，但工作日需依赖小九/云端补刷。**
