# 🔴 紧急 · v8 盘后算法链定时任务失效修复（阿狸咪 2026-08-03 21:10）

## 问题
v8 的**盘后数据（选股策略页 / 盘后数据页 / 共振日历页）**自 08-01 起一直陈旧，最旧到 07-31。
根因：盘后算法链工作流 `v8_algo_run` 的 GitHub schedule 自 07-31 创建以来**从未被调度器派发**
（GitHub API 实测 `total runs = 0`）。同一 `cn` 自托管 runner 跑 `v8_cn_fetch` 正常（30 次），
证明 runner 健康 —— 是 schedule 注册失效，不是机器/网络问题。

## 已处理（阿狸咪今晚做掉）
1. **核查 v8/algorithms/ —— 盘后算法脚本早已齐全且自包含、不依赖 v6**：
   `scanner.py` / `guanlan_extractor.py` / `fetch_maharo_signals.py` / `build_candidate_pool.py` /
   `generate_top10.py` / `calc_crds.py` / `fetch_lhb.py` / `fetch_sector_rs.py` / `fetch_inst_trade.py` /
   `gen_triple_consensus.py` / `gen_cockpit_*.py` / `backtest_*.py` / `fetch_orphan_*.py` /
   `run_algorithms.py` / `api_push_raw.py` / `sync_v6_to_v8.py` 全在（27 个脚本）。
   `run_algorithms.py` 的 `step_v8_self_sufficiency()` 自产 4 类上游输入（gold_pool/scan_result/guanlan_*/maharo），默认不回退 v6。
   ⚠️ **因此不要用 v6 的算法脚本覆盖 v8** —— v8 版带 `V8_OUT_DIR` 等原生钩子
   （如 build_candidate_pool 的 `DATA = ../out`），覆盖会回退导致全链失效。v6 停了不影响 v8 算法。
2. **已在 v8 main 重新注册 schedule**：`.github/workflows/v8_algo_run.yml` 加 20:00 冗余 cron
   （与 18:30 主跑错峰不并发），并 re-touch 强制 GitHub 重新注册 schedule。
   commit `f46e855` 已 push 到 v8 main。未来自动跑恢复。

## 小九今晚立即执行（拿今晚数据）
v8 算法链已自包含，只需手动跑一遍即可补齐今晚数据：

```
cd E:/workspace/quant-scanner-v8
git pull origin main
set V8_PUSH=1
python algorithms/run_algorithms.py
```

- 约 15–30 分钟跑完全链（scanner → 各算法 → stage_to_raw → push raw_data）。
- 跑完 `api_push_raw.py` 自动推 `raw_data/` → 触发 `v8_build_deploy` 部署。
- 验证：`raw_data/gold_pool.json`、`raw_data/lhb_data.json`、`raw_data/candidate.json` 等
  `update_time` 应为 **2026-08-03**。

## 备选
- 也可在 GitHub 对 `v8_algo_run` 点 **workflow_dispatch** 手动触发（同一 cn runner 在线，会立刻算）。
- 之后每日 **18:30**（及 20:00 备份）自动跑，无需再手动。

## 注意
- 今日数据页 / 实时数据页正常（由 `v8_cn_fetch` 产出），不受影响。
- 若日后又发现盘后页陈旧，先查 `v8_algo_run` 的 schedule 是否再失效，手动 workflow_dispatch 补算即可。
- 本地 `index.html` 有一处未提交的「极端风险联动策略」卡片改动，我已 stash 保留（未丢失），与主流程无关。
