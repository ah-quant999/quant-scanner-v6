# 九宝量化 v6.0 — 2026-07-15 交接报告

> **发起人:** 小九 (本机 WorkBuddy)
> **接收人:** 阿狸咪 (家机)
> **时间:** 2026-07-15 21:00
> **状态变化:** 本机今晚无人，云端成为主力部署

---

## 📋 一、今日重大问题修复

### ✅ 1. 前端全旧/全空（用户15:10截图报）
- **症状:** AI市场速览停在09:12、概念排名"昨天"、两融"昨天"
- **根因:** 13:07 有不明进程（非本机WB/非Windows任务）push 了旧 build 到 gh-pages（commit `b3ebd6a`），覆盖了 11:59 的新 build
- **处理:** 15:04/15:23/15:41/16:18 四次手动部署覆盖；所有数据块恢复新鲜
- **当前状态:** ✅ 已解决

### ✅ 2. MARGIN_DATA 两融数据"4-17"（误报）
- 排查结论：`data/margin_data.json` 正常（60条/7-15 15:16），`find_block_end` 注入正确
- 之前的"324条/4-17"是 13:07 旧部署的残余，后续已被覆盖
- **当前状态:** ✅ 已解决

### ✅ 3. CONCEPT_RANKING 概念排名（东财被墙）
- **根因:** 东财 push2.eastmoney.com 被网络封锁（`RemoteDisconnected`），akshare `stock_board_concept_name_em()` 全失败
- **修复:** 重写 `fetch_concept_ranking.py` **改同花顺 THS 源**
  - 使用 `ak.stock_board_concept_name_ths()` → `ak.stock_board_concept_info_ths()`
  - 12 线程并发抓 371 概念，~29s 完成
  - 旧脚本保留为 `fetch_concept_ranking_em.py`（东财版备用）
  - 已 commit `1e0596a` + push origin/main
- **当前状态:** ✅ 已解决

---

## 🏗️ 二、云端改造（本次最大变更）

### 背景
用户明确：**"云端才是理想的，我不可能一直双机开着"**。所有本机暂停的自动化（09:14/45/17:30/18:30/19:30 等）**不再恢复**。改为云端 GitHub Actions 做全部工作。

### 改动清单（7个commit）

| Commit | 改动 | 文件 |
|---|---|---|
| `1e0596a` | CONCEPT_RANKING 改同花顺源 | `fetch_concept_ranking.py`, `_em.py` |
| `169e27d` | NEODATA_TOKEN 注入 + CLOUD_RUNNER | `cloud_intraday.yml`, `cloud_post_close.yml`, `cloud_data_fetch.yml`, `sync_neodata_to_gh_secret.py` |
| `fcb30c9` | 全部 fetch 脚本加 `timeout --kill-after=10 60` 保护 | `cloud_intraday.yml`, `cloud_post_close.yml`, `cloud_data_fetch.yml` |
| `82b93f0` | cloud_data_fetch 改成**并行执行**（25脚本 2min）| `cloud_data_fetch.yml` |
| `61e0b2a` | 换 SIGKILL 兜底、去 tail 管道 | `cloud_data_fetch.yml`, `cloud_intraday.yml` |
| `3f362fa` | 添加 data 回写 main 步骤 | `cloud_data_fetch.yml`, `cloud_intraday.yml` |

### 云端 workflow 现状

#### 能部署（推送 gh-pages）
| Workflow | 文件 | 时间 | 干啥的 |
|---|---|---|---|
| `cloud_intraday` | `cloud_intraday.yml` | 09:20/10:31/11:46/13:31/14:31 | 全量抓取+扫描+构建+部署 |
| `cloud_post_close` | `cloud_post_close.yml` | 15:30/16:15/16:30 | 扫描+龙虎榜+三卡片+部署 |
| `cloud_scanner` | `cloud_scanner.yml` | - | 扫描+部署 |

#### 不部署（只抓数据+commit main）
| Workflow | 文件 | 时间 |
|---|---|---|
| `cloud_data_fetch` | `cloud_data_fetch.yml` | 17:31 |

### 云端 vs 本机能力对比

| 能力 | 云端 | 本机 | 说明 |
|---|---|---|---|
| fetch 全量脚本 | ✅ (并行 2min) | ✅ | 25 个脚本 |
| scanner (mootdx) | ✅ (CLOUD_RUNNER=true) | ✅ | 云端用 Ubuntu V8 直连通达信 |
| neodata 数据 | ⚠️ 依赖 GitHub Secret | ✅ (直连) | 云端需 17:25 本机同步 token |
| 构建前端 | ✅ (update_data_v2.py) | ✅ | 一致 |
| 部署 gh-pages | ✅ (peaceiris/gh-pages) | ✅ (deploy_now.py) | 一致 |
| 概念排名 | ✅ (THS 同花顺) | ✅ | 同花顺源不受东财封锁 |

---

## 🔑 三、neodata token 机制（唯一残留依赖）

### 原理
- 本地 WorkBuddy 的 `connect_cloud_service` 返回 `tempToken`（`tk_` 开头）
- `refresh_neodata_token.py` 写入 `repo-temp/.neodata_token`
- `sync_neodata_to_gh_secret.py` 用 PyNaCl sealed_box 加密后 PUT 到 GitHub API
- 云端在 workflow 启动时从 `${{ secrets.NEODATA_TOKEN }}` 读出，写入 `data/.neodata_token`

### 当前状态
- ✅ 已同步到 GitHub Secrets（18:00 CST）
- ⏳ 过期时间: **2026-07-16 10:01 CST**
- 明日 09:20 cloud_intraday ✅ 可用
- 10:01 之后云端所有 neodata 脚本（`fetch_52w_high` / `fetch_sector_fund_flow` / `fetch_sector_rs`）将 401 静默跳过

### 需要阿狸咪做
**如果明早 10:01 之前有机会开一下本机 WorkBuddy**：
1. 17:25 的自动化 `automation-1784084629462`（小九-每日刷新neodata token）会自动跑
2. 自动化会：调用 `connect_cloud_service` → 取 `tempToken` → 写入 `.neodata_token` → **同步到 GitHub Secrets**
3. 云端后续 10:31~17:31 的 8 次部署就全有 token 了

如果不方便开，影响分析：
- **不影响** 的块: MAIN_STOCK、CONCEPT_RANKING、MARGIN_DATA、ETF_SUBSCRIPTION、MARKET_ALERTS、NT_DATA、MAIN_WEEK、SH_SZ_HISTORY 等~22个脚本
- **跳过** 的块: 52w_high（52周新高）、sector_fund_flow（板块资金流）、sector_rs（板块RS）
- **建议未来**：把这3个改 akshare 源，彻底去掉 neodata 依赖

---

## ⏱️ 四、明日自动化安排

### 云端自始至终（无需人工）

```
09:20 → cloud_intraday → 抓数据+扫描+部署（✅ token 活着）
10:31 → cloud_intraday → 除 neodata 外新鲜（❌ token 过期）
11:46 → cloud_intraday → 同 10:31
13:31 → cloud_intraday → 同 10:31
14:31 → cloud_intraday → 同 10:31
15:30 → cloud_post_close
16:15 → cloud_post_close
16:30 → cloud_post_close
17:31 → cloud_data_fetch
```

每个 cloud_intraday 即使在 token 过期后，仍有 ~22 个脚本成功抓取并构建部署。

### 如果在家机开了 WorkBuddy 17:25 自动化会触发
```
17:25 → [本机] 刷新 neodata token → 同步 GitHub Secrets → 云端后续 8 次部署全部带 token
```

---

## 🔧 五、阿狸咪接手可继续的工作

### 高优先级（影响日常使用）
1. **定时关闭 13:07 的幽灵部署来源**
   - 目前未知哪个进程 push 旧数据到 gh-pages
   - 怀疑是某个被 PAUSED 的旧 WorkBuddy 自动化残留
   - 可以通过 `git log origin/gh-pages --format='%H %an %s'` 看谁提交的

2. **neodata token 云端自续命**（零本机方案）
   - 现状：云端依赖本机 17:25 sync token
   - 理想：云端 GitHub Actions 自身能拿到 tempToken
   - 难点：`connect_cloud_service` 是 WorkBuddy 内置工具
   - 可能方案：写一个 GitHub Action 步骤直接调用 neodata API（无需 WorkBuddy），用 PAT 或 App install token 代替

3. **把 fetch_52w_high.py / fetch_sector_fund_flow.py / fetch_sector_rs.py 从 neodata 改成 akshare**
   - 这样完全不依赖 neodata token，云端就能100%全活
   - 搜索 `akshare` 有无替代功能

### 低优先级
4. `generate_recommend.py` 是 stub（标注废弃），`recommend.json` 不存在 → RECOMMEND 前端块为空
5. scanner 云端跑约 25min（与本机相当），如需加速可改 partial scan
6. `cloud_intraday.yml` 的数据抓取步骤还是串行（`cloud_data_fetch` 已改并行，如法炮制即可）

---

## 📸 六、验证方法

### 检查云端是否成功
```bash
# 查看 gh-pages 最新 commit
git log origin/gh-pages --oneline -5

# 查看 build stamp
git show origin/gh-pages:index.html | grep 'build-stamp'

# 检查关键数据块时间
git show origin/gh-pages:index.html | grep -oP '"update_time":\s*"\K[^"]+' | head -10
```

### 浏览器验证
1. 打开 https://ah-quant999.github.io/quant-scanner-v6/
2. Ctrl+Shift+R 强刷
3. 看右下角运维卡片 build-stamp
4. 确认各卡片数据是否当天

### 触发云端测试
```bash
# 手动触发 cloud_data_fetch (17:31版本)
curl -X POST -H "Authorization: token gho_xxxx" \
  https://api.github.com/repos/ah-quant999/quant-scanner-v6/actions/workflows/313023834/dispatches \
  -d '{"ref":"main"}'

# 查看运行状态
https://github.com/ah-quant999/quant-scanner-v6/actions
```

---

## 📁 七、关键文件位置

| 文件 | 盘符 | 用途 |
|---|---|---|
| `D:\stock-scanner-repo\repo-temp\.github\workflows\cloud_intraday.yml` | D: | 盘中 5 次云部署 |
| `D:\stock-scanner-repo\repo-temp\.github\workflows\cloud_post_close.yml` | D: | 收盘 3 次云部署 |
| `D:\stock-scanner-repo\repo-temp\.github\workflows\cloud_data_fetch.yml` | D: | 17:31 数据抓取 |
| `D:\stock-scanner-repo\repo-temp\sync_neodata_to_gh_secret.py` | D: | token 同步到 GitHub Secrets |
| `D:\stock-scanner-repo\repo-temp\fetch_concept_ranking.py` | D: | THS源概念排名（新） |
| `D:\stock-scanner-repo\repo-temp\fetch_concept_ranking_em.py` | D: | 东财源概念排名（备用） |
| `D:\stock-scanner-repo\repo-temp\.neodata_token` | D: | 本机 token（gitignored） |
| `D:\stock-scanner-repo\repo-temp\.gh_pat` | D: | GitHub PAT（gitignored，暂未设） |
| `E:\workspace\stock-scanner\.workbuddy\memory\2026-07-15.md` | E: | 本日完整工作日志 |

> ⚠️ D盘 junction 关系：`E:\workspace\stock-scanner\repo-temp` → `D:\stock-scanner-repo\repo-temp`
> 文件从 E: 访问也行，但真实 .git 在 D:（非坚果云同步区，避免 坚果云抢 index.lock）

---

*本交接由小九 WorkBuddy 自动生成，2026-07-15 21:00*
