# 交接单 · 2026-07-31 18:10 · 小九 → 阿狸咪

## 一、背景（用户下班前口述）
用户反馈 **v8 主站（quant-scanner-v8）和本地页都少了两张卡**：
1. 🌡️ **涨停热力矩阵 - 近10日板块涨停家数**（截图显示：卡片在但"加载中…"，即卡片容器存在、数据没出来）
2. 一张**全年的柱形图，一直流出 2.万亿那个**（= 资金流时间轴 phMffChart，market_fund_flow.json，选"全部/1年"即全年主力资金滚动净流入，累计常为 -2 万亿级）

用户原话："那个很重要，给我找回来并上线。改好后写进仓库分支部署，和阿狸咪交接好。我下班啦。"

## 二、已完成（小九 18:08 完成并 push）
- **涨停热力矩阵卡片已补回 v8 主模板 `v8/index.html`**，位置：实时数据区（sec-rt）末尾、盘后数据区之前（与线上 v8 站之前版本位置一致）。
- 改动全部集中在 **stock-scanner 仓库 `v8-temp` 分支**（v8 模板真相源，符合单部署者铁律）：
  - commit `d812eed8` — "v8: 找回涨停热力矩阵卡片(近10日板块涨停家数) · 盘后数据区补齐 limit_up_heatmap 渲染"
  - 已 `git push origin v8-temp-work:refs/heads/v8-temp`（fast-forward，未强推）
- 具体改动（96 行插入 / 1 行删除）：
  1. CSS：注入 `.limit-up-content` / `.lu-h0~lu-h5` 色阶 / `.lu-ignite` 启动日高亮 / `.limit-up-analysis` / `.limit-up-legend`（红=涨停多/热，符合 A股红涨）
  2. HTML：新增 `<div id="limitUpHeatCard">` 整行卡片（`limitUpHeatTime` / `limitUpHeatAnalysis` / `limitUpHeatContent`）
  3. JS：新增 `renderLimitUpHeatmap(data)`（取参模式，适配本地 `fetch('../data/limit_up_heatmap.json')` 体系，非线上的 `window.LIMIT_UP_HEATMAP` 内联方式）
  4. 数据加载：`initPostMarket()` 的 `files` 列表追加 `'limit_up_heatmap.json'`，并在渲染链加 `if(d['limit_up_heatmap.json'])safe(renderLimitUpHeatmap,d['limit_up_heatmap.json']);`
- **全年2万亿柱形图（资金流时间轴 phMffChart）模板本就有**（market_fund_flow.json → renderMff），已确认保留，无需补。

## 三、关键待办（阿狸咪 / 部署流程负责）
- **数据层才是用户截图"加载中"的真因**：卡片容器已在，但 `limit_up_heatmap.json` 在部署产物里缺/过期。
- v8 仓库已有 `fetch_limit_up_heatmap_v8.py`。请确认 **update_v8 / deploy 流程在构建时已生成 `data/limit_up_heatmap.json`**（结构需含 `update_time` + `dates[]` + `sectors[{name,data[]}]`，与 `renderLimitUpHeatmap` 字段一致）。
- 若数据就绪，下次部署（小九 15:30 / 阿狸咪 18:30+ / 云端定时）pull `v8-temp` 后 `update_v8 + deploy` 即自动带这两张卡上线，不再"加载中"。

## 四、双机同步铁律（再次强调）
- v8 模板真相源 = **stock-scanner 仓库 `v8-temp` 分支的 `v8/index.html`**。
- 任何一方（你或云端）定时任务用本地旧模板跑 `update_v8+deploy` 会把线上覆盖回旧版（"今日事件回退"即此因）。**改 v8 模板必须 commit+push `v8-temp`**，对方 `git pull` 同步。
- 本次已 push，请阿狸咪 `git pull` 确认本地 v8 模板 = `d812eed8`。

## 五、验证命令（部署后自检）
```
# 线上 v8 站 grep 应出现：
grep -c "limitUpHeatCard"  <(curl -s https://raw.githubusercontent.com/ah-quant999/quant-scanner-v8/main/index.html)
# 本地模板应已含：
grep -n "renderLimitUpHeatmap\|limit_up_heatmap.json" v8/index.html
# 数据文件应存在且 update_time 为今天：
ls -la data/limit_up_heatmap.json && grep update_time data/limit_up_heatmap.json
```

---
小九 18:10 交班。模板已就绪，卡在"数据生成进部署流程"，等你/云端那边把 fetch_limit_up_heatmap_v8.py 串进构建即可全亮。
