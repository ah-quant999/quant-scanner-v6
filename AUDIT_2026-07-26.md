# 九宝量化 v6 算法链路审计报告

**审计日期**：2026-07-26  
**审计机器**：阿狸咪（本机 / 家中机）  
**审计范围**：新旧算法残留 + 新严格三重共识 / 驾驶舱 A 档全链路接线  
**审计人**：阿狸咪（AI 助手）  
**对应 git 提交**：`c88ac878`（仓库卫生清理）、`e33edb7f`（deploy 强制刷新修复）

---

## 1. 审计背景与术语

项目中有两个名称相近但完全不同的功能，必须先分清，否则会把活跃核心误当成废弃残留：

| 名称 | 所属文件 | 身份 | 处理结论 |
|---|---|---|---|
| **三线共振** | `scanner.py` | 主站核心扫描器，产出 `scan_result.json` / `gold_pool.json` / `recommend.json` | **保留，活跃** |
| **三重共识** | `gen_triple_consensus.py` | 新上线严格筛选算法：主站 TOP10≥70 ∩ 驾驶舱 A 档 ∩ 基本面 A 档 | **保留，活跃，已修正口径** |
| **三线共振历史追踪**（旧页） | 原 `triple_resonance.html` | 旧独立页面，已下线 | **已删除** |

---

## 2. 旧「多维三线 / 短线 / 中长线」残留审计

### 2.1 结论：无功能残留

- `scanner.py` 里的「三线共振扫描 v4.0」是**当前主站数据源**，不是废弃算法。
- `index_master.html` 里出现的 `短线` / `中长线` 只是**面板时间维 UI 标签**（例如「短线·盘中」「中长线·盘后」），不是算法逻辑。
- 旧「金股页三栏构建函数」等废弃代码已于 2026-07-25 删除。

### 2.2 真实残留（已修复）

| 残留项 | 位置 | 风险 | 修复动作 | 修复提交 |
|---|---|---|---|---|
| 根 `index.html` 两条死链指向已删页面 | `index.html:1001`、`index.html:1007` | 非线上残留，但仓库不整洁 | 改指向新的 `triple_consensus.html` | `c88ac878` |
| `.gitignore` 白名单保留两个不存在的旧脚本名 | `.gitignore:23-24` | 无直接风险，但白名单失准 | 改为真实文件名 `update_triple_resonance_history.py` | `c88ac878` |

### 2.3 验证命令

```bash
# 根 index.html 死链应为 0
grep -c "multi_resonance\|triple_resonance.html" index.html

# .gitignore 旧名应为 0
grep -c "generate_triple_resonance_history\|update_triple_resonance_daily" .gitignore
```

---

## 3. 新「严格三重共识 / 驾驶舱 A 档」wiring 审计

### 3.1 全链路状态

| 环节 | 文件 | 关键证据 | 状态 |
|---|---|---|---|
| 生成器 | `gen_triple_consensus.py:94` | `if grade == "A":` | ✅ 正确 |
| 生成器 | `gen_triple_consensus.py:171` | criteria 含 `基本面A档` | ✅ 正确 |
| 数据 | `data/triple_consensus.json` | `criteria` = `主站TOP10≥70 · 驾驶舱A档 · 基本面A档` | ✅ 正确 |
| dist 数据 | `dist/data/triple_consensus.json` | 与源一致 | ✅ 正确 |
| 部署兜底 | `deploy_now.py:466-489` | 强制从 `origin/main` 刷新 `gen_triple_consensus.py` 并重生成 | ✅ 已修复 |
| 前端 | `standalone/triple_consensus.html` | `基本面A档` 出现，`基本面≥B` = 0 | ✅ 正确 |
| 前端 | `standalone/gold.html` | `基本面A档` 出现，`基本面≥B` = 0 | ✅ 正确 |
| 前端 | `standalone/guide.html` | 第 4.4.1 节明确列出三栏构成 | ✅ 正确 |
| 线上 | `origin/gh-pages` | 核验 criteria = `基本面A档`，suspensionCard = 0 | ✅ 已上线 |

### 3.2 刷新机制无缺口

新算法数据并非只在部署时刷新。以下自动化都会触发 `deploy_now.py --force`，从而强制重生成 `triple_consensus.json`：

- 17:35 / 18:35 收盘兜底链
- 20:30 数据自修复看门狗（`automation-1785047388251`）

因此不存在「部署之外 triple_consensus 不刷新」的缺口。

---

## 4. 云端双机自动化审计

### 4.1 本机（阿狸咪）

通过权威 API + 原始 SQLite 只读库双重核验：

- **21 条活自动化**，`cwds` 全部正确指向 `E:\workspace\stock-scanner`。
- 无坏 cwd、无孤立重复项。
- 所有活跃管线脚本（含 `gen_triple_consensus.py`、`update_triple_resonance_history.py`）均已入库且在 `origin/main`。

> 注：某探查子代理曾报告 `automation-1784448887937` 等异常，但经原始库复核，该 ID 不存在于 `automations` 表（可能来自已删除行或子代理解析错误），已排除。

### 4.2 云端（小九 / CAT）

**本机环境无法直接访问小九机器**，因此未能直连审计其 WorkBuddy DB。但有两层保证：

1. **部署强制刷新机制**：无论小九本地 `gen_triple_consensus.py` 是否陈旧，`deploy_now.py --force` 都会先 `git show origin/main:gen_triple_consensus.py` 覆盖本地，再生成 A 档数据。这是 2026-07-26 加上的关键补丁（`e33edb7f`）。
2. **源脚本已入 main**：`origin/main` 上的 `gen_triple_consensus.py` 已固定为 A 档逻辑，小九下次 `safe_pull` 后会拿到正确版本。

### 4.3 需小九复核清单

请小九在单位机执行：

```bash
# 1. 确认本地 main 与 origin/main 一致
cd E:\workspace\stock-scanner
git fetch origin main
git merge-base --is-ancestor origin/main HEAD && echo "本地已包含 origin/main" || echo "需要 safe_pull"

# 2. 确认 gen_triple_consensus.py 是 A 档逻辑
sed -n '94p' gen_triple_consensus.py
# 应输出：if grade == "A":

# 3. 确认 deploy_now.py 有强制刷新
python -c "import pathlib; s=pathlib.Path('deploy_now.py').read_text(encoding='utf-8'); print('强制刷新存在:', 'git show origin/main:gen_triple_consensus.py' in s)"

# 4. 跑一键审计脚本（见下方）
python audit_v6_deploy_chain.py
```

---

## 5. 已知风险与后续建议

| 风险 | 等级 | 说明 | 缓解措施 |
|---|---|---|---|
| 小九本地 `gen_triple_consensus.py` 仍为旧版 | 中 | 若小九在未部署时直接运行旧脚本，可能生成 B 档数据污染 `data/` | `deploy_now.py` 已强制刷新；建议小九先 `safe_pull` 再跑任何生成器 |
| 41 个历史 stash 残留 | 低 | 此前多次失败 `safe_pull` 遗留 | 可择机 `git stash clear`（需先确认无有用内容） |
| 根 `index.html` 不被部署 | 低 | 该文件只是本地开发预览 | 已更新死链，保持与 dist 一致 |

---

## 6. 结论

1. **旧算法无功能残留**：「多维三线/短线/中长线」的活跃部分就是当前主站；废弃部分已删。仅清理了两处仓库卫生问题。
2. **新算法本机全链路接通**：生成器、数据、前端、部署兜底均正确，`基本面A档` 已上线。
3. **本机双机自动化干净**：21 条自动化配置正确，无坏 cwd/重复项。
4. **小九端已给出复核命令和交接文档**：待其执行 `audit_v6_deploy_chain.py` 并反馈结果。

---

**附件**：
- `audit_v6_deploy_chain.py`：可一键复跑的审计脚本
- `HANDOVER_小九_2026-07-26.md`：给小九的交接文档
