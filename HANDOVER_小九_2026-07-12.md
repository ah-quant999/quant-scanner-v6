# 交接单 · 阿狸咪 → 小九（2026-07-12 周日）

> 本单覆盖 **2026-07-11 下午 ~ 07-12** 阿狸咪在家机做的全部代码修复。
> 上一单（HANDOVER_阿狸咪_2026-07-11.md）只覆盖到 07-11 08:26 的 deploy 锁修复（fe535c7），**本单之前的所有 7/11 下午与 7/12 改动都还没写给小九**，请务必逐项核对。

---

## 🚨 第一要务：先 git pull + 重装 pre-commit 钩子

### 1. 拉取全部最新代码
```bash
cd E:/workspace/stock-scanner
git pull origin main
```
阿狸咪 07-11~07-12 推了一堆修复到 main（最新提交 `2491064`）。**你不 pull 就跑任务，会用旧脚本，前面修的 bug 全白修。**

### 2. 重装本地 pre-commit 钩子（最关键，不做=防误删失效）
`git_hooks/pre-commit` 源文件已修好并进了 main，但 **`.git/hooks/pre-commit` 不被 git 跟踪、不会自动安装**。小九机若没手动装，跑的还是旧钩子（读 `.txt` 实际是 `.md` + awk 解析 markdown 失败 → 机制完全失效）。

```bash
cp git_hooks/pre-commit .git/hooks/pre-commit
chmod +x .git/hooks/pre-commit
```
装完验证：`git rm --cached --quiet DO_NOT_DELETE.md` 应被 🛑 拦截（别真删，按 Ctrl+C 或加 `--dry-run` 思路测；或 `git commit --dry-run` 后看钩子是否报错）。钩子现在读 `DO_NOT_DELETE.md`，支持 `*` 通配与 `/` 目录前缀。

---

## 📋 本次改动清单（都已 commit + push main，pull 即生效）

| # | 文件 | 改动 | 为什么 |
|---|------|------|--------|
| 1 | `deploy_now.py` | 3 处 push + 2 处 fetch 强制 `git -c http.version=HTTP/1.1`（绕 GitHub HTTP/2 偶发断连）；`_auto_push_source` 末尾加 `git reset -q -- dist/`（只同步源码，不再把 dist/ 改动误提交进 main）；`finally` 块加 `shutil.rmtree(tmpdir, ignore_errors=True)` 兜底（异常路径不再泄漏 `/tmp/gh-pages-deploy-*`）；`_fix_unmerged_files` 在 reset 前先 `git fetch origin main --depth=1` | 修部署偶发失败 + 防 tmpdir 泄漏 + 防 dist 误提交 |
| 2 | `build_crisis_radar.py` | `color_of` / `status_word` / `composite_status` 阈值由 **40/70 统一为 30/50/70**（与综合危机指数档位一致） | 原先卡片颜色和综合指数档位不一致，误导 |
| 3 | `fetch_macro_data.py` | `safe_call` 增加失败聚合 `_FETCH_FAILURES` + 运行末 `report_fetch_failures()` 汇总横幅（≥3 失败写 `data/.fetch_errors.json`） | 原来单接口静默失败无感知 |
| 4 | `fetch_limit_up_heatmap.py` | 移除 `tool_trade_date_hist_sse` 坏调用（该接口在 akshare 1.18+ 已删），只留 sina 源 | 避免每次跑都报不存在的接口 |
| 5 | `update_data_v2.py` | 移除 `compute_lhb_consecutive.py` 死调用（该脚本不存在）；接入 `fetch_stock_names.py`（stock_names.json 超 7 天自动刷新） | 清死代码 + 股票名称能自动更新 |
| 6 | `build_market_signal.py` + `refresh_standalone_and_deploy.py` | `guide.html` 锚点改为 `guide.html#数据监控`（幂等匹配线上手工版）；新增 `step3c_build_market_signal()` 接入 refresh 管线 | market_signal 孤儿脚本正式进自动部署 |
| 7 | `git_hooks/pre-commit` | 重写为读 `DO_NOT_DELETE.md`（回退 `.txt`），正确解析 markdown 表格第一列，支持 `*` 通配与 `/` 目录前缀 | 修机制完全失效（见上"第一要务 2"） |
| 8 | `DO_NOT_DELETE.md` | 由 `.txt` 改为 `.md`；补标 `industry_map.json`+shard、`candidate_pool/crisis_data/stock_names/macro_data/dxy_hist/usdcnh_hist`、`fetch_worldcup.py`/`inject_weekend_run.py`/`diag_parallel.py`、`HANDOVER*.md` 等 | 清单文件格式统一 + 补全防护项 |
| 9 | `.gitignore` | 加 `!fetch_worldcup.py` `!inject_weekend_run.py` `!diag_parallel.py` 白名单（原被 `*.py` 忽略，游离未跟踪） | 三个根脚本纳入版本控制 |

---

## ⚠️ 已知坑 / 待办（小九侧需留意）

1. **DXY / USDCNH 危险分仍是硬编码阈值兜底（非真实历史分位）**
   - 当前值真实新鲜（Sina 实时），但危险分 55/25 是硬编码，非真实分位。
   - 方案 A（本地逐日累加 Sina 值到 `data/dxy_hist.json` / `usdcnh_hist.json`）**尚未实施**，待主人授权。修完前对全球维度贡献为伪分，但偏中性未严重扭曲综合指数。
2. **MEMORY.md 不在 git（`.workbuddy` 被 gitignore）** → 小九机**收不到**阿狸咪的 MEMORY.md 更正（deploy 段、防误删段）。本单已把关键动作写进交接，不依赖 MEMORY.md。
3. **概念涨跌幅东财封禁**：仍未恢复（阿狸咪本机 IP 限流），保留旧数据不覆盖；待小九单位机侧自动恢复。
4. **main 线性历史被重置成只有 2 条提交**（`5c29065` `2491064`）。07-11~07-12 的多次自动同步提交（含 `85e79a7` market_signal 等）在 reflog 里变成孤立提交，但文件内容完整保留在 `2491064`。**小九若本机有未 push 的本地提交，先 pull 再核对，避免被这次 history 状态影响。**
5. **行业映射 `industry_map.json`**：被 `.gitignore` 显式忽略，但它是重生成物、易因东财限流变空，**已在 DO_NOT_DELETE 标记 + 季度强制 `git add -f`**。小九周一 06:30 行业映射任务跑通后记得 `git add -f data/industry_map.json` 提交。

---

## ✅ 小九开工核对清单

- [ ] `git pull origin main`（含本单 + 07-11 08:26 单的 fe535c7 / f1f711c）
- [ ] `cp git_hooks/pre-commit .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit`（重装钩子）
- [ ] 测钩子：对 DO_NOT_DELETE.md 做删除应被 🛑 拦截
- [ ] 确认 `deploy_now.py` 含 `http.version=HTTP/1.1` 与 `finally` 的 rmtree（grep 验证）
- [ ] 确认 `build_crisis_radar.py` 阈值为 30/50/70
- [ ] 周一 06:30 行业映射跑通后 `git add -f data/industry_map.json` 提交

---

*写于 2026-07-12 12:48，阿狸咪（家里机，常在线主机）*
