# 📋 修改交接文档（2026-07-07 收盘后 → 阿狸咪接手）

> **HH（九宝）** 17:57 下班前部署完成
> 所有修改均已部署到 https://ah-quant999.github.io/quant-scanner-v6/
> 源码已推送 main 分支 + gh-pages 分支

---

## 一、🔥 涨停联动热力矩阵显示错误（今日最关键修复）🔴

### 现象
网页「🔥涨停联动」卡片显示旧数据：
- 其他板块单日 **70只、合计1435**（正确应为 29 / 645）
- 固态电池、医药 **全0**（正确应各有数据）
- 只有 8 个板块有数据（正确应为 15 个全显示）

用户无痕模式打开仍是旧数据 → 排除浏览器缓存问题。

### 根因（关键认知）
前端页面读的是 **HTML 里内联的 `window.LIMIT_UP_HEATMAP` 变量**，**不是**直接 fetch `data/limit_up_heatmap.json` 文件。
之前只更新了 JSON 文件、没把新数据重新注入 HTML → 线上永远显示旧内联数据。

### 修复步骤
1. `python update_data_v2.py` —— 把正确的 `data/limit_up_heatmap.json` 重新注入到 `dist/index.html` 内联变量
2. `python deploy_now.py` —— 重新部署（带 CDN 缓存失效戳 20260707180318）

### 验证（线上已正确 ✅）
- 其他 29只、机器人 13只、半导体 8只、固态电池 29、医药 160
- 15 板块全部正常显示，每日合计与 akshare 涨停总数吻合

### ⚠️ 阿狸咪必记铁律
**改任何 `data/*.json` 后，必须两步：**
1. `python update_data_v2.py`（注入到 HTML 内联变量）
2. `python deploy_now.py`（部署）
只改 JSON 不重注入，线上看不到任何变化（这是今天踩的坑）。

---

## 二、📊 部署阻断 signal_backtest.json 缺失（已处理）

### 现象
第一次 `deploy_now.py` 被审计拦截：
```
❌ [选股观测台] signal_backtest.json 文件不存在
```

### 原因
`signal_backtest.json` 由 `scanner.py` 的 `calc_signal_backtest()` 产出，今日 scanner 未跑 full 故文件缺失。

### 修复
```bash
python -c "import scanner; scanner.calc_signal_backtest()"
```
生成成功（金股池39只，胜率38.5%）。再次部署即通过。

### ⚠️ 若再遇此阻断
跑上面一行命令生成即可（依赖金股池 `data/gold_pool.json`，已存在）。

---

## 三、🌍 本周其他改动（上下文，无需今日处理）

### 1. 世界杯独立页恢复 + 保护
- `standalone/worldcup.html` 恢复旧版（加权预测冠军 + 5000次蒙特卡洛），决赛置顶 + 整表倒序
- `worldcup.html` / `guide.html` / `update_worldcup_standalone.py` / `standalone/index.html` 均已加 **⚠️禁止删除** 注释
- 主站 `index_master.html` 已彻底移除世界杯/彩票代码（主站不显示，仅独立页展示）

### 2. localhost:8080 服务修正
- 8080 端口必须 serve **`dist/standalone/`**（不是 `dist/`），否则 root 显示主站排版、独立页两卡片看不到
- 启动命令：`cd repo-temp/dist/standalone && python -m http.server 8080`

### 3. 新闻增量步骤移除
- 全部定时任务中的 `fetch_overnight_brief.py --news-only` 已移除（东方财富持续封禁，新闻步骤必失败且浪费 token）

---

## 四、🐛 已知未修隐患（待 HH 确认）

- `standalone/triple_resonance.html` 与 `multi_resonance.html` 是 07-06 旧副本
  （主站链接指向根目录新鲜版，独立导航页点进去看到旧数据属正常，**主站为准**）
- `fetch_limit_up_heatmap.py` 每日定时跑（120分钟/次）全量重建逻辑已修好，无需手动干预

---

## 五、✅ 自动化状态
- 所有 17 个定时任务正常运行
- 9:30 午后扫描 / 16:15 收盘扫描 / 09:20 盘前更新 均已移除新闻增量步骤
- 系统健康看板全绿（仅北向资金、主力个股动向为官方停更，非故障）

---

## 六、📞 阿狸咪接手 checklist
1. 用户若反馈某卡片"数据没更新" → 先确认是否改过 `data/*.json` 但忘了重跑 `update_data_v2.py`
2. 部署报错 `signal_backtest.json` 缺失 → 跑 `python -c "import scanner; scanner.calc_signal_backtest()"`
3. 任何部署前，先 `check_trading_day.py` 确认交易日（非交易日跳过扫描）
4. 世界杯/逻辑详解两页**禁止删除**（文件头有注释保护）
