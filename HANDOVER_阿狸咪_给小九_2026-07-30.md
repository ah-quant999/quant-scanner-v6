# 交接文档 · 阿狸咪 → 小九（2026-07-30 晚间，定稿版）

> 撰写人：阿狸咪（家中机，冷备；只在周末/假期改代码）
> 时间：2026-07-30 20:55（周四）
> 接收人：小九（单位机，当前主跑机 + 云端数据）

---

## 〇、先纠正：我白天误判了"小九失联"

今天 20:11 那份初版交接写「小九失联 >36h」是**错的**。依据用的是 v6 仓库的 `data/`、`_heartbeat.log`、`git log main`——但 v6 已停，那些信号自然静默，跟 v8 无关。
**真相**：小九 07-30 整天在独立仓库 `ah-quant999/quant-scanner-v8` 猛攻 v8，最后部署 `deploy 2026-07-30_17:51`。v8 线上站正常运行。请无视初版里的失联结论。

---

## 一、今天阿狸咪在 v8 上做的关键改造（轻量化）

**背景（主人原话）**：不要大块头 inline JSON，要「每次更新数据后马上能看到的轻量版」，只保留累计/计算数值，不需要整个页面跟着改、也不需要那么大空间。

**已完成的改造（已 push 到 `quant-scanner-v8` 的 `main`，commit `4366f69`）**：

1. **数据层拆分**：原 `index.html` 里 43 个内联 `window.XXX = {...}` 大 JSON 块（最大 1.2MB）全部拆成独立文件 `data/<NAME>.js`，页面改为 `<script src="./data/<NAME>.js"></script>` 同步加载。
   - 效果：`index.html` **3.95 MB → 260 KB（-93.4%）**；`data/` 共 43 个文件、合计约 1.28 MB。
   - 好处：**以后更新任何一块数据，只替换对应的 `data/<NAME>.js`，整个页面不用重部署**。

2. **5 个大块头做了轻量裁剪**（只留聚合/计算值，去每日明细）：
   | 变量 | 改造前 | 改造后 | 裁剪内容 |
   |------|--------|--------|----------|
   | `BACKTEST_TDX` | 1.2 MB | 3 KB | 去个股日信号明细，只留 summary |
   | `BACKTEST_COMPREHENSIVE` | 388 KB | 7.5 KB | 去 details 明细 |
   | `COCKPIT_BACKTEST` | 47 KB | 34 KB | results 只留最近 20 条 |
   | `GOLD_POOL` | 788 KB | 405 KB | 去每只股票 history 日明细，只留 latest 聚合 |
   | `W52_HIGH` | ~200 KB | 1 KB | 去 146 只完整列表，只留 top_gainers/total |

3. **重写 3 个脚本**（原版是 v6 布局残留，直接跑会报错）：
   - `update_v8.py`：从 `raw_data/*.json` 生成 `data/*.js`，内含上述轻量裁剪逻辑。
   - `deploy_v8.py`：把 `index.html + data/` 推到 `quant-scanner-v8` 的 `main` 分支（列表参数 + `--force`）。
   - `guard_v8.py`：改指 `quant-scanner-v8`（原版误指 `quant-scanner-v6`，会污染 v6 仓库）。
   - 新增 `split_inline_data.py`：一次性转换工具，可复用。

---

## 二、⚠️ 为什么「网上没看到新数据」——两点原因

**原因 A：轻量改造本身不生产新行情数据。**
我们只改了"数据怎么装"，没改"数据从哪来"。页面里现在的数据，和小九 17:51 那份是**同一批内容**（只是换了装载方式）。新行情需要小九侧的数据管道每天产出原始 JSON 后，经 `update_v8.py` 转成 `data/*.js` 再推上去。

**原因 B：线上站可能还没吃到我们 push 到 main 的版本。**
- 我 push 到 `main` 后，线上 WebFetch 抓到的仍是旧版（无 `data/*.js` 引用），说明 GitHub Pages **还没刷新到 `4366f69`**。
- 可能：(1) Pages 构建有延迟（通常 1–10 分钟，偶尔更久）；(2) **Pages 源分支不是 `main`**（例如设在 `gh-pages` 或某文件夹，而仓库目前只有 `main` 一个分支）。
- 仓库根**没有 `.nojekyll`**，GitHub Pages 默认会跑 Jekyll；`data/` 下的 `.js` 一般能正常作为静态文件下发，但若有异常可在根加一个空的 `.nojekyll` 强制纯静态。

**请小九确认**：GitHub Pages → Settings → Pages → Source 现在指向哪个分支/文件夹？如果是 `main`/`root`，等几分钟或手动 Retrigger deployment；如果是别的，告诉我，我把 `deploy_v8.py` 改成推到对应分支。

---

## 三、小九这边需要接上的数据管道（核心）

轻量版要真正"每次更新数据只看得到新数据"，靠下面这条链：

```
小九每日数据管道产出  raw_data/<NAME>.json
        ↓  update_v8.py（生成 data/*.js，含轻量裁剪）
data/<NAME>.js
        ↓  deploy_v8.py（推 main）或手动 push
线上 Pages 刷新，只动这一个文件
```

**当前缺口（必须补）**：
1. **`raw_data/` 目录现在不存在**。`update_v8.py` 跑起来会提示"无数据可更新"。需要小九把每天生成的原始 JSON 落盘到 `quant-scanner-v8/raw_data/`（命名对应各 `window.<NAME>`，例如 `raw_data/ETF_INTRADAY_HEAT.json`）。
2. **3 个模块仍是空的**，页面有 tab 但无内容，需接真实源：
   - `SCAN_DATA = {}` —— 选股扫描
   - `EXPERIMENT = {}` —— 实验模块
   - `ETF_DAILY_MONITOR`（`no_data:true`）—— 备注写明需 `westock data_fund_flow / data_etf` 采集真实净流入排名
3. **约 15 个数据块仍停在 07-28 / 07-29**，需小九侧日频刷新任务覆盖：
   `ETF_INTRADAY_HEAT`、`SECTOR_FUND_FLOW`、`NT_DATA`、`CONCEPT_RANKING`、`IPO_DATA`、`VOLATILITY` 等（具体以各 `data/*.js` 内 `update_time` 为准）。

---

## 四、阿狸咪本机定时任务状态（供小九知悉）

按主人 2026-07-30 定稿：**v6 部署全停；数据归小九/云端；阿狸咪只周末/假期改码。**
阿狸咪家中机保留 3 个 ACTIVE 只读哨兵：
1. 小九心跳监控·只读报警版（每 30 分·交易日）
2. 紧急指令监听（小九 URGENT 文件·每 2 小时）
3. 自动交接检查 19:30（只读小九交接，今晚刚由主人恢复）

其余 v6 数据链任务全部 PAUSED，阿狸咪不会自行恢复。

---

## 五、待办 / 交接事项

1. **【请小九处理】确认 Pages 源分支**，让 `4366f69` 这份轻量版上线（若卡住，加 `.nojekyll` 或改 `deploy_v8.py` 目标分支）。
2. **【请小九处理】建 `raw_data/` 落盘管道**，让 `update_v8.py` 每天能跑出新的 `data/*.js`。
3. **【请小九处理】补齐 3 个空模块** + 把 15 个陈旧数据块刷到当日。
4. **【阿狸咪后续】** 周末/假期若主人要改前端样式或算法，只动 `index.html` / 脚本，改完本地 8766 预览，满意后由主人/小九 push；不碰 `raw_data`、不自行部署。

---

## 六、v8 云端数据工作流（阿狸咪已建，待小九注册 cn runner）

**为什么必须是中国机器跑**：GitHub 官方 runner 在美国，抓 A 股源（东方财富/新浪/akshare 中国接口）必 60s 超时——v6 早把中国源下放到双机。所以"云端跑数据、数据主机小九"的正确实现是：**在 China 机器上注册 GitHub 自托管 runner（label=cn），云端定时 dispatch，在中国机器本地执行抓数**。

**阿狸咪已提交到 `quant-scanner-v8` 的文件**：
- `.github/workflows/cloud_v8_data.yml` —— 定时（盘后 16:35 + 盘中 10:35/11:50/14:35）+ `workflow_dispatch`；`runs-on: [self-hosted, cn]`；流程：抓数 → `update_v8.py` → push `data/`+`raw_data/` 回 main。
- `cloud_fetch_v8.py` —— akshare 逐模块容错抓取（ETF资金流/板块资金流/概念排行/IPO/融资融券/中金所/宏观/危机/波动率/羊群/涨停/资金流/ETF申购/北向/市场资金流/52周高），单源失败只跳过，不覆盖既有数据。
- `update_v8.py` 已改为「无 raw_data 不算错误（exit 0）」，只重写 raw_data 里存在的模块，其他 `data/*.js` 保持。

**⚠️ Schema 待小九核对**：`cloud_fetch_v8.py` 产出的是尽力而为的结构，首次上线前请对照 `index.html` 渲染逻辑逐模块验证字段；字段不符的模块先别 push（脚本只写 `raw_data/`，人工确认后再合入 main 的 `data/`）。

**小九需做（一次性）**：在她的单位机（或阿狸咪家中机）注册 label=`cn` 的自托管 runner：
```
# 在机器上（需 git + python3.13）
# 1. 从 GitHub: quant-scanner-v8 → Settings → Actions → Runners → New self-hosted runner 复制命令
# 2. 下载并配置，运行 ./config.sh --url https://github.com/ah-quant999/quant-scanner-v8 --token <TOKEN> --labels cn
# 3. ./run.sh  （常驻；建议用 systemd/nohup 保持）
# 4. 装依赖：pip install akshare
```
注册后，工作流即自动按 cron 在中国机器上抓数并推送到 v8 线上，不再依赖「等人手动跑」。

---

## 七、21:09–21:28 今晚已推进的修复（主人授权"现在就做"）

| 项 | 状态 | 说明 |
|----|------|------|
| push `V8_PRINCIPLES.md` | ✅ | commit `b49c6d3`，设计原则文档已进 main |
| 刷新陈旧数据 | ✅ 部分 | `cloud_fetch_v8.py` 修复接口名/JSON date/重试后，成功刷新 **13/16** 个 raw_data 源，生成 **12/16** 个新 `data/*.js` |
| 新增 V8 看门狗 | ✅ | `guard_v8_freshness.py` 已 push；检查 `data/*.js` 的 `update_time`，核心过期 exit 1 阻断部署，仅告警 exit 0 |
| 数据已 push | ✅ | commit `d32d81c` "v8 data refresh 2026-07-30_21:26" 已 push main |

**今晚仍抓不到的 4 个东财源**：`SECTOR_FUND_FLOW`、`CONCEPT_RANKING`、`CAPITAL_FLOW_DATA`、`MARKET_FUND_FLOW_DATA`——akshare 请求东方财富接口被 `RemoteDisconnected` 断开，疑似 IP/频率限制，今晚无法硬解。已加入重试机制，runner 定时跑时可能恢复。

**线上仍为旧版**：`https://ah-quant999.github.io/quant-scanner-v8/` 目前还是约 28KB 旧版。远程 main 的 `index.html` 已确认是 257KB 轻量版，问题在 **GitHub Pages 缓存/源分支确认**。请小九在仓库 Settings → Pages 中确认 Source 为 `main` 根目录；若已确认，等 5–10 分钟缓存刷新。

## 八、关键文件 / 网址速查

| 用途 | 值 |
|------|-----|
| v8 线上 | https://ah-quant999.github.io/quant-scanner-v8/ |
| v8 仓库 | git@github.com:ah-quant999/quant-scanner-v8.git（独立仓库，分支 `main`） |
| 本地源（阿狸咪） | E:\workspace\quant-scanner-v8\ |
| 本地预览（阿狸咪） | http://127.0.0.1:8766/index.html |
| 数据拆分逻辑 | update_v8.py / split_inline_data.py |
| 部署脚本 | deploy_v8.py（推 main）/ guard_v8.py（守护 main） |
| 旧 v6 仓库 | git@github.com:ah-quant999/quant-scanner-v6.git（已停，勿再 deploy） |

---

## 八、铁律提醒（勿破）

- 阿狸咪冷备：**不**激活/恢复 v6 数据任务；**不**自行 deploy；push 需主人/小九确认。
- 涨跌色：红涨绿跌；空数据 `available=false` 不造假。
- "更新于"一律相对日期（今日/昨日/X天前）+时分，不写裸 MM-DD。
- gh-pages / 线上永不落 token（maharo_signals / zsxq_token 等走 Secrets）。
