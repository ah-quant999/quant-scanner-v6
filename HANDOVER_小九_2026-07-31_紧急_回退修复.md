# 🔴 紧急 · v8 线上被旧版回退 / 日历消失 — 修复交接（给小九）

**时间**: 2026-07-31 早上（主人发现并指令阿狸咪处理）
**来源指令**: 「怎么又被退回旧版了，改好的日历怎么都没了？赶紧恢复，新改的别又丢了，写到仓库 v8 分支里去，也和小九说别用旧版恢复！」

---

## 一、结论（先说清楚）
**日历 / 暗色 AI 速览 / ETF 热度 全部没丢。** 它们一直好好在仓库里。
线上之所以"回退旧版"，是部署环节用到了**旧本地副本**覆盖好版本，不是代码丢失。

当前仓库正确状态（已用 `git grep` 核实）：
- `origin/main` = `a40c2c62` 「盘后数据区板块资金三合一融合为v6风格」→ **含日历 + 暗色 AI 速览 + ETF 热度 + 三合一**，全部齐全。
- 新增 `origin/v8` 分支 = `a40c2c62`，作为**防丢安全快照**（阿狸咪已强推）。
- 即：`git rev-parse origin/v8` == `git rev-parse origin/main` == `a40c2c62`。

---

## 二、根因（已定位，避免再犯）
`deploy_v8.py` 的部署逻辑是：
1. `git clone --depth=1` 远端 main（好的）
2. **用本地工作区的 `index.html` + `data/` 覆盖**克隆下来的文件
3. `git add -A` → `commit` → `git push origin main --force`

问题在 **第 2 步**：如果**你本地工作区的 `index.html` 是旧版**（没有日历 / 没有暗色化 / 没有 ETF 热度），
一跑 deploy 就会把线上打回旧版。日历代码本身从没丢，只是被旧本地副本覆盖掉再强推上去了。

---

## 三、你该怎么做（恢复线上）
**先同步本地到正确版本，再部署 —— 不要从旧本地副本部署。**

```bash
cd <quant-scanner-v8 仓库>
git fetch origin
git checkout -f origin/main        # 或 git reset --hard origin/main
python deploy_v8.py
```

此时本地 `index.html` = `a40c2c62`（含日历），部署后线上即恢复，与 `origin/v8` 快照一致。

---

## 四、⚠️ 绝对不要做
- ❌ 不要从「旧本地副本 / 旧备份 / 旧 commit（f8d348d 之前）」部署 —— 那是回退源头。
- ❌ 不要 `git reset --hard` 到带日历之前的旧提交。
- ❌ 不要用任何「旧版恢复 / 回滚脚本」覆盖 `index.html`。
- ❌ 本地若有未提交改动，先 `git commit` + 推到 main，再部署；不要拿「未提交但旧的」`index.html` 去覆盖。

---

## 五、防复发（重要）
以后每次跑 `deploy_v8.py` 前，先：
```bash
git fetch origin && git checkout -f origin/main   # 或 git pull --rebase
```
确保本地 `index.html` 与 `origin/main` 一致，再部署。
若对 `index.html` 有本地新改动，务必先 commit 并推到 main，使「本地 == origin」后再 deploy。

`origin/v8` 分支已锁死在 `a40c2c62`（完整好版本），万一 main 被误操作回退，可 `git checkout -f origin/v8` 拉回正确版本再部署。

---
阿狸咪 · 2026-07-31 紧急交接（双机铁律：阿狸咪只改源码+commit/push，不跑 deploy；恢复动作由小九执行）
