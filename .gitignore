# ============================================
# DO NOT DELETE: 保护 standalone/ 等关键目录
# 每次修改前必须核对 DO_NOT_DELETE.md
# ============================================

__pycache__/
*.pyc
# 机器角色标识（每台机器独立：ALIMI / XIAOJIU），不进 Git 共享
.machine_role
# 交接读取状态（每台机器独立记录已读文件），不进 Git 共享
.handoff_read_state.json
# 忽略所有 Python 脚本（核心算法保护）
*.py
# 仅保留部署和运维必需的脚本
!deploy_now.py
!update_data_v2.py
!backup_daily.py
!send_alert.py
!deploy_audit.py
!fetch_analyst_ratings.py
!fetch_policy_density.py
!generate_triple_resonance_history.py
!update_triple_resonance_daily.py
!heartbeat_xiajiu.py
!evening_failover_check.py
!auto_handoff_read.py
!fetch_crisis_data.py
!build_crisis_radar.py
!build_market_signal.py
!refresh_standalone_and_deploy.py
!batch_update.py
!verify_frontend_deploy.py
!is_trading_day.py
!fetch_cffex_holdings.py
!fetch_inst_trade.py
!fetch_limit_up_heatmap.py
!fetch_worldcup.py
!inject_weekend_run.py
!diag_parallel.py
!refresh_neodata_token.py
!pre_market_cloud_failover.py
!triple_select_scan.py
!fetch_neodata_daily.py
!capital_flow_summary.py
!EVENING_FAILOVER.md
*.bat
.wrangler/
worker/
backup/
backup_*/
# 数据目录：全部走坚果云实时同步，不进 Git（避免双机数据冲突）
# 用 data/* 而非 data/ ，否则 !data/xxx.json 例外会因父目录被排除而失效
data/*
# 例外：DXY/USDCNH 累加历史（方案A 自愈真实分位）须进 Git 持久化+双机共享，
# 否则 data/ 被忽略→跨机/部署 git reset 清空→永远到不了20点→危险分恒为阈值假值
!data/dxy_hist.json
!data/usdcnh_hist.json
# 例外：neodata 三表由本机每日 17:31 抓取(云端无 token 跑不了)，须进 Git 让云端构建也保留
!data/52w_high.json
!data/sector_fund_flow.json
!data/sector_rs.json
# 例外：本机运维状态(兜底结论/neodata有效期)供前端运维卡片读取，须进 Git 让云端部署也展示
!data/.ops_status.json
!data/capital_flow_summary.json
!data/experiment/*.json
# 敏感文件：显式忽略，防止被 git add -A / -f 重新纳入版本控制（泄露风险）
data/zsxq_token.json
data/mahoro_signals.json
.neodata_token
dist/data/
# 生成产物：由 update_data_v2.py 生成，不进 Git
dist/index.html
dist/index_master.html
dist/triple_resonance.html
deploy_log.txt
scanner_full.log
.workbuddy/
workbuddy/
# 部署锁（通过 git add -f 强制管理，不参与 git status）
.deploy_lock
# 运行时状态文件：本地/坚果云同步，不进 Git
.batch_heartbeat.json
.scanner.lock
HANDOVER_LOG.jsonl
node_modules/
*.log
.env
pwd.yaml
backups/2026-06-18/
*.bak
*.bak.*
# 坚果云冲突文件（双机同步产生，不应进入版本控制）
*-冲突-*
*-conflict-*
dist/data/.enhance_log.txt
dist/data/zsxq_selenium_snapshot.txt
!enhanced_backup.py
_gh_pages_deploy/
.index_master_md5_lock
standalone/
!standalone/guide.html
!standalone/worldcup.html
# 构建产物：dist/standalone/ 不进 Git（由部署脚本动态生成）
dist/standalone/

# 暂不上架的待观察改动，不进 Git 共享
暂不上架/

# 临时预览页（本地看，不进仓库）
_preview_*.html
