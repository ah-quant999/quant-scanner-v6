# 九宝量化 — 交接单（阿狸咪 → 小九）

**日期**：2026-07-17 07:53（周五）
**方向**：阿狸咪（家用机） → 小九（单位机）
**站点**：https://ah-quant999.github.io/quant-scanner-v6/

---

## 一、昨晚（7-16）改了什么

### 1. fetch_lhb.py — 机游共振口径重构
- **分类改 4 档**：机游共振 / 机构独买 / 游资独买 / 不达标
- **yz 剔除北向**（按站点「数据口径说明」表，"本页不涉及北向"）
- **机构数据源从新浪改为东财**（新浪把深股通卖出误并入机构卖出）
- 验证：华天科技 东财+3.48亿 = 另一站点的机构净买入TOP5 ✅
- 结果：7-16 数据 — 机游共振12只/机构独买4只/游资独买6只/不达标63只
- commit `988ca3e`

### 2. index_master.html — 龙虎榜渲染从内联阈值过滤改为用 s.category
- 旧：`inst_net_万 > 8000 && yz_net_万 <= 8000 && bxNet(s) <= 0`
- 新：`s.category === '机游共振'` 等字段匹配
- 已在 main

### 3. 时间显示统一 + 盘后卡时间修复
- `setPostMarketTime()`：rawTime 是今天时优先用它，不硬性 POST_CLOSE_TIME
- `capFlowTime()` 同理
- `fmtCardTime()` 统一输出「今日/昨日/N天前 HH:MM」
- **4 张盘后卡**（LHB/资金流向/多维80+/股池构成）不再全部显示"昨日 15:30"
- commit `f1f3d70`

### 4. fetch_cffex_holdings.py — fallback 今天→昨天→前天
- 旧：`today_str` 直接调 CFFEX 接口，当日数据未发布时返回空
- 新：候选 3 个交易日逐日试，找到有数据的为止
- commit `b99ac48`

### 5. build_candidate_pool.py — 数据源顺序调换
- 旧：东财优先→新浪→mootdx
- 新：**新浪优先**(已验证18s/5528行) → 东财→mootdx
- commit `9e49eb6`

### 6. .gitignore — candidate_pool 加例外
- 加 `!data/candidate_pool.json`，让云端能 commit 该文件
- git add -f 强制跟踪（当前 7-14 的陈数据）
- commit `9c4a295`

### 7. 云端 dispatch 机制复活
- `.gh_pat` 已存好 PAT（详见文档底部"五、.gh_pat"）
- 本机 `trigger_cloud_dispatch.py` 优先读 `.gh_pat`，不再走会挂死的 git credential
- 昨晚测试 dispatch cloud_scanner.yml → 23/23 步全部 success ✅
- dispatch cloud_worldcup.yml → 13/13 步全部 success ✅

---

## 二、当前已知问题（需你处理）

### 候选池 (candidate_pool.json) — 7-14 陈数据
- 家用机 `build_candidate_pool.py` 连东财接口挂死（已改新浪优先但家用机网络夜间不稳定）
- **云端**跑 `cloud_scanner.yml` 时 build_candidate_pool.py 也静默跳过（`|| echo "⚠️ 候选池跳过"`）
- 云端的 cloud_scanner.yml line 86 没有把数据直接写入 git
- 云端的 deploy 把 dist/ 推到 gh-pages（含旧 version 的 candidate_pool）
- **方案**：你今天白天跑一次 `python build_candidate_pool.py` + `python deploy_now.py`（单位机网络通）

### 全扫描 (scan_result) — 52/350 只
- 家用机数据源大面积超时（290/350 fail）
- 云端 `cloud_scanner.yml` 能跑通（核心扫描已完成）但部署时用的是家用机的旧文件
- **方案**：单位机跑 `python scanner.py full` + `update_data_v2.py --fast` + `deploy_now.py`

### 中信期货 (cffex_holdings) — 7-16 数据
- 家用机已更新（`fetch_cffex_holdings.py` fallback 修复）但云端没跟上
- 当前线上已部署到 7-16 ✅

### worldcup 数据
- 云端 dispatch 成功，线上有「数据更新：2026-07-15 08:22」
- 后续世界杯数据小九用本地跑也行

---

## 三、明天（7-18 周六）注意事项
- 今天 17:30 云端 `cloud_data_fetch.yml` 正常跑（cron 恢复后不需要手动发令枪）
- 18:31 `cloud_scanner.yml` 正常跑（含候选池+扫描+部署）
- 如果云端没跑 -> 本机 dispatch 补枪：`python3 -c "... use PAT from .gh_pat"`
- 周日 07:30 `cloud_weekly.yml` T+1 数据补全

## 四、小九开机必做
```
git pull                        # 拉最新 main（含全部修复）
python build_candidate_pool.py   # 重新生成候选池
python scanner.py full           # 全市场扫描
python update_data_v2.py --fast  # 重建前端
python deploy_now.py             # 部署上线
```
原因：家用网络（东财接口封禁）无法完成候选池/全扫描，需要单位机的网络环境跑通。

---

## 五、`.gh_pat`
已存于根目录 `.gh_pat`（已加 .gitignore）。如果下次 PAT 过期（401），从 GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) 重新生成 `repo` 权限 token，写入 `echo "ghp_xxx" > .gh_pat` 即可。**切勿提交到 git。**
