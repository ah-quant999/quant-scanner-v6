# 阿狸咪 → 小九｜收盘闭环方案（2026-07-16 晚间）

> 目的：把你下午那几张架构图里「省略的依赖关系」补清楚，并定下「云端按时跑、数据+部署都到位」的完美闭环职责划分。
> 结论先放最前：**我俩没有冲突**——你画的是愿景（云端=主力），我补的是真相（4 个主力 workflow 之前没自主定时、只靠你发令枪）。不是吵架，是补图。

---

## 一、先澄清：今晚到底发生了什么（根因，已查证）

| 项 | 事实 |
|------|------|
| 小九（CAT）今天在 `HANDOVER_LOG.jsonl` 写了几条 | **0 条**（单位机自动化今天基本全停） |
| 云端 18:31 `cloud_scanner` 跑了吗 | **没跑**——它之前**只有 `workflow_dispatch`，只靠你发令枪触发**；枪停→云端不跑 |
| 阿狸咪 19:30 做了啥 | 用**本地陈旧数据** `close_deploy` 部署，把云端 17:30 的好数据**覆盖**了 |
| 结果 | 主站 / 手机独立页看到的不是最新 → 这就是你问「为什么看不到最新」的答案 |

**所以根因不是「云端不可靠」，而是：18:31 这个档位只有你一根发令枪，没有第二重保险。**

---

## 二、你图里漏掉的 3 件事（补清楚）

### 1. 「云端 7 次/日 = 主力」这句话要加定语
真相：7 个 workflow 里，**只有 3 个（backup / weekly / worldcup）+ safety-net 有 cron 自主定时**；**另外 4 个主力（intraday / post_close / data_fetch / scanner）之前只有 `workflow_dispatch`，完全靠你发令枪**。所以云端不是「自己会跑」，是「你下令才跑」。

### 2. 「本机小九 ACTIVE 的只有 5 样」要把发令枪拆出来
你那 11 次 `trigger_cloud_dispatch.py`（09:20→18:31 共 10 档 + 1 次 token 写入）被归成「1 样」了。它才是云端能跑的**真正开关**，比「全天监控」重要 100 倍。正确清单应是「1 个发令枪脚本 × 10 个档位时间表」。

### 3. 架构图缺了「发令枪 → GitHub API → workflow_dispatch」这条线
4 个主力 workflow，每个档位都是 `小九发令枪 → GitHub API → workflow_dispatch → 云端跑`。你那张图只画了 17:25 token → Secret → 云端 17:31，漏了另外 9 条发令枪箭头。

---

## 三、闭环新架构（三层防御，已落地）

| 层级 | 角色 | 何时动 | 改了啥 |
|------|------|--------|--------|
| **① 云端 cron（主力）** | 10 个档位**自主定时**，不依赖任何机器 | 每档到点自动跑 | ✅ 给 4 个主力 workflow 加回 `schedule` 块（step 里的 `if: github.event.schedule==...` 早就写好了，零逻辑改动） |
| **② 小九发令枪（降级为监控+补触发）** | 每 15 分判定；**云端近 60min 已跑则跳过**，仅云端失败才补触发 | 云端 miss 时 | ✅ `trigger_cloud_dispatch.py` 加第 3 层幂等（查 GitHub 运行历史），不再与 cron 双跑 |
| **③ safety-net（最后兜底）** | 每 15 分高频轮询；数据过旧时补触发**全部 4 个主力**（含 17:31 / 18:31） | 双机都挂时 | ✅ 扩展触发范围，原来只补 intraday+post_close，漏了 data_fetch/scanner |
| **④ 阿狸咪 19:30（最终兜底守卫）** | **云端已更新就跳过、绝不覆盖**；仅云端缺失/更旧才兜底部署 | 每晚 19:30 | ✅ `close_deploy` 的部署步换成 `close_deploy_guarded.py`（比新鲜度） |

**定时档位总表（北京时间，全部云端自主）：**

```
09:20  intraday(盘前全景)     10:31  intraday
11:46  intraday             13:31  intraday    14:31  intraday
15:30  post_close(全量扫描)  16:15  post_close(龙虎榜)
16:30  post_close(全模块+部署)
17:31  data_fetch(收盘抓取)   18:31  scanner(收盘扫描+部署) ★今晚事故档
+ 原有：21:00 backup / 周日06:30·周五19:30·周六07:30 weekly / 周末07:30 worldcup
```

---

## 四、已落地的代码改动（commit `99731f3`，已推 main）

| 文件 | 改动 |
|------|------|
| `.github/workflows/cloud_intraday.yml` | +5 个 cron（09:20/10:31/11:46/13:31/14:31）；`if` 改为 `schedule=='' && slot==''` 才「全量刷新」，否则按档选 |
| `.github/workflows/cloud_post_close.yml` | +3 个 cron（15:30/16:15/16:30）；同步修 `if` 选档逻辑 |
| `.github/workflows/cloud_data_fetch.yml` | +1 个 cron（17:31） |
| `.github/workflows/cloud_scanner.yml` | +1 个 cron（**18:31**，今晚事故根因修复） |
| `.github/workflows/safety-net.yml` | 兜底补触发扩到 data_fetch + scanner |
| `trigger_cloud_dispatch.py` | +`recent_success()` 第 3 层幂等（查运行历史，云端已跑则发令枪跳过） |
| `close_deploy_guarded.py` | **新文件**：比线上/本地 scan_time，云端更新就跳过部署 |
| `batch_update.py` | `close_deploy` 部署步由 `deploy_now.py` → `close_deploy_guarded.py` |

---

## 五、小九明天要做的（职责不变，角色微调）

1. **`git pull` 必须跑（最重要）**：`trigger_cloud_dispatch.py` 和 `batch_update.py` 改了。⚠️ 坚果云只同步工作目录的**文件副本**，**不同步 git 版本历史**——你单位机的 git 仓库不会自动拿到我推的 commit，必须手动 `git pull` 拉 origin/main 才能拿到新脚本（commit `99731f3`/`94e23c3`）。若 `git pull` 报冲突，先 `git stash` 再 pull。否则你单位机明天跑的还是旧版发令枪（无幂等层，会和云端 cron 双触发）。
2. **你的 OS 定时器（每 15 分调发令枪）保留不动**：现在它是「幂等监控」，云端 cron 跑过了它就自动跳过，不会双跑、不浪费 token。它存在的意义变成「云端 cron 万一失败时的补枪」。
3. **核对 10 档 cron 是否按时触发**：GitHub → Actions → 看 `cloud_intraday` / `cloud_scanner` 等是否出现 `schedule` 触发的运行（不是只有 `workflow_dispatch`）。
4. **今晚事故档 18:31 重点看**：明天 18:31 后查 `HANDOVER_LOG.jsonl` 应有 `cloud_close` 的成功记录（云端自己写的），不再依赖你发令。

---

## 六、一句话总结给主人

> 云端现在**自己会按时跑**（10 档 cron 自主），小九发令枪退居「监控+补枪」，safety-net 守最后底线，阿狸咪 19:30 改成「云端更新就别动、别覆盖」。四层防御，任何一层挂了下一层顶上——这就是完美闭环。
