# HANDOVER — 阿狸咪→小九 2026-07-20 夜间

## 改动清单（全部已推 main）

### 1. pre_market 并行化 + 提前到 08:45 🚨

`batch_update.py` 的 pre_market 模式做了两处改动：

**① 前 5 步改为并行组**（wall-time 从 13min 缩到 5min）
```
旧：guanlan(5m) → mahoro(2m) → sector_fund_flow(2m) → concept_ranking(2m) → market_alerts(2m)  = 13min
新：[guanlan, mahoro, sector_fund_flow, concept_ranking, market_alerts] 五个同时跑              =  5min
```

**② 启动时间从 09:20 提前到 08:45**
- 08:45 启动 → 09:21 部署完毕 → **开盘前 9 分钟主站全量刷新** ✅
- 无需再用 08:49 scanner full 前置喂养，pre_market 自带 scanner.py full（10min）

> **⚠️ 你那边必须改 schtasks 才能生效**

### 2. pre_market 加了中国源

之前 sector_fund_flow / concept_ranking / market_alerts 只在 10:30 morning_plus 才第一次刷新。现在 pre_market 08:45 一并抓了，中国数据开盘前就新鲜。

### 3. 新鲜度闸门（所有云端 workflow）

新建脚本 `check_freshness_vs_ghpages.py`，所有 5 个云端 workflow
（cloud_scanner / cloud_intraday / cloud_data_fetch / cloud_post_close / cloud_weekly）
在部署前都调用它：**对比本地数据 vs gh-pages 当前数据，本地旧超 1h → 跳过部署**。
防止云端用旧数据覆盖 gh-pages 上的新鲜数据。

### 4. safety-net 加了中国源检查

之前 safety-net 只看 `SCAN_DATA.scan_time`，中国数据过期看不见。
现在每 15 分钟读 gh-pages 上的 sector_fund_flow / concept_ranking / market_alerts 的 update_time，过期 2h+ 标黄告警 + 触发兜底。

### 5. close_deploy_guarded 不再信任云端盲盒

之前云端部署**成功**了就跳过不部署。现在云端成功但数据 < 今日 18:30 → 判定为"云端旧数据"→ 继续本地兜底。

### 6. watchdog gold_pool 自愈修正

gold_pool.json 的自愈脚本从 `build_candidate_pool.py`（不生成金股池）改为 `scanner.py full`。

---

## 你那边需要做的（仅 1 步）

### 改 schtasks 启动时间

```cmd
schtasks /Change /TN "九宝量化-盘前更新0920" /ST 08:45
```

如果任务名不一样，找那个跑 `batch_update.py pre_market` 的任务改时间就行。
**不改的话，08:45 代码改了但 09:20 才触发，白搭。**

### 无需调整

- 其他所有任务（morning_scan / morning_plus / morning_report / afternoon / 收盘链）**不受影响，无需改动**
- 08:49 scanner full 可以保留，但**不再必需**（pre_market 自带 scanner）

---

## 明天（07-21）早上预期

```
08:45 — pre_market 启动（并行抓 guanlan/mahoro/3中国源）→ 5min
      — build_candidate_pool → scanner → generate → build → deploy
09:21 — ✅ gh-pages 全量刷新（开盘前 9 min）

09:30 — cloud_intraday 云端启动
      — 新鲜度闸门：本地(main=08:45数据) vs gh-pages(=09:21数据)
      → 本地新鲜，闸门通过 → 部署 ✅（US源+扫描数据叠加）

09:03 — 阿狸咪 盘中看门狗启动 → 监控 + 自愈
```

所有今晚的改动已 commit 到 main（`6c6056d`），你那边 `git pull` 就能拿到。

不出意外明天早上 09:21 主站自己刷新，**无需人工介入**。
如果开盘时（09:30）主站还没更新 → 看主站右下角运维卡片，或检查阿狸咪侧的盘中看门狗日志。
