# 周末改动汇总交接 — 2026-07-27（阿狸咪 → 小九）

**发件**：阿狸咪（家中机）  
**收件**：小九（单位机）  
**主题**：07-25 ~ 07-27 全部改动汇总 + 三重共识顶栏对齐（新增）+ 开机必做清单

---

## 0. TL;DR（小九开机第一件事，避免崩溃/覆盖新版）

1. **main 历史已被 force-push 重写**（安全修复）。旧的 `safe_pull`(rebase) 会把已擦除的
   泄露文件重新 rebase 回 `origin/main`，导致泄露复现 → **开机必须先**：
   ```bash
   cd E:\workspace\stock-scanner
   git fetch origin main
   git reset --hard origin/main
   ```
2. **仓库根必须有 `.site_pw.json`**（gitignored，勿提交），否则部署 fail-closed。
3. `origin/main` 已含全部最新修复（含本次三重共识顶栏对齐），**直接 pull 即用**，
   不要手动跑任何迁移脚本去"覆盖"。

---

## 1. 周末两天都改了什么

### 1.1 驾驶舱系列 UI / 逻辑修复（07-25）
- ④⑤⑥⑦ 左侧数字实心圆加深（主题色背景 + 白字 11px）。
- A/B 档胶囊 `· 基本` → `· 基本面` 修复；`standalone/guide.html` 新增 `4.4.1 驾驶舱精选(A/B档)` 章节。
- ③共振候选股 3 个逻辑 bug 修复（含：无≥80 时提前 `return` 吞掉 70-79 观察区）。
- **部署脚本根因修复（fc50d387）**：`_auto_push_source()` 移到 gh-pages 部署 + dist 重建
  **之后**调用，dist 产物自动回写 main，彻底消除"每次部署后工作区 dist 残留 dirty"。

### 1.2 新旧算法残留审计 + deploy 补丁（07-26）
- 结论：旧「多维三线/短线/中长线」无功能残留；新「严格三重共识/驾驶舱 A 档」全链路接通。
- `c88ac878`：清理根 `index.html` 两条死链 + 修正 `.gitignore` 旧白名单。
- `e33edb7f`：`deploy_now.py` 部署时**强制刷新 `gen_triple_consensus.py`**（criteria=基本面A档）
  并重生成 A 档数据，防止云端/本机旧脚本生成 `基本面≥B` 污染线上。
- 详见 `HANDOVER_阿狸咪_2026-07-26_晚.md`。

### 1.3 凭据泄露安全闭环（07-27 晨）⚠️ 重点
- `main` 历史 force-push 重写，擦除 `dist/data/.wc_jwt_cache.json` 与 `dist/data/maharo_signals.json`。
- `index_master.html` / `index.html` 明文密码 → 占位符；`deploy_now.py` 从环境变量或本地
  gitignored 的 `.site_pw.json` 读取，**fail-closed**（无则不注入明文）。
- `safe_pull` 改为 `git fetch` + `git reset --hard origin/main`。
- **小九必须**：一次性 `git reset --hard origin/main` 拿新历史 + 新 safe_pull；仓库根放 `.site_pw.json`
  （admin/guest 新密码，详见 `HANDOVER_阿狸咪_2026-07-27_晨.md`，该文件含密码、本地未提交、勿进公开仓库）。
- 详见 `HANDOVER_阿狸咪_2026-07-27_晨.md`。

### 1.4 三重共识顶栏对齐（本次新增，07-27）✅
- **问题**：金股观测页顶栏三重共识卡片比历史追踪页多显示「观察（70≤评分<75）」分组，
  两页口径不一致（主人截图指出对应不上）。
- **修复**：`index_master.html` 的 `renderTop10Daily` 隐藏「观察」分组，只显示
  「重点关注(3/3 严格) + 优先观察(主站评分≥75)」，与历史追踪页 `triple_consensus.html` 对齐。
- **提交**：源码 `90370ac5`；dist 同步 `bb2320af` / `c09e9456`（已 push `origin/main`，
  gh-pages 已上线，全层 `观察（2/3 候补 · 评分 ≥70）` 计数 = 0）。
- 历史追踪页 `triple_consensus.html` 为独立维护页，此前已隐藏观察组，本次无需改动。
- **踩坑记录**：auto-sync 会在部署后把本地 `dist/` 回退成 `origin/main` 旧版；本次用
  "重建 + 校验 + 提交 + push" 单进程原子脚本锁定，才把 `origin/main` 的 dist 也刷成最新。
  后续若再出现 dist 与源码不一致，重跑该原子流程即可（不要手写 `git checkout` 覆盖）。

---

## 2. 小九开机操作清单（按顺序）

```bash
cd E:\workspace\stock-scanner

# 1. 拿新历史 + 新 safe_pull + 全部修复（关键，勿用旧 rebase）
git fetch origin main
git reset --hard origin/main

# 2. 确认 .site_pw.json 存在（无则创建，含 admin/guest 新密码，gitignored 勿提交）
#    密码值见 HANDOVER_阿狸咪_2026-07-27_晨.md

# 3. 一键审计，确认无 ❌
python audit_v6_deploy_chain.py
```

跑完 `audit_v6_deploy_chain.py` 后，正常跑自动化即可；需要部署时用 `python deploy_now.py --force`。

---

## 3. 严禁 / 必做（避免崩溃或覆盖新版）

| 类型 | 操作 |
|---|---|
| ❌ 严禁 | 跑 `_patch_gold_triple.py` / `_patch_index_triple.py`（一次性迁移脚本，再跑匹配 0 处报错） |
| ❌ 严禁 | 手写 `git pull --rebase` 或用旧 `safe_pull`（会把泄露历史 rebase 回灌） |
| ❌ 严禁 | 直接改根 `index.html`（线上用 `dist/index.html`）；改 `index_master.html` 后必须走完整部署链 |
| ✅ 必做 | 新增/改 `.py` 必须 `git add -f`（`.gitignore` 全局忽略 `*.py`） |
| ✅ 必做 | 部署铁律：改源码 → 重建 dist(`update_data_v2.py --fast`) → 同步独立页(`ensure_standalone_sync.py`) → commit+push main → `deploy_now.py --force` |
| ✅ 必做 | 若审计报 criteria 仍是 `基本面≥B`：先 `git show origin/main:gen_triple_consensus.py > gen_triple_consensus.py` 再 `python gen_triple_consensus.py` |

---

## 4. 待你反馈
1. `audit_v6_deploy_chain.py` 有无 ❌ / ⚠️？
2. 是否成功手动部署一次验证（含三重共识顶栏对齐生效）？

---

交接人：阿狸咪  
交接时间：2026-07-27
