# 安全修复交接 — 2026-07-27 晨（作者：阿狸咪）

## 一、本次已完成（本机/阿狸咪）
1. **凭据泄露彻底闭环**：公开仓库 `main` 历史已 `force-push` 重写，彻底移除
   `dist/data/.wc_jwt_cache.json` 与 `dist/data/maharo_signals.json`（含 worldcup26.ir 匿名 JWT）。
   `origin/main` 当前 HEAD = `965991f3`，`git log main --` 已查无上述文件。
2. **密码明文暴露消除**：
   - 根模板 `index.html` / `index_master.html` 的明文 `cat999`/`hjd666` 改为占位符
     `__PWD__` / `__GUEST_PWD__`。
   - `deploy_now.py` / `update_data_v2.py` 改为从环境变量 `QB_PWD`/`QB_GUEST_PWD`
     或本地 **gitignored** 的 `.site_pw.json` 读取，绝不写死默认口令；两者皆无则
     **fail-closed**（保留占位符，不注入明文）。
   - 已生成本机 `.site_pw.json`（mode 600，gitignored，未进仓库）含新强密码。
3. **`safe_pull` 改为 `git fetch` + `git reset --hard origin/main`**：使 `main` 历史被
   重写并 force-push 后，双机仍能干净对齐，不会把旧历史 rebase 回 `origin/main`。

## 二、小九（单位机）必须执行 ⚠️
因 `main` 历史已被 force-push 重写，小九机器若仍用**旧** `safe_pull`（rebase）拉取，
会把旧历史（含已擦除的泄露文件 commit）重新 rebase 回 `origin/main`，导致泄露复现。
请在小九机器执行**一次性对齐**（拿到新 `safe_pull` 代码 + 新历史 `H'`）：

```
cd <仓库根>
git fetch origin main
git reset --hard origin/main
```

执行后小九的自动化 `safe_pull` 即为 reset --hard 版，后续每次部署自动对齐，安全。

## 三、小九机器需放置 `.site_pw.json`
部署阶段从仓库根 `.site_pw.json` 读取密码注入公开站点。小九机器若缺此文件，
部署会 **fail-closed**（占位符不替换 → 站点无法用密码登录）。请在**小九机器仓库根**
创建 `.site_pw.json`（gitignored，切勿提交）：

```json
{
  "admin": "<新 admin 密码>",
  "guest": "<新 guest 密码>"
}
```

新密码值见本机 `.site_pw.json`（或下方对话），请勿写入任何会进公开仓库的文件。

## 四、新密码（仅本机/小九本地持有，勿公开）
- admin : `7ukWIBmVLq6tR2Kc`
- guest : `FCpCPuGuvbVYJQy3`

> 说明：公开站点（gh-pages）运行时的 `index.html` 必然含密码明文（前端 JS 校验所必需），
> 新密码为非公开默认值，替换了原先广为人知的 `cat999`/`hjd666`。若要彻底不在前端暴露，
> 需改架构（后端校验/不前端校验），本次未做。

## 五、验证要点（小九侧跑完可核对）
- `git log main --oneline -- 'dist/data/.wc_jwt_cache.json' 'dist/data/maharo_signals.json'`
  应为空。
- 公开站点登录用新密码；`data/.wc_jwt_cache.json` 不应出现在 `origin/gh-pages`。
