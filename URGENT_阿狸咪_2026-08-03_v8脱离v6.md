# 🔴 URGENT：v8 已完全切断 v6 依赖 — 待小九验证后退役 v6

> 阿狸咪 → 小九｜2026-08-03 21:30｜v8 仓分支 `feat/v8-detach-v6`

## 一、结论先行
**v8 早就自包含，正常运营下已无任何 v6 运行时依赖。** 本次只是把最后一小块缺口补上 + 清掉残留的 v6 路径字面量，让 v8 在代码与配置层面彻底不提 v6。验证通过后即可退役 v6 仓。

## 二、本次做了什么（已 push 到 v8 `feat/v8-detach-v6`）
1. **复制 v8 真缺失的资产**（只这 2 个，v8 原没有）：
   - `algorithms/fetch_sector_fund_flow_westock.py` — 腾讯自选股 westock 第三源拉取器，孤儿脚本 `fetch_orphan_sector_fund_flow.py` 引用但它原缺失（之前静默跳过）。
   - `raw_data/sector_fund_flow_history.json` — 板块资金流趋势历史累加器（144KB），原只存 v6，v8 冷启动会从 v6 复制；现直接内置 v8，永不读 v6。
2. **切断残余 v6 路径字面量**（改为指向 v8 仓内）：
   - `v8_cloud_watchdog.py`：token 候选路径 `v6 .workbuddy/v8_gh_token.txt` → `v8 自身 .workbuddy/v8_gh_token.txt`（密钥跨仓引用消除；CI 实际用 `secrets.GITHUB_TOKEN`，不受影响）。
   - `fetch_orphan_sector_fund_flow.py` / `fetch_orphan_suspension.py` / `run_algorithms.py`：3 处 `V6_DATA_DIR` 默认值由 `E:\workspace\stock-scanner\data` 改为 v8 仓内路径。
3. **禁用仅有的主动拉 v6 工作流**（`if: false` 变 inert）：
   - `v8_sync_v6_data.yml`、`v8_sync_legacy.yml`（二者原本定时已停用、仅留手动 dispatch；现手动也失效，杜绝误拉陈旧 v6 数据覆盖 v8）。

## 三、重要提醒：未复制任何 v6 算法代码
**v8 的算法脚本是带原生钩子的增强版（比 v6 新、含多处修复）。** `migrate_v6_algos.py` 已明确警告"切勿重跑覆盖"。本次审计确认 v8 已具备全部算法生产者（scanner/build_candidate_pool/generate_top10/calc_crds/fetch_lhb/gen_triple_*/fetch_sector_rs/fetch_inst_trade/fetch_maharo_signals/gen_cockpit_*/backtest_*/fetch_stock_names/fetch_fundamental_quality/fetch_orphan_*），**没有覆盖它们**。请勿反向把 v6 算法拷回 v8。

## 四、小九验证步骤（在 cn 机器 v8 仓执行）
```bash
cd /path/to/quant-scanner-v8
git fetch origin
git checkout feat/v8-detach-v6

# 1) token 就绪（二选一即可，CI 用 secrets.GITHUB_TOKEN 无需本地文件）
#    - 设环境变量 V8_GITHUB_TOKEN，或
#    - 把 token 放到 v8 仓 .workbuddy/v8_gh_token.txt

# 2) 跑全链（约 15–30 分钟，自动推 raw_data → 触发 build_deploy 部署）
set V8_PUSH=1
python algorithms/run_algorithms.py

# 3) 验证
grep -rn "stock-scanner" --include="*.py" . | grep -vE "docstring|#|migrate_v6_algos|sync_v6_to_v8|_patch_add_commodity"
# 应只剩：孤儿脚本文档注释 / migrate_v6_algos.py(迁移工具) / sync_v6_to_v8.py(映射辅助) / _patch 临时脚本 —— 均非运行时读取
```
- 检查 `raw_data/gold_pool.json`、`lhb_data.json`、`candidate.json`、`sector_fund_flow_history.json` 的 `update_time` 是否 = 今日。
- 若全链无报错、数据日期刷新、选股/盘后/共振页恢复 → 验证通过。

## 五、验证通过后合并
```bash
git checkout main
git merge feat/v8-detach-v6
git push origin main
# 可选：git branch -d feat/v8-detach-v6（远程归档分支保留作追溯）
```
合并后 **v6 仓即可停**（git 远端归档/只读均可）。v8 完全独立。

## 六、回滚
若合并后出问题：`git checkout main && git revert <merge_commit> && git push`，分支 `feat/v8-detach-v6` 保留可重做。
